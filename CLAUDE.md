# CLAUDE.md — Obligo

This file is read automatically by Claude Code at the start of every session in this repo. It is the operating contract for how work gets done here. If anything below conflicts with a request in chat, **flag the conflict and ask before proceeding** — don't silently override this file, and don't silently override the request either.

The full design authority is `OBLIGO_ENGINEERING_BLUEPRINT.md` at the repo root. This file is a summary and a set of guardrails, not a replacement — when in doubt, go read the relevant section of the blueprint rather than guessing.

Full history of closed phases and checkpoints lives in `docs/PROJECT_HISTORY.md` — read it if you need details on a specific past decision. Nothing there is superseded; it was moved verbatim to keep this file inside its context budget. It currently holds all of Phase 3 and eighteen closed Phase 4 entries: the seven checkpoints that make up the compiler pipeline (IR v1 spec → parser → typechecker → Extractor + Span Grounder → IR Compiler → parse-error repair loop → pipeline wiring), plus eleven later checkpoints, investigations, and decisions (obligations-persistence investigation → verifier → ir_hash → eval-harness pilot → leading-subordinate-clause grounding-fix → gold-set corpus and batch 1 → gold-set recalibration → scorer-wiring → failure-driver investigation and v0.33 amendments → compile-stage bottleneck investigation → the 2026-08-26 classifier-hardening deferral decision).

---

## What this project is

Obligo compiles natural-language obligations (from contracts, email, transcripts) into a typed, formally verifiable intermediate representation, detects contradictions between obligations using an SMT solver (Z3), and monitors each obligation against real-world evidence until it's fulfilled or breached.

The differentiator is the compiler (LLM candidate extraction → Lark grammar parse → semantic typecheck) and the verifier (Z3 lowering + unsat-core explanation). Everything else in the stack exists to support those two things. If a proposed change doesn't serve the compiler, the verifier, or making them demoable, treat it with suspicion.

---

## Local dev environment — DEVIATION FROM BLUEPRINT, READ THIS FIRST

**No Docker on the local machine**, by deliberate choice (not a temporary gap — don't try to "fix" this by installing Docker). This changes how Phase 1 and beyond are executed locally:

- **Postgres:** not run locally. Use a **Neon free-tier branch** (with the `pgvector` extension enabled) as the dev database. Connection string goes in `.env` (git-ignored), never committed.
- **Redis:** not run locally. Use **Upstash's free-tier Redis** (HTTPS/REST-based, no local process needed).
- **The three services run natively**, not in containers: Spring via `./gradlew bootRun`, FastAPI via `uvicorn` in a venv, Next.js via `npm run dev`.
- **Dockerfiles and `compose.yml` still get written** per the blueprint — they're needed for CI image builds (§20.4) and eventual deployment (§20.5) — but they are **not exercised locally** and are not a dependency for local development to work. Do not block any Phase 1 task on "the Compose stack running locally," because it won't be, by design.
- **`make dev` in this project means:** start Spring, start FastAPI, start Next.js natively, pointed at the cloud Postgres/Redis — not `docker compose up`. Write the Makefile target accordingly.
- Docker becomes actually relevant again around Phase 9 (deployment) or whenever CI needs to build and push real images — not before.

---

## Current phase

**Phase 4 — The Compiler & Verifier ★** (see blueprint §21 / §3 Phase 4 deliverables). Blueprint's own words: this is the differentiator phase the entire project exists to contain — "do not compress this phase, compress Phase 8 instead."

**The compiler pipeline — candidate extraction → span grounding → compilation → repair → typecheck — is complete and CI-verified end to end.** `graphs/pipeline.py`'s `run_pipeline()` sequences all five stages for a real segment against real Neon Postgres and real party-resolution data (5 tests, one per terminal outcome — see the pipeline-wiring checkpoint in `docs/PROJECT_HISTORY.md`), and `ci-brain.yml` is green on that commit (223 passed, 5 skipped, 0 failed — see that checkpoint's own audited count). Checkpoints closed to get there: IR v1 spec (`packages/ir-spec/`) → real Lark grammar/AST/parser → typechecker (party-alias symbol resolution + temporal/business-day underspecification detection) → Extractor + Span Grounder (the first LLM in the pipeline, real Groq adapter, versioned prompt registry, span-grounding gate proven against real Groq output) → IR Compiler (deterministic GroundedCandidate → DSL → AST bridge) → parse-error repair loop (`graphs/repair.py`, `compile_quarantine` V18) → pipeline wiring (`graphs/pipeline.py`'s `run_pipeline()`, plus the migration-drift guard `make check-migrations`).

**"Compiler pipeline complete" is not "Phase 4 complete" — do not read it that way anywhere in this file.** Precise about what's still open, per blueprint §21's own deliverable list (full list and acceptance criteria in the scope paragraph below): amendment scope/precedence — the typechecker's fourth named sub-deliverable — was not built in the typechecker checkpoint and is not being folded into "done" silently. The LangGraph extraction pipeline itself is still not wired (Extractor/Grounder/IR-Compiler/Repair/Pipeline all exist as plain functions — see the repair-loop checkpoint in `docs/PROJECT_HISTORY.md` for the falsifiable adoption trigger, still unmet as of the pipeline-wiring checkpoint). The §13.8 schema-failure repair loop (a *different* loop with a different budget — see below) is not built. Model router expansion — budget enforcement, provider failover, content-hash caching, beyond the versioned prompt registry that's already built — is not built. `ir_hash`'s canonicalization+hash function is built (see the ir_hash checkpoint in `docs/PROJECT_HISTORY.md`) — but that is deliberately only half of "`ir_hash` reconciliation for idempotent re-extraction": the dedup-against-storage half (the `obligations(org_id, ir_hash)` unique index, the exclusion constraint, and the actual reconciliation/upsert decision) is still not built, and needs the `obligations` table (Phase 5), the identical scope boundary the obligations-persistence investigation in `docs/PROJECT_HISTORY.md` already drew. Embeddings/hybrid search is not built. The two-tier eval harness is not built. The 100-obligation gold set is *started, not built* — a verified 28-document corpus, a v0.29 annotation guideline, and **18 locked items** exist (batch 1 complete at 10; **batch 2 paused at 8 of 10 pending a consolidation pass**); 82 items and the scoring harness do not (see the gold-set corpus and batch 1 checkpoint in `docs/PROJECT_HISTORY.md`, and the recalibration checkpoint after it). **The 100-item target itself is now explicitly a working figure, not an analytically justified one** — see guideline §19.5. 2 of the original 6 deferred property-based tests are still not built — idempotence, temporal totality (each needs the normalizer/a typecheck-rule decision this phase hasn't reached, neither Z3-shaped). Of the other four: grounding invariant was closed at the Extractor + Span Grounder checkpoint in `docs/PROJECT_HISTORY.md`; **typecheck soundness and conflict symmetry both closed at the verifier checkpoint in `docs/PROJECT_HISTORY.md`** (`tests/verifier/test_properties.py`) — the Z3 dependency both were recorded as blocked on now exists; **hash stability closed at the ir_hash checkpoint in `docs/PROJECT_HISTORY.md`** (`tests/compiler/test_ir_hash.py`) — no persistence involved, pure canonicalization only. An `obligations` table to persist a compiled/typechecked `Obligation` into is explicitly Phase 5's deliverable, not this phase's — see the debt list and the obligations-persistence investigation in `docs/PROJECT_HISTORY.md`.

**The verifier — Z3 lowering, the conflict-candidate set definition, and unsat-core → plain-English explanation — is no longer unstarted; it was scoped in a dedicated conversation before any code was written, then built (see the verifier checkpoint in `docs/PROJECT_HISTORY.md`).** This closes half of the phase's own stated differentiator (this file's own opening section: "the differentiator is the compiler... and the verifier"), but "built" is not "complete" — its coverage is real but partial, and the partiality is documented, not hidden. See `docs/VERIFIER.md`'s own "Known, accepted limitations" section for the full list; in short: only `BY`/`DURING` with resolved dates lower to a concrete window (`WITHIN`/`RELATIVE_TO_TRIGGER`/`EVERY`/any `bd` duration abstain, because `symbols.resolve_trigger()`/`resolve_date()` still always return `None`), object-class matching is exact-string on an open vocabulary (both a recall and a precision hazard, both named and accepted), condition-contradiction detection is limited to literal negation over matching raw text, there is no numeric/cap conflict class (IR v1 has no such field), the action-antagonism table is two deliberately-seeded pairs rather than a general theory, `ir_hash`-keyed verdict caching is not built — the canonicalization+hash function itself now exists (see the ir_hash checkpoint in `docs/PROJECT_HISTORY.md`), so this is no longer blocked on `ir_hash` itself; it stays blocked on there being no persisted obligation anywhere to compute a cacheable `ir_hash` against, i.e. the `obligations` table (Phase 5) — and nothing is persisted anywhere (`findings`/`verification_runs` don't exist, same boundary as `PipelineResult.typechecked`). Not to be confused with the *IR's* own "single largest schedule risk" framing below (§21's pre-Phase-4 risk note, already mitigated by freezing IR v1) — that's a separate, earlier claim about a different risk.

**§21's own five acceptance criteria are a narrower, specific bar — worth stating precisely rather than folding into the open-items paragraph above, in three distinct states (Met / Blocked / Unmeasured), not a collapsed Met/Not-Met binary.** Quoting blueprint §21 exactly, matching the scope paragraph below verbatim — it's one combined bullet for compile-success-and-span-grounding, not two separate criteria, and a session discussion on 2026-08-14 miscounted it that way in chat (never previously written into this file) before being corrected here. **Met** means proven by a real, cited test suite, not merely "should work in theory." **Blocked** means the specific missing component that makes the criterion structurally impossible to satisfy is named. **Unmeasured** means the mechanism may already be sound but nothing has run it against the real corpus/gold-set numbers §21 asks for — the same UNRESOLVED-not-null distinction this project's own IR uses for a data field (`PartyRef`/`DateRef`/`TriggerRef`: unresolved isn't wrong, it's honestly not-yet-known), applied here to a phase's acceptance bar instead:

| # | Criterion (§21, verbatim) | State | Evidence / blocker |
| :-- | :--- | :--- | :--- |
| 1a | *(part of bullet 1)* span-grounding rate exactly 100% — zero ungrounded obligations reach the database, by construction | **Met** — for the grounding mechanism itself | `ground_candidates()` structurally rejects anything not grounded before it can reach compile/typecheck; proven exhaustively at the Extractor + Span Grounder checkpoint in `docs/PROJECT_HISTORY.md` — `test_ground.py`, 20 cases including 2 hypothesis properties that closed out `test_properties.py`'s deferred "grounding invariant". Caveat already on record above (the pipeline-wiring debt bullet): the criterion's literal "reach the database" framing isn't checkable end-to-end even so, since no `obligations` table exists at all yet — a Phase 5-scope gap, not a gap in the grounding gate's own guarantee |
| 1b | *(part of bullet 1)* compile success ≥90% on the dev corpus | **Unmeasured** | The dev corpus now exists (28 documents, verified byte-for-byte, `corpus_manifest.json` v0.5) but no eval harness has been run against it, so no rate has been computed — see the gold-set corpus and batch 1 checkpoint in `docs/PROJECT_HISTORY.md` |
| 2 | Tier-2 fully-correct-IR rate ≥80% on the gold set | **MEASURED, over 18 of the intended 100 items — so still NOT a criterion-2 verdict. Its DENOMINATOR changed 2026-08-25: guideline v0.33 put §9's dual denominator IN FORCE** | **Criterion 2 is now `3/8 = 37.5%` over `len(known_gaps) == 0`** — items IR v1 can represent faithfully — with **`6/18 = 33.3%` over all items reported alongside and labelled NOT the criterion** (`report.py` G3). It is 18% of the specified gold set, not the gold set, so criterion 2 itself is undecided either way. **The all-items figure moved 27.8% → 33.3% purely from §5 clause 5's number rule, and its entire `+1` is `C14-01` — a knowingly OVERSTATED IR** (§8.2: *"stronger than the one the contract imposes"*), now disclosed inline at the numerator per G6 and excluded from the in-force criterion. The dual denominator was adopted on **two independent grounds**: reachability — 7 of 18 locked items (38.9%, 95% CI [20.3%, 61.4%]) are structurally uncompilable, 9 of 18 counting `corpus_artifact_in_span`, so the all-items ceiling is ~39–61% and ≥80% is unreachable there at every point in the interval (**contingent on that rate**); and validity — the all-items numerator can count an IR that is knowingly not a faithful representation as `FULLY_CORRECT`, which **does not depend on the rate** and was already true of `C03-02`/`C04-02` before this session. See guideline §9.1/§19.5 and the failure-driver investigation checkpoint in `docs/PROJECT_HISTORY.md`. **SUPERSEDED 2026-09-02 by the v0.48 freeze pass — the figures above are a dated record and `3/8 = 37.5%` MUST NOT be quoted.** F7's ruling emptied `C04-02`'s `known_gaps`, so it entered the in-force denominator: **criterion 2 is now `? / 9`, denominator known and numerator NOT YET MEASURED.** `3/9 = 33.3%` if the old numerator of 3 holds, `4/9 = 44.4%` if `C04-02` itself scores `FULLY_CORRECT` — and that cannot be resolved from disk, because the same batch staled `C04-117`'s three cassettes, which are `C04-02`'s own. **The item that moved the denominator is exactly the item whose evidence was invalidated.** Resolving it is a dedicated live-spend checkpoint (see the freeze-pass entry in `docs/PROJECT_HISTORY.md` for its four preconditions); guessing the numerator is forbidden. **RESOLVED 2026-09-03, guideline v0.49 — the narrow path, not the full §10 conform.** `C04-117` re-recorded live (3 runs, 4 model calls, `openai/gpt-oss-120b`, prompt `v3`, guideline `v0.48`) and scored by pure replay: `C04-02`'s three runs are `PARTIAL`/`MISSED`/`MISSED` — three genuinely different failure modes (an `obligee` clause miss, a run where nothing was extracted at the segment, and a run whose best candidate span is IoU 0.25, below the 0.5 alignment threshold) — so the modal outcome is `MISSED`, never `FULLY_CORRECT` on any run. **The numerator holds at 3: in-force criterion 2 is `3/9 = 33.3%`. `4/9 = 44.4%` is now closed, not merely undecided — do not quote it.** **CAVEAT, stated inline rather than left implicit, same footing as `C14-02`'s §22.1 non-conformance:** this figure is measured over a population that is still guideline-mixed — the 18-item cassette-covered set carries `v0.28×16`/`v0.48×2` (`C04-02`, `E01-01`) — because the full §10 conforming pass (restamping the remaining 16 items and re-recording all 12 segments, ~35 cassettes) was deliberately not run this session; only the mandatory `C04-117` re-record was, on the ground that the queue entries a full conform would need to close (F5/F6/F9/F12) are real open design questions, not paperwork, and F6 is structurally blocked regardless (§9.4's missing anchor field). See guideline §9.2 (RESOLVED v0.49) and its v0.49 changelog entry for the full record, and guideline §22.3 for a related structural finding this same re-record surfaced: restamping `C04-117` to serve `C04-02` made the cassette newly stale against its own sibling `C04-01` (still `v0.28`, same segment) — staleness didn't disappear, it moved, which is now recorded as a general property of a single-`guideline_version`-per-cassette scheme, not an artifact of this one item |
| 3 | A planted cross-document conflict detected, with a minimal unsat core rendered as one readable sentence | **Met** | `verifier/verify.py`'s worked example A (`tests/verifier/test_verify.py::test_example_a_prohibition_containing_duty_window_is_a_conflict`) — two `ast.Obligation`s built from different `source.segment_id`s (a genuine cross-document pair), a real Z3 `unsat` verdict, a deletion-minimized core, rendered deterministically to one sentence citing both obligations' real dates. Caveat, same shape as criterion 1a's: proven against in-memory obligations from `run_pipeline()`, not against two actually-uploaded documents end to end, since there's still no `obligations` table for a "planted" pair to be read back from — see `docs/VERIFIER.md`'s own scope note |
| 4 | All property tests pass, including round-trip and hash stability | **Blocked — normalizer** | 6 of blueprint §19.3's 8 named properties now built and passing: round-trip, termination (Real obligation parser checkpoint), grounding invariant (Extractor + Span Grounder checkpoint), typecheck soundness and conflict symmetry (closed at the verifier checkpoint in `docs/PROJECT_HISTORY.md`, `tests/verifier/test_properties.py`, real Z3 throughout, both checked against independent Python oracles), and — closed at the ir_hash checkpoint in `docs/PROJECT_HISTORY.md` — **hash stability** (`tests/compiler/test_ir_hash.py`, a genuine hypothesis property plus explicit whitespace/condition-order/confidence regression tests, no persistence involved). The remaining 2 — idempotence, temporal totality — need the normalizer (`normalize.py`) and/or a typecheck-rule decision this phase hasn't reached; neither is Z3-shaped or `ir_hash`-shaped anymore |
| 5a | *(new sub-criterion, not in blueprint §21 verbatim — split out the same way criterion 1 was split into 1a/1b)* the `ir_hash` mechanism itself: same real-world obligation always hashes the same regardless of insignificant differences (whitespace, alias choice once resolved, condition order, confidence, exact span boundary); different obligations never collide | **Met** — for the hash mechanism itself | `compiler/ir_hash.py`'s `canonicalize()`/`ir_hash()`, proven at the ir_hash checkpoint in `docs/PROJECT_HISTORY.md`: `tests/compiler/test_ir_hash.py` (19 tests — exclusion of confidence/`object.raw_text`/source char offsets, inclusion of `segment_id`, alias/condition-atom whitespace insensitivity, a condition-order-swap regression proving multi-condition ordering is canonicalized, a documented-gap regression proving AND/OR-internal ordering is deliberately *not* canonicalized, field-flip distinctness for modality/action/`object.class_`/`segment_id`/alias, and 3 hypothesis properties) plus `tests/compiler/test_ir_hash_db.py` (2 tests, real Neon Postgres, real `typecheck()`: two different real aliases of the same registered party resolve to the same `party_id` and hash identically) |
| 5 | Re-extracting the same document twice produces zero duplicate obligations | **Blocked — `obligations` table + reconciliation logic (Phase 5)** | Proven here for the hash mechanism itself (see 5a); the criterion's literal "zero duplicate obligations" framing isn't checkable end-to-end regardless, since there is still no `obligations` table for a duplicate to be inserted into or rejected from, and no reconciliation/upsert logic exists to make that decision — a Phase-5-scope gap, not a gap in the hash function's own guarantee. Same shape of caveat as criterion 1a's and criterion 3's |

One of the five bullets (3) is now fully `Met`, and one sub-component of another (1a) is. This is the identical set of gaps the open-items paragraph above already names, read against the specific pass/fail bar §21 sets for the phase rather than against the general deliverable list — not new work, just a different lens on the same list. Criterion 3 closing is the verifier checkpoint's own direct contribution to this table; 1b, 2, and 5 are untouched by it (none of the three names Z3 as a blocker).

**The broader deliverables paragraph below — model router expansion (failover, content-hash caching, beyond the budget-tracking-capable prompt registry already built) and embeddings + hybrid search — is real, named Phase 4 scope, but distinct from the five acceptance criteria above.** Neither is referenced by any of the five; §21 lists them as deliverables without tying either to a pass/fail bar. Don't read "acceptance criteria unmet" and "deliverables incomplete" as the same claim — a future session could plausibly clear all five acceptance criteria while embeddings/hybrid search and the rest of the model router remain unbuilt, and that would be a legitimate, if narrow, reading of "Phase 4's acceptance bar is met."

**Blueprint discrepancy resolved at the repair-loop checkpoint, recorded here because this file propagated it:** the Phase 4 scope paragraph below says "parse-error repair loop (≤3 retries, parser message fed back)", copied verbatim from blueprint §21's deliverable list. **The real budget is `max_repairs = 2`** (3 total compile attempts). §21 is the sole outlier; §2's metrics table, §3.9's diagram (`≤2 retries` … `fail 3×`), §6.3 stage 3, §6.8's `max_repairs=2` and §6.11's metrics all say 2, and §3.9 disambiguates it arithmetically (1 initial + 2 repairs = 3 attempts). The blueprint's genuine "≤3" belongs to §13.8's *schema*-failure row ("Output fails schema | Pydantic validation | Retry ≤3 with error fed back"), a separate loop that is **not built** — `SCHEMA_INVALID` is still a terminal rejection with no retry. The scope paragraph below is left quoting §21 verbatim rather than edited, so the discrepancy stays visible; this note is the correction.

Phase 3 (Ingestion, Storage & Segmentation) is closed. **Its full ten-bullet record, including the verdict on each of blueprint §21's four acceptance criteria — the presigned upload flow, file-security hardening, the apps/brain tenancy port, PyMuPDF extraction and its AGPL status, the OCR dev/prod split, async segmentation, SSE streaming, and the fixture corpus — now lives in `docs/PROJECT_HISTORY.md`.** Two rows of the locked-in tech table below (PyMuPDF, OCR engine) point into that record by name, and both carry a **revisit at Phase 9 deployment** decision that is still open.

The debt below is what Phase 3 and Phase 4 have left behind and is *not* archived — it is actively referenced, and every entry is real, tracked, and unresolved unless its own text says otherwise.

Carried-forward technical debt (real, tracked, not blocking Phase 4):
- No public revoke endpoint for invitations — revocation is internal-only (exercised by re-invite rotation). Add a real one if a use case needs it standalone.
- The full HTTP-level cross-tenant leakage suite (§17.8) is still not built out as its own standalone gate — more protected endpoints now exist (sources upload/commit/segment/stream) than when this was first deferred in Phase 2, which makes this more overdue, not less.
- **No generic Idempotency-Key/X-Request-Id mechanism exists anywhere in `apps/core`**, despite both being listed as non-negotiable rules. This was explicitly flagged during Phase 3 as "build as its own dedicated slice before Phase 4, not bundled into unrelated work" — it wasn't built. Carrying forward as overdue debt, not a newly-discovered gap: `invite`, `accept`, `role-update`, and every new Phase 3 endpoint still have none, and no response carries `X-Request-Id`. **`apps/brain`'s own `api/v1/extract_preview` (blueprint §6.1's module layout names it, nothing implements it) is deliberately not being added at the pipeline-wiring checkpoint for the identical reason** — a new mutating-adjacent endpoint should be built correctly against a real Idempotency-Key/X-Request-Id mechanism from the start, not retrofitted once this debt is finally paid off. Tied to this bullet by name so it isn't rediscovered as a separate, vaguer "add an endpoint later."
- **Interim shared-secret gate between apps/core and apps/brain; real per-request org authentication still doesn't exist.** `BRAIN_SERVICE_TOKEN` proves "a caller that knows the secret reached this endpoint," not "this `org_id` claim is trustworthy" — anyone holding the secret (including a buggy or compromised apps/core) can still assert any `org_id`. The real fix (a short-lived, per-request token apps/core mints only after its own JWT verification has established which `org_id` a request may actually touch, cryptographically verified by apps/brain instead of trusted from the body) still doesn't exist. **More relevant entering Phase 4, not less** — the compiler/extraction pipeline will add more apps/brain-side operations touching org-scoped data across this exact boundary.
- Genuine Neon branch-per-PR CI isn't built — CI runs against one persistent branch instead. Real future work if per-PR isolation ever becomes worth the added complexity; apps/brain's Flyway-CLI self-migration step was deliberately written to be correct under either setup already.
- **FIRST OBSERVED INSTANCE, 2026-09-03: a single Neon connect timeout fails the ENTIRE `ci-brain.yml` run before any test executes, because the Flyway step has no retry of any kind.** Not "CI flaked once" — the specific, actionable fact is that step 8 (`Apply apps/core Flyway migrations`) is one unguarded `flyway … migrate` invocation, and steps 9 (`make check-migrations`) and 10 (`Run tests`) are **`skipped`**, not failed, when it dies. A run that never executed a single test reports as a failed run, indistinguishable at a glance from a real test failure.
  - **Evidence pair, same commit `4a60c46`, zero code change between them:** run **`33793954004` FAILED** — `ERROR: Unable to obtain connection from database (jdbc:postgresql://…neon.tech/neondb?sslmode=require) for user 'neondb_owner'`, `SQL State 08001`, `Caused by: java.net.SocketTimeoutException: Connect timed out`, with steps 9/10 skipped. Run **`33794155442` SUCCEEDED** at the identical `headSha`, all 14 steps executed, **542 passed, 5 skipped, 0 failed** in 14m28s. The retry is what *proved* transience rather than the failure merely being *assumed* transient.
  - **Attribution was proven, not assumed**, the same discipline the unapplied-local-migrations entry above was filed under. The commit touches **3 markdown files and 1 eval script**; `git show --name-only 4a60c46 | grep -Ei "migration|\.sql$|\.github|\.ya?ml$|src/"` returns **nothing**, so no path exists from the diff to a Flyway CLI's TCP connection. The parent commit `9cf58f9` passed on run `33754321647` ~7 hours earlier against the same workflow, secrets and Neon branch, and the eight preceding `ci-brain.yml` runs were all `success`. The error is a **socket connect timeout**, not auth (`28P01`), not a checksum conflict, not a schema error — consistent with the free-tier compute being suspended and its cold start exceeding the JDBC connect timeout.
  - **Verified by reading the workflow, not inferred from the failure:** `grep -n "connectRetries\|retry\|retries\|continue-on-error" .github/workflows/ci-brain.yml` returns **nothing**. Flyway's own `-connectRetries` flag is not set, there is no `continue-on-error`, and no step-level retry action. The fix, if one is wanted, is likely a one-line `-connectRetries=<n>` on the existing invocation rather than anything structural — **deliberately not applied here**, because it is a workflow change and this entry is the tracked observation, not the change.
  - **This is the first measured cost of the single-persistent-CI-branch decision directly above**, which is why it is filed immediately after it rather than with the Upstash/Celery flakes: those are broker contention under concurrent load, this is one shared endpoint's availability gating every run with no isolation and no retry. It is also a live instance of the local-versus-CI asymmetry the unapplied-migrations entry names, reached from the CI side — **and a sibling of Standing Principle 7's family**: a red run that means "the database was unreachable" reads identically to one that means "your code is broken," so the conclusion alone is not evidence until the failing *step* has been looked at.
- `source_versions`/versioning (§11.3) is still deliberately not built — `sources.storage_key` still hardcodes `v1`.
- **Accepted, unmitigated gap: PDF structural validation runs in-process, not sandboxed.** `PdfStructuralValidator` runs Apache PDFBox directly against untrusted upload bytes inside the main Spring process; its 10s timeout is the only mitigation against a parser-level DoS, with no isolation against a parser-level memory-safety issue.
- No ArchUnit-equivalent static check yet proves every DB access in apps/brain actually goes through `tenant_scope()` rather than a raw `engine.connect()` — the Python-side analog of the Java `TenantIsolationArchitectureTest` gap that already exists on the Java side, left as follow-up there too.
- Reprocessing/supersession semantics for re-segmenting an already-segmented source are still undecided — currently a safe, loud `409`, not real idempotency. Named as an open question in three places (`V12`'s migration comment, `segmentation_jobs`' `UNIQUE` constraint comment, the segment-trigger endpoint) without being resolved in any of them.
- **A full-suite-only, non-reproducible flake in `test_segment_source.py::test_post_segment_creates_a_queued_job_row_and_returns_202`** — errored once in two consecutive full runs with DB env (412 passed + 1 error, then 413 passed + 0 errors), and reproduced in **no** subset: not in isolation, not in `tests/api` alone, not with `tests/evals`, not in a 49-test `api`+`tasks`+`ingestion` run. The test depends on a live Upstash Redis broker and the failing run took 7m47s. Direct evidence for the two entries below — the unverified Upstash concurrency ceiling and the Celery visibility-timeout rough edges under concurrent access — rather than a defect in the code under test. Characterized and tracked deliberately rather than chased: a single non-reproducible occurrence does not justify more 8-minute runs. Observed 2026-08-22 during the gold-set harness build; git-verified as unrelated to that work, which touches no file under `tests/api`, `src/obligo_brain/api`, or `src/obligo_brain/tasks`.
- **SECOND INSTANCE of the full-suite-only Upstash/Celery flake class, 2026-08-25 — a different file from the one above, same shape.** `test_celery_broker_integration.py::test_a_real_delay_call_is_picked_up_and_executed_by_a_real_worker` (`subprocess.TimeoutExpired`) and `::test_acks_late_prevents_loss_and_the_restore_and_execute_path_is_correct` (*"worker did not report ready within 45.0s"*) both **failed in a full-suite run and both passed in isolation immediately afterward** (exit 0, 36.6s for the pair). Attribution checked rather than assumed: the session's diff touched only `evals/harness/`, `tests/evals/` and two docs — **zero** files under `tests/tasks/` or `src/obligo_brain/tasks/` — and the same two tests had passed in that same session's earlier full run. The failing run took **794s against 307s** for the earlier one, consistent with broker contention rather than a code defect. Direct further evidence for the two entries below (unverified Upstash concurrency ceiling; Celery visibility-timeout rough edges under concurrent access). **Also a live re-demonstration of Standing Principle 7's exit-code sibling**: the run was very nearly reported as clean because a `grep -oE "[0-9]+ passed"` over the summary line matched `451 passed` and silently cut off the `2 failed` immediately preceding it — the pytest exit code is the only reason it was caught. A summary-line grep is itself a detector that fails silently on unhandled input.
- **`ci-brain.yml` does not run on feature-branch pushes at all — only on `push` to `main` or on `pull_request`.** Discovered 2026-08-25 when `evals/batch02-gold-set` was pushed for the first time (27 commits) and `gh run list` returned nothing. Consequence worth stating plainly: **work can accumulate on a branch indefinitely with zero CI coverage**, and this branch carried the entire gold-set effort — corpus, guideline, harness, cassettes, scorer — before any of it was ever CI-verified. Nothing in the record was falsified by this (every gold-set checkpoint entry correctly said "CI not yet checked", and the two entries claiming "CI checked for real" were verified against runs `31871497136`/`31967436769`, both `branch=main event=push conclusion=success`), but the gap is real. **Mechanism used to get coverage: `workflow_dispatch`, which the workflow already enables** — `gh workflow run ci-brain.yml --ref <branch>`.

  **OPEN REPO-POLICY DECISION, deliberately not resolved unilaterally.** The question is *"should feature branches carrying multi-session work get automatic CI coverage, or is `workflow_dispatch`-on-request the intended pattern?"* — a genuine two-sided call, not a defect with a known fix. Three legitimate answers, none pre-selected: **(a)** add feature branches to the `push` trigger — most coverage, but every branch push burns CI minutes and a Neon connection against the single persistent CI branch, which interacts with the unverified Upstash concurrency ceiling below; **(b)** open PRs early so `pull_request` fires — coverage without changing trigger policy, at the cost of long-lived draft PRs; **(c)** keep it as-is and treat `workflow_dispatch` before any checkpoint close-out as the standing rule — zero config change, but it depends on remembering, which is exactly how this branch reached 27 uncovered commits. **This entry is the tracked decision, not a to-do**: whoever takes it should pick deliberately rather than defaulting to (a) because it sounds safest.
- **The local Neon dev branch has NO migrations applied — `V1` through `V18` all report `Future` — so `make check-migrations` fails locally against it. Filed 2026-09-02, deliberately NOT fixed in the session that found it.** Found while running the full suite with real DB env during the v0.48 gold-set freeze pass; `check_migrations.main()` returns 1 and prints all eighteen migrations as unapplied, with its own instruction to run the Flyway migrate recipe against the database.
  - **Attribution was proven, not assumed.** The freeze-pass diff touched only gold-set JSON, `evals/harness/report.py`, two `tests/evals/` files and three markdown documents — **zero** migration, schema, or script files — and the identical failure was reproduced at `HEAD` in a clean `git worktree` with none of that diff present. It is pre-existing environment state, not a regression.
  - **CI is unaffected and stays green**, because `ci-brain.yml` runs its Flyway-apply step *before* `make check-migrations` (run `33624977535`, `542 passed, 5 skipped`). This is exactly the local-versus-CI asymmetry the `V17`-sat-unapplied entry above already names: *"local development has no equivalent, and `make dev` does not migrate."* That entry recorded the gap; this is the gap actually biting.
  - **Deliberately not folded into the freeze-pass commit, on reviewer instruction**: applying Flyway is environment maintenance against a shared branch, and mixing it into a rule-execution commit would blur what that session changed. **It gets its own commit and its own verification** — apply the migrations, then show `check_migrations.main() == 0` against the real branch — the same discipline every other item here is tracked under rather than folded in opportunistically.
- **THE ENTRY ABOVE IS FALSIFIED, 2026-09-18 — THE MIGRATIONS WERE NEVER UNAPPLIED, AND `Future` MEANS THE OPPOSITE OF WHAT IT WAS READ TO MEAN. The bullet is left as written per the corrections-are-new-text discipline; this is the correction, and it is a Standing Principle 7 case whose wrong answer is INVERTED rather than merely wrong.** Found while clearing this entry as the stated precondition for the §10.1 F5/F12 ruling session. **The database was fully migrated the whole time**, proven by querying it directly rather than by re-running the detector that produced the claim: `select version, description, success from flyway_schema_history` returns **18 rows, V1–V18, every one `success=True`**.
  - **THE REAL CAUSE IS CWD, NOT DATABASE STATE, and it was proven by execution rather than inferred.** `check_migrations.MIGRATIONS_DIR` is `apps/core/src/main/resources/db/migration` — **relative to cwd**, deliberately, to match `ci-brain.yml`'s own `-locations=filesystem:…` string rather than compute an absolute path from `__file__`. Run from anywhere but the repo root that path does not exist, Flyway resolves **zero local migrations**, and every row already in the history table comes back **`Future`** — which reads as *"not applied yet"* and in fact means *"applied in the database, unresolvable on disk."* **Controlled comparison, same process, same `.env`, same database, same minute: `main()` returns `0` from `/Users/rajitagrawal/obligo` and `1` from `/Users/rajitagrawal/obligo/apps/brain`.** Nothing else differed.
  - **The failing test was making an unstated assumption, and the module's own comment asserts it as fact.** That comment reads *"every caller of this script (the Makefile target, `ci-brain.yml`, pytest) already runs from the repo root"* — true of the first two, and true of pytest only when pytest is **invoked** from the repo root, which is an invocation convention rather than anything pytest guarantees. `cd apps/brain && pytest` is an ordinary way to run this suite and it silently broke the live test. **CI passes for the same reason the Makefile does**: `ci-brain.yml`'s test step is `uv run --project apps/brain pytest -v` with no `working-directory`, i.e. from the repo root. So the 2026-09-16 entry's *"attribution is now PROVEN — the test PASSES in CI"* is correct about the observation and **wrong about the mechanism**: it is a cwd difference, not the local-versus-CI environment asymmetry it was filed under.
  - **FIXED, in the test rather than the module** (`tests/scripts/test_check_migrations.py`): the live test now `monkeypatch.chdir`s to the repo root, computed from `__file__`, preserving the module's deliberate relative-path design instead of overriding it. **A second test pins the cause so it cannot silently return** — from a `tmp_path` cwd, `main()` must return `1` **even against a fully-migrated database**. Verified by execution: **7 passed from `apps/brain`**, the cwd that used to fail.
  - **The general lesson, and it is the one Standing Principle 7 keeps restating in new costumes: a red result is evidence about a PATH before it is evidence about a DATABASE.** This entry cost two sessions of being carried as real environment debt, including a deliberate decision to give it "its own commit and its own verification," for a detector that was answering a different question than the one being asked. The verification that settled it was the one that could not be fooled by the detector at all — reading the history table directly.
- **OPEN, FILED 2026-09-22: THIS MACHINE'S DNS RESOLVER REFUSES THE WHOLE `ap-southeast-1.aws.neon.tech` ZONE, SO THE DEV DATABASE IS UNREACHABLE BY HOSTNAME LOCALLY. THE NEON ENDPOINT IS ALIVE AND HEALTHY — that was proven, not assumed, and it is the opposite of this entry's own first diagnosis.** Found while trying to confirm batch 4's criterion-2 figures with a real `run_scoring.run()`; the run died in `psycopg` with `failed to resolve host 'ep-lively-fog-az71ccd7.c-3.ap-southeast-1.aws.neon.tech'`.
  - **MY FIRST DIAGNOSIS WAS WRONG AND WAS STATED TO THE REVIEWER BEFORE IT WAS CHECKED: *"the local dev endpoint is gone or rotated."* It is neither.** Corrected here rather than quietly dropped, because the wrong answer would have sent the fix at re-provisioning a database that never needed it.
  - **Proven by execution, in the order that narrows rather than confirms.** The system resolver returns `5(REFUSED)` for the endpoint; `github.com` and `neon.tech` resolve fine on the same resolver, so it is not general DNS failure. **The refusal covers the PARENT ZONE, not just the endpoint** — `ap-southeast-1.aws.neon.tech` and `c-3.ap-southeast-1.aws.neon.tech` are both `REFUSED` — which is what rules out "the endpoint was deleted" (a deleted endpoint gives `NXDOMAIN`, and its parent zone keeps resolving). **Both `8.8.8.8` and `1.1.1.1` resolve it correctly**, and a raw **TCP connect to port 5432 on the resolved address SUCCEEDS**. So the endpoint exists, listens, and is reachable; only this machine's configured resolver — an IPv6 link-local router at `fe80::…%en0`, i.e. the LAN gateway — refuses to answer for that zone.
  - **A STANDING PRINCIPLE 7 INSTANCE IN ITS PUREST FORM, and worth recording as one: `host <name>` against the default resolver looks authoritative while measuring THE ROUTER'S WILLINGNESS TO ANSWER, not the name's existence.** `REFUSED` reads as "this host is not there" and in fact means "I decline to tell you." Same family as the `Future`-means-the-opposite entry directly above — a red result is evidence about a PATH before it is evidence about a DATABASE — reached through a resolver instead of a cwd.
  - **A WORKAROUND EXISTS THAT NEEDS NO SYSTEM CHANGE, and it was used to close batch 4's open claim.** libpq accepts `hostaddr`, which bypasses DNS while keeping `host` for SNI and certificate validation: resolve the endpoint against a public resolver and append `&hostaddr=<addr>` to `DATABASE_URL` for the one command. With that, `run_scoring.run()` executed end to end against the real Neon branch and **confirmed** the figures batch 4's commit could only argue — in-force criterion 2 `5/10 = 50.0%`, all-items `7/15 = 46.7%`, 15 scored, cassette-unscoreable `21 → 33` (exactly the 12 new items, no more and no fewer), teardown clean.
  - **THE REAL FIX IS A CHANGE TO THE USER'S MACHINE, NOT TO THIS REPO, AND IS DELIBERATELY NOT MADE: point the machine (or just this network's interface) at `1.1.1.1`/`8.8.8.8`.** Editing a developer's DNS settings or `/etc/hosts` is outside what a repo task should do unilaterally, and the `hostaddr` workaround removes any urgency. **Nothing in the repo is at fault and no repo change would fix it.**
  - **CI IS UNAFFECTED AND THIS IS NOT THE LOCAL-VERSUS-CI ASYMMETRY THE ENTRIES ABOVE DESCRIBE** — the runner resolves its own `DATABASE_URL` normally, and run [35727516952](https://github.com/Arrkkkk/obligo/actions/runs/35727516952) is green with the Flyway-apply and migration-drift steps both `success`. Filed as its own entry rather than folded into the migrations debt above precisely because the mechanism is different: that one was a **cwd** fault in a detector, this is a **resolver** fault in the network path, and neither is the other.
- **OPEN, FILED 2026-09-24: FIVE OF THE SIX EDGAR CORPUS DOCUMENTS NO LONGER MATCH `corpus_manifest.json`, AND `evals/corpus.py verify` NOW EXITS 1. THE CAUSE IS A 110-BYTE ANTI-BOT SCRIPT TAG EDGAR INJECTS INTO ITS HTML RESPONSES — IT IS PROVEN INERT FOR EVERY DERIVED ARTIFACT, SO THIS IS A PROVENANCE DEFECT, NOT A DATA DEFECT.** Found by running `corpus.py fetch` to read document E02's preamble while authoring `evals/registry/E02.json` for batch 5 — the first session with a reason to touch the corpus in months. **The fetch's own `verify` step failed: `5 of 28 documents do not match the manifest`.**
  - **MEASURED, AND THE DELTA IS THE SAME TO THE BYTE IN ALL FIVE CASES, WHICH IS WHAT RULES OUT CORRUPTION:** `E01 +110`, `E02 +110`, `E03 +110`, `E05 +110`, `E08 +110`. **`E07` matches exactly (`+0`)** and is the one EDGAR document with no injection point — its exhibit carries no HTML `</BODY>`. The injected bytes are a **single, byte-identical** tag in all five, `<script type="text/javascript"  src="/NgtUJUOBPgRUqhLOgA3Ns4KT/…"></script>` — **one distinct `src` value across all five files**, which is a server-side WAF/bot-challenge insertion, not per-document content.
  - **PROVEN INERT, AGAINST THE STRONGEST GATE THE PROJECT OWNS, rather than assumed from the byte count.** The tag **does not survive `extract_text()`**; the pool rebuilds to **1,547 segments exactly** — the pool's own known-answer gate, the one §8.8.3 and §8.8.4 each run before reading any count off it; and **all 50 drawn segments in `batch04/draw.json` reproduce byte-for-byte**, `E02-006` included. So **no gold item, no pool statistic and no drawn segment is affected**, and the corpus of record for every locked item is unchanged. This is why the entry is filed as provenance rather than as a corpus emergency.
  - **§18.2's CLAIM IS NOW A DATED RECORD, AND THE INVERSION IS THE POINT.** *"All 28 documents re-acquired and verified byte-for-byte: 22/22 CUAD, 6/6 EDGAR, zero mismatches"* no longer reproduces. §18 calls hash verification *"the one check in §18 with no interpretation in it"* — so **the one check with no interpretation is now the only one that fails, while every check §18 flagged as a reconstruction still passes.** The CUAD half is untouched: 22/22 still verify, and `CUAD_v1.zip` is cached and hash-pinned.
  - **THE PREVIOUSLY-MATCHING COPIES WERE OVERWRITTEN BY THE FETCH AND ARE NOT LOCALLY RECOVERABLE WHILE THE INJECTION PERSISTS. THIS WAS MY CALL AND I SHOULD HAVE RUN `verify` FIRST.** `fetch` re-downloads EDGAR unconditionally (only the CUAD zip is cached), so the five files on disk are all dated the fetch. **I therefore cannot establish what was on disk beforehand** — `.corpus/` had existed since 2026-08-18 and no `verify` had ever been run against it in this session, so "the old copies matched" is an inference from the manifest's provenance, not an observation. Stated plainly rather than glossed: the honest claim is *"unknown"*, not *"they matched and I destroyed them."*
  - **WHAT A FUTURE SESSION SHOULD ACTUALLY DO, so the diagnosis is not rebuilt from scratch.** In order of preference: **(a)** re-run `corpus.py verify` periodically and, if EDGAR has stopped injecting, re-fetch — the manifest hashes then reproduce and nothing else is needed; **(b)** if the injection is persistent, decide whether `corpus.py`'s EDGAR fetch should **strip the challenge tag before hashing**, which restores hash verification at the cost of making the manifest hash a hash of *normalised* bytes rather than of what the server served — a real trade that changes what §18.2's guarantee means and must not be made silently; **(c)** re-hash the manifest against the new bytes — **rejected on sight and recorded as rejected**, because it makes the manifest track whatever EDGAR happens to serve and destroys the only unambiguous check in §18. **Do NOT treat a red `verify` as evidence the corpus is wrong until the delta has been read**: a constant byte delta with an identical injected `src` across documents is this entry, not corruption.
  - **THE GATE THAT SETTLES IT IS THE POOL REBUILD, NOT THE HASH**, and that is the reusable lesson: the hash answers *"are these the bytes we recorded?"* while the pool rebuild answers *"is the text we reason from unchanged?"*, and those came apart here for the first time. Same family as the `Future`-means-the-opposite and the `REFUSED`-resolver entries above — **a red result is evidence about a CHANNEL before it is evidence about the DATA**, reached this time through an HTTP response wrapper instead of a cwd or a resolver.
  - **CI IS UNAFFECTED and this is NOT the local-versus-CI asymmetry the entries above describe.** `.corpus/` is git-ignored and absent on the runner; the five corpus-gated tests `skipif` themselves out there, which is exactly the arrangement the §8.8.4 close-out recorded. No CI step fetches, verifies or hashes the corpus.
  - **A RECURRENCE of Standing Principle 7's compound-command sibling surfaced in the same run and is logged as a recurrence, not a new entry** — counting that principle's list rather than incrementing it, it holds eight, and the 2026-09-05 case set the precedent that a same-mechanism repeat is a recurrence. A backgrounded `( … ); echo "EXIT=$?" > file` was announced by the harness's job-completion notification as **"exit code 0"** for a `fetch` that exited **1**. New domain (a corpus-integrity check), identical mechanism. **It was caught because the status was captured to a file and read, which is the principle's own corrected remedy (a)** — sequencing, not luck.
- Upstash's actual max-concurrent-connections ceiling for the free tier is still unverified — `worker_concurrency=2` is a deliberately conservative fallback, not a confirmed-safe number.
- A periodic Celery Beat sweep calling `restore_visible()` proactively (rather than relying on worker idle-polling, which has real, verified rough edges under concurrent access) is valid future hardening, not built — the Postgres staleness-reconciliation check is the actual load-bearing safety net today.
- apps/web's dashboard fetches the access token once at load with no silent/scheduled refresh — not a security gap (the token is cryptographically dead at 15 min regardless) but a UX one, worth a note before Phase 6's frontend work.
- **`apps/brain`'s lint/type-check gates exist as a tooling description, not an enforced CI gate.** Blueprint's own `ci-brain.yml` design (§19's CI table) specifies `ruff → mypy --strict → pytest → hypothesis property suite → ...`, but the real `.github/workflows/ci-brain.yml` has only ever run `pytest -v` — confirmed via `git log`: no `ruff` or `mypy` step, and no `ruff.toml`/`mypy.ini`/pyproject `[tool.ruff]`/`[tool.mypy]` section has existed anywhere in this repo's history since the workflow was first scaffolded. Found while auditing what actually gates `apps/brain` during the Phase 4 obligation-parser checkpoint (`compiler/grammar/obligation.lark`, `ast.py`, `parser.py`) — that new code is real pytest+hypothesis-tested but was never linted or type-checked by CI, same as everything else in `apps/brain`. Pre-existing since the workflow's creation, not introduced by that checkpoint. Same shape of gap as the Idempotency-Key mechanism and the HTTP-level cross-tenant leakage suite above: a non-negotiable-sounding piece of tooling that was never actually wired into the gate that's supposed to enforce it.
- **`sources` is missing columns blueprint §8.2 itself specifies (`effective_date`, `jurisdiction`, `page_count`, `lock_fence`) — never migrated, confirmed by reading `V10__create_sources.sql` directly.** Found during the Phase 4 typechecker checkpoint (`compiler/typecheck.py`/`symbols.py`) while tracing what data type rules 2 (`BY` requires a date resolvable "after constant folding against effective_date") and 6 (business-day durations require a jurisdiction to resolve a calendar) actually have to resolve against — the answer is nothing, for either, so `symbols.resolve_date()` unconditionally returns `None` and any `bd`-unit `Duration` is unconditionally flagged `underspecified` in v1. Not fixed here: adding `effective_date`/`jurisdiction` now, with no constant-folding logic or calendar model behind them yet, would be dead schema. **This is a schema decision for whichever future checkpoint actually builds constant-folding against an effective date and/or a real business-day calendar** — add the column(s) then, scoped to what that checkpoint needs, not speculatively ahead of it. `page_count`/`lock_fence` are the same shape of already-specified-but-unmigrated gap, noted for completeness though neither blocks a Phase 4 type rule today.
- **No declared-event-type or defined-terms registry exists anywhere in the repo**, so type rule 4 (`trigger` must be a declared event type or a defined term in the document) has nothing to resolve against — `symbols.resolve_trigger()` unconditionally returns `None`; every `TriggerRef` stays `UNRESOLVED` in v1, same "real function, honestly always-null in v1" treatment as `resolve_date()` above. Building this registry is explicitly **the LangGraph extraction graph checkpoint's scope** (blueprint §6.8/§21: Router → Loader → Segmenter → Extractor → Span Grounder → IR Compiler → Normalizer → Critic → Linker) — a defined term ("the Delivery Date") or declared event ("a Security Incident") can only be identified from the source document's own text, which that graph has access to and this typechecker deliberately does not. Tied to that named future checkpoint, not left as a vague "someday."
- **No cassette demonstrates a real model actually hallucinating a span, actually paraphrasing instead of quoting, or actually emitting malformed/wrongly-shaped JSON** — attempted during the Extractor → Span Grounder checkpoint (2026-08-10), not skipped. Four adversarial segments, each crafted to elicit one specific grounding-gate failure, were sent to the real `llama-3.3-70b-versatile` extraction prompt (temperature 0) exactly once each, no retries. **Real finding: the model was materially more robust than the segments assumed** — only the injection-canary case reproduced its intended category on the first try. A convoluted, line-broken, nested-clause sentence meant to tempt paraphrasing was quoted in full, verbatim (`paraphrase.json`, kept and repurposed as proof Tier B grounds a hard real sentence correctly). A telegraphic/tabular segment meant to tempt synthesis of non-existent wording instead produced two verbatim quotes and one real-but-different failure — a hallucinated `obligor_alias` on a genuine span (`span_not_found.json`, kept and repurposed as a real `NESTED_FIELD_NOT_IN_SPAN` proof, arguably more valuable than the case it was recorded trying to produce). A content-free exhibit placeholder meant to confuse response shape instead got a clean, valid, schema-compliant `{"obligations": []}` back (`malformed_json.json`, kept and repurposed as proof the model doesn't hallucinate obligations from empty content). None of this is a gap in the grounding gate itself — `tests/graphs/test_ground.py` already proves hallucinated spans and paraphrase-shaped rejections exhaustively against synthetic input (including a hypothesis property), and `tests/graphs/test_extract_candidates.py` (added the same session) proves the malformed/wrong-shape-JSON path the same way with a fake `chat_model`, no live call needed. What's still missing is a real, live-recorded example of a model actually failing each of these three ways. Revisit with differently-crafted adversarial segments — or accept this model/prompt combination may simply not fail this way easily — whenever the eval harness (§19.7, not yet built) needs real negative examples. See `apps/brain/tests/cassettes/extraction/README.md` for the full per-file account.
- **Audit, not assumption: every prior Phase 4 checkpoint's "full suite clean" claim was checked against the real commit, not left as a hunch after the repair-loop checkpoint surfaced the silent-skip failure mode.** Only three checkpoints ever stated a suite-wide pass/skip/fail count in this file — typechecker, IR-Compiler, and the repair loop; the IR-spec, parser, and Extractor+Grounder checkpoints make no such claim, so there was nothing to falsify for those three. For the two that do, each commit was checked out into an isolated `git worktree` (main's working tree — this uncommitted repair-loop work — was never touched) and `pytest -q` was run twice: once with no DB env, once with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set against the real Neon branch. Verdict, checkpoint by checkpoint:
  - **Typechecker (`d2c9330`): the claimed "96 passed, 5 skipped, 0 failed" is correct and was genuinely run with DB env set.** Without DB env the same commit produces 63 passed / 38 skipped; with it, exactly 96/5 — a bit-for-bit match. No correction needed; recorded here as a real check, not a rubber stamp, since the failure mode existing elsewhere doesn't mean it's everywhere.
  - **IR-Compiler (`e717b7a`): the claimed "140 passed, 39 skipped, 0 failed" is the *without-DB-env* number, silently, exactly as suspected.** Running the identical commit with DB env set gives **173 passed, 6 skipped** — 33 tests that were skipping for lack of `DATABASE_URL` are read back as "0 failed... clean" with no disclaimer that real-database coverage was absent, unlike the typechecker checkpoint's own bullet in `docs/PROJECT_HISTORY.md`, which explicitly says "proven against real Neon Postgres throughout" before its count. The original bullet is left as written (migrations-are-immutable discipline applied to this file too — corrections are new text, not silent edits) with this note as the correction. The repair-loop checkpoint in `docs/PROJECT_HISTORY.md` already flagged the headline number in passing; this entry is the full audit trail: which checkpoints were checked, which commits, and the exact with/without-DB numbers for both.
- **A migration can sit merged-but-unapplied for a whole checkpoint without anything noticing, and one did.** `V17__create_agent_runs.sql` was written at the Extractor + Span Grounder checkpoint and had **never been applied to the Neon branch** — discovered at the repair-loop checkpoint, when running the Flyway CLI to apply V18 reported "Current version: 16" and migrated 17 *and* 18. Nothing caught it because every test written against `agent_runs` up to that point used a fake `chat_model` and never touched the database. Direct consequence: a real bug shipped undetected (see the repair-loop checkpoint's `RETURNING` finding in `docs/PROJECT_HISTORY.md`). **The general gap: there is no check anywhere — local or CI — that the migrations in the repo are actually applied to the branch the tests run against.** `ci-brain.yml` runs Flyway before its tests, so CI would have applied V17 on the next run; local development has no equivalent, and `make dev` does not migrate. **Closed at the pipeline-wiring checkpoint**: `apps/brain/src/obligo_brain/scripts/check_migrations.py` (`make check-migrations`, a `ci-brain.yml` step run right after that workflow's own Flyway-apply step, and a new dependency `make dev` and `make test` both take on it) — see that checkpoint's entry in `docs/PROJECT_HISTORY.md` for what it actually checks and how it was proven. Left here, not deleted, per this file's own migrations-are-immutable-in-spirit discipline: the incident and the gap it exposed stay part of the record even once fixed.
- **No real-database test exists for `run_extraction()` beyond the single `agent_runs`-write regression test** added at the repair-loop checkpoint (`tests/graphs/test_repair_db.py::test_run_extraction_can_actually_write_its_agent_runs_row`, which lives in that file because it shares its fixture). The Extractor checkpoint's own three-layer strategy has no Layer-3-against-Postgres tier at all — that is exactly how the `RETURNING` bug survived. Anything else in `run_extraction`'s DB path (the segment fetch, the `SegmentTooLargeError` guard's interaction with a real oversized row) is still unproven against real infrastructure.
- **The §13.8 schema-failure repair loop ("Output fails schema | Pydantic validation | Retry ≤3 with error fed back; then review queue") is not built.** `SCHEMA_INVALID` (`graphs/extraction.py`'s `_parse_response`) is still a terminal rejection with no retry. Named here specifically so it is not silently assumed covered by the parse-error repair loop, which is a different loop with a different budget (2, not 3), a different feedback payload (field-level compiler diagnostics, not a Pydantic error) and a different trigger point (post-grounding, not pre-grounding).
- **`compile_quarantine` (V18) has no reader.** Nothing selects from it; `obligo_brain` holds `INSERT` only and `obligo_app` has no grant at all. The intended consumer is Phase 5's review queue (§21 Phase 5's obligations aggregate + UI), which will need its own `SELECT`-only grant migration. Same shape as `agent_runs`' write-only status — deliberate, but it means the "never silently drop" guarantee is currently *durable but invisible*.
- **Confirmed, now that `graphs/pipeline.py` is real (see the pipeline-wiring checkpoint in `docs/PROJECT_HISTORY.md`): a successfully typechecked `ast.Obligation` — including an underspecified one with an unresolved party — is persisted nowhere.** This was originally recorded as a pre-committed scope decision before the code existed, specifically so it couldn't be quietly revisited once code was in front of it; this is the confirmation, not a restatement of the plan. `run_pipeline()` returns it in `PipelineResult.typechecked`; the caller discards it, because there is no `obligations` table to write it into. This is a sharper gap than `compile_quarantine`'s "durable but invisible" one above — a quarantined candidate at least survives in a table with no reader yet, while a *successful* compile doesn't survive the call at all. Deliberate, not an oversight: the `obligations` table is explicitly Phase 5's deliverable ("Obligation aggregate + Spring StateMachine", §21 Phase 5), an event-sourced aggregate owned by `apps/core` (Java), not a table `apps/brain` should insert into directly — `packages/ir-spec/SPEC.md` §11 draws the identical line, excluding the table's lifecycle columns from the compiler's own IR scope. Also affects how literally to read §21 Phase 4's acceptance criterion "zero ungrounded obligations reach the database, by construction" — read here as satisfied for the grounding gate itself (proven by `ground_candidates()`/`tests/graphs/test_ground.py`) and not literally checkable end-to-end even with this checkpoint closed, since there is still no database row for a *compiled* obligation to reach until Phase 5 exists to receive `PipelineResult.typechecked`. Revisit this bullet the moment Phase 5's aggregate exists and something calls `run_pipeline()` and actually persists its output. **A follow-up session tried to build this persistence prematurely and was redirected — see "Obligations-persistence investigation" in `docs/PROJECT_HISTORY.md`** for the confirmed shape (an `apps/core`-owned endpoint `apps/brain` calls, not a direct `apps/brain` write) and why it's still not built.
- **The extraction prompt's clause-boundary handling was only validated for the leading-subordinate-clause failure mode found live in the eval pilot (see the leading-subordinate-clause grounding-fix checkpoint in `docs/PROJECT_HISTORY.md`) — trailing comma-set-off qualifiers, nested "provided that" clauses, and multi-obligation/multi-sentence spans are untested against `prompts/extraction/v2.yaml`'s new instruction and may have analogous issues.** Deliberately not investigated at that checkpoint, on explicit instruction — real, separate, unscoped work. Should be investigated before or during the real 100-item gold-set build, since that's where broader extraction quality gaps would actually surface at scale, not invented speculatively ahead of it.
- **Trailing/adjacent punctuation is a proven recurring risk specifically for regex-anchored, fixed-format-token classifiers over real extracted text** — `ir_compile.py`'s temporal classifier is the one instance that exists today, already patched at its three anchor points (`BY`'s date check, `EVERY`'s unit anchor, `DURING`'s second date anchor). Named as its own item, separately from the clause-boundary bullet directly above, after checking the two were NOT the same root cause before conflating them: that bug was punctuation getting swept into a fixed-format token a regex expects to match exactly; the leading-subordinate-clause grounding bug is a clause-scope/boundary decision in `span_text`, with no regex or token-level parsing involved anywhere in `ground_candidates()` at all (confirmed by re-reading `_locate_span()`/`_is_grounded_substring()` — pure substring containment, Tier A/B, no anchored patterns). A real, checked data point that looked superficially related but wasn't causal: the real v1 cassette for the leading-condition-clauses case did drop a trailing period from `span_text` (`"...to Customer"` vs. the segment's `"...to Customer."`), while the leading-temporal-clause case's v1 `span_text` kept it — and in neither case did the period's presence or absence cause the `NESTED_FIELD_NOT_IN_SPAN` rejection; the excluded leading clause did, regardless. Any future component that parses real quoted text via anchored regex against a closed format — most likely `normalize.py`, not yet built — should be audited for the same anchor-strictness class before shipping, not discovered via a live bug afterward. Explicitly NOT the Span Grounder: `ground_candidates()` does pure substring containment, no regex-anchored token parsing, so it isn't structurally exposed to this specific bug class (its own real gap is the separately-tracked clause-boundary bullet directly above, a different mechanism).

- **The gold-set scoring harness must register parties per-document, not once per org — and this is a hard constraint discovered against real data, not a preference.** `symbols.resolve_party()` returns `None` when its query matches more than one row (`len(rows) != 1`, a deliberate "ambiguous match is still UNRESOLVED, not a guess" decision). Measured across the 28-document corpus: `Client` is a defined party role in **both E02 and E07**, and `Provider` and `Recipient` in **both C17 and E01**. Registering all 28 documents' parties into one eval org would therefore make those three aliases resolve to nothing, flipping locked gold items `E07-01` (obligee `Client`) and `C17-01` (obligor `Provider`, obligee `Recipient`) from `underspecified: false` to `true` and failing §5 clause 8 on both — a scoring failure caused entirely by harness setup, attributed to extraction. **Fix: one org (or one `parties` snapshot) per source document, torn down between documents**, matching what the eval-harness pilot already did for its 2 synthetic parties. Tied by name to **the two-tier eval harness / gold-set scoring-harness checkpoint (§19.7, not yet built)** — this is a precondition of that checkpoint, not a follow-up to it. Found while adjudicating probe finding F5 during the gold-set consolidation pass; the registry-membership rule it depends on is guideline §3.9/§20.4 (F5, option (b)), and the full build requirements — including R2's case-sensitivity trap and R5's startup self-check that makes this class of fixture bug a hard failure rather than a silent clause-8 loss — are guideline §21 R1–R5. A second-order consequence, recorded because it is the same fixture decision: a shared registry would also map two documents' `Vendor` to one `party_id` for any future cross-document conflict-detection work, which is a *different* failure (wrong identity rather than no identity) from the ambiguity failure above.
- **THE BULLET ABOVE IS CORRECT ABOUT THE RULE AND FALSIFIED ON ITS EVIDENCE, 2026-09-24. ALL THREE CITED COLLISIONS WERE CHECKED AGAINST THE CORPUS AND NONE REPRODUCES.** The bullet is left as written per this file's corrections-are-new-text discipline; this is the correction. Found while authoring `evals/registry/E02.json` for batch 5's `E02-006` — **the first time any session had reason to read E02's preamble**, which is why a claim about E02 survived from the 2026-08-22 consolidation pass until now.
  - **`Client` in E02: FALSE.** E02 defines **no bare `Client` role at all**. All **22** occurrences of the token in the document are inside `Client Facility`, which E02 defines as *"the departments and facilities of the CBay customer that originates dictation and work types pursuant to an agreement between the CBay customer and CBay"* — **an unnamed third party guideline §21 R3 excludes in terms**, so E02's registry could never have carried the entry and the E02 half of that collision cannot arise. E07's half is correct: it defines `Client` = Instinet Corporation.
  - **`Provider`/`Recipient` in E01: FALSE.** E01 defines **`Service Provider`** (78 occurrences) and **`Service Recipient`** (57), and **zero** bare `Provider`/`Recipient` — checked for definitional quotation marks as well as by raw count. C17's half is correct and its committed registry carries both as roles.
  - **THE STATED CONSEQUENCE WAS TESTED BY EXECUTION, NOT ARGUED, WHICH IS WHAT MAKES THIS A FALSIFICATION RATHER THAN A QUIBBLE.** A single registry merged from all committed per-document ones resolves `Client` → Instinct Corporation, `Provider` → Provider and `Recipient` → Recipient — **each uniquely**. So `E07-01` and `C17-01` **would not flip**, and the specific scoring failure this bullet predicts does not occur.
  - **THE RULE STANDS AND NOTHING IN THE REPOSITORY CHANGES BECAUSE OF THIS.** Per-document registries remain correct on the ground that survives: `resolve_party()`'s `len(rows) != 1` behaviour is real and deliberate, and `Provider` against E01's `Service Provider` is a genuine **near-miss** that a different document — or a registry authored with E01's own role terms in it — could turn into a real collision. What is falsified is the **measurement offered in support**, not the design. Same footing as guideline §8.9's `on`/`until` rows and §2.4's *"6 segments, contained"* claim: **the evidence was wrong, not the instruction.**
  - **Scoped honestly: measured over the ELEVEN committed registries, not over all 28 documents.** It establishes that the three cited collisions do not reproduce; it does **not** establish that no collision exists anywhere in the corpus, and no such claim is made.
  - **A THIRD COPY OF THE SAME CLAIM EXISTED AND IS CORRECTED TOO — this is the hand-maintained-duplicate hazard this list already records for the §8 tag vocabulary, reached through a docstring instead of a dict.** The claim lived in guideline §21 R1, in `evals/harness/registry.py`'s module docstring, and here. All three are corrected in the same commit; none would have been caught by a test, because none is executable.

- **OPEN, FILED 2026-09-24 PROACTIVELY RATHER THAN WHEN AN ITEM NEEDED IT: five documents carry locked gold items with NO committed `evals/registry/*.json` at all — `C05` (1 item), `C06` (1), `C10` (2), `C13` (3), `E08` (4) — 11 items across 5 documents.** Swept deliberately after `E03-046`'s annotation pass raised a narrower registry question, precisely to break the pattern the `E02-01` checkpoint set, where a missing registry was found *reactively* because an item's `underspecified` happened to turn on it. Not urgent and **not currently wrong**: `run_scoring.py`'s `registry_backed_items()` (the §10.1 F16 fix) already excludes these items from scoring and **discloses them by id**, so nothing is silently mis-scored — but they are unscoreable for a reason that is fixable with authoring work rather than live spend, which is not true of the cassette-stale exclusions they sit alongside in the same G8 list. **A SECOND, SEPARATE FINDING FROM THE SAME SWEEP, AND IT CORRECTS THE NARROWER FLAG THAT PROMPTED IT: all ELEVEN committed registries read `"status": "DRAFT — pending reviewer adjudication"`, including `E02.json`, which was authored AND reviewer-approved on 2026-09-24 (see `E02-01`'s own `annotator_notes`).** So the field is stale across the board and tracks nothing — it is not an `E03`-specific provenance gap, which is how it was first flagged in session before being measured. No harness code reads `status`; it is provenance only. **Deliberately NOT fixed here** — editing registry provenance mid-annotation-pass is the wrong moment, and authoring five registries is its own scoped piece of work, not a bullet to clear opportunistically.

- **UNFIXED PRODUCTION DEFECT: an `UNLESS` carve-out arriving from extraction compiles silently into an ordinary condition, which `packages/ir-spec/SPEC.md` §6 explicitly forbids.** `ir_compile._build_dsl()` wraps every `condition_raws` entry in `_q()` and emits `IF "<raw text>"`, so a carve-out never reaches the grammar's `UNLESS` production at all. Verified by execution, not by reading: `condition_raws = ["unless an exemption is provided"]` returns a clean `ast.Obligation` carrying `Condition(predicate=AtomPredicate(raw='unless an exemption is provided'))` — no `PARSE_ERROR`, no `UNMAPPABLE_TEMPORAL`, no quarantine. **This directly contradicts a frozen spec requirement.** `SPEC.md` §6 chose reading 1 ("no `UNLESS`/exception construct exists in v1's grammar at all") and states the required behavior in terms: a real `UNLESS` in source text *"must cause a loud, routed parse failure — fed back through the repair loop and quarantined for human review if unrepairable. It must **never** be silently dropped, and never silently absorbed into `condition` as if it had been an `IF` clause. Silently discarding a legal carve-out is a correctness bug with real stakes for a compliance tool, not a cosmetic parsing gap."* The IR-spec checkpoint's own mechanical verification (`SPEC.md` §9) proved the **grammar** rejects a literal `UNLESS` — which is true, and which guards only the DSL path that the extraction pipeline never takes. **This is the third instance of the same silent-absorption pattern**, after §17.3's finding that `ir_compile.py` never builds an `AndPredicate`/`OrPredicate` from LLM output despite the AST and grammar both supporting them, and the IR-Compiler checkpoint's trailing-period bug — in every case a guarantee proven at the grammar/DSL layer that the real extraction path bypasses, and in every case found by testing rather than by reading. **Not fixed here** — the fix is not a one-line regex change but a decision about whether `condition_raws` needs real structural validation before compiling, which is the same question §17.3 already flagged as *"a revisitable compiler decision, not a frozen spec boundary."* **Tied by name to the Normalizer checkpoint** (`normalize.py`, blueprint §6.8's Router → Loader → Segmenter → Extractor → Span Grounder → IR Compiler → **Normalizer** → Critic → Linker — not built), which is the named future component that would own structural normalization of condition text, and which the two remaining deferred property tests (idempotence, temporal totality) already block on. Found while adjudicating decision 5 of the gold-set consolidation pass; the gold-set consequence — that `exception_unsupported` items fail *silently* as a clause-7 `PARTIAL` rather than loudly in criterion 1b, putting them on the wrong side of §15.3's loud-versus-silent taxonomy — is recorded in guideline §8.2 and §3.8.1.

- **[ANSWERED 2026-08-25 — see the failure-driver investigation checkpoint in `docs/PROJECT_HISTORY.md`. The narrow reading is FALSIFIED: `conditions.py` measured the model emitting 22 condition strings, 9 of them non-`if`-headed including an `upon`-headed one, so it reads *"'if'-type"* as a CATEGORY. The `v4` bump this entry anticipated is therefore NOT the indicated fix; 3 of 6 non-`if` gold entries were never emitted for reasons unrelated to the marker word, and `E07-01`'s span is byte-identical to gold (IoU 1.000) with the phrases filed in no field at all. Left as written per this file's corrections-are-new-text discipline.]** ~~Unverified prompt-versus-gold alignment on what counts as a `condition`, and it is only settleable by a real extraction run.~~ `prompts/extraction/v2.yaml` asks the model for `condition_raws` as *"a list of literal **'if'-type** conditional phrases from span_text that this obligation depends on."* The gold set, across its 18 locked items, annotates 8 condition entries of which **only 2 are literally `if`-headed** — the rest use `upon` (`"upon the death or mental incapacity of a Principal"`, `"upon reasonable notice and approval by TIBCO"`), a bare participial (`"giving any notice required or permitted under this IP Agreement"`), `As requested by`, `solely to the extent`, and `further provided that`. **The open question is which side is wrong**: either the model reads *"if"-type* as a category (and will emit these), in which case the prompt's wording is merely loose, or it reads it narrowly (and will emit `condition_raws: []` for them), in which case **every one of those 6 entries becomes a guaranteed §5 clause-7 count mismatch** — a scoring failure attributable to prompt wording rather than to extraction quality, and invisible without this being checked. **Not resolvable by reading either artifact**: it is a fact about model behaviour under a specific prompt version, the same class of question as the leading-subordinate-clause finding, which also could only be settled by sampling real output. **Tied by name to the eval harness's first real run against the gold set** (the two-tier eval harness / §19.7 scoring-harness checkpoint, not built) — that run is the first point at which prompt-versus-gold alignment becomes empirically checkable, and the check is cheap there: compare emitted `condition_raws` against gold `conditions` per item and inspect every mismatch whose gold entry is non-`if`-headed. If the narrow reading holds, the fix is a prompt version bump (`v3`) enumerating the markers, **not** a change to gold. Found while adjudicating probe finding F8 during the gold-set consolidation pass; the annotation-side rule that F8 settled (the object-scope removal test, guideline §3.8) is unaffected either way.

- **BLOCKING, DISCOVERED 2026-08-22: the extraction model `llama-3.3-70b-versatile` no longer exists on this Groq account, and every live-measured finding in this project was measured against it.** Found while running the gold-set harness's rate-limit probe (stage 4 step 3) — a minimal `max_tokens=1` completion returned **HTTP 404**, verbatim: `{"error": {"message": "The model \`llama-3.3-70b-versatile\` does not exist or you do not have access to it.", "type": "invalid_request_error", "code": "model_not_found"}}`. `GET /v1/models` on the same key returns **13 models, none of them a Llama chat model**: `allam-2-7b`, `canopylabs/orpheus-arabic-saudi`, `canopylabs/orpheus-v1-english`, `groq/compound`, `groq/compound-mini`, `meta-llama/llama-prompt-guard-2-22m`, `meta-llama/llama-prompt-guard-2-86m`, `openai/gpt-oss-120b`, `openai/gpt-oss-20b`, `openai/gpt-oss-safeguard-20b`, `qwen/qwen3.6-27b`, `whisper-large-v3`, `whisper-large-v3-turbo`. Total spend confirming this: **zero tokens** — both the 404 and the model list returned `usage: {}`.
  - **What is configured against it.** `prompts/extraction/v1.yaml` and `v2.yaml` both pin `model_constraints.model_id: llama-3.3-70b-versatile`, as does `prompts/repair/v1.yaml`. v1.yaml's own comment records *"model_id verified live 2026-08-10"* — true when written, false now. Twenty-one files reference the id in total.
  - **All nine committed extraction cassettes are now historical-only** (`happy_path`, `injection_canary`, `malformed_json`, `paraphrase`, `span_not_found` from 2026-08-10; the four `leading_{temporal,condition}_clause{s}_v{1,2}` from 2026-08-16). They still **replay** — that is what a cassette is for, and the tests that consume them keep passing — but **no live call can reproduce them**, so they can no longer be re-recorded, extended, or used to validate a prompt change. The harness's own staleness rule (stage 3, `model_id` mismatch → hard refusal) is correct and now bites: any cassette recorded under a new model is incompatible with these by design.
  - **The extraction prompt's tuning validity is now unknown.** `v2.yaml` exists specifically to fix the leading-subordinate-clause failure, and that fix was validated by replaying two real segments live against this model twice (the leading-subordinate-clause grounding-fix checkpoint in `docs/PROJECT_HISTORY.md`). Whether v2's wording improves, does nothing, or *regresses* on a different model is unmeasured. The v1→v2 registry flip was an evidence-backed decision whose evidence is no longer reproducible.
  - **The eval pilot's live findings are now attributable to a model that is unavailable**, and should be read that way rather than as properties of "the pipeline": temperature 0.0 not being a determinism guarantee (2 of 10 items differing across identical runs); the `PROVIDE`/`REPORT` action ambiguity that motivated §3.4's action accept-sets; the leading-subordinate-clause exclusion behaviour reproduced identically across two runs; and the adversarial-cassette finding that *"the model was materially more robust than the segments assumed"* — only 1 of 4 crafted failures reproduced. **None of these is invalidated as a historical record; all of them are now un-re-measurable as stated.** The 74%/84% `WITHIN`-parenthetical figures are NOT affected — those are corpus measurements taken against `_WITHIN_RE`, not model output.
  - **Not fixed, and deliberately not worked around.** Substituting a model is not a one-line config change: it invalidates nine cassettes, requires a prompt version bump under this repo's own "prompts are versioned, never edited in place" rule, and changes what every prior live measurement means. Picking one also touches the locked-in tech table's spirit. **Scoped as its own conversation**, the way the verifier and `ir_hash` checkpoints were, covering at minimum: which of the 13 available models are even plausible for structured-JSON extraction (most are audio, guard, or non-English models); whether re-validation against the original adversarial cases is warranted or excessive; what happens to the nine cassettes and the pilot's findings; and whether the `condition_raws` prompt-alignment question in the debt list above must be re-asked from scratch, since its answer is a fact about a specific model's behaviour.
  - **Immediate consequence: the gold-set harness's live recording run (stage 4) is BLOCKED.** Its offline half is complete and committed — limiter, capturing transport, recorder with budget/resume/429 handling, scorer, alignment, report layer, 100 tests. Nothing about that work depends on which model is chosen; only the recording does.

- **`model_id` lives in the prompt YAML, so a model-catalogue change forces a prompt version bump even when the prompt text is unchanged — misrepresenting what the version history says changed.** `prompts/extraction/{v1,v2}.yaml` and `prompts/repair/v1.yaml` each pin `model_constraints.model_id`. Under this repo's own "prompts are versioned files, never edited in place — bump the version" rule, replacing a retired model means cutting a new prompt version whose diff is a single identifier, which makes the version sequence claim a prompt change that never happened. **Deliberately NOT fixed with a bespoke intermediate solution** (reviewer-ruled 2026-08-23): moving `model_id` to `registry.yaml` or inventing a separate config now risks duplicating or conflicting with the model router's eventual design. **The proper fix is `model_id` living in the model router's config**, when that checkpoint is built — see the deliverable below. Recorded so the coupling is a known, accepted cost rather than an unexamined default.
- **The retired-model incident is concrete evidence for the model router's priority, recorded here rather than left as an anecdote.** Blueprint §21 lists "model router with budget enforcement, provider failover, content-hash caching" as a Phase 4 deliverable; it is not built. On 2026-08-22 this project discovered — via a manual verification call during an unrelated build, not via any monitoring — that the single model every prompt pins had been silently removed from the provider's catalogue, invalidating nine cassettes and blocking a checkpoint. **A hard, undetected dependency on one provider's live model catalogue, with no failover and no drift detection, is precisely the production risk that deliverable exists to close.** Weigh this when Phase 4's remaining deliverables are next prioritised: the failure mode is no longer hypothetical, it has happened once and cost a blocked stage.

- **A versioned-artifact indirection is only as real as the tests that don't bypass it.** A registry flip is designed to need no code change; every test asserting a version literal silently converts that design property into a lie, discoverable only when someone actually flips it. **Three instances found in one codebase on 2026-08-23**, all revealed rather than caused by the `gpt-oss-120b` migration: `tests/prompts/test_registry.py`, `tests/graphs/test_repair.py`, and `tests/graphs/test_repair_db.py` each pinned a prompt version literal. The first is the sharpest — **its docstring already claimed to assert against "whatever version is currently active" while its body hardcoded one**, and the clause-boundary-fix checkpoint had moved that literal from `v1` to `v2` rather than removing it, so the claim and the code had disagreed for two versions. The two repair tests had been latent since the repair prompt was written, because `repair` had never been bumped until the migration; their names are about *"carrying the prompt identity"* and *"writes its own agent_runs row"*, neither of which depends on which version is active. All three now read the active version from `registry.yaml` itself. **Related to Standing Principle 7 but mechanically distinct, which is why it is recorded separately rather than folded in:** Principle 7 is about a *detector* silently defaulting to a wrong-but-plausible answer on input it does not handle; this is about a *test* pinning the very thing the mechanism under test exists to vary, so the test passes while the guarantee it appears to verify goes unexercised. **Check for this whenever any indirection is added** — `registry.yaml`'s prompt versions today, and by the same logic the model router's config when that checkpoint is built (see the S1/S2 entries above), since moving `model_id` there would create exactly the same class of bypassable indirection.

- **NAMED MODEL FAILURE MODE, found 2026-08-24: `openai/gpt-oss-120b` reproducibly fails with an empty-content HTTP 400 on the reciprocal-duty (`mutual_obligation`) construct in `C17-021`, apparently from reasoning tokens exhausting the completion budget; this is invisible to both `SCHEMA_INVALID` and the unbuilt §13.8 repair loop, since it never reaches response parsing.** The provider returns `400 invalid_request_error` with `"code": "json_validate_failed"` and — the diagnostic detail — an **empty** `failed_generation`, i.e. the model emitted zero content tokens, so Groq's server-side `response_format: {"type": "json_object"}` validation rejected the completion. The bake-off's own measurement makes the mechanism plausible rather than speculative: reasoning tokens are **85% of all completion tokens** across the 35 recorded cassettes (77,017 of 90,278), so a long enough reasoning trace leaves no content behind. `C17-021` is the reciprocal-duty segment (*"Provider and Recipient shall **each** use its commercially reasonable efforts … into the Systems of the other Party"*) that motivated guideline §8.4's `mutual_obligation` gap class — a construct IR v1 cannot represent at all, which fits an unusually long trace.
  - **Reproduced three consecutive times**, the third as the very first call of a cold process (`slept=0s`), so it is not load-, pacing-, or rate-limit-related. Runs 1 and 2 of the **byte-identical** request had both recorded cleanly, so it is stochastic but strongly segment-correlated, not deterministic and not transient.
  - **Why no repair loop catches it.** It arrives as an HTTP error from the provider *before* `_parse_response` ever runs, so it is neither the `SCHEMA_INVALID` terminal rejection nor the §13.8 schema-failure retry that is still unbuilt. The recorder retries **only** 429 (`graphs`-side and `evals/harness/record.py` alike), so a 400 is fatal by design — correctly, since a 400 is not a pacing problem.
  - **Deliberately NOT worked around, on explicit instruction.** No completion cap, temperature, prompt or other request parameter was changed to force a third recording: a cassette recorded under different parameters is not comparable with the other 35, trading one clean data point for a confound across the whole set. `MODEL_BAKEOFF.md` had already considered and rejected a raised-cap re-test on its own grounds. `C17-021` is accepted at **2 runs**; guideline **§6.1** (added v0.29) is the rule this created, and requires the run count and reason to be stated **inline wherever `C17-021`'s stability number appears**, not in a footnote.
  - **Real evidence for the model router's priority**, alongside the retired-model incident already recorded above: this is a second undetected, unmonitored dependency on one provider's live behaviour, found by a manual run rather than by any alerting.

- **Stage-4 gold-set recording run, closed 2026-08-24 at 35 of 36 cassettes — `apps/brain/evals/cassettes/gold/`, 12 segments × 3 runs less `C17-021`/run3.** Recorded live against `openai/gpt-oss-120b`, extraction prompt `v3`, repair prompt `v2`, guideline `v0.28`, real Neon Postgres, real `run_pipeline()`, one org per source document per guideline §21 R1. **41 of 80 approved calls spent** (38 landed in cassettes, 3 were the failed 400s above); the full set holds **73 model calls over 35 invocations = 2.09 calls/invocation**, distribution `{1: 6, 2: 20, 3: 9}`, against **223,723 total tokens** (133,445 prompt + 90,278 completion). The 2.0-calls sizing was correct; the eval pilot's 1.3 would have under-provisioned by ~60%. Three findings, each measured rather than inferred:
  - **The account's real ceiling is 8,000 TPM, not 12,000.** The limiter read `x-ratelimit-limit-tokens: 8000` off the first response and adapted down from its bootstrap. That is now a **third** mutually contradicting rate-limit figure, alongside the eval pilot's measured 12,000 and blueprint §13.3's stated ~6,000 — direct vindication of `ratelimit.py`'s "the ceiling is discovered, never hardcoded" design, which is the only reason it was caught.
  - **The `C17-021` 400**, recorded as its own debt entry directly above.
  - **Cross-run instability is high.** Of the 11 fully-recorded segments, only **3** (`C03-192`, `C04-087`, `C22-048`) produce identical typechecked/rejected/quarantined counts across all three runs; `C02-021` swings from `0 typechecked / 4 rejected` on run 1 to `3 / 0` on runs 2 and 3. This is a **bucket-count proxy, not** §6's per-item modal outcome, which needs the scorer — but it is independent evidence that the 3× requirement is load-bearing rather than ceremonial.
  - **Guarantee status, stated precisely rather than collapsed into "all held."** *Proven:* staleness (35/35 verified against current segment text/model/prompt/guideline, 0 stale; the missing cassette was refused by `load()` with exactly its intended message rather than yielding a 2-run stability number silently); write-on-completion, proven the hard way when the first process crashed mid-set and lost nothing; idempotent resume, exercised three times (18→30→35 skipped). *Proven separately, and only by a dedicated replay pass, because a recording run structurally cannot exercise them:* **overflow and under-consumption** — all 35 cassettes replayed through `StrictPlayer` against the real pipeline, **0 overflow, 0 under-consumed**, every one consumed exactly `N/N`. *NOT exercised, and therefore unproven here:* the **budget halt** (never came close to exhaustion) and **discard-partial-on-429** (**zero** 429s across the entire run).
  - Teardown held on every invocation including the crash; final residue `{organizations: 0, users: 0}`. One orphaned org from the earlier deliberate crash test was cleared first so the post-run residue check would be a clean signal.
  - **Next step, not started: wire the scorer against these 35 cassettes** (plus `C17-021`'s 2-run partial, caveated per §6.1) and produce the first real criterion-2 numbers. Nothing here moves criterion 1b or criterion 2 — both remain **Unmeasured**; this checkpoint recorded the evidence the scorer will run against, and scored nothing.

- **TRACKED MODEL FINDING, measured 2026-08-25: `openai/gpt-oss-120b` produces run-unstable span boundaries on identical input, varying by more than 3x on one segment.** `C22-048`, same segment text, same prompt v3, same temperature 0.0, three recorded runs: `span_text` came back **151 chars (run 1), 507 chars (run 2), 507 chars (run 3)** against a gold span of 106. Run 1 aligned at IoU 0.702; runs 2 and 3 fell to 0.209 and scored `MISSED`. The over-extension runs swallow a trailing *deemed-delivery* provision that is not part of the duty. **Recorded as its own finding rather than folded into a scoring fix**, because it is model behaviour, not a threshold problem: §4.1's IoU ≥ 0.5 is correctly refusing to pair a 507-char prediction with a 106-char gold span, and lowering the threshold far enough to admit it would make alignment meaningless. This is the same non-determinism-at-temperature-0 already on record from the eval pilot, now measured on span boundaries specifically rather than on field values. **It does NOT explain the 27.8%**: `C22-01`'s aligned run scored `PARTIAL` anyway, failing on `conditions`, so fixing the boundary would change its modal outcome from `MISSED` to `PARTIAL` and contribute **nothing** to criterion 2's numerator.

- **THE BOTTLENECK IS THE COMPILER, NOT EXTRACTION — and this reverses the reading the first scoring run invited.** Refining the miss diagnosis to separate *nothing was extracted* from *the clause was extracted and grounded, then the compiler rejected it* changed four of the nine per-run misses: **`E01-01` (run 2) and `E03-01` (run 1) each had a candidate quarantined at IoU 1.000 — a byte-perfect span — and `C04-03` at 0.988 on all three runs, `C17-02` at 0.738 on all three.** Extraction found those clauses, grounded them, and `run_repair` exhausted its budget with `REPAIR_MADE_NO_PROGRESS`. Span quality across the set is in fact strong (`E07-01` IoU 1.000 on all three runs; `C14-01` 0.994/1.000). **Reported as `MISSED` with no further detail, every one of these reads as an extraction failure and would point the next investigation at the wrong stage entirely** — which is precisely what happened for a full exchange before the refinement existed. `C17-02` is the known `within_preposition` gap (`_WITHIN_RE` demands `of`; the text reads *"within 24 months **following**"*) reaching quarantine exactly as the debt list predicted it would.

- **A design defect in the cassette staleness rule, found while conforming a gold item and NOT fixed: `guideline_version` is checked as a staleness dimension, but the guideline never enters the model request.** `Cassette.verify()` raises `StaleCassette` on a `guideline_version` mismatch. Yet the prompt comes from `prompts/`, the segment text from the corpus, and the guideline governs only how an answer is **scored**, offline and downstream — `cassette.py`'s own docstring justifies staleness in terms of *"the segment text, the prompt version, or the model id"* and never argues for the guideline version, which is checked anyway. **Consequence: the gold set cannot be amended without invalidating all 35 cassettes and re-spending live model calls** — and `C17-021` run 3 is known unobtainable, so a full re-record is not even available. That inverts the purpose of recording cassettes and creates a standing incentive never to correct a rule once it is found wrong, which is the exact failure mode this project has spent repeated sessions undoing elsewhere. **DECIDED 2026-08-25: both fixes REJECTED and the general problem deliberately deferred** (guideline §22.1) — a known wrong number with an honest label beats a harness in which fixing a broken rule costs more than leaving it broken, and the design should not be fitted to a sample of one item. The forcing function to wait for is a second, independent instance: a later correction that also requires conforming. Recorded here as a decision, not a to-do. The two candidate fixes, for that future conversation only, were (it weakens a guarantee that was approved as strict, so it needs its own decision): drop `guideline_version` from the staleness test while keeping it recorded as provenance, and relax `run_scoring.guideline_version_from_items()` to permit mixed stamps pre-freeze — §10 already permits them, batch 1 legitimately spanned six versions, so that invariant is stricter than the guideline requires. Full statement in `docs/eval/GOLD_SET_GUIDELINE.md` §22 and §22.1. **Until decided, `C14-02` stays annotated under the superseded §8.3.1 v0.23 rule and keeps scoring `MISSED`** — a known, stated wrong number in preference to an unexplained one.

- **A first measured instance of the cost the bullet above describes only in the abstract: restamping a shared-segment item does not clear cassette staleness, it moves it to the sibling.** Found 2026-09-03 while resolving criterion 2's `C04-02` numerator (the freeze-pass entry's precondition 2). `C04-117` backs two locked items — `C04-01` (`v0.28`) and `C04-02` (restamped `v0.48` at the freeze pass) — sharing one cassette with one `guideline_version` field. Re-recording it at `v0.48` to unblock `C04-02`'s score flipped which sibling it verifies against: before, valid for `C04-01`, stale against `C04-02`; after, valid for `C04-02`, stale against `C04-01`. **Not a net gain or loss — a transfer of validity between the items sharing the recording.** Costless here only because `C04-01` was never in criterion 2's denominator; a future segment shared by two denominator-relevant items would make this transfer directly costly. Full record, including why this is a general property of a single-`guideline_version`-per-cassette scheme rather than a fact about this one segment: `docs/eval/GOLD_SET_GUIDELINE.md` §22.3.

- **APPROVED AND NOT RUN: the Tier-B prompt probe for the three conditions failures.** Guideline §3.8.2 (v0.33) writes down **gold's** side of the quote-extent and entry-count conventions, and says in terms that **neither rule is validated against model behaviour and neither may be assumed to fix anything.** Whether the extraction prompt can be worded to produce either convention is a fact about `openai/gpt-oss-120b`, settleable only live. **The approved shape: a candidate `v4` probed against exactly `E07-010`, `C11-094` and `C04-117`, recorded as probe cassettes under `evals/probes/` — OUTSIDE the gold set, so no gold cassette goes stale.** Direct precedent: the leading-subordinate-clause checkpoint validated v1→v2 on 2 real segments, live, twice each, before flipping the registry. **Deliberately not started in the v0.33 session**: it needs a drafted `v4` wording (a design decision in its own right), a stated call budget, and live spend, and bundling it into a scoring-rule session would have made the cassette-invalidation boundary harder to see, not easier. **The registry flip itself is Tier C and stays deferred to the §10 freeze re-record regardless of what the probe finds** — a `prompt_version` bump makes all 35 gold cassettes stale at once, and `C17-021` run 3 is unobtainable (§6.1). Do not treat §3.8.2 as evidence of an extraction defect until this probe has run; `C04-01` in particular is recorded there as a **symmetric** ambiguity in which gold's choice was arbitrary.
- **THREE LOCKED ITEMS ARE QUEUED FOR ACCEPT-SET WIDENING AT THE §10 FREEZE PASS, AND ALL THREE KEEP SCORING AS FAILURES UNTIL THEN** — `C02-03` (`invoice_costs`, a compound of two members of its own accept-set), `C11-01` (`principal_interest`, the possessor-anchored reading where all three existing entries anchor on the thing owned), `C02-01` (`"Antares or its Subcontractor"`, the whole coordinated phrase, which §3.5.1's rider now requires and which the model emitted stably on every aligned run). Permitted **only** by §3.4's v0.33 bounded exception, which admits widening during §10's pre-scheduled conforming pass and nowhere else. The resulting understatement — up to 2 items on the in-force criterion — is real, dated, and reported by §9's inline disclosure rather than left to be rediscovered. **Do not widen any of the three ad hoc**: between passes it forces a conforming pass, mixed stamps, and all 35 cassettes stale at once, which is §22's blocker.

- **NEW, UNRECORDED PRODUCTION DEFECT, found 2026-08-26 and NOT fixed: `ir_compile._BY_RE` accepts the AGENTIVE and INSTRUMENTAL `by` and silently compiles it into a deadline. This is the FIRST *loose* defect in the temporal classifier; every other tracked temporal gap is a *strictness* failure.** `_BY_RE` is `^by\s+(.+)$`, so any quoted phrase beginning with the word `by` becomes a `BY` temporal with the remainder as a `DateRef` alias. Verified by execution against the production classifier, not inferred from the pattern: `'by Bellicum'` → `BY "Bellicum"`, `'by law'` → `BY "law"`, `'by the JSC'` → `BY "the JSC"`, `'by the authorized representative of each Party'` → `BY "…"`. All four are agentive/instrumental `by` from real passive constructions in the corpus (*"ordered **by** Bellicum"*, *"as required **by** law"*, *"established **by** the JSC"*).
  - **Nothing downstream catches it, which is why it is a defect and not merely a curiosity.** The phrase is a verbatim substring so `ground_candidates()` passes it; `BY` is a legal grammar form so `parser.parse()` passes it; the alias becomes an `UnresolvedDate` so `typecheck()` only marks the obligation `underspecified` — the identical, honest-looking state a genuinely unresolvable date produces. A fabricated deadline would reach `PipelineResult.typechecked` looking exactly like a correct one.
  - **EXPOSURE IS CURRENTLY ZERO AND IS STATED THAT WAY RATHER THAN INFLATED: this is LATENT, NOT OBSERVED.** Across all 35 gold cassettes the model never quoted an agentive `by` into `temporal_raw` — measured, not assumed. It is one narrowing away, though: `prompts/repair/v2.yaml` asks the model to hunt for a shorter substring that is *"a clean timing phrase"*, and `by Bellicum` / `by delivery …` are the **only two** classifying substrings anywhere in `C04-087`'s span, which is a span the repair loop was asked about three times.
  - **Same family as this list's existing "regex-anchored, fixed-format-token classifiers over real extracted text" entry, reached through a different mechanism** — not trailing punctuation swept into a fixed-format token, but a genuinely ambiguous English preposition that the anchor cannot distinguish from the temporal one. Also the mirror image of Standing Principle 7's usual shape: the detector does not fail silently on unhandled input, it *succeeds* silently on input it should never have handled. **Not fixed here**, and the fix is not obvious — distinguishing temporal `by` from agentive `by` is exactly the kind of free-text disambiguation `ir_compile.py`'s own docstring declines for boolean connectives (*"genuinely ambiguous… a misclassification hazard a closed-vocabulary regex cannot safely resolve"*), so this may belong with the Normalizer checkpoint rather than to a regex tweak.

- **PROVEN BY EXECUTION AND NOT FIXED: the repair prompt's temporal-narrowing instruction is UNSATISFIABLE for the whole `within` class, by grammar construction — so a repair call spent on one is guaranteed waste.** `prompts/repair/v2.yaml` tells the model to *"return a contiguous substring of the span_text you were given… containing just the timing phrase itself and nothing else."* For `within N <unit> <preposition> <trigger>` the natural reading of that instruction is to return the bare duration — and `grammar/obligation.lark`'s `within_form: "WITHIN" duration "OF" trigger_ref` makes the trigger **mandatory**, so a trigger-less `WITHIN 24mo` has **no production at all**. Confirmed against the real `parser.parse()`: `WITHIN 24mo OF "the Commencement Date"` parses, `WITHIN 24mo` is rejected. The prompt instruction and the grammar are in direct conflict for this form.
  - **It is the model's DEFAULT narrowing instinct, observed twice rather than once**: `C17-066` run 3 proposed `'within 24 months'`, and `C11-094` run 1 first proposed `'within a reasonable time'` — both keeping the `within` head and dropping the trigger — before that segment's second call got it right as `'after the Principal's death…'`. Each wasted attempt costs one of only two repair calls per segment.
  - **Not fixed, deliberately.** Any fix touches a versioned prompt (`prompts/` is never edited in place, so it is a `v3` repair prompt) or the grammar, and a prompt version bump is a **Tier C** change under the gold-set guideline: `prompt_version` is a `Cassette.verify()` staleness dimension, so it makes **all 35 gold cassettes stale at once**, and `C17-021` run 3 is unobtainable (§6.1). Same blocker shape as §22's. Tied by name to whichever session next takes a prompt version bump — most plausibly the Tier-B `condition_raws` probe already queued above, which is the only other pending change of the same Tier.

- **A GUIDELINE ARGUMENT FALSIFIED BY MEASUREMENT, recorded here because its exposure is ~8 of the 100 gold items: `GOLD_SET_GUIDELINE.md` §15.3 places the vague-temporal class on the SILENT side of its loud-versus-silent taxonomy, and the class in fact fails LOUDLY.** §15.3 argues *"This is **not** the §8 pattern. §8's known gaps fail loudly — `UNMAPPABLE_TEMPORAL` surfaces in criterion 1b as a visible compile failure. A vague-temporal mismatch would fail silently as a `PARTIAL` in criterion 2 with no attribution."* Measured across the 35 cassettes: whenever the model quotes the vague qualifier into `temporal_raw` — which it does — the class produces exactly the §8 behaviour, `UNMAPPABLE_TEMPORAL` rejecting the **whole candidate**, so the item scores `MISSED`, not `PARTIAL`. **5 instances reached the classifier and all 5 failed**: `'promptly'` (`C14-076` run 2) and `'as soon as practicable'` (`C17-021` run 1) were quarantined; `'within a reasonable time after the Principal's death or mental incapacity'` (`C11-094`, all 3 runs) failed first-pass and was rescued only by repair. A sixth, `'at reasonable intervals'` (`C02-021` run 1), never reached the classifier — lost at schema validation.
  - **Scoped precisely rather than overclaimed: §15.2's RULE is unaffected and only §15.3's CLASS PLACEMENT is wrong.** §15.3's load-bearing argument for `underspecified = false` — that §3.9's trigger list is closed, `typecheck.py` returns `None` for an absent temporal without appending to `missing_fields`, and annotating `true` would fail clause 8 on every such item — is untouched and still correct. What is falsified is the separate claim about **which side of the loud/silent line the class sits on**, and therefore what kind of loss it causes: the class costs **recall, loudly** (a `MISSED`, visible in criterion 1b) rather than **precision, silently** (a `PARTIAL` with no attribution). Different failure, different fix, and §15.4's "report alongside" headline count does not capture it.
  - Recorded in the guideline at **v0.34** as a measurement correction with **no rule changed, no item restamped and no cassette stale** — the same footing as §8.9's own probe-pass-3 data correction, which likewise corrected §8.9's evidence table without touching its rule.

- **§7's held-out check HAS NOW RUN (2026-08-29) and returned REDESIGN — `K = 14/32 = 43.8%`, Wilson₉₅ [28.2%, 60.7%], against bands fixed before the comparison (PROCEED ≤2 / DIAGNOSE 3–5 / REDESIGN ≥6). Read the verdict precisely: it is NOT "the gold set needs to be rebuilt."** Full record in `apps/brain/evals/goldens/holdout/` (pre-registration, seeds, leak classification, sealed cold annotations, comparison, run log) and guideline §7.1. **Correcting a stale figure elsewhere in this file while I am here: the gold set stands at 32 locked items across three batches at guideline v0.42, not the "18 locked items … v0.29 annotation guideline" the Phase 4 scope paragraph above still says** — that text is left as written per this file's corrections-are-new-text discipline, and this is the correction.
  - **§7 could not be run as specified, and that is itself a finding.** Its prospective 2-of-10 withhold was never performed for any batch and is **unperformable under this project's cadence**: every locked item is reviewer-signed-off item-by-item as it is drafted, so no unreviewed item exists to withhold, and §14.4's own rule empties the holdout pool at 100% review coverage. What ran is **one-sided blinding** — a fresh cold annotator blind to the drafts, a reviewer blind to nothing — over a **census** of all 32 items / 22 segments. It is a **cold second-annotator check, not a held-out one**, and every published K must say so, alongside four disclosures: correlated model-family error (K is a lower bound), no reviewer blinding, guideline leakage, and within-pass order-dependence.
  - **The structural result is larger than K: 41 cold items against 32 drafter items over the same segments, while spans agree at IoU ≥ 0.95 on 29 of 32.** Both annotators find the same obligations in the same places. The disagreement is in **labelling** and in **how many items a sentence yields** — not in span identification, which is what §21 criterion 1a certifies and which this run leaves untouched.
  - **HIGHEST-PRIORITY OPEN ITEM, AND IT OUTRANKS EVERYTHING ELSE IN THE RESPONSE: `known_gaps` is not one of §5's eight scored clauses, so no disagreement on it can move K — and the two annotators disagree on it for 19.4% of matched items.** That disagreement moves §9's **in-force criterion-2 denominator** (`len(known_gaps) == 0`) from **15 scoreable items to 17**, a ~13% swing in the denominator of this phase's own headline acceptance figure, entirely invisible to the instrument measuring annotation quality. **The REDESIGN verdict was therefore computed by an instrument blind to a disagreement class that swings the number it exists to support. No future K is trustworthy until this is fixed** — including any K computed to check whether the REDESIGN response worked. **OPEN DECISION, deliberately not taken:** either **(a)** extend §5 to a ninth clause requiring `known_gaps` to match — simple and uniform, but it changes what `FULLY_CORRECT` means and §5 is also the *pipeline's* scoring predicate, and the pipeline cannot emit `known_gaps` at all, so the clause would be vacuous on one side or demand a prediction that cannot be made; or **(b)** a separately-tracked, separately-**pre-registered-threshold** agreement rate reported alongside K and never folded into it, with §9's dual denominator carrying an explicit annotator-swing sensitivity band. The choice turns on a question this project has never asked: **is §5 one predicate serving both pipeline scoring and annotator comparison, or two predicates that happen to share eight clauses?** Ask that first.
  - **What REDESIGN actually scopes — resolve these before drawing any new gold items, in this order.** (0) The `known_gaps` instrument fix above. (1) **§3.6 needs a specificity convention** — among strictly-nested `object_class` label pairs the cold annotator was **more generic 7 times and more specific 0 times**, and where such items nonetheless agreed, agreement was bought entirely by gold's accept-set happening to contain the generic form; `object_class` is also the largest single failure clause (6 of 23). Same critique §5's own number-rule discussion already made — *"a coin flip, not a measurement"* — on a larger, unaddressed axis. (2) **§3.8.2's two conventions need real rulings** — quote extent (`C04-01`: *"further provided that"* vs *"provided that"*) and entry count (`C13-03`: one entry vs two). **Not deferred to the approved-but-never-run Tier-B `v4` probe: this run has made that probe partially redundant**, since a second annotator disagreeing is stronger evidence of underdetermination than model behaviour would have been. (3) **Investigate the exclusion/splitting directional signal** — 6 of 22 segments disagree on item count and **all 6 run the same way, the cold annotator finding MORE items**; determine which specific §2 exclusion rules and §4.3 splitting tests produce it. (4) **Three already-tracked items are marked REINFORCED, not re-litigated** — `C02-03` and `C11-01` are 2 of the 3 items §3.4 already queues for accept-set widening at the freeze pass, and the cold annotator independently produced the very labels that widening exists to admit; `C14-02` is §22.1's deliberately-retained non-conformance, whose predicted consequence duly occurred (it is the single unmatched gold item).
  - **A fourth finding, kept separate from the tracked `temporal_raw` punctuation debt above because it is a different field and a different mechanism:** 19 of 31 matched items differ **only** by a trailing `.` or `;` — gold inclusive, cold exclusive, **zero the other way**. Costless for alignment (IoU stays ≈0.99), but §3.1 does not settle it and two careful annotators split 19–0 on it.
  - **A caveat on the band, recorded as a forward-looking design note and explicitly NOT as grounds to reinterpret this result.** §7's thresholds were designed for a genuinely held-out sample under a **frozen** guideline; this set is none of those things — nothing withheld, guideline DRAFT at v0.42, and authored *from* these very items. The bands were nonetheless fixed before K was known, precisely to prevent post-hoc adjustment, and the substantive findings above do not depend on the exact boundary. **Any argument that the band is mis-calibrated applies to the NEXT run's design, made openly and in advance — never retroactively to this one.**
  - **A leak-controlled comparison is not available from this corpus, and that is a permanent property, not a fixable gap.** Measured: **20 of 32 items have their `span_text` quoted verbatim in the guideline**, and grading the remaining 12 by citation leaves **3** uncontaminated. The L0/L1/L2 gradient ran 0/3, 1/3, 13/26 — disagreement *highest* where leakage is worst — which is **uninterpretable in its intended direction** because leak level is confounded with difficulty: the guideline works through those segments precisely because they were hard. The one channel that survives is the one the guideline never states for any segment — **item count and exclusion decisions** — which is why (3) above carries disproportionate weight.

- **NEW TRACKED FINDING, 2026-09-04, surfaced by the §2.7 (v0.50) per-sentence disposition retrofit — deliberately NOT investigated here, and deliberately NOT folded into `SPLITTING_EXCLUSION_INVESTIGATION.md`'s decisions 2–4, since it is structurally different: a new discovery affecting an already-locked segment's band-eligibility, not a known open question resurfacing.** Retrofitting `C14-076`'s dispositions surfaced two un-annotated sentences beyond its two locked items (`C14-01`, `C14-02`): *"Each party will be solely responsible for any and all taxes imposed thereon, including, without limitation, all income taxes, sales taxes, goods and services taxes"* and *"Israel value added tax shall be added, if applicable, to all amounts payable hereunder and will be paid against submission of appropriate tax invoices."* If both are genuine obligation-bearing clauses, the segment totals **4**, over §2's 1-3 band — a shape identical to `C14-139`'s own exclusion, not merely an under-annotation.
  - **The precedent that looked like it settled this does not, checked directly rather than taken on citation.** `C02-04`'s own `annotator_notes` cite *"the same 'responsible for X' = genuine MUST reading ruled at C14-139's Q1"* — but `C14-139` was excluded whole for exceeding the band, and that ruling survives only as **forward guidance recorded inside its own `exclusions.json` entry**, never cashed into an actual locked item. It was never tested against a real gold item before this session inspected it directly.
  - **Neither `C14-076` candidate matches either real precedent cleanly, in different ways.** `C02-045`'s locked item (`C02-04`) names an explicit act and recipient — *"solely responsible for **the timely payment of** all such VAT and taxes **to the applicable Governmental Authority**"* — which is exactly why it maps to `PAY`. `C14-076`'s first candidate ("solely responsible for any and all taxes imposed thereon") has **no act-verb and no recipient at all** — it only allocates which contracting party bears a cost, not what conduct is required, and is closer in shape to `C14-139`'s own bare "remain fully responsible for the performance of all obligations... jointly and severally with the Permitted Subcontractors" — but that ruling turned specifically on allocating accountability for a **third party's** (subcontractors') conduct with joint-and-several liability language, a materially more substantive allocation than a boilerplate "you pay your own taxes" clause. The second candidate has clearer act-verbs (ADD, PAY, closer to `C02-045` on that axis) but is **fully agentless** — no party named anywhere in the sentence, a shape none of §3.5.3's worked passive-clause cases actually cover (they all name at least one party).
  - **Two real sub-questions, named precisely rather than left implicit:** **(a)** does bare cost-allocation language with no stated act-verb clear §2.5's monitorable-stakes bar, or is "who bears this cost" categorically different from "what must be done" — a question §2.5/§2.5.1's existing worked examples (all rights/discretion clauses) never actually reach; **(b)** how does §3.5.3's agentless-passive rule handle a sentence naming **no party at all**, obligor and obligee both structurally `ABSENT` — a shape §3.5.3's motivating and confirming cases don't cover, since both name at least one in-span party.
  - **Scope and urgency, stated so this isn't picked up piecemeal.** Same tier as `SPLITTING_EXCLUSION_INVESTIGATION.md`'s decisions 2–4 (a dedicated session, not a quick ruling) — it can wait alongside them; there is no urgency forcing it ahead of batches 4–10 resuming. **`C14-076`'s two already-locked items are not themselves in question** — only whether the segment is under-annotated. Full disposition record and both AMBIGUOUS entries: `apps/brain/evals/goldens/batch02/segments/C14-076.json`.
- **INVESTIGATED AND SUPERSEDED 2026-09-05 — `apps/brain/evals/goldens/holdout/C14_076_INVESTIGATION.md`. The entry above is left as written per the corrections-are-new-text discipline; THREE of its claims do not survive checking, and the third changes which of the two candidates is urgent.**
  - **(1) The "no disposition on record" premise is FALSE, and this is a REPO-WIDE record-scope gap, not a slip on one segment.** The cold annotator disposed of **both** sentences explicitly, with rules cited, in `holdout/cold/17_C14-076.json`'s `segment_notes` — candidate 1 as *"a copular 'will be solely responsible' status assertion, which is §8.8's measured 'not an obligation clause at all' class"*, candidate 2 with the **competing §3.5.3 reading written out in full** (obligor `ABSENT`, obligee `ABSENT`, the two-verb split, `if applicable` as a conditions entry). **Cause, and it is not a defect in any script:** `splitting/records.py` scopes itself correctly to *"is there ANY **gold-side** disposition"* — the right question, since §2.1 step 4's rejection sample can only sample gold-side logs — and the §2.7 retrofit **generalised that result** into *"neither annotator recorded why"*, which is a different and falsifiable claim, then offered it as **evidence for one of two competing readings**. **Audited across all 22 retrofitted segments** (`holdout/band_risk/cold_dispositions.py`, six spans pinned as a known-answer check): **all 10 `AMBIGUOUS`/`NOT_ANNOTATABLE` spans have a cold-side disposition** — 8 are full cold gold items with field-level adjudication, 2 are reasoned prose exclusions; **and over the 21 sentences §2.7 actually reaches, 21 of 21 do** (7 items, 14 notes), **including all 10 the gold-side search calls "nothing anywhere."** Three segments assert an explicit false negative across five spans — `C11-094` ×2 and `C04-117` ×1 (*"No disposition of any kind exists on record"*; all three are cold items at IoU 0.994–0.996) and `C14-076` ×2. **The corrections do not all cut the same way:** `C11-094`/`C04-117` cut toward the clause being obligation-bearing; `C14-076`'s candidate 1 cuts toward exclusion. **CLOSED**: `records.py` and `disposition_cost.py` now carry `holdout/cold/*.json` as a labelled **fourth** source, reported alongside and **never merged into** the gold-side tally — a cold disposition is evidence, not authority, and does not satisfy §2.1's safeguard. `SPLITTING_EXCLUSION_INVESTIGATION.md` §3's *"no record anywhere"* sentence is corrected in place. **One cold disposition is CONTAMINATED and must not be relied on**: cold's second ground for rejecting candidate 2 is *"admitting it would take the segment to 3-4 obligation-bearing clauses, pressing §2's 1-3 band for no gain"* — reasoning backwards from eligibility, exactly what `C14-139`'s process fix forbids. Its first ground survives; candidate 1's disposition is uncontaminated.
  - **(2) §2.5 is the WRONG RULE for candidate 1, and the citation that looked like it settled the question does not hold.** §2.5/§2.5.1's operative trigger is **unreviewable discretion**, which candidate 1 has none of; all three worked cases (`C04-026`, `C11-046`, `C14-044` S1) are discretion clauses and none is structurally similar. The governing family is **§8.8's copular status-vs-conduct class**. Cold cites `C04-163`'s *"shall not be responsible for payments"* as *"the same construction affirmatively phrased"* — **it is not**: `C04-163` differs on **two axes at once**, negative polarity **and** an act-nominalisation complement (`payments`), so it cannot settle candidate 1 by citation. **This was found by a detector failing its own known-answer check**, not by re-reading the rule — `responsible_for.py`'s first draft predicted `C04-163` would classify as an act complement and returned "thing" because its vocabulary held `payment` and the text says `payments`; reading the flagged case falsified the complement-type-alone hypothesis the script was written to confirm. **Measured** over the 1,547-segment pool: 121 `be/remain responsible|liable for` sentences in 106 segments (6.9%), as a polarity × complement grid — **act-nominalisation + affirmative 47** (holds every annotatable precedent: `C02-045` ✅, `C14-139`(4) ✅, `C14-028`(5) ✅), **negative 22** (holds the one excluded precedent, `C04-163`), other 37, and **affirmative + BARE BURDEN 15 — the cell candidate 1 falls in, with NO adjudicated instance anywhere in the gold set, across 9 documents** (`C02` `C03` `C10` `C11` `C14` `C15` `C17` `E03` `E08`), including `C15-050`'s near-twin *"responsible for all Taxes or surcharges levied or imposed on it by any Governmental Authority."*
  - **(3) THE BAND RISK IS NOT CONJUNCTIVE, and this reverses which candidate is urgent.** The entry above says *"if this and the next sentence are both genuine, the segment totals 4."* **Candidate 2 alone does it.** Its two verbs have **DIFFERENT OBLIGORS** — the payee adds VAT to its invoice, the payer pays it — and **one item has one obligor slot**, so it cannot be one item however §4.3 is read (§4.3 agrees independently on independent breachability; `C14-139`'s own exclusion counted split clauses (5)–(6) separately, so counting at splitting granularity is established). So: candidate 1 excluded + candidate 2 genuine = **4, over band**. **Candidate 2 alone decides `C14-076`'s eligibility and therefore `C14-01`/`C14-02`'s locked status.**
  - **On sub-question (b), §3.5.3 is NOT uncovered — its rule TEXT anticipates the shape in terms** (*"Where no party is named at all… `obligee` = `ABSENT`"*); only its worked cases don't reach it, so field assignment is decidable by citation. Thing-subject alone doesn't exclude either — `C14-05` is a locked thing-subject agentless passive. **The genuinely novel part is downstream: NO locked item among the 32 has BOTH slots `ABSENT`** (the only two obligor-`ABSENT` items, `C04-03`/`C14-05`, each carry an in-span obligee), so under §5's *"`ABSENT` matches `ABSENT`"* this would be the **first item where two of the eight scored clauses pass on a vacuous match** — a §5/§5.1 question never asked. Recurrence measured: **62 fully-partyless agentless performance passives, 3.1% of modal-bearing sentences** across the 10 registry-backed documents (221, 11.0%, before narrowing).
  - **ESCALATION RE-READ, 2026-09-05 — `C11-094` and `C04-117`'s scopes were set under the same false premise, and are NARROWED, not resolved.** Done before either §14.4 session, so neither starts on a wrong scope. Full record: `C14_076_INVESTIGATION.md` §7. **`C11-094` #2** (*"the executor shall use best efforts to transfer"*): its second reading said *"no annotator or reviewer has ever recorded a reason for treating this differently from `C11-01`"* — cold ran exactly that §4.3.1 step-1 comparison, named the three differing fields, and annotated it `CONFIDENT` with 14 rules cited. **AND THE RETROFIT'S OWN BEST-ATTEMPT READING IS WRONG ON `temporal`** — it asserts `RELATIVE_TO_TRIGGER` *"within 12 months **of** the Principal's death"*, but the contract reads *"within twelve (12) months **from** the date"*; the reading silently substituted the one preposition `_WITHIN_RE` accepts. On the real text it is a `WITHIN` construction and §8.6 rules `temporal: null` + `within_preposition`; it also gives one `conditions` entry where §3.8.2/§17.1 give two. **A reviewer approving that draft would have approved a wrong item** — narrowed scope is *approve/reject cold's item*, never the retrofit's reading. **`C11-094` #3 and `C04-117` #1** (the two `MAY` clauses): each bundles a substantive question with a procedural one, and cold settles only the first — concordantly, and near-verbatim on `C04-117` (identical three stakes, same order, same §2.5.1 `C14-044` distinction). **Both narrow to DECISION 3 ALONE — retroactivity over segments locked at `v0.28` (`C11-01`/`C04-01` both stamp it; §2.5 is v0.39, §2.5.1 v0.41)** — which cold structurally cannot touch, having annotated from the current guideline. **One Decision 3 ruling now disposes of both spans**, not two sessions re-arguing eligibility. Concordance is a **lower bound on error, not proof** (§7.1: correlated model-family error, guideline leakage, no reviewer blinding).
  - **NEW, FILED AS GUIDELINE §10.1 F13 — §8.6 and §8.9 give OPPOSITE instructions for the same structural problem, and §3.7 sides against §8.6.** Both gaps are *a preposition the production regex won't accept* — §8.6 is `_WITHIN_RE` wanting `of`, §8.9 is `_RELATIVE_RE` wanting `before`/`after` — yet §8.6 rules **`temporal: null`** while §8.9 rules **annotate the form**, and §3.7 says *"Known-gap forms (§8) are annotated **normally**. Do not avoid them."* §8.6 also makes gold assert `temporal: null` where the text plainly carries a timing phrase, which §3.7 permits *"only when the obligation genuinely carries no timing phrase."* **Measured: the inconsistency is in the GUIDELINE, not the annotation** — both annotators follow each section literally and consistently (`within_preposition` → null: gold `C17-02`, cold ×2; `relative_trigger_preposition` → the form: gold `C13-03`/`C22-02`, cold ×5). §8.9 (v0.28) was written explicitly as §8.6's sibling and never reconciled it. **Exposure, from §8.6's own measurement: 59 pool-wide occurrences on the rejected side** (`after` 34 / `from` 17 / `following` 8 against `of` 40). Surfaced by the cold annotator, who flagged the tension, resolved it per §8.6 as written, and applied that resolution to `C17-066` too. **NOT a prerequisite for either escalation.**
  - **BOTH REMAIN ESCALATED (§14.4) AND NEITHER IS RULED. Candidate 2 is now the MORE URGENT of the two** — its own session, on the §5/§5.1 both-`ABSENT` scoring question, because it alone decides band eligibility. **Candidate 1 is §10 amendment territory, not a per-item call** (§14.4: *"repeated uncertainty is a missing rule"*) — 15 instances across 9 documents; the starting point is *act-nominalisation complement → obligation-bearing, bare-burden complement → §8.8 status class*, which **must be drafted against `C03-024`** (*"shall remain liable for such Affiliate's **failure** to satisfy its obligations"* — a bare noun complement doing `C14-139`(4)'s accountability-allocation work), since the line is sharp for cost allocation and fuzzy for accountability allocation. `C14-01`/`C14-02`'s field annotations are **not** in question; only the segment's eligibility is.
- **CANDIDATE 1'S ESCALATION IS CLOSED, 2026-09-17, guideline v0.56 — the §10 amendment is `GOLD_SET_GUIDELINE.md` §8.8.4, and the bullet directly above is a DATED RECORD from here on. `C14-076` CANDIDATE 2 AND THE BAND QUESTION ARE UNTOUCHED AND STILL ESCALATED.** Set stays at **35** items. **No rule outside §8.8.4 changed, no item restamped, no cassette staled, and no published figure moved — in-force criterion 2 stays `3/9 = 33.3%`, all-items `5/15`, `K` `14/32`, confirmed by a real `run_scoring.run()` before and after rather than by hand.** Full record: `apps/brain/evals/goldens/holdout/C14_076_INVESTIGATION.md` §9, guideline §8.8.4 and its v0.56 changelog entry.
  - **THE RULE: in `be`/`remain` + `responsible`/`liable` **for** X, THE COMPLEMENT DECIDES, NOT THE CONSTRUCTION.** A complement naming a **performance** the obligor must render or procure is obligation-bearing; one naming a **burden** it must absorb — a cost, a risk, or **another person's breach or failure** — is §8.8's copular status class and `NOT_OBLIGATION_BEARING`. It is §8.8.1's own removal test (conduct or status?) asked of the complement, so it is a §10.2 Part 1 **clarification** and reaches back. Active-verb allocations (*"shall bear the cost of"*) are ordinary obligations and outside the section: §8.8's excluded class has always been the **copular** one.
  - **THE STARTING POINT ABOVE COULD NOT CLASSIFY THE CASE IT WAS REQUIRED TO BE DRAFTED AGAINST, which is why the instruction to draft against `C03-024` mattered more than it looked.** The proposed binary has **no cell** for *"such Affiliate's **failure** to satisfy its obligations"* — no cost word, no act nominalisation — so the census files it in `OTHER`. Not merely untested against the edge case: **structurally unable to reach it**, and invisible from `C14-076`, whose complement the binary classifies fine. The bullet above also calls the line *"fuzzy for accountability allocation"*; it is not fuzzy there, it is **absent**.
  - **`C03-024` is ruled ABSORB, and its contrast pair is in the SAME DOCUMENT** — `C03-016`'s *"responsible for any and all **obligations** of any such Affiliate"* (RENDER) against `C03-024`'s *"liable for such Affiliate's **failure** to satisfy its obligations"* (ABSORB). Same obligor, same Affiliate-accountability subject, opposite complements, so accountability for a third party **splits 4–3** rather than falling to one side. `C04-088` repeats the pair inside one segment. `C03-024` carries an independent second ground: a `so long as` proviso **conditioning a permitted assignment**, not a standalone undertaking.
  - **THE CLASS IS LARGER THAN THE ENTRY ABOVE RECORDS — 24 instances across 11 documents, not 15 across 9.** The earlier cell counted only the cost sub-shape; the class also holds risk allocation (4) and another's-fault (3). **`C03-024`'s own limb is the smallest at 3**, which is exactly why it needed drafting rather than discovering later.
  - **A CITATION IN A COMMITTED ARTIFACT IS FALSIFIED, and it is the reason the rule is about the complement.** `C14-076` candidate 1's `AMBIGUOUS` disposition rested on *"`C02-045`'s locked item uses almost identical 'shall be solely responsible for' language."* `C02-045` is *"responsible for **the timely payment of** all such VAT and taxes **to the applicable Governmental Authority**"* — act, payee, standard; RENDER; locked as `C02-04`, `PAY`. `C14-076` is *"responsible for **any and all taxes imposed thereon**"*. Near-identical construction, opposite complement.
  - **Measured over the same 1,547-segment pool, both §8.8.2 discriminators applied** (`evals/goldens/holdout/band_risk/responsible_for_rule.py`, 11 known answers gated before any total): 121 sentences / 106 segments (6.9%); polarity removes 22; of 99 affirmative, **RENDER 72** (17 docs) vs **ABSORB 24** (11 docs). Discriminator 1: **64 explicit pay/bear-the-cost duties across 17 documents, including all 9 of the 9** that also carry a cost-allocation instance, and **two sentences carry the contrast internally** (`C02-048`'s *"Subject to Antares' **obligation to cover the costs**… AMAG shall be **responsible for** all costs"*; `E08-041`'s *"**shall share equally** … **but shall otherwise be responsible for**"*) — the same evidence shape as §8.8.2's *"failed to defend or indemnify"*.
  - **Three PRIOR filters are named because two already fire on LOCKED segments** — polarity (§8.8.1, now including **subject** negation, `C15-052`), non-party subject (§3.5, `E07-010`), and not-the-main-predicate (§3.2, `C04-117`, whose *"negotiate … **which Party(ies) is/are responsible for** payment"* sits **inside locked item `C04-02`'s own span** as the content of a negotiation rather than an allocation). **`C04-163` is the two-axis proof**: its complement reads RENDER while polarity excludes it — and the classifier's first known-answer block asserted the affirmative class for it and correctly **FAILED**, the detector refusing to let one axis answer for the other.
  - **Exposure against locked segments is ONE, measured not assumed:** of the 22, the construction appears in four and three need nothing — **but they are NOT the same shape, and an earlier draft of this bullet and of §8.8.4 itself got one of them wrong.** `C02-045` is `ANNOTATED` (locked item `C02-04`, `PAY`) and already consistent; `E07-010` is `NOT_OBLIGATION_BEARING` (§3.5, non-party subject), already excluded; **`C04-117` is `ANNOTATED`, not excluded** — it is locked item `C04-02`, whose obligation is `NEGOTIATE`, and prior filter 3 prevents the construction inside it from yielding a **second** item because the responsibility is the *content* of that negotiation. The original wording said `C04-117` and `E07-010` were both "already excluded by the prior filters", which is false of `C04-117`; **caught by pulling the four committed segment files directly rather than trusting the summary**, and corrected in place in §8.8.4, the investigation §9.4 and here. The conclusion is unchanged (no locked item is touched) but "excluded" and "annotated as something else" are not interchangeable. The single ABSORB hit is `C14-076` candidate 1.
  - **AGAINST INTEREST, TWICE, and the first is the load-bearing one: ruling candidate 1 does NOT rescue the band.** Candidate 2 **alone** takes `C14-076` to 4 obligation-bearing clauses (different obligors on its two verbs), so the segment stays `ESCALATED_BAND_RISK`, candidate 2 stays `AMBIGUOUS`, and `C14-01`/`C14-02`'s locked status stays undecided — **the ruling that would most help the segment is the one this amendment declines to make**. Second: the model **does** emit a candidate at candidate 1's span on run 1 of 3 (found by running the scorer, not reasoned about); it was previously an UNEXPECTED prediction and is now set aside as `not_annotatable`. That the extractor reads the clause as obligation-bearing is disclosed rather than left to be found — it is not authority, since §8.8's whole excluded class is populated with clauses that look like obligations.
  - **THE PROXY'S OWN ERROR RATE IS PUBLISHED RATHER THAN PATCHED AWAY: 4 of 99 = 4.0%**, enumerated with reasons in `READING_OVERRIDES`, with raw and read-corrected counts printed side by side. **Two more Standing Principle 7 faults, the fourth and fifth in `band_risk/` alone, both caught by known-answer checks and both kept in the file:** a skip-the-adjectives head extractor that silently returned **adjectives** as heads on 15 instances (*"all **regulatory** activities"*), and — after replacing it with a first-vocabulary-hit scan — a gerund test that classified *"all travel and **living** expenses"* as RENDER on a deverbal **adjective**. **A new §14.4 escalation trigger is named rather than forced:** `C03-107`'s *"responsible for such **determinations**"*, where the determinations are already made earlier in the same sentence, is a nominalisation of a **completed act of the obligor's own** — neither rendering nor absorbing; `determination` is deliberately in **neither** vocabulary.
  - **A FOURTH INSTANCE of §10's own close-out drift, corrected in place: the guideline's `**Status:**` line still claimed 34 items after v0.55 locked `C11-03` as the 35th.** Found by running §10's process note rather than by it being flagged — the note exists precisely because nothing in the normal editing flow revisits that line.
  - **CI DOES NOT RE-VERIFY THIS RULING'S CORPUS EVIDENCE, and the tests say so rather than leaving it to a skip count.** Of the 13 new tests, the 5 that rebuild the 1,547-segment pool are `skipif`-gated on `.corpus/`, which is git-ignored and absent on the runner — **found the hard way: the first CI run for this work FAILED with 5 `FileNotFoundError`s**, because no other test in `tests/evals/` had ever touched the raw corpus and the convention was invisible until it broke. The other 8 read only committed gold JSON and run everywhere; they are the ones covering anything a scoring run can see (the `not_annotatable` wiring, the band staying escalated, no item restamped). **A green CI therefore verifies the ruling's effect on the gold set, not §8.8.4's corpus measurements** — those are checked locally (13 passed with the corpus present; 8 passed / 5 skipped with it hidden, both verified by execution). Same family as the audit entry above where a checkpoint's "0 failed… clean" was a without-DB-env number with 33 tests silently skipping.
  - **CLOSED 2026-09-17 — commits `d33106d` (the amendment) and `156f99a` (the corpus gate), CI run [35247476583](https://github.com/Arrkkkk/obligo/actions/runs/35247476583) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `572 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `156f99a65dfe7349d75deeae8488905910e3bde5`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNTS RECONCILE EXACTLY against the preceding green run (`d9404c6`: 564 passed, 5 skipped), which is the check that they mean something: `564 + 8 = 572` and `5 + 5 = 10` — the 13 new tests, 8 running in CI and 5 corpus-gated, and nothing else moved.**
  - **Forward note, not a finding:** the RENDER side holds a **functional-domain** sub-shape (*"all matters relating to compensation and benefits"*, *"the logistics associated with its marketing efforts"*, *"all regulatory activities"*) — obligation-bearing but unspecific, so likely §8.8 genuine-gap on `action` and hard for §3.6 on `object_class`. Named so the next annotator recognises it rather than rediscovering it.

- **THE §5/§5.1 BOTH-`ABSENT` QUESTION IS ANSWERED, 2026-09-05 — `apps/brain/evals/goldens/holdout/C14_076_INVESTIGATION.md` §8, guideline v0.51. The bullet above is left as written per the corrections-are-new-text discipline; the part of it that said candidate 2 needs "its own session, on the §5/§5.1 both-`ABSENT` scoring question" is DISCHARGED — that question no longer gates anything.** §5 handles a both-`ABSENT` item and is **CONFIRMED FINAL with no amendment**. The premise behind the question — that `ABSENT matches ABSENT` would make it the first item where 2 of 8 clauses *pass vacuously* — **is falsified three ways**, and the concern in fact ran backwards.
  - **`ABSENT` is a HARDER target than a named party, measured, not argued.** Over the 35 gold cassettes aligned with the harness's own `align()`/`iou()`: clause 3 passes **0/3** where gold says `ABSENT` against **32/41 (78.0%)** where gold names a party; clause 4 **13/17 (76.5%)** against **24/27 (88.9%)**. A vacuous clause would score *above* the named-party rate; neither does. Mechanism: §5 can express "absent" only as an empty alias and the model supplies a hallucinated party instead — **`C04-03` emits `"Miltenyi"` on all three runs, the possessive-on-a-**location** inference §3.5.3's reviewer ruling forbids in terms.** Stated limitation: the obligor cell is n=3 (one item × 3 runs, `C14-05`'s segment is not cassette-covered) and is load-bearing only with the 0/81 below.
  - **NEW, FILED AS GUIDELINE §10.1 F14 — the prompt never states that an empty alias is permitted, so §5 clause 3 is measuring an unstated prompt convention rather than extraction quality.** Across all **81** emitted candidates: `obligee_alias` empty **40/81 (49.4%)**, `obligor_alias` empty **0/81 (0.0%)**, Wilson₉₅ upper **4.53%**, both empty **0/81**. `prompts/extraction/v3.yaml` carries exactly one worked example with `"obligee_alias": ""` and **no obligor counterpart** — §3.5 already called that *"undesigned behavior, not a chosen rule"*, and the measurement shows the undesign is **one-sided**. Same class as the tracked `condition_raws` *"'if'-type"* alignment question: a scored clause whose rate is fixed by prompt wording. **Not a capability gap — verified by execution**: a both-`ABSENT` candidate grounds (`_is_grounded_substring` returns `True` on an empty needle, by design), compiles to `MUST "" PAY "" …`, and parses to `UnresolvedParty(alias='')` on both slots. **Tier C** — a `prompt_version` bump stales all 35 cassettes at once and `C17-021` run 3 is unobtainable (§6.1) — so it is **bundled with the already-approved-and-never-run `v4` probe**, not taken alone.
  - **NEW, FILED AS GUIDELINE §10.1 F15 — the genuinely vacuous clause is 8, not 3 and 4, and on a partyless span it is a PROOF rather than a rate.** Grounding requires each alias to be a substring of the span; no substring of a partyless span can resolve; so `_resolve_party()` appends both roles to `missing_fields` and `underspecified` is `True` unconditionally. Gold says `True`. **Clause 8 passes for any prediction whatsoever, including a wholly wrong one** — checked against the real `C14` registry. Set-wide, across the **24** locked items whose documents carry a committed registry, `underspecified` is predicted with **zero** mismatches by *NOT(obligor resolves AND obligee resolves AND temporal is null)*: **clause 8 is a function of clauses 3/4/6's inputs, not an independent eighth check.** The §5 tension this exposes, stated plainly: **§5's own closing line excludes `missing_fields` from the predicate while clause 8 scores `underspecified`, which §3.9 states IS `bool(missing_fields)`.** Two candidate resolutions are recorded and **neither is decided** — (i) keep clause 8 scored and disclose the guarantee, or (ii) move it to reported-not-scored, which is a **retroactive change to what `FULLY_CORRECT` means** and would move criterion 2's numerator. Reaches the **62 partyless agentless performance passives (3.1%)** already measured, not just `C14-076`.
  - **The "first item where 2 of 8 pass vacuously" COUNT is also wrong as stated: 10 of 32 locked items already carry two absence-matched scored slots** (obligee `ABSENT` + `temporal: null`), 30 of 32 carry at least one, and gold `temporal` is null on **27/32 (84.4%)**. A both-`ABSENT` item would be the first with two *party* clauses absent — a smaller and different claim.
  - **A FOURTH detector fault in this family, caught by its own known-answer check and preserved in the script** (Standing Principle 7, third in `band_risk/` alone): `clause8_vacuous.py`'s first draft tested `ABSENT`-ness where the code keys on **resolvability**, reporting 4 spurious mismatches (`C14-01`, `C14-02`, `C02-04`, `C06-01`) — every one a *named but unresolvable* party, which §3.9 trigger 1 covers explicitly. The looser predicate was the defect, not the data; corrected run is 0 of 24.
  - **SCOPE, STATED SO IT IS NOT MISREAD AS A GATE: none of this blocks `C14-076`'s band-eligibility ruling, and F14/F15 are NOT prerequisites for it.** Candidate 2's field assignment — `obligor: ABSENT`, `obligee: ABSENT`, `underspecified: true`, both roles in `missing_fields` — **stands on §3.5.3's and §3.9's existing rule text regardless of how F14 and F15 resolve**, and the band question itself runs on §2's clause count and §4.3, touching §5 nowhere. The ordering is free in both directions; the one wrong move is deferring the band ruling *because* F14/F15 are open. See `C14_076_INVESTIGATION.md` §8.0, which states the non-dependence in full. **No rule changed, no item restamped, no cassette staled, and no published figure moved — criterion 2 stays `3/9 = 33.3%`.**

- **RESOLVED 2026-09-16 — `run_scoring.run()` now executes end to end against the committed gold set. Commit `94a2daa`, CI run [35087036051](https://github.com/Arrkkkk/obligo/actions/runs/35087036051) green: real pytest summary 562 passed, 5 skipped, 0 failed, 0 errors, headSha confirmed `94a2daa6400bf362c5cb167a784febe16fc73c6a` — not just the run's reported conclusion.** Fix: `guideline_version` is now checked per ITEM (`run_scoring.runs_for_item()`), not per run — `cassette.StaleCassette` carries which dimension(s) mismatched, so a guideline-only mismatch excludes just that item (disclosed by id and reason, `report.py`'s new G8 section) while every other staleness dimension stays exactly as strict and fatal as before; §22.1's own "must not happen" list (weakening a staleness dimension, restamping to force one stamp) is untouched. A second, same-shape latent blocker (R5's self-check and the segment loop both assumed a committed §21 R1-R3 registry for every item's document, which batch 3's never-spent documents lack) was found getting the fix to actually complete and closed the identical way — `registry_backed_items()`. **Confirmed by a real run:** 17/32 items disclosed cassette-unscoreable — exactly the 3 already-named known-stale items (`C04-01`/`E01-01`/`C17-02`) plus batch 3's 14 items, no more, no fewer — 15 scored, in-force criterion 2 reproduces `3/9 = 33.3%` for the first time by a run rather than by hand. Real tests added throughout, including planted defects (a guideline-only `StaleCassette` vs. a structural one; an item cannot be both scored and disclosed-unscoreable). **The CI path getting there surfaced two unrelated, pre-existing infrastructure gaps, confirmed and fixed separately from this code change**: (1) the CI Neon/Upstash environment had a DNS-resolution failure reaching the Upstash Redis broker on 10 unrelated tests (`test_segment_source.py`, `test_segmentation_task.py`) — reproduced identically on `main`'s own current HEAD before any of this branch's code was involved, proving it wasn't this diff, and resolved by an Upstash credential rotation; (2) after that, 2 more tests (`test_celery_broker_integration.py`) failed with `redis.exceptions.ConnectionError: Connection closed by server` — diagnosed by local reproduction as a `redis://` vs `rediss://` (TLS) scheme mismatch on `CELERY_BROKER_URL`, confirmed by flipping the scheme inline and watching both tests pass, then fixed by updating the GitHub Actions secret (a local `.env` fix alone does not touch the CI secret — two separate places). No annotation rule changed, no item restamped, no guideline-version bump — this is a harness/tooling fix. **Original entry follows, left as written per this file's corrections-are-new-text discipline:** *BLOCKING, FOUND 2026-09-05 AND NOT FIXED — `run_scoring.run()` CANNOT EXECUTE AGAINST THE COMMITTED GOLD SET AT ANY SINGLE `guideline_version`. Filed as guideline §10.1 **F16** and explicitly ranked ABOVE F5/F6/F9/F10/F12, because it is a broken invariant, not a design question.** `Cassette.verify()` compares `guideline_version` by exact string equality against **one run-level value** — `run_scoring.run()` passes a single version to every `available_runs()` call — and a stale cassette **propagates by design** (`available_runs`' own docstring: *"A stale cassette is not silently treated as a missing run -- it propagates, because replaying it would answer a question that was never asked."*).
  - **Verified by execution against the committed artifacts, not inferred from the code.** Cassettes stand at **`v0.28 ×32` / `v0.48 ×3`**. At `guideline_version="v0.28"` the run dies on **`C04-117`** (all three cassettes `StaleCassette`); at `"v0.48"` it dies on **11 segments**. Separately, all **10 batch-3 segments have no cassette at all**, which raises `"segment <id> has no usable cassette; cannot score it"`. There is no third value that works.
  - **Cause is already on the record and was not a surprise to the artifacts, only to the entry point.** The v0.48 freeze-pass batch restamped `C04-02` and `E01-01` **alone** — precisely the move guideline §22.1's own *"what must NOT happen"* list forbids: *"conforming `C14-02` alone (it yields mixed stamps and the scorer will refuse to run)"*. §9.2 and the v0.49 changelog **do** disclose the resulting mixed-stamp *population* and caveat `3/9 = 33.3%` accordingly; **neither states that the entry point itself stopped executing**, so the breakage has been carried as a caveat about a number rather than as a defect in the instrument that produces it.
  - **Consequence, stated precisely rather than softened: every criterion-2 figure since v0.48 was produced by a scoped or per-segment path, and no session record says which one.** That is a reproducibility gap on this phase's own headline acceptance figure. It also **blocks live confirmation of both 2026-09-05 rulings' stated costs** (§8.6.1's zero-effect claim and Decision 3's denominator arithmetic are computed by hand below and cannot currently be checked by a run).
  - **Not a licence to weaken a staleness dimension.** §22.1 rejected exactly that and its reasoning is untouched. The candidate resolutions — a per-item rather than per-run `guideline_version` comparison; an explicit scoped-subset entry point that names which items it scored; or a full §10 conform restoring a single stamp — are a real choice and **F16 deliberately pre-selects none**. Decide it **before** executing the freeze pass, because the freeze pass is the thing that would otherwise hide it.

- **THE TWO `PENDING_ANNOTATION` SPANS ARE NOW REAL GOLD ITEMS, 2026-09-16, guideline v0.53 — `C11-02` and `C04-06`. The gold set moves 32 → 34. The §8.8 taxonomy question v0.52 flagged into both segment files is RULED, and the two verbs come out on OPPOSITE SIDES of the same test. No published figure moves: in-force criterion 2 stays `3/9 = 33.3%`, all-items `5/15`, `K` `14/32` — and for the first time since F16's fix, that is confirmed by a real `run_scoring.run()` rather than by hand.**
  - **`purchase` → `PROCURE` is DEFENSIBLY MAPPABLE; `defer` → `WITHHOLD` is a GENUINE GAP.** Both adjudicated in one new guideline section (§8.8.3) under §8.8.2's unchanged *"same performance?"* test, over the same 1,547-segment pool, **every detector validated against a named known answer before any count was read off it** (Standing Principle 7; the pool rebuild reproducing 1,547 exactly is its own gate). For `purchase`: **10 of the 12 `procure` sentences in this corpus use the obtain-a-thing sense, including outright buying** (`C06-064` *"merchandise **procured by Agent**"*, `E08-002` *"domestic **procurement**"*), and both §8.8.2 discriminators come back negative — purchase and procure co-occur in **1 of 251** sentences, and that one is a header-spliced fragment with the words in different grammatical roles, not two enumerated duties. §8.8's own accepted `arrange`→`PROCURE` is a **looser** fit than this one. For `defer`: **all 41 withhold-family sentences keep back something OWED, DUE or REQUESTED** — consent ~24, tax-from-payment ~9 (including §8.8's own `deduct`→`WITHHOLD` worked case at `C14-076`), held-back payment/delivery/document ~8 — and **not one postpones a step not yet due**, while the corpus's own boilerplate enumerates the two as **separately failable in 10 of 41 (24.4%) across 5 documents**: *"not to be unreasonably **withheld, conditioned or delayed**"*, the same evidence shape as §8.8.2's *"failed to defend or indemnify"*. Miltenyi's deferral keeps back nothing owed — no duty to include the Products exists, which is exactly what the Parties have yet to agree — so `WITHHOLD` would assert a **stronger** proposition than the contract's, the overstating direction §9.1 ground 2 names as a validity failure.
  - **An argument FOR mapping `defer` was considered and REJECTED, recorded so it is not reinvented:** that nothing is lost because the end-point is carried by the item's own `temporal` field. Unsound — it makes an `action` ruling depend on another field's content for one item, so a bare *"may defer"* with no end-point would decide the same verb the opposite way. **Stated against interest: `defer` occurs EXACTLY ONCE in the whole pool** — `C04-117`'s own sentence — so no base rate for it is claimed; the 41-sentence measurement is of the member being mapped *onto*.
  - **The published-figure consequence v0.52 said this would decide, plus the part v0.52 could not have known.** `C11-02` carries `known_gaps: []`, so it **does** belong in §9.1's in-force denominator: **+1 denominator, +0 numerator** (`3/9 = 33.3%` → `3/10 = 30.0%` **in isolation** — not a prediction of the post-freeze figure, since the freeze pass admits other items at the same moment). **It does not move today.** Both items are stamped `v0.53` against `v0.28` (`C11-094`) and `v0.48` (`C04-117`) cassettes, so **F16's per-item check makes both cassette-unscoreable** and `report.py`'s G8 discloses them. Confirmed by execution: scored items hold at **15**, cassette-unscoreable moves **17 → 19** (exactly the two, no more), both criterion-2 figures unmoved. **Backdating either stamp to force a cassette match was considered and REJECTED** — precisely §22.1's forbidden *"restamping to force one stamp"*, and false besides, since §2.5/§2.5.1/§10.2/§8.8.3 are all rules these items were annotated under and none existed at v0.28. `C04-06`'s exclusion from the in-force denominator was **confirmed rather than assumed**, as its drafting instruction demanded: it carries `relative_trigger_preposition` **and** `action_not_in_taxonomy`, either alone sufficient.
  - **A REAL DEFECT FOUND BY THE TEST SUITE, and the fix is the interesting part: four tests reproducing the PUBLISHED `K = 14/32` run loaded gold by globbing the live directory, so adding any item silently re-baselined a dated, published measurement.** Not a stale assertion to bump from 32 to 34 — **cold annotated at BOTH new spans**, so both new items *pair*, and an unscoped reproduction would have recomputed a published `K` over gold the 2026-08-29 run never saw. Fixed by deriving the run's **population** from the sealed `comparison.json` itself (`published_population()`), so the pin stays correct for every future addition automatically and **fails loudly if a published item ever goes missing** — the failure a hardcoded count cannot distinguish from an addition. Two tests added pinning exactly that: that the two new items are outside the published population *and not inert*, and that the guard fires on a vanished item. **Same family as the tracked "a versioned-artifact indirection is only as real as the tests that don't bypass it" debt entry**, reached from the opposite direction — there a test pinned a version literal the mechanism existed to vary; here a test pinned the *numbers* of a historical run while letting its *population* drift.
  - **Suite: 562 passed, 6 skipped, 1 failed.** The one failure is `test_check_migrations.py::test_main_finds_no_drift_against_the_real_dev_branch` — **the already-tracked local-Neon-unapplied-migrations debt entry above, not this work**; attribution proven the way that entry requires rather than assumed: the diff is 5 modified files and 2 new ones, all gold-set JSON, two `tests/evals/` files and one markdown document, and `git status --porcelain | grep -Ei "migration|\.sql$|\.github|\.ya?ml$|scripts/"` returns **nothing**, so no path exists from it to a Flyway CLI's view of the database. **CLOSED 2026-09-16 — commit `43d1cbe`, CI run [35140465205](https://github.com/Arrkkkk/obligo/actions/runs/35140465205) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `564 passed, 5 skipped` (0 failed, 0 errors), `headSha` confirmed `43d1cbe9f05f901c6eb9a5dad238e990944442fa`, and all 14 steps `success` — "Run tests" genuinely EXECUTED rather than `skipped`, which is the specific thing the Flyway-timeout entry above requires be checked rather than inferred from a conclusion.** **The local failure's attribution is now PROVEN, not merely argued: `test_main_finds_no_drift_against_the_real_dev_branch` PASSES in CI**, where the Flyway-apply step runs before `make check-migrations` — exactly the local-versus-CI asymmetry the unapplied-migrations entry describes. The two counts reconcile exactly, which is the check that they are the same suite: local `562 passed + 1 failed + 6 skipped = 569`, CI `564 passed + 5 skipped = 569`; the `+2` is the migration test flipping `failed → passed` and one locally-skipped test running. Both new tests pass in CI.
  - **Still open on `C11-094`, deliberately untouched: span #2** (*"the executor shall use best efforts to transfer"*) stays `AMBIGUOUS` and the segment stays `ESCALATED` — Decision 3 never reached it, and its own §14.4 session must approve or reject **cold's** drafted item, **not** the retrofit's best-attempt reading, whose `temporal` is wrong. `C04-117` by contrast now **reconciles fully at 3 of 3** and moves to `RECONCILED`. ~~Both new items are `reviewer_status: PENDING_REVIEW` — eligibility was reviewer-ruled at v0.52, the field-level drafting is not yet reviewed.~~ **SUPERSEDED 2026-09-17, guideline v0.54 — BOTH ITEMS ARE REVIEWER-APPROVED AS DRAFTED (`adjudicated_by: reviewer`), and each of the drafter's three flagged calls was settled EXPLICITLY rather than by silence.** (1) **The singleton `action_accept_set` is KEPT** — its stated cost, an in-force-denominator item near-certain to fail clause 2, is **accepted** rather than cured by a widening made *after* the prediction was known, which §3.4's justification test forbids; the freeze pass remains the venue if the set is later judged under-scoped from the sentence alone. (2) **The sibling `object_class` divergence is accepted as TEMPORARY** — `C11-01` `franchise_interest` against `C11-02` `principal_interest`, converging when §10.1 F1's already-queued widening lands. (3) **`conditions: []` is CONFIRMED and GENERALISED INTO A RULE** at guideline §3.8 with a cross-reference at §2.5.1: **a reviewable-standard phrase is not a `conditions` entry** — *"acting reasonably"*, and by the same logic *"in good faith"* / *"to its reasonable satisfaction"*, governs **how** a right is exercised, not **whether** the obligation applies. `C04-06` is the first instance; no locked item had reached the question. **The apparent tension is the point rather than a defect**: the same phrase decides eligibility under §2.5.1 (it is that section's own discriminator against *"sole business judgment"*) and is excluded from `conditions` under §3.8, because the two sections ask different questions. **NOTHING IS RESTAMPED AND NO FIGURE MOVES** — both items keep `guideline_version: v0.53` deliberately, since the v0.54 rule **confirms** their treatment rather than changing it and restamping a confirmed item is the gratuitous conforming event §22.1 and `C11-01`'s own record-only correction both refuse; `reviewer_status`/`adjudicated_by` are provenance read by no harness code, confirmed by a real run reporting in-force criterion 2 unchanged at `3/9 = 33.3%` and all-items `5/15`. **CLOSED 2026-09-17 — commit `96924f8`, CI run [35218133136](https://github.com/Arrkkkk/obligo/actions/runs/35218133136) green on `workflow_dispatch`: real pytest summary `564 passed, 5 skipped` (zero `FAILED`/`ERROR` lines in the whole log), `headSha` confirmed `96924f8215b304593c79b58225754331706f48d6`, all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. The count is bit-for-bit identical to `43d1cbe`'s run, which is the expected result and the check that it means something: this commit changes two provenance fields and two markdown files and should move no test.** **A LOCAL-ENVIRONMENT FACT WORTH RECORDING, and a correction to a guess made in the same session: the full local suite for this commit was KILLED BY THE OS FOR LOW MEMORY mid-run — it did not fail, and no local full-suite result for `96924f8` exists.** The run stalled at 12% with the log unflushed (pytest buffers when stdout is a file rather than a tty, so a stalled log is not by itself evidence of a stalled run — `pgrep` showed the process alive). That stall was **initially and wrongly attributed in-session to the tracked full-suite Upstash/Celery contention flake**; the kill notification falsified that. The targeted `tests/evals` subset was run instead (**250 passed**) and CI was treated as the authoritative full-suite check, which is what closed it. **This is a new local-environment failure mode, distinct from every entry in the flake family above** — those are broker contention producing real test failures; this is the OS reaping the runner, producing *no result at all*, which is a state that reads as neither pass nor fail and must not be reported as either.

- **`C11-094`'S LAST ESCALATED SPAN IS RULED, 2026-09-17, guideline v0.55 — `C11-03`. THE SEGMENT RECONCILES FULLY AT 3 OF 3, AND NO `AMBIGUOUS` OR `PENDING_ANNOTATION` SPAN REMAINS ANYWHERE IN THE GOLD SET.** Set moves 34 → 35. **No rule added or changed** — this applies §8.6.1, §3.5.3, §3.4 and §4.3.1 as they stand. **No published figure moves: in-force criterion 2 stays `3/9 = 33.3%`** (`C11-03` carries `within_preposition`, so it is outside that denominator by construction), all-items `5/15`, `K` `14/32` — confirmed by a real `run_scoring.run()` reporting 35 items, 15 scored, 20 cassette-unscoreable.
  - **Cold's drafted item is APPROVED IN SUBSTANCE AND CORRECTED IN FOUR FIELDS**, each checked against real rule text or by execution rather than taken on citation. Surviving unchanged: distinctness from `C11-01` (§4.3.1 step 1), `MUST` (§16.2's *"shall use best efforts"* row), `TRANSFER` not `USE` (§16.3), verbatim `the executor` obligor, and **two** `conditions` entries (§3.8.2 Rule B + §17.1's marker count, internal `or` kept per §17.2).
  - **(1) `temporal` `null` → the `WITHIN` form** `{WITHIN, 12, mo, "the date of the Principal's death"}`. **Cold is not at fault** — it annotated pre-v0.52, applied §8.6 as literally written, and flagged the tension itself; §8.6's `null` was struck **retroactively** by §8.6.1. **(2) `action_accept_set` → `[TRANSFER, ASSIGN]`** — `CONVEY`/`SELL` are off-taxonomy (execution-checked); §16.3's *"generous"* means generous **within** the taxonomy. Now byte-identical to locked `C11-01`'s. **(3) `obligee` → `ABSENT`**, cold's §3.5.3 citation failing on **two** independent grounds: that section governs **agentless passive** clauses and this one is **active with a stated obligor**, and its rule requires the party *"**named** inside the span"*, which an indefinite future transferee is not — with locked `C11-01`, same segment and same construction, `ABSENT` and `RULED_BY_REVIEWER`. **(4) `missing_fields` gains `"temporal.of"`** — the `WithinTemporal` branch's real path, **not** `"temporal.trigger"`.
  - **THE PHRASE TRIPS TWO GAPS AND ONLY ONE IS TAGGED, verified by execution rather than reasoned about:** `within twelve (12) months **from** …` fails on the preposition (§8.6) **and** on the parenthetical numeral (§8's separate gap); `within 12 months **of** …` is the only variant that classifies. §8.6 forbids filing the two together and the numeral gap has no tag, so `known_gaps` is `within_preposition` **alone**. This is the first item to exercise that boundary, and the first drafted *under* §8.6.1 rather than corrected *by* it — closing §8.6.1's now-dated *"no locked item carries a `WITHIN` temporal, zero of 32"*.
  - **A STALE CROSS-REFERENCE CORRECTED, AND IT HAD ALREADY PROPAGATED INTO A WORK INSTRUCTION — worth recording as a pattern, not a one-off.** `C11-094`'s own 2026-09-05 note says *"§8.6 rules temporal null + within_preposition"*: true when written, **false since v0.52**, and the session instruction for this very ruling inherited it, asking for *"§8.6.1's null/within_preposition treatment"* — a phrase that conflates the superseded §8.6 (`null`) with §8.6.1 (**annotate the form**). Flagged and ruled on the real rule text rather than complied with silently. **The general gap: §10's retroactive sweeps screen ITEMS, not the prose of segment notes, so a superseded rule quoted inside a segment file outlives the amendment that superseded it.** Same family as the tracked "a versioned-artifact indirection is only as real as the tests that don't bypass it" entry — a stale copy of a rule surviving inside an artifact nobody re-screens. **CLOSED 2026-09-17 — commit `d9404c6`, CI run [35221386239](https://github.com/Arrkkkk/obligo/actions/runs/35221386239) green on `workflow_dispatch`: real pytest summary `564 passed, 5 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `d9404c637ddca8e4181d97c7d9c2f66ba021528e`, and the Flyway-apply, migration-drift and "Run tests" steps all `success` — the test step genuinely EXECUTED rather than `skipped`. The count is identical to the two preceding runs (`43d1cbe`, `96924f8`), which is the expected result and the check that it means something: this commit adds a gold item and adjusts two count pins, so it should move no test outcome.**

- **`C14-076`'S BAND-ELIGIBILITY ESCALATION IS RULED AND CLOSED, 2026-09-18, guideline v0.57 — candidate 2 yields ONE item, `C14-06`, from VERB 2 ONLY. THE SEGMENT IS ELIGIBLE AT 3 CLAUSES and `C14-01`/`C14-02` STAY LOCKED, no longer at band risk.** Set moves **35 → 36**. **No rule added or changed** (applies §3.1, §3.2, §3.5.3, §4.3, §8.3.1, §2 as they stand), **no item restamped, no cassette staled, and no published figure moved — in-force criterion 2 `3/9 = 33.3%`, all-items `5/15`, `K` `14/32`, confirmed by a real `run_scoring.run()`** reporting 36 items, 15 scored, cassette-unscoreable 20 → 21 (exactly `C14-06`, `v0.57` against `C14-076`'s `v0.28` cassettes, disclosed by G8). Full record: `holdout/C14_076_INVESTIGATION.md` §10–§11, guideline §10.1's v0.57 changelog entry, `C14-06`'s own `annotator_notes`. **No `AMBIGUOUS` or `PENDING_ANNOTATION` span now remains anywhere in the gold set.**
  - **THE RULING SPLITS THE SENTENCE.** Verb 1 (*"Israel value added tax shall be added, if applicable, to all amounts payable hereunder"*) is a **stative pricing / gross-up statement**, not a commanded act — its object is an **amount**, and its work is to say the sums stated elsewhere are VAT-exclusive. Verb 2 (*"and will be paid against submission of appropriate tax invoices"*) is the **genuine duty**: `MUST`, `PAY`, `obligor: ABSENT`, `obligee: ABSENT` (§3.5.3, same treatment as `C04-06`/`C11-03`), conditioned on invoice submission. **Neither "it is an obligation" nor "it is not" is the ruling**, and stating either flatly loses it.
  - **STATED EXPLICITLY BECAUSE IT IS UNUSUAL: THIS IS NOT RESOLVED BY ADJUDICATED PRECEDENT, SINCE NONE EXISTS.** A full census (`band_risk/tax_treatment.py`, 9 shapes cross-referenced against locked items, committed dispositions and all three exclusion logs) finds **9 tax-treatment sentences in 8 segments across 8 documents, of which EXACTLY ONE had ever been classified — this sentence itself, as `AMBIGUOUS`.** The class is ordinary; the gold set had simply never drawn another. The ruling rests on two things, **each disclosed as what it is and neither oversold**: **(a) `C02-044`'s REAL BUT SINGULAR structural twin** — *"Such VAT and taxes, if any, **will be payable in addition to** the Transfer Price"* — which the corpus itself treats as the **non-obligation half of the same two-job split**, writing the payment half separately and **with an explicit by-agent** (*"will be paid **by AMAG** to Antares"*), same modal; suggestive corpus evidence, one instance, **not binding precedent**. **(b) THE MODEL'S OWN REASONING TRACES** — it reached the sentence **as an obligation on every run** (2 of 3 saying *"Two obligations"*, reaching the split independently) and failed **only** on the known, **unrelated** §10.1 **F14** obligor-schema gap (*"we cannot extract obligor_alias because not present … So skip"*), **not on any judgment that the clause states no duty**; model behaviour, **not authority**. **Against interest on (a):** §3.5.3 exists to admit agentless obligations and two locked items (`C04-03`, `C14-05`) are thing-subject agentless passives, so the payer-naming convention is a **tendency, not a rule**.
  - **A CORRECTION TO MY OWN DISCLOSURE, made before the ruling and against the lean I had given.** The first version of the §10.2-Part-3 disclosure said only *"the model emits nothing at candidate 2's span, 0 of 3 runs"*, which **reads as "the model does not consider this an obligation" and is backwards.** The arithmetic stands (annotating lowers criterion 2 either way) but the characterisation did not; the traces say the opposite. Corrected at investigation §10.6. **A second correction in the same pass: the claim that the construction was "unique pool-wide" was a NARROW-REGEX ARTIFACT** — one pattern's opinion offered as a census — corrected at §10.4 by the 9-instance census above. Both were found only because the reviewer asked for the disclosure to be **shown** and the precedent question to be **checked**, rather than taken on the summary.
  - **§4's ARITHMETIC IS CORRECTED, and it is why the escalation ran for two weeks on a false binary.** *"Candidate 2 ALONE puts the segment at 4"* holds **only** if BOTH verbs are obligation-bearing — a premise §4 never stated. What §4 established is narrower and stands: the two verbs cannot be **one item covering both**. The real decision was **three-way**, and **two of its three branches kept the segment eligible**. Also corrected: the §8.8.4 amendment left `obligation_bearing_clauses` as the integer **3**, silently encoding one of the three live readings; it was moved to an explicit indeterminate while the question was open, **and the regression test asserting the 3 was rewritten to assert it was NOT a number** — a test pinning that integer would have locked in a pre-decision of the escalated question.
  - **SPAN — §8.3.1's option 4, for the same structural reason, with one difference recorded rather than conflated.** §3.1 asks for the minimal contiguous substring stating the **complete** obligation; the bare predicate does not say **what** is paid and would put `object_raw_text` outside the span, which is §8.3.1's option 3, marked *"`NESTED_FIELD_NOT_IN_SPAN` — unreachable by the pipeline"*. So the span runs from the shared subject, `[420:570]`, and unavoidably contains verb 1 — **not an inconsistency**, since `C14-02`'s span `[12:253]` already contains a `NOT_OBLIGATION_BEARING` connective in this same segment. Tagged `shared_subject_split`; **the difference is that for `C14-02` the preceding clause is a sibling ITEM, here a NON-OBLIGATION**, so the spans are not nested against another item and §4.1 cannot collide.
  - **CLOSED 2026-09-18 — commit `d340df2`, CI run [35275037668](https://github.com/Arrkkkk/obligo/actions/runs/35275037668) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `573 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `d340df213c809fc3dad3489edd68d0ddc2759501`, all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNT RECONCILES EXACTLY against the preceding green run (`156f99a`: 572 passed, 10 skipped): `+1` passed is the one net new test — the §8.8.4 file gained `test_c14_06_is_stamped_v057_and_is_therefore_cassette_unscoreable` while its band tests were rewritten in place rather than added — and the skip count is unchanged, since the 5 corpus-gated tests are unaffected.**
  - **`C14-06` IS THE FIRST ADDED ITEM WITH NO COLD COUNTERPART**, and the `K`-population guard now pins that positively. Cold **excluded** this sentence in `segment_notes` prose rather than annotating it, so unlike `C11-02`/`C04-06`/`C11-03` — each of which pairs, and so **could** have moved a published `K` had the reproduction not been scoped — `C14-06` **could not have moved `K` even in principle**. Asserted as its own fact so a future §7 re-run does not assume every added item brings a pair with it.

- **TWO FREEZE-PASS QUEUE ENTRIES RULED TOGETHER, 2026-09-18, guideline v0.58 — §10.1 F12 (accept-set scope) and §10.1 F5 (breadth audit). NO ITEM ADDED, RESTAMPED OR RE-AUTHORED; NO CASSETTE STALED; NO MEMBER REMOVED; NO PUBLISHED FIGURE MOVED — in-force criterion 2 `3/9 = 33.3%`, all-items `5/15`, `K` `14/32`, confirmed by a real `run_scoring.run()` BEFORE AND AFTER rather than by hand.** Set stays at **36**. They are ruled in one pass because they are one question seen twice: F12 fixes what *counts* as a legitimate accept-set member, F5 decides how many to *add* — so F12 constrains F5, and on one item they collide outright. Full record: guideline §3.6.2, §3.6's v0.58 breadth ruling, and the v0.58 changelog entry; screens preserved at `apps/brain/evals/goldens/holdout/accept_sets/`.
  - **F12 — §3.6.1 EXTENDS to the whole `action_accept_set`, retroactive, widening-only, at ZERO measured cost.** Clause 2 tests `prediction ∈ action_accept_set`, so the co-variance argument that makes §3.6.1 a *correction* runs through **membership**; a screen sweeping only the slot was measuring something narrower than its own justification. **Exposure is THREE items, not one** — the queue row's *"exactly ONE"* filled the slot × accept-set-verb cell (`C04-04`, which reproduces exactly) and the member × accept-set-verb cell had **never been measured**: `C04-01`'s `royalty_payment` and `C14-01`'s `payment_deduction` both restate `PAY`. **35 of 36 items already conform**, so the extension restamps nothing. Members are **never** removed — `C04-03`/`product_delivery`'s reason stands.
  - **A FOURTH nominalisation miss by the §3.6.1 screen, recorded as a miss rather than folded into the ruling it produced: `C22-01`'s slot `notice` restates its own `action = NOTIFY`** — inside the **v0.44 screen's own declared scope**, while §3.6.1's v0.48 re-run names only `C02-01` and `C14-01` as remaining slot-level hits. **`notify → notice` is SUPPLETIVE, a different family from the three `y`/`e`-boundary misses** (`deliver→delivery`, `indemnify→indemnification`, `comply→compliance`), so **no stemmer of any kind reaches it** and "stem more carefully" — the fix the first three invited — would not have caught it. This ruling's screen is a hand-authored forms table gated on **12 known answers including all four**, before any count was read off it.
  - **`C22-01` is ADJUDICATED EXPLICITLY under carve-out 1 rather than left an implicit pass, and it CLEARS**: the span reads *"giving any **notice** required or permitted … will give the **notice** in writing"*, so `notice` is the document's own word for the object. **Stated as the hardest instance carve-out 1 has faced**, on two axes at once — the precedents (`retained_sample`, `withholding_tax`) put the restating token on a **modifier** over a distinct head, leaving a clean head-only label available (`sample`, `tax`), while `notice` is a **1-token label whose entire content is the action nominal**; and it is the **only item in the set with no member free of restated material**.
  - **THE COGNATE-OBJECT RESIDUE IS ACCEPTED AS IRREDUCIBLE, NOT DESIGNED AROUND.** For `NOTIFY`/notice, `GRANT`/grant, `REPORT`/report, clauses 2 and 5 co-vary because the object of the duty simply **is** the action's nominal; forcing a non-cognate label would buy instrument independence with an unfaithful annotation, the worse trade for a gold set whose purpose is faithfulness. **§3.6.1 BOUNDS the co-variance defect; it does not eliminate it**, and §3.6.2 now says so rather than letting a clean screen be read as clean independence.
  - **F5 — NO SYSTEMATIC RETROACTIVE BREADTH PASS. F1's three items only, plus a forward head-only rule for batch 4 onward. The audit's scope is REDUCED, not expanded.** Measured, the six clause-5-failing items decompose into three mechanisms: three head-only-depth items, F1's two, and **one genuine model error** — `C17-01`'s `efforts`, which no defensible head-noun reading of its span supports and which must **not** be widened, since clause 5 is the only check on an open vocabulary.
  - **AGAINST INTEREST, and it is the finding that decided the scope: the largest STRUCTURAL breadth gap is worth ZERO on the in-force criterion today.** Head-only member coverage is **13/36 (36%)** against the model emitting head-only **28%** of the time — so the gap is real and §3.6/Ruling 3's v0.45 anchor survives the four items added since — but `C04-03` and `E01-01` carry `known_gaps` and sit outside §9.1's in-force denominator, and `C02-01` already passes clause 5 on runs 2–3. Retro-widening those three moves nothing **while being authored with the failure list visible**, so it is **refused, not deferred**.
  - **F1's VALUE IS NOW MEASURED BY A REAL RUN, NOT HAND-COMPUTED — `3/9 = 33.3%` → `5/9 = 55.6%`.** Applied to a **scratch copy** of the goldens and scored against the real cassettes and a real database, with the repo untouched. **Movers are `C02-01`** (`obligor` its **only** failing clause; the model emits *"Antares or its Subcontractor"* on every aligned run, which does not resolve in the committed `C02` registry — verified by reading it — and so takes `_party_matches`' unresolved branch where §3.5.1's `obligor_accept_set` is read) **and `C02-03`** (`object_class` its **only** failing clause, with the repair loop rewriting the off-taxonomy `INVOICE` → `PROVIDE`, visible in the cassette, so clause 5 is genuinely reached rather than the candidate being rejected before it). **`C11-01` is worth ZERO, stated first rather than buried: the item this project has cited most often as breadth's motivating case fails `conditions` and `obligee` independently**, so its widening removes one of three failing clauses and changes no verdict.
  - **SEQUENCING IS NOW BINDING RATHER THAN ADVISORY.** At a measured **+2 on a 9-item denominator**, §3.6's monotone-widening hazard is live at full strength — a delta that large straddling two published baselines makes pipeline improvement and a loosened bar indistinguishable. **F1 lands as ONE ATOMIC freeze-pass event**, never split across baselines, with its post-widening figure re-confirmed by a real run at the moment it lands. **F1's EXECUTION REMAINS QUEUED — this ruling authorises and sizes it, and does not perform it**; a regression test asserts the three widenings are still absent, so the sequencing is guarded rather than remembered.
  - **A DRAFTING OVERCLAIM IN THIS RULING, CAUGHT BY ITS OWN TEST AND CORRECTED BEFORE IT LANDED.** The first draft of the head-only rule's third bound said the requirement *is* auditable after authoring, because "1-token-ness is mechanical and span-presence is mechanical." **`C04-03` falsifies it**: its set carries `goods`, which is 1 token **and** genuinely present in the span (*"the shipped goods"*), so it passes the mechanical check — while the object phrase is *"Each quantity of Miltenyi Product(s)"*, the head noun is `product`, and the model emits exactly `product`. The mechanical check would have marked the item **compliant** and changed nothing about its failure. The bound now reads **PARTLY auditable**, with *"which noun is the head"* named as genuinely unsolved — §9.4's missing-anchor blocker reaching the breadth item too, in a weaker form than it reaches F6. **Found because a regression test asserting `C04-03` had no head-only member FAILED, and the assertion rather than the data was wrong.**
  - **The forward head-only rule carries its collision NAMED IN ADVANCE**, rather than discovered as F3's was: a head-only member and the cognate-object residue collide on precisely the cognate items, since `C22-01`'s only available head-only label **is** its action nominal — so §3.6's rule 2 exempts that case explicitly.
  - **A SILENT-ZERO IN THIS SESSION'S OWN SCREEN, kept in the file rather than patched away** (Standing Principle 7, and the reason the preserved script asserts a load floor): the first draft printed **"12/12 PASS" over ZERO items** — its goldens path was one level short, every glob returned empty, and the known-answer gate passed anyway because **the gate checks the MATCHER and not that anything was LOADED**. A passing gate above an empty corpus is the same shape one layer out. `load_items()` now raises below 36 items.

- **§10.1 F9 AND F10 RULED, 2026-09-18, guideline v0.59 — §8's tag vocabulary gains a `kind` axis (`GOLD_SET_GUIDELINE.md` §8.10) and §9.1's in-force denominator is KIND-SCOPED. THIS IS THE FIRST QUEUE RULING SINCE F16's FIX THAT MOVES A PUBLISHED FIGURE, AND IT MOVES IT DOWN: in-force criterion 2 is `3/10 = 30.0%`, NOT `3/9 = 33.3%`.** Every `3/9 = 33.3%` above is a **dated record from here on and must not be quoted as the current criterion** — including the acceptance-criteria table's row 2; left as written per this file's corrections-are-new-text discipline, and this bullet is the correction. All-items holds at `5/15` and `K` at `14/32`. Set stays at **36**; no item added, restamped or re-authored, no cassette staled, no annotation rule changed. Measured by a real `run_scoring.run()` before and after, never hand-computed.
  - **THE FINDING THAT MADE THE RULING NARROW: direction was never the first axis, and a `kind` filter had already been operating unnamed.** `report.py`'s `GAP_DIRECTION` classified 5 of 10 live tags and left 5 `UNCLASSIFIED` — and the 5 it classified are **exactly** the 5 where IR v1 has no form for the clause, while the 5 it could not are **exactly** the 5 where the IR is not the thing at fault. So F10's five tags were never five missing decisions; they were **one wrong question asked five times**. `kind` is the first axis, `direction` a sub-axis of `REPRESENTATIONAL` alone.
  - **THE TAXONOMY IS PROMOTED, NOT AUTHORED — which matters because it was drafted with the failure list visible.** The `REPRESENTATIONAL`/`REACHABILITY` line is §8.6.1's own principle verbatim (*"`temporal: null` marks what the IR cannot REPRESENT, never what the classifier cannot REACH"*), and each remaining kind is quoted out of the section that already defines its tag — §8.7's *"not a v1 compiler gap"* → `CORPUS_DEFECT`, §8.3.1 v0.31's *"spans are nested"* → `ANNOTATION_CONVENTION`, §8.1 → `WITHHELD_VALUE`. **All five of F10's tags resolve with no fresh judgment call**, which is the test the ruling set itself: a taxonomy needing five new rulings to apply would be the masquerade F10 was filed separately to prevent.
  - **THE DENOMINATOR IS KIND-SCOPED AND NOT UNIFORMLY SO, each kind on its own stated ground.** `REPRESENTATIONAL` excludes on §9.1 **ground 2** (knowing unfaithfulness). `REACHABILITY` excludes on **neither existing ground** — gold is faithful — but on a **third**, stated separately rather than smuggled into ground 2: a **structurally guaranteed** compile failure, so admitting it seats unwinnable items in the headline denominator. `CORPUS_DEFECT` and `ANNOTATION_CONVENTION` **enter**, because ground 1's own text (*"items IR v1 can represent faithfully"*) describes them exactly. `UNCLASSIFIED` excludes **loudly**, so a tag added without a kind ruling can never drift into this phase's acceptance figure — a test fails on any live tag lacking one.
  - **`WITHHELD_VALUE` IS ITS OWN FIFTH KIND AND IS HELD EXCLUDED, on a principled asymmetry rather than a hedge (reviewer-selected).** It is not representational (no IR change helps) and not a corpus defect (nothing is wrong with the text — the value is absent from the world the contract describes). Its **direction** is genuinely contested: gold asserts `null` for a field the contract does populate, which reads as INCOMPLETENESS, yet the loss is not the IR's. **Membership cannot be settled on ground 2 while ground 2's own input is open**, so the status quo stands and the direction question is filed as **F18**. Admitting it reads **`3/11 = 27.3%`** — measured, not projected.
  - **EXPOSURE, MEASURED: whole set 17/36 → 20/36, movers `C04-01`, `C04-03` (`corpus_artifact_in_span`) and `C14-06` (`shared_subject_split`)** — of which only `C04-03` is cassette-scoreable today, which is why the scored figure moves by one rather than three; the other two land at the freeze pass. **`C14-02` correctly does NOT move**: it carries `mutual_obligation` alongside `shared_subject_split`, and exclusion is by **any** excluding tag (§9's v0.22 membership rule, untouched — only the predicate it applies changed). **The numerator does not move at all**, so the ruling can only LOWER the figure — the safe direction for §3.6's monotone-widening hazard, and the exact opposite of F1's `+2`.
  - **AGAINST INTEREST, AND IT WAS NOT ASKED FOR: the kind axis RE-OPENS an annotator disagreement the legacy scope had collapsed to zero.** `gap_agreement`'s `d_gold`/`d_int`/`d_uni` are still legacy-scoped; recomputed kind-aware they move — all-pairs band `(16,17)` → `(17,19)`, **conforming `(15,15)` → `(16,17)`**. v0.48's own test docstring called that point *"the substantive result… the two annotators no longer disagree at all about which items are scoreable"*; under the kind axis **they disagree again**, because agreeing an item is *tagged* is not agreeing on what KIND of thing the tag is. That bears directly on this file's own highest-priority REDESIGN item (`known_gaps` disagreement invisible to `K`). **Deliberately NOT taken here** — it moves a published, dated band, its own decision — and filed as **F17 with the numbers already measured**, so the future entry is not hand-wavy.
  - **THE LATENT BLOCKER IT WOULD OTHERWISE HAVE CREATED, found by reading the consumer rather than by a failing test: `render()`'s coherence check compares `gap_agreement.d_gold` against the in-force denominator and RAISES on mismatch.** Kind-scoping one side alone would have made that check fire the moment anyone wired `gap_agreement` into `run_scoring.run()` — which today it is not, so no test would have caught it. **Same shape as F16**, and closed the same way rather than left: the check now compares against an explicitly-retained **legacy** denominator, and the render prints a line stating the band and the criterion are different scopes instead of printing them adjacent for a reader to conflate.
  - **QUEUE ARITHMETIC STATED PLAINLY RATHER THAN AS A TALLY: this closes two entries and opens two.** Of the five design questions open at v0.52, **only F6 remains** (still blocked on §9.4's missing anchor field, unchanged by this ruling) — but F17 and F18 are new, real, and both filed with measured numbers.
  - **CLOSED 2026-09-18 — commit `8ea2f47`, CI run [35334105981](https://github.com/Arrkkkk/obligo/actions/runs/35334105981) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `616 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `8ea2f4738f47b38caea5a2fc24af75ea594f1432`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped` — the specific thing the Flyway-timeout entry above requires be checked rather than inferred from a conclusion. THE COUNT RECONCILES EXACTLY against the immediately preceding green run (`f3e6855`: 587 passed, 10 skipped): `587 + 29 = 616`, the 29 being `tests/evals/test_harness_gap_kinds.py` in full, and the skip count is unchanged because those tests read only committed gold JSON and are not corpus-gated.** The run's conclusion was read with `gh run view --json conclusion` and the summary out of the downloaded log, **not** off the polling wrapper's exit code — Standing Principle 7's eighth entry is precisely a `gh run watch` wrapper reporting 0 for a failed CI run.

- **THE GREEN-RUN CHAIN HAD A MISSING LINK, AND IT BROKE A RECONCILIATION THE VERY NEXT SESSION: §10.1 F18 was RULED, COMMITTED AND CI-VERIFIED on 2026-09-18 and NO BULLET IN THIS FILE RECORDS IT.** Filed here as its own entry rather than folded into F19's below, because the defect is about **this file's record**, not about either ruling. Until now the latest close-out recorded above was `8ea2f47` at **616 passed** (F9/F10), while the real branch history runs `8ea2f47` → **`395a3b6` (F18)** → `fb76238` (F19).
  - **F18's run, verified to this file's own close-out standard rather than assumed from the queue row:** commit **`395a3b6`**, CI run **[35337045944](https://github.com/Arrkkkk/obligo/actions/runs/35337045944)** green on `workflow_dispatch`, `headSha` confirmed `395a3b6be5626e24aee3bfc6cdd4263bb90e8393`, real pytest summary **`620 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines in the whole log, **all 14 steps `success`** with "Run tests" genuinely `success` rather than `skipped`. **It reconciles against `8ea2f47`: `616 + 4 = 620`**, the `+4` being a net of **15 tests added and 11 renamed away** as F18's ruling reworked the `WITHHELD_VALUE` direction tests — which is exactly why the delta is small and the churn is not.
  - **THE MEASURED COST OF THE GAP, stated because it is the point of the entry: the F19 session reconciled its own CI count against 616 — this file's latest recorded number — got `+15`, and had to diff two worktrees to discover that the baseline rather than the count was wrong.** The arithmetic was recomputed rather than trusted, so nothing false was published; but the *first* reconciliation was wrong, and it was wrong because a green run existed in CI and nowhere in this file. **A close-out chain is only a baseline if every link is in it** — one unrecorded run makes the next session's delta silently absorb two commits' churn, and a delta that absorbs an unseen commit looks exactly like a delta that does not.
  - **Not fixed by back-writing an F18 ruling narrative here, deliberately.** That ruling's substance belongs to the session that made it and to `GOLD_SET_GUIDELINE.md` §8.10.1, which does record it in full; inventing a summary of work this session did not do would be a worse record than an honest gap. What is closed is the **baseline chain**: the run above is now on file, so the next session reconciles against `631`, not `616`.

- **THE SAME MISSING-LINK DEFECT RECURRED AND WAS PAID FOR AGAIN ON 2026-09-24 — THREE LINKS THIS TIME, AND IT PRODUCED A WRONG BASELINE THAT WAS RIGHT BY COINCIDENCE. This bullet closes the chain through `5b72e85`.** The entry directly above closed the chain at `fb76238`/`631`; the links recorded after it stopped at **`0d0bc7f` (§8.8.5, `664 passed, 10 skipped`)**, while the real branch history runs `0d0bc7f` → **`5f6d1ad`** → **`8c32a74`** → **`5b72e85`**. All three are now on file below, so **the next session reconciles against `679`, not `664`.**
  - **`5f6d1ad` — the link that was missing, and the one that cost something.** Commit `5f6d1ad`, CI run [35904357271](https://github.com/Arrkkkk/obligo/actions/runs/35904357271) green on `workflow_dispatch`, `headSha` confirmed `5f6d1adf187f3f9c05a33966d952ba87fac09d9c`, real pytest summary **`664 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines in the whole log, **all 14 steps `success`** with "Run tests" genuinely `success` rather than `skipped`. It is the docs close-out for the §8.8.5 ruling and it **reconciles at `+0`** against `0d0bc7f`'s run — markdown only, so it correctly moved no test outcome. **That `+0` is exactly why it went unrecorded and exactly why that was a mistake**: a link whose delta is zero is still a link, and omitting it moves the *baseline commit* even when it does not move the *number*.
  - **THE MEASURED COST, and it is a sharper instance than the 2026-09-18 one because the arithmetic came out RIGHT.** The session closing §8.13 predicted its CI delta against this file's latest recorded number and would have reconciled `664 + 15 = 679` — **the correct answer, from the wrong baseline**, since `0d0bc7f` and `5f6d1ad` happen to share a count. The error was caught only because the reconciliation was done against `gh run list` as the standing rule requires, not against this file. **"Right by coincidence" and "right by checking" are not the same state**, and a chain with a hole cannot tell them apart — which is the whole argument for recording every link rather than only the ones that move a number.
  - **`8c32a74` — RECORDED AS COVERED TRANSITIVELY, NOT AS INDEPENDENTLY VERIFIED, because it has NO CI run of its own.** Verified by listing the branch's runs rather than assumed: `gh run list` shows runs for `0d0bc7f`, `5f6d1ad` and `5b72e85` and **none for `8c32a74`**. It was pushed in the same push as `5b72e85`, and **`ci-brain.yml` does not run on feature-branch pushes at all** — the already-tracked trigger gap above — so no run could fire for an intermediate commit. It is a **CLAUDE.md-only** commit (1 file, +9) and is contained in `5b72e85`'s tree, so `5b72e85`'s green run covers its content. **Stated this way deliberately**: a future session reading "green" against a commit that never had a run would be reading a claim no artifact supports.
  - **`5b72e85` — the §8.13 ruling.** Commit `5b72e85`, CI run [35965142836](https://github.com/Arrkkkk/obligo/actions/runs/35965142836) green on `workflow_dispatch`, `headSha` confirmed `5b72e85af1380175542936fc1968364df377374f`, real pytest summary **`679 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines, **all 14 steps `success`** with "Run tests" genuinely EXECUTED. **`664 + 15 = 679`**, skip count unchanged.
  - **THE `+15` WAS VERIFIED BY DIFFING COLLECTED TEST IDS ACROSS WORKTREES, AND THE PREDICTION IT REPLACED WAS WRONG — recorded because the correction is the point, not the number.** That session predicted **`+13`** from counting the new test file's tests by hand; the real figure is **15 added, ZERO removed or renamed**, all in `tests/evals/test_harness_within_parenthetical.py`, and the independent collect-only counts reconcile too (`674 + 15 = 689`). **The arithmetic was never wrong; the input was.** Same family as this list's own repeated lesson about tallies — a hand count offered in place of a diff is a detector nobody gated.
  - **The skip count holding at 10 is the expected result and is stated rather than passed over.** The new tests read only committed gold JSON and the production regexes, so none is `.corpus/`-gated. **Consequence worth knowing: a green CI verifies §8.13's effect on the gold set and on both tag vocabularies, but NOT its 179-deadline corpus measurement**, which is `skipif`-gated out on the runner and checked locally by `holdout/within_parenthetical/screen.py`. Same shape as the §8.8.4 close-out's own corpus-gate note.

- **THIRD INSTANCE OF THE MISSING-LINK DEFECT, 2026-09-25 — AND THE TWO OMISSIONS NOW HAVE A COMMON SHAPE THAT NEITHER PRIOR ENTRY IDENTIFIED: THE LINKS THAT GET DROPPED ARE THE ZERO-DELTA ONES.** *(Ordinal arrived at by COUNTING the entries above — the 2026-09-18 one and the 2026-09-24 one — not by incrementing a previous claim.)* The chain was recorded through `5b72e85`; **`64a0ee9` ran green after it and was never written down.** Both links are now on file, so **the next session reconciles against `679` at `3d74230`.**
  - **`64a0ee9` — the missing link.** CI run [35969366575](https://github.com/Arrkkkk/obligo/actions/runs/35969366575) green on `workflow_dispatch`, `headSha` confirmed `64a0ee9cf2cef61dac279bb2901efb6dca351c6e`, real pytest summary **`679 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines in the whole log, **all 14 steps `success`** with "Run tests" genuinely EXECUTED rather than `skipped`. It is the docs commit that closed the chain through `5b72e85` and it **reconciles at `+0`** — markdown only, so it correctly moved no test outcome.
  - **`3d74230` — batch 5 positions 7-11.** CI run [36134548522](https://github.com/Arrkkkk/obligo/actions/runs/36134548522) green on `workflow_dispatch`, `headSha` confirmed `3d742305e770293ad0dcf134c82930805248b038`, real pytest summary **`679 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines, **all 14 steps `success`**, "Run tests" EXECUTED. **`679 + 0 = 679`**, skip count unchanged. **The delta was PREDICTED IN ADVANCE from a collected-ID diff across worktrees (`689 = 689`, zero added, zero removed) and then matched, which is what makes it evidence rather than a reading** — four gold items and five assertion-value edits moved no test.
  - **THE COST THIS TIME WAS AVOIDED, BUT ONLY BY NOT TRUSTING THIS FILE.** The batch-5 session predicted its CI delta against `gh run list` rather than against the latest close-out recorded here, and so took `64a0ee9` as its baseline. **Had it read this file it would have taken `5b72e85` — and got the right answer anyway, because both runs report `679`.** That is the *same* "right by coincidence" trap the 2026-09-24 entry names, hit a second time **with the same number**, which is as close to a controlled repeat as this gets.
  - **THE NEW FINDING, and it is the reusable half: BOTH omissions were DOCS-ONLY commits whose delta was `+0`.** `5f6d1ad` (2026-09-24) and `64a0ee9` (today) were each dropped for the same reason — a link with no number to write down does not feel like a link. **The rule that follows is to record the LINK, not the NUMBER:** a zero-delta run still moves the baseline COMMIT, and a chain that skips it cannot distinguish "right by checking" from "right by coincidence" on the next reconciliation. Two instances of one mechanism is a pattern, not a coincidence.
  - **THE REGRESS IS ACKNOWLEDGED RATHER THAN HIDDEN: the commit carrying this bullet is itself a docs-only, zero-delta link, i.e. EXACTLY the class that keeps getting dropped, and it cannot record its own run inside itself.** Its evidence belongs to whichever session closes out next, the same way `8b4a769` was closed by a line written after it. Stated here so that omission is a known debt rather than a fourth instance discovered later.

- **THE DEBT THE ENTRY ABOVE PREDICTED IS PAID, 2026-09-26 — AND THIS IS DELIBERATELY *NOT* LOGGED AS A FOURTH INSTANCE, because the case does not belong to that list.** The bullet above named its own commit as an unrecorded zero-delta link **in advance** and called it *"a known debt rather than a fourth instance discovered later"*. `cce66be` is that commit; `ceaeb26` is the next one, closed out in the same pass rather than left to accumulate. **A predicted, named, promptly-settled omission is the mechanism working, not recurring** — the instance count stays at THREE. *(Checked by re-reading the three entries above and asking whether this case belongs to their list, rather than by incrementing the last number — the failure mode this file's own tally note describes.)* **The next session reconciles against `679` at `ceaeb26`.**
  - **`cce66be` — the chain close-out itself.** CI run [36164135035](https://github.com/Arrkkkk/obligo/actions/runs/36164135035) green on `workflow_dispatch`, `headSha` confirmed `cce66be2d6141ed839cab62f839431fccc7348bc`, real pytest summary **`679 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines in the whole log, **all 14 steps `success`** with "Run tests" genuinely EXECUTED. `CLAUDE.md`-only, so **`+0`** — correctly moving no test outcome, which is precisely the shape that keeps going unrecorded.
  - **`ceaeb26` — batch 5 position 12 (`E01-004` → `E01-04`, `E01-05`).** CI run [36230425813](https://github.com/Arrkkkk/obligo/actions/runs/36230425813) green on `workflow_dispatch`, `headSha` confirmed `ceaeb26921541e5dffc5c5b69802495845feb7e9`, real pytest summary **`679 passed, 10 skipped`**, **zero** `FAILED`/`ERROR` lines, **all 14 steps `success`**, "Run tests" EXECUTED. **`679 + 0 = 679`**, skip count unchanged.
  - **THE DELTA WAS PREDICTED IN FULL BEFORE THE EDITS, NOT READ OFF A FAILURE — and that is the substantive improvement over `3d74230`'s pass.** Seven values were forecast and every one held: items 56, segments 33, head-only census `(33,56)`, legacy `no_known_gaps` denominator 23, kind-scoped 28, collected-ID delta 0, CI delta 0. **The evals subset went green FIRST TRY.** At `3d74230` the same six pins were discovered by failure, two of five were misread in-session as "bookkeeping" when both carried real information, and a second assertion inside the gap-kinds test surfaced only after a failed re-run. **Forecasting the values converts a discovery into a verification**, and it is cheap: the numbers come from one script over the committed goldens.
  - **A SMALL TOOLING SLIP WORTH THE LINE, because its failure mode is the family this file keeps cataloguing: a `gh --jq` step-check returned EMPTY rather than wrong**, from escaped quotes inside a double-quoted shell string. **An empty answer is not a negative answer** — it was re-run through Python rather than read as *"no failing steps"*. Same shape as the 2026-08-25 `${PIPESTATUS[0]}`-in-zsh case, where a status expression evaluated to nothing and the emptiness read as benign.
- **§10.1 F19 RULED AND CLOSED 2026-09-18, guideline v0.61 — `E03-01`'s temporal owes a SECOND known gap, and the annotator's stated MECHANISM for it is WRONG. `lead_time_unrepresentable` is added (`REPRESENTATIONAL` / `INCOMPLETENESS`). Set stays at 36; NO item restamped, NO cassette staled, and NO published figure moves — in-force criterion 2 `3/10 = 30.0%`, all-items `5/15 = 33.3%`, `K` `14/32`, confirmed by a real `run_scoring.run()` BEFORE AND AFTER rather than by hand.** Full record: `apps/brain/evals/goldens/holdout/E03_01_TEMPORAL_INVESTIGATION.md`, guideline §8.11 and its v0.61 changelog entry.
  - **The CONCLUSION survives and the MECHANISM does not, and separating them is the whole contribution.** Verified through the production `compile_candidate()`, not read off the regexes: the phrase as emitted (3 of 3 runs) **and** a plausible unredacted one both return `UNMAPPABLE_TEMPORAL`, so *"would fail even unredacted"* is confirmed. But it is **not** the §7 composition gap the notes claim. Neither claimed operand is available for this text — `Duration` is `{amount, unit}` with **no `UNRESOLVED` alias variant** (unlike `DateRef`/`TriggerRef`), so `each Calendar Quarter` has no form; `RelativeToTriggerTemporal` is `{direction, trigger}` with **no duration slot**, so `at least ** before` has no form — and the note omits `during the Term` entirely, which is §7's *actual* operand.
  - **THE DECISIVE TEST, and it is what makes this a different gap class rather than a quibble: granting FREE COMPOSITION of all five v1 forms does NOT rescue the clause.** Composition joins whole forms, and a mechanical slot check over all five shows **no form pairs a `duration` with a `direction`**. The nearest composable approximation, `WITHIN 30d OF X` ∧ `BEFORE X`, denotes `X−30d ≤ t < X` — the **COMPLEMENT** of *"at least 30 days before X"* (`t ≤ X−30d`). So the gap is **single-form expressiveness**, not composition, and §7's own resolution (*"compile as `EVERY` alone, and warn"*) has nothing to compile alone here.
  - **NOT RESTAMPED, and the alternative was MEASURED rather than argued.** `known_gaps` is not one of §5's eight scored clauses, so this cannot change how the item scores. Restamping would stale `E03-005`'s three cassettes and move all-items **`5/15 = 33.3%` → `5/14 = 35.7%`** purely by dropping a `PARTIAL` item out of the denominator — **a published figure improving for no improvement**, the direction §3.6's monotone-widening hazard exists to guard. Same refusal as `C11-01`'s v0.52 record-only correction, on a measurement rather than a presumption.
  - **A FOURTH INSTANCE of the tracked "grammar-layer guarantee the extraction path bypasses" pattern — and the FIRST that had already PROPAGATED into the gold set.** *(Count arrived at by COUNTING the list, not by incrementing a previous claim — a first draft of this bullet said "fifth". The list holds three: `UNLESS` absorption, which names itself the third, after §17.3's never-built `AndPredicate`/`OrPredicate` and the IR-Compiler trailing-period bug. Same drift Standing Principle 7's own tally note warns about, caught the same way.)* §8's gap table said `EVERY` + `DURING` composed → *"`EVERY` only, with a composition warning"*. True of the **grammar**; false of the **extraction path**, where `_EVERY_RE` is `$`-anchored after the unit so `every 30 days during …` matches no form at all and the whole candidate is rejected `UNMAPPABLE_TEMPORAL`. Verified in **both** directions (the grammar path does still warn, so the guarantee is real and simply unreachable). Unlike the prior instances — `UNLESS` absorption, the never-built `AndPredicate`/`OrPredicate` (§17.3), the IR-Compiler trailing-period bug — this one is also a **documentation** defect that was **cited by the cold annotator as a reason not to tag**. Row corrected.
  - **AGREEMENT BETWEEN THE TWO ANNOTATORS IS NOT CORROBORATION, recorded because it reads as corroboration at a glance.** Cold's `known_gaps` is `["redacted_value"]`, identical to gold — but cold **explicitly considered and declined** the composed reading and named a **different** composed element (`DURING`, not `EVERY` ∘ `RELATIVE_TO_TRIGGER`). Two mutually inconsistent characterisations of one clause converging on one tag. **Set equality on `known_gaps` is agreement about a TAG, never about the MECHANISM behind it** — the same blind spot §7.1 already names for `K`, reached one level down.
  - **A SELF-CORRECTION MADE BEFORE THE RULING LANDED, and it is the one that mattered: F19 is NOT verdict-neutral, and this session's own first pricing said it was.** The **all-pairs** `G` stays `REDESIGN` (`6/31 → 7/31`), which is what "verdict-neutral" was read off — but the **conforming-scoped** `G` (§5.1 clause 9) is a **second instrument with its own `n`**, and there the tag moves `4/26 → 5/26` and **crosses its `REDESIGN` anchor**. So v0.48's framing that restricting `G` to conforming pairs *"moves it off its own REDESIGN trigger"* is **true of the published run and false of the current set**. Caught by the test suite, not by re-reading; corrected in the F19 row, the changelog, the §20 status line and the investigation doc. **`G_swing` on conforming pairs stays `0` and the band stays the point `(15,15)`**, so the scoreability finding this file's REDESIGN entry cites is unaffected — what moved is `G_overall`, which counts *any* set inequality.
  - **F17 IS UNMOVED AND STRUCTURALLY CANNOT MOVE, measured on a scratch copy: the kind-scoped band holds at `(17, 19)`.** `redacted_value` is `WITHHELD_VALUE`, already an excluding kind, and membership is by **any** excluding tag — so `E03-01` is out on both sides before and after, under the legacy emptiness scope, the kind scope, and any sub-population. F19 is exclusion-neutral for F17 exactly as that entry predicted, and **not** neutral for `G`, which the entry did not anticipate.
  - **A REAL STRUCTURAL DEFECT FOUND WHILE PRICING `G`, AND CLOSED HERE: BOTH real-data `G` reproductions read `known_gaps` LIVE, and the published `G = 6/31` had been RIGHT BY COINCIDENCE for three consecutive rulings.** Measured at the seal commit `628f67c`, **four** items have drifted since (`C04-02`/F7 emptied, `C10-01`/F11, `E01-01`/F8, `E03-01`/F19) — and `G` held at 6 only because **`C04-02` leaving `swing` and `C10-01` joining `superset` cancelled exactly**, while `G_swing` (2 → 1) and the band ((15,17) → (16,17)) had already moved unnoticed. **A live `G` that happens to equal the published one is not a reproduction of it, and nothing in the suite could tell those apart.** Closed the same way K's population was pinned for `C11-02` at v0.53: population now derived from the sealed `comparison.json` (failing **loudly** if a published item goes missing), `PRE_TAG`/`PRE_RESTAMP` holding the four drifted items' sealed tags, a genuine **historical** reproduction test alongside the live-set one, plus tests pinning that the two **disagree** and that no override has gone **inert**. `comparison.json` seals the population and K's per-clause fails but **not** `known_gaps`, so the override map is the only mechanism available — **filed as F20** (should a future §7 run seal `known_gaps` into its artifact?), deliberately not taken.
  - **A SECOND HAND-MAINTAINED COPY OF §8's TAG VOCABULARY CAUGHT THIS SESSION OUT, and it is the cross-instrument shape this file's debt list already hunts.** Adding the tag to `gap_kinds.GAP_KIND` alone left `annotator_agreement.SECTION_8_TAGS` stale, which marked `E03-01` **`NON_CONFORMING`** and moved **`A`** — a failure in a **different instrument** from the one the tag was added for, and one no test in the `gap_kinds` file would have caught. Fixed, and pinned with a **subset invariant** (`GAP_KIND` ⊆ `SECTION_8_TAGS`, deliberately not equality, since the latter legitimately holds tags no item uses yet) so the two cannot drift again.
  - **TWO SMALL ITEMS SURFACED AND DELIBERATELY NOT TAKEN**, named so they are not rediscovered: **`redacted_clause`** sits in `SECTION_8_TAGS` with **no ruled §8.10 kind** (harmless today — no item uses it and `UNCLASSIFIED` excludes conservatively — but it is the next tag that would trip the forcing function), and the `_EVERY_RE` `$`-anchor leaves the grammar's **`during_bound?` production unreachable from the pipeline**, i.e. dead code from extraction's point of view — the code-level counterpart to the §8 documentation row corrected above.
  - **CLOSED 2026-09-18 — commit `fb76238`, CI run [35352685302](https://github.com/Arrkkkk/obligo/actions/runs/35352685302) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `631 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `fb76238bafc20c9b3306b81cbaaae9dec34450d0`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNT RECONCILES EXACTLY against the immediately preceding green run — `395a3b6` at `620 passed, 10 skipped`, the link recorded in the entry directly above: `620 + 11 = 631`, with the skip count unchanged. The `+11` was verified by DIFFING COLLECTED TEST IDS across worktrees rather than by counting by hand: 11 added, ZERO removed or renamed.** The conclusion was read with `gh run view --json conclusion` and the summary out of the downloaded log, **not** off the polling wrapper's exit code — and the push was confirmed by diffing local `HEAD` against `git ls-remote`, which no shell dialect can fool (Standing Principle 7's corrected remedy (d)).

- **§10.1 F1 IS EXECUTED, 2026-09-19, guideline v0.62 — the three queued accept-set widenings land as ONE atomic freeze-pass event, and this is the FIRST queue entry ever to move this phase's headline acceptance figure UPWARD: in-force criterion 2 `3/10 = 30.0%` → `5/10 = 50.0%`, all-items `5/15` → `7/15 = 46.7%`.** Every `3/10 = 30.0%` above is a **dated record from here on** — including the acceptance-criteria table's row 2, twice-superseded now (`3/9` → `3/10` → `5/10`); left as written per this file's corrections-are-new-text discipline, and this bullet is the correction. `K` holds at `14/32` and `A` at `7/27`. Set stays at **36**; no item added or restamped, no cassette staled, no member removed, no annotation rule changed. Measured by a real `run_scoring.run()` before and after, never hand-computed. Full record: guideline §3.4's and §3.6's v0.62 execution notes and the §10.1 F1 row.
  - **THE WIDENINGS:** `C02-01`'s `obligor_accept_set` gains the whole coordinated phrase *"Antares or its Subcontractor"* (§3.5.1's v0.33 rider); `C02-03`'s `object_class_accept_set` gains `invoice_costs`; `C11-01`'s gains `principal_interest`. **Atomic in both directions** — the regression test that used to assert all three ABSENT now asserts all three PRESENT and fails if any one is missing, which is what "atomic" has to mean once they are in.
  - **THE v0.58 SIZING WAS RECONFIRMED RATHER THAN CARRIED FORWARD, and it needed to be: it was stated on a `/9` denominator that v0.59's kind-scoping had since moved to `/10`.** Quoting `5/9 = 55.6%` today would have been quoting a superseded denominator. Re-measured against the committed set, **the `+2` reproduces exactly** — movers `C02-01` (`obligor` its only failing clause) and `C02-03` (`object_class` its only failing clause, the repair loop rewriting `INVOICE` → `PROVIDE` so clause 5 is genuinely reached). **`C11-01` is worth ZERO as predicted, confirmed rather than restated**: modal stays `PARTIAL`, failing clauses `['object_class', 'conditions', 'obligee']` → `['conditions', 'obligee']`. The item this project cites most often as breadth's motivating case is the one widening that cannot move criterion 2.
  - **NOT RESTAMPED, and the alternative was MEASURED rather than argued: widen-and-restamp reads `3/7 = 42.9%`.** Restamping stales all three cassettes, so **both movers leave the denominator entirely** — the numerator does not move at all and the figure "improves" `30.0% → 42.9%` **purely by shrinking**, the exact shape F19 refused. Stamps held at `v0.28`. **Scoreability is unchanged across the widening — 15 scored / 21 cassette-unscoreable before and after** — so the `+2` is bought by outcomes moving, not by items dropping out.
  - **A SECOND-INSTRUMENT EFFECT THE v0.58 SIZING NEVER PRICED, disclosed rather than left to be found.** §3.6.1's v0.45 rule requires a retroactive edit to state its effect on **both** predicates; the sizing priced only §5. Measured: `K` **unmoved** at `14/32` (verdict `REDESIGN` unchanged) and in-force `A` **unmoved** at `7/27` — **but the published LEGACY per-clause profile would have drifted `5_object` 6 → 5 of 23**, via `C11-01`, because `A` compares slots by mutual membership and reads gold's set directly where §5 clause 5 never reads gold's slot. That is a change to a **dated, published** run, so it is held by the same `PRE_RESTAMP` mechanism §10.1 F11 and F13 already use — extended to a **widening** rather than a restamp, which is new. The published run still reproduces exactly, item by item and clause by clause.
  - **A LATENT DEFECT IN THAT VERY MECHANISM, FOUND WHILE EXTENDING IT AND FIXED HERE: `PRE_RESTAMP` held the key `C10-01` TWICE, so F19's v0.61 `known_gaps` block silently overwrote F11's v0.44 `object_class` slot correction — and Python reports nothing.** **It was INERT, stated rather than glossed:** measured both ways against the sealed artifact, `C10-01` fails `5_object` under either reading, so `K` reproduced at `14/32` with the correct profile throughout and **no published figure was ever wrong**. What was lost is the **guard** — dead for two rulings, and it would have failed silently the moment `C10-01`'s own set was widened, which is precisely what this commit does to three other items. Merged into one entry, plus an **AST-based** duplicate-key test (the evaluated dict is already collapsed, so the evidence is gone by the time any ordinary assertion can see it) **verified RED against the reintroduced defect before being trusted** — Standing Principle 7's discipline applied to my own detector. **Same family as the tracked "a versioned-artifact indirection is only as real as the tests that don't bypass it" entry**, reached through a third door: not a test pinning a version literal, nor one pinning a historical run's numbers while its population drifted, but an **override map silently ceasing to override**.
  - **§3.6's OWN INVARIANTS WERE RE-RUN, NOT ASSUMED INTACT:** §3.6.2's retroactive no-restatement screen returns a **byte-identical** result over the widened set (the three new members introduce zero restatement hits), and head-only member coverage **holds at 13/36**. §3.4's justification test was satisfied from the sentence alone in all three cases, with no reference to any prediction.
  - **A STALE LIVE FIGURE IN GUIDELINE §9.2.1, corrected in place: it still carried the claim *"the figure to quote today is still `3/9 = 33.3%`"*.** v0.59's kind-scoping had already superseded that to `3/10` and v0.62 moves it to `5/10`; neither ruling revisited the sentence. `C11-02`'s own marginal arithmetic there is **unaffected and still correct** — a `+1 denominator, +0 numerator` claim holds against any baseline; only the baseline it was expressed against moved.
  - **I FIRST FILED THAT AS "A FIFTH LOGGED INSTANCE OF §10's CLOSE-OUT DRIFT" AND IT IS WRONG TWICE OVER — ON THE CLASS AND ON THE ORDINAL. Recorded as its own bullet rather than folded into the one above, because the way I got it wrong is the point: I INCREMENTED THE PREVIOUS CLAIM INSTEAD OF COUNTING THE LIST.** That is the exact habit this file's own tally note warns about (*"each count is arrived at by counting the list, never by incrementing the previous claim"*), and it produced a wrong number in a close-out summary that had already been reported as done.
    - **THE CLASS IS THE SUBSTANTIVE HALF, and it cuts against my own framing: §9.2.1 is NOT an instance of that drift, and the check is not at fault for missing it.** §10's process note reads **exactly three places** — the `**Version:**` line, the `**Status:**` line, and the newest changelog entry. **§9.2.1 is body prose and is none of the three.** The *mechanism* is identical; the *class* is the **body-text analogue** — the same gap §10's retroactive sweeps already have against segment-note prose (v0.55's `C11-094` case, where a stale §8.6 citation outlived §8.6.1 inside a committed artifact and then propagated into a work instruction). **The real finding is therefore a so-far-unclosed SCOPE GAP in the check — a live figure quoted in a body section is exactly as quotable as one in the Status line, and nothing screens it — not another tally mark.** Filed here because it is now the second logged instance of that *body-prose* class, after v0.55's.
    - **ON THE ORDINAL, AND THE COLLISION IS REAL RATHER THAN MY MISREADING: the guideline keeps a RUNNING tally with explicit ordinals, and it already stood at FOUR before v0.56** — (1) the header's *"no items have been annotated"* and (2) the v0.15/v0.17 changelog pair, both corrected at v0.26 and cited by §10 as *"the first two"*; (3) v0.28's own header/changelog still reading *"no v0.28 rule is adopted"*, labelled **THIRD** in v0.28's changelog; (4) the Status line's *"consolidation pass in progress"*, labelled **FOURTH** at v0.29 and the first the check caught in the session that staled it. **So §20's v0.56 note — and this file's own bullet above quoting it, *"A FOURTH INSTANCE of §10's own close-out drift"* for the Status line still reading 34 after v0.55 locked the 35th — labels as "fourth" an ordinal that was already taken. It is the FIFTH of that tally.** Both are **dated records and are left as written**; this is the correction. My "fifth" was that mislabel plus one, so it inherited the error and then compounded it by applying it to the wrong list entirely. *(§19.3's local heading "four false claims corrected" is NOT a rival tally — it counts the v0.15 and v0.17 entries separately and its own closing parenthetical reconciles it to the running count in terms: "the count in §10's own prose — 'three times' — is now four.")*
  - **CLOSED 2026-09-19 — commit `7eab3c6`, CI run [35439336217](https://github.com/Arrkkkk/obligo/actions/runs/35439336217) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `635 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `7eab3c6175476521f166176096e5e4b5ede7dc49`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNT RECONCILES EXACTLY against the preceding green run (`fb76238`: 631 passed, 10 skipped): `631 + 4 = 635`, skip count unchanged. The `+4` was verified by DIFFING COLLECTED TEST IDS across worktrees rather than counted by hand — 5 added, 1 renamed away (the sequencing guard flipping from "widenings ABSENT" to "widenings PRESENT"), net `+4`.** The conclusion was read with `gh run view --json conclusion` and the summary out of the downloaded log, **not** off the polling wrapper's exit code — Standing Principle 7's eighth entry is precisely a `gh run watch` wrapper reporting 0 for a failed CI run. **The docs-only follow-up commit `8b4a769` (this bullet's close-out line, §9.2.1's drift correction) is closed by its own run, [35439993241](https://github.com/Arrkkkk/obligo/actions/runs/35439993241), green against `headSha` `8b4a769394d32974158c90e249fbea02eb6e45e1`: `635 passed, 10 skipped`, zero `FAILED`/`ERROR`, all 14 steps `success`. The count is BIT-FOR-BIT IDENTICAL to `7eab3c6`'s run, which is the expected result and the check that it means something — that commit is markdown only (no test reads `GOLD_SET_GUIDELINE.md` or `CLAUDE.md`, verified by grep over `apps/brain/tests`), so it should move no test outcome.**


- **THE TWO LOGGED `temporal-composition` INSTANCES ARE RULED, 2026-09-23, guideline v0.63 — §8's vocabulary gains `temporal_composition` (kind `REPRESENTATIONAL`), three items gain it (`E03-01`, `C02-07`, `C04-08`), and AXIS 2 GAINS A THIRD STATE, `DIRECTION_DEFERRED`. NO PUBLISHED FIGURE MOVES: in-force criterion 2 `5/10 = 50.0%`, all-items `7/15 = 46.7%`, `K` `14/32`, `A` `7/27`, F17 band `(16,17)`, and — unlike F19 — `G` holds at `7/31`; measured by a real `run_scoring.run()` BEFORE AND AFTER and by running both agreement instruments, never hand-computed.** Set stays at **48**; no item added or restamped, no cassette staled, no annotation rule changed. Full record: `docs/eval/GOLD_SET_GUIDELINE.md` §8.12, its v0.63 changelog entry, the §10.1 **F21** row, and the preserved screen at `apps/brain/evals/goldens/holdout/temporal_composition/screen.py`.
  - **THE GAP IS A SLOT GAP, NOT A FORM GAP, so the sibling is `compound_action` (§8.3) and NOT §8.11's `lead_time_unrepresentable`** — `ast.Obligation` carries ONE `temporal` field while the source states several elements, each constraining the duty differently. §8.11's decisive test (*"free composition would NOT rescue the clause"*) comes out the **other** way here.
  - **BOTH LOGGED NOTES ARE FALSIFIED ON THEIR CENTRAL CLAIM, and the correction cuts against the distinction they were drawing.** *"Each of the three elements HAS a v1 form on its own"* is false as written: measured through the production `_classify_temporal()`, **one of three** classifies for instance one (`during the Term of this Agreement` → `None`, `until Bellicum has secured an alternate source…` → `None`) and **two of three** for instance two. Instance one's own note measures those two `None`s a few lines below the claim without reconciling them. Mechanically `until <event>` has no v1 form at any layer — `DuringTemporal` is `{start: DateRef, end: DateRef}`, no end-only slot, no trigger-typed terminus — so that element is itself §8.11-shaped and **instance one is a MIXED case**. The §8.11 distinction survives **on instance two alone**.
  - **THE SECOND CORRECTION IS THE ONE THAT CHANGED THE RULING: both notes reason about DIRECTION from the PIPELINE's greedy-swallow output, where §8.10.1's test is whether GOLD departs from the document.** Gold never swallows — it records one element and drops the rest — so *"closer to a misstatement"* is not a candidate direction at all, and no third direction VALUE is minted for it.
  - **DIRECTION IS DEFERRED BY RULING, NOT ASSIGNED, AND AN `OVERSTATING` ASSIGNMENT WAS DRAFTED AND REJECTED.** It is attested in **2 of 2** instances (both drop a scope bound), it matches `unless_unsupported`'s own ground (a dropped narrowing bound is a carve-out in the time dimension), and in instance one gold has **no choice** — its narrowing bounds are exactly the unrepresentable ones. It was rejected anyway, on the reviewer's ruling, because **the direction varies by instance AND WITHIN ONE ITEM**: `C04-08` drops an ADDITIVE recurring deadline (INCOMPLETENESS-shaped — a monitor never checks it) *and* a NARROWING scope bound (OVERSTATING-shaped — the recurrence never ends). Winning 2-of-2 on instance count is not describing the class, and a tag-level value would certify a direction that does not describe a specific item — §9.1 ground 2's own failure mode. **`DIRECTION_DEFERRED` is spelled EXPLICITLY, never as a missing `GAP_DIRECTION` entry**, for the reason §8.10 keeps `None` and `UNCLASSIFIED` apart one level up: an absence is indistinguishable from *"someone forgot"*. Three answers now, all distinct, with a G6 bucket of its own. **It costs nothing because §9.1's denominator reads KIND** — `REPRESENTATIONAL` excludes on its own — which is the only reason deferring was available.
  - **SCOPE IS THE SLOT LIMIT, NOT THE GREEDY `(.+)$` SWALLOW**, which is a symptom confined to surplus sitting inside the quoted phrase; scoping a `REPRESENTATIONAL` tag by a classifier artefact would be incoherent. Consequence, ruled explicitly: **`C02-047`'s logged near-miss IS in scope and `C02-07` is tagged** — its note's mechanics stand and were re-verified, and its refusal to inflate the instance count stands, because **what counts as an INSTANCE for the forcing function is not what falls in the TAG'S SCOPE** (the instance count behind the amendment is still two). **`E03-01` is swept in** from §8.11's own observation that F19's note *"omits `during the Term` entirely"* — a connection made from the guideline's own text, not from either logged instance.
  - **EXPOSURE IS EXACTLY THREE ITEMS, and the screen that says so is PRESERVED and two-sided-gated rather than asserted.** A marker sweep flags 11 of 48 spans; 8 are hand-rejected (agentive `by`, non-temporal `upon`, object-scope `during`, and two RESTATEMENT-not-composition cases of which `C02-06`'s *"promptly (but no later than [***] of receipt)"* is the closest call). Standing Principle 7: the gate requires the three tagged items to be flagged **and** four named false-positive shapes to be flagged-then-rejected, so it proves the sweep is loose and the hand pass load-bearing; `load_items()` raises below 48 items, because a known-answer gate over an empty corpus passes — v0.58's preserved screen printed *"12/12 PASS"* over zero items for exactly that reason.
  - **ALL THREE ITEMS WERE ALREADY OUTSIDE §9.1's IN-FORCE DENOMINATOR on existing tags, so the amendment is EXCLUSION-NEUTRAL** — asserted on data by a test that fails if any future tagged item would be excluded by this tag **alone**, not trusted from the ruling's prose. The whole before/after diff of the scoring report is three things: the new G6 legend line, `1 items carry 'temporal_composition'`, and `E03-01`'s gaps list.
  - **NOT RESTAMPED, on F19's precedent and for its own measured reason.** `E03-01` keeps `v0.28`, the batch-4 pair keep `v0.62`; `C02-07`/`C04-08` have no cassette, and restamping `E03-01` would stale `E03-005`'s three and drop a scored `PARTIAL` item out of the all-items denominator — a figure improving for no improvement.
  - **A SIXTH INSTANCE OF §10's CLOSE-OUT DRIFT, corrected in passing: the guideline's Status line still read *"36 items locked"* after batch 4 locked the set at 48**, batch 4 having touched no guideline file. **The ordinal was arrived at by COUNTING §19.3's running tally, not by incrementing the last claim** — header's *"no items have been annotated"* (1), the v0.15/v0.17 changelog pair (2), v0.28's header/changelog (3), v0.29's Status line (4), v0.56's Status line (5, itself mislabelled "fourth" at the time).

  - **CLOSED 2026-09-23 — commit `142fd98`, CI run [35857252335](https://github.com/Arrkkkk/obligo/actions/runs/35857252335) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `654 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `142fd989ea5930b8992236004c5d879ae651ebe5`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNT RECONCILES EXACTLY — and against the RIGHT baseline, which is the point: the preceding green run is `1d9eab1` at **`639 passed, 10 skipped`**, NOT the `635` this file last recorded at `8b4a769`, because batch 4 and two docs commits landed in between without close-out lines. `639 + 15 = 654`, skip count unchanged, the `+15` being `tests/evals/test_harness_temporal_composition.py` in full with nothing removed or renamed. **Reconciling against this file's latest recorded number rather than against the branch's actual last green run is exactly the failure the missing-link entry above describes**, and it was avoided here by reading the run list rather than this file. The conclusion was read with `gh run view --json conclusion` and the summary out of the downloaded log, **not** off the polling wrapper's exit code — which reported `0` here too, as Standing Principle 7's eighth entry says it would regardless.**

- **§8.8's SUB-CHOICE IS RULED AND ITS *"pending confirmation"* FLAG DISCHARGED, 2026-09-23, guideline v0.64 (§8.8.5, §10.1 F22) — the single-verb `action_accept_set` STANDS, and the §8.8-vs-§14.3 tension turns out to be a ROUTING ERROR AT §14.1 rather than a clash between two live rules. NO ITEM ADDED, RESTAMPED OR RE-AUTHORED, NO TAG ADDED, NO CASSETTE STALED, AND NO PUBLISHED FIGURE MOVES: in-force criterion 2 `5/10 = 50.0%`, all-items `7/15 = 46.7%`, `K` `14/32`, `A` `7/27` — measured by a real `run_scoring.run()` BEFORE AND AFTER, never hand-computed. Set stays at 48.** Full record: `docs/eval/GOLD_SET_GUIDELINE.md` §8.8.5, its §14.3 amendment note, the v0.64 changelog entry and the §10.1 F22 row; `E08-016`'s own observation is CLOSED rather than deleted.
  - **THE REFRAME, and it is the whole contribution: §14.3 never reached this class.** §14.3 routes an ambiguity in an accept-set field to a widening; §8.8's genuine-gap branch forbids exactly that; three items logged the collision. But **§14.3's precondition is not met** — §14.1 defines `AMBIGUOUS` as *"**The sentence** admits ≥2 defensible readings"* and glosses it *"a property of the text"*, while in a genuine-gap item the text is not in doubt (`C10-02` says *add*, `C14-04` says *complete*, `E08-04` says *dispute*) and what is uncertain is the **encoding** — §14.1's `UNCERTAIN`, which routes to §14.4. **All three in fact took §14.4**, every one carrying `adjudicated_by: reviewer`. Same shape as F9/F10's *"one wrong question asked three times"* and F13's ruling-on-a-principle.
  - **A SECOND, INDEPENDENT GROUND IS STATED BECAUSE `C10-02`'s CASE IS GENUINELY ARGUABLE, and saying so is cheaper than defeating it.** `ESTABLISH`-as-literal-act against `GRANT`-as-legal-effect can fairly be called two readings of the sentence. It does not matter: **§14.3's REMEDY presupposes each competing reading is REPRESENTABLE.** Widening records that the sentence admits both *and both are expressible* — the eval pilot's `PROVIDE`/`REPORT` case, where both verbs are **correct**. In a genuine gap **none** is correct, so a widening records indecision among known-wrong answers, which is **§14.2's prohibition** (a scored field must never encode annotator confidence) applied to clause 2 instead of clause 8. The principle is promoted from §14.2 rather than authored, in §8.6.1's shape: **`action_accept_set` records what the SENTENCE admits, never what the TAXONOMY forces the annotator to settle for.**
  - **THE NARROW FIX IS REJECTED ON A MEASUREMENT, NOT A PREFERENCE — across 4 of 4 items that recorded a runner-up it admits NOTHING.** `C10-02`'s `GRANT`, `C14-04`'s `DELIVER` and `E08-04`'s `COMPLY`/`NEGOTIATE` are all **competitors** to the nearest verb reached from a different angle, not synonyms of it; and `E03-03`'s genuine synonyms of its nearest verb — `GUARANTEE`, `WARRANT`, `ASSURE` — are **all off-taxonomy**, which is what made the item a gap. So it is not a smaller version of widening, it is **the status quo with extra machinery**, and on `C10-02` it excludes the very competitor that motivated it. Its other reading, *near-synonyms of the REAL verb*, is §8.8.2's mappability test verbatim and returns NO for the whole class by construction. It also has **no mechanical test** — §8.8.2's *"same performance?"* is the only verb-closeness relation this guideline owns — which is §8.3.2's own sustained objection to trading a checkable boundary for an unfalsifiable one.
  - **THE EMPTY-SET ALTERNATIVE IS RE-PRICED AGAINST THIS RULING'S OWN CONCLUSION AND STILL REJECTED.** v0.59's kind-scoping has since removed every item in this class from §9.1's in-force denominator, so §8.8's decisive v0.24 cost — *"converts a measurement question into a guaranteed failure for roughly one item in four"* — now lands **only** on the all-items figure, which §9.1 labels *"reported alongside, never as the criterion."* **It is materially cheaper than when it was deferred, and a confirmation that hid that would be worthless.** Refused on the narrower ground that **both figures are recoverable from the committed data either way** (the `action` slot holds the nearest verb whichever the set is), so the choice is only which is the default reading — and the singleton keeps clause 2 answering a real if low-value question where the empty set makes it answer nothing.
  - **THE CLASS IS EIGHT ITEMS, NOT THREE, and it was counted rather than inherited from the observation's "INSTANCE THREE".** `C10-01`, `C10-02`, `C14-04`, `E01-01`, `C04-06`, `C02-07`, `E03-03`, `E08-04` — **16.7% of 48**, inside §8.8's sampled 23%, 95% Wilson CI [11%, 42%], which is deliberately **NOT restated** per §8.8.2's own rule that a census of locked items does not move a sample-based estimate. **Every one already holds a singleton**, so the rule has been applied uniformly to eight items and the three logged instances are where the collision was **noticed**, not where it bites.
  - **COST IS MEASURED ON FOUR INDEPENDENT GROUNDS, each verified by execution — and the load-bearing half is that A WIDENING COULD NOT HAVE MOVED A FIGURE EITHER, so this ruling could not have been reached for to protect a number.** (1) the tag is `REPRESENTATIONAL`, so §9.1 excludes the whole class; (2) **all eight items are cassette-unscoreable today**, for three different reasons the run itself names; (3) `C10-02` and `C14-04` are `NON_CONFORMING` under §5.1's A1 gate — cold's paired slots are `INSURE` and `COMPLETE`, both off-taxonomy — so they sit outside **both** sides of `A` and were already removed from `G`, and `E08-04` is outside the published population entirely; (4) **no LEGAL widening can flip the legacy published `K`'s `2_action`** for either item, since legacy clause 2 is `cold_slot ∈ gold_set ∪ {gold_slot}` and §3.4 admits only **taxonomy** verbs into a gold accept-set — a structural guarantee read off `compare_clauses`, not a measurement of one option.
  - **A FIRST DRAFT OF THAT COST TABLE WAS WRONG AND IS CORRECTED RATHER THAN QUIETLY FIXED: it said no tagged item had a cassette.** `E01-01` and `C04-06` **are** cassette-backed (`E01-047`, `C04-117`); they are unscoreable for a different reason — a §22.3 guideline-version mismatch. Caught by reading the run's own **G8 list** instead of the cassette directory, which is the same move that keeps catching this class: the artifact that answers the actual question beats the one that looks like it does.
  - **WHAT WAS ADDED, because a confirmation must not bank the section's own complaint and publish the figure anyway.** §8.8's sub-choice paragraph states the cost in its own words — *"an extractor scores clause 2 correct only by picking the SAME near-miss the annotator picked"* — and that objection **survives this ruling**. New rule: wherever criterion-2 figures appear, clause 2 on an `action_not_in_taxonomy` item is disclosed as a **FORCED-CHOICE AGREEMENT CHECK, not an action-identification measurement** (`report.py` **G9**), items named on the spot. The honest half of the empty-set alternative without its data change, on §6.1's own ground that a reader who never reaches the methods section still gets the caveat. **The entire before/after diff of the scoring report is that one block** — nothing else moved, which is how the "no figure moves" claim was established rather than asserted.
  - **`E08-04`'s PROVISIONAL DISPOSITION IS DISCHARGED, NOT CHANGED — not one field moves, and it is NOT restamped.** The ruling confirms the rule it was interim-decided under, so `PROCESS` + singleton + tag stand and become final. Stamp holds at `v0.62` on `C11-02`/`C04-06`'s v0.54 precedent (a rule that **confirms** rather than changes earns no restamp). `E08-016` has no cassette so a restamp would be **free** — **and "free" is not a reason**, the same discipline §22.1 applied in the opposite direction.
  - **`annotator_confidence` IS DELIBERATELY NOT CORRECTED on the four `AMBIGUOUS` items** (`C10-01`, `C10-02`, `C14-04`, `E01-01`) despite the routing finding. `C10-02`'s reading is arguable, and the field is **read by no code under `apps/brain`** — verified by execution, `grep` exits 1 over the whole tree, 0 lines. Flipping provenance labels retroactively on an arguable reading buys nothing. Worth noting as a live signal rather than a rule: the four items stamped `v0.53` and later are all `CONFIDENT`, so the practice had already drifted to the correct §14.1 state before the rule said so.
  - **A PLANTED DEFECT THAT FAILED FOR THE WRONG REASON, KEPT IN THE TEST FILE RATHER THAN TIDIED AWAY — Standing Principle 7 turned on my own detector.** The first defect planted against G9 (key on a singleton accept-set instead of on the tag) raised `AttributeError` — `ItemReport` carries no accept-set — so the four tests that "caught" it caught a syntax-level break and **proved nothing about the predicate**. Replaced with a defect that actually runs (`if i.known_gaps` — any tag at all), which the original four tests **would have passed**; it is caught only by a test added for it, pinning that `C03-02` (`compound_action` alone, whose `action` IS a taxonomy member denoting the performance) stays out of G9. **A defect that fails to compile is not a red test, it is an absent one.**
  - **CLOSED 2026-09-23 — commit `0d0bc7f`, CI run [35902457533](https://github.com/Arrkkkk/obligo/actions/runs/35902457533) green on `workflow_dispatch` against `evals/batch02-gold-set`: real pytest summary `664 passed, 10 skipped`, zero `FAILED`/`ERROR` lines in the whole log, `headSha` confirmed `0d0bc7fc247a5be1b25244de275a185bf3abc9c5`, and all 14 steps `success` with "Run tests" genuinely EXECUTED rather than `skipped`. THE COUNT RECONCILES EXACTLY, AND AGAINST THE RIGHT BASELINE — which is the check that it means anything: the branch's actual last green run is **`de5a8cd` at `654 passed, 10 skipped`**, read off `gh run list` rather than off this file, whose own last recorded close-out is `142fd98`. `654 + 10 = 664`, skip count unchanged, the `+10` being `tests/evals/test_harness_forced_choice_action.py` in full. **The `+10` was verified by DIFFING COLLECTED TEST IDS across worktrees rather than counted by hand: 10 added, ZERO removed or renamed.** The conclusion was read with `gh run view --json conclusion` and the summary out of the downloaded log, **not** off the polling wrapper's exit code — Standing Principle 7's eighth entry is precisely a `gh run watch` wrapper reporting 0 for a failed CI run, and the push was confirmed by diffing local `HEAD` against `git ls-remote`, which no shell dialect can fool.**
  - **THE LOCAL FULL SUITE IS CLEAN BUT IS NOT THE SAME CHECK, and the difference is stated rather than folded in: `620 passed, 54 skipped`, zero failures, with the real `pytest_exit=0` read off the LOG'S OWN FIRST LINE** — the harness's job-completion notification reported exit 0 regardless, as the compound-command sibling of Standing Principle 7 says it would, since the invocation ended in a `tail`. `620 + 54 = 674` reconciles exactly against the collect-only count. **The 54 skips against CI's 10 are the no-`DATABASE_URL` difference**, so the DB-backed tests — including the tracked local-migration one — **skipped rather than ran** locally. CI is the authoritative full-coverage check here, the `96924f8` precedent.

- **TWO RULINGS CLOSED 2026-09-05, guideline v0.52 — §10.1 F13 and `SPLITTING_EXCLUSION_INVESTIGATION.md`'s Decision 3. One item restamped, three cassettes newly stale with the re-record deliberately DEFERRED, no published figure moved: in-force criterion 2 stays `3/9 = 33.3%` and `K` stays `14/32`.**
  - **F13 — §8.6 vs §8.9/§3.7 is RESOLVED for §8.9's treatment; §8.6 is amended at guideline §8.6.1, RETROACTIVE.** Ruled on a principle rather than a vote count: **`temporal: null` marks what IR v1 cannot REPRESENT (a bare vague qualifier, §15.2), never what the classifier cannot REACH.** Verified by execution — `within 24 months of the Commencement Date` classifies to `WITHIN 24mo OF "the Commencement Date"`, so the IR expresses the form and `_WITHIN_RE`'s single-preposition alternation is the whole obstacle. **Two sections beyond the two F13 named were found siding against §8.6: §11 decision 1 (v0.8), which rules "annotated honestly, exactly as the document writes them" for the parenthetical gap on this same regex, and §15.5 (v0.28), whose existing `C11-01` treatment is CONFIRMED rather than changed.** §8.6's own *"why its own tag"* paragraph shows it was written looking straight at §11 and reconciled only the tagging question. Second, independent ground: under the superseded rule a model that **drops** the timing phrase **passes** clause 6, crediting the IR for silently discarding a deadline — the direction of loss `SPEC.md` §6 calls a correctness bug for a compliance tool.
  - **Exposure is one item, and the §10 re-check is logged rather than asserted.** A screen validated on a known answer first (Standing Principle 7), plus a deliberately looser sweep for **any** `within` across all 32 locked spans, returns exactly **two** hits — `C17-02`, restamped, and `C11-01`, which §15.5 already governs and which is untouched. `C17-02`'s `temporal` moves `null` → `{"form": "WITHIN", "amount": 24, "unit": "mo", "trigger": "the Commencement Date"}`, confirmed against `score._canonical_gold_temporal`/`_canonical_temporal` (note `unit` is the AST's `"mo"`, not the document's `"months"`). **Supporting evidence worth its own line: NO locked item carried a `WITHIN` temporal — zero of 32 — so the superseded rule had removed one of §3.7's five frozen forms from the gold set by rule.**
  - **Criterion 2 unmoved on both denominators, measured not assumed:** `C17-02` carries `within_preposition` so it is outside §9.1's in-force denominator either way, **and** it scores `MISSED` either way — the model quotes the rejected preposition into `temporal_raw` on **3 of 3 runs**, repair returned `{"repairs": []}` on runs 1–2 and proposed the grammar-unsatisfiable `"within 24 months"` on run 3, and the emitted `action: "OBTAIN"` is not in `ACTIONS` besides. **Dual-predicate disclosure per §3.6.1's v0.45 forward requirement (the word "free" is never used unqualified): `K` unchanged at `14/32`, published clause profile moves `6_temporal` 2 → 3 of 23.** **Under the IN-FORCE predicate the effect is ZERO** — §5.1 A1 marks `C17-02` `NON_CONFORMING` (cold's `action` slot `OBTAIN` is off-taxonomy), so it sits outside both `A`'s numerator and denominator and **`A` holds at `7/27`**; the `14/32` movement is to the *legacy* pre-§5.1 published run, which `tests/evals/test_harness_annotator_agreement.py` still reproduces exactly via its `PRE_RESTAMP` map, **extended by this ruling rather than relaxed**.
  - **`C17-066`'s three cassettes are now STALE and the re-record is DEFERRED to §10's freeze pass on reviewer instruction** — zero criterion-2 effect either way, so it does not earn its own live-spend session and rides along with the batched re-recording. Until then `C17-02` is a **known-stale, honestly-labelled** item, the same footing §22.1 gives `C14-02`. **§22.3's validity-transfer does NOT apply**: `C17-066` backs only `C17-02`, checked against the cassette directory rather than against a queue row.
  - **Decision 3 — §2.5/§2.5.1 REACHES v0.28-era segments; the general policy is written into guideline §10.2 and both `MAY` spans are ELIGIBLE.** The reframe is the ruling: the investigation scoped this as *"§3.6.1 retroactive vs §22.1 retention, pointing opposite ways"* — **they point at two different axes.** §3.6.1 reached back because it corrects the **instrument** and was affordable; §22.1 declined **not on the merits** (it calls `C14-02` *"a known wrong number carrying an honest label"*) but because conforming was **mechanically blocked**. §10.2's three parts: **(a)** a clarification reaches back, a new criterion does not; **(b)** reach and conforming cost are independent, and a known-wrong retained item must be **labelled**; **(c)** direction is the safeguard — retroactively *adding* an item is monotone, *removing* one or *narrowing* an accept-set is §3.4's forbidden direction. An "affordability" fourth part was considered and **rejected**: cost decides timing, never truth.
  - **The retroactivity answer does not actually decide the two spans, which is why it was a safe case to settle general policy on.** §2's exclusion row is **segment-level and gated on "only"** (*"Contains **only** a party's right with no correlative duty"*) and both segments carry annotated duties, so it could not reach either clause at v0.28. Both branches converge on annotate. **Disclosed against interest BEFORE the ruling, now a standing §10.2 requirement:** the model emits at each span on **1 of 3 runs**, so both items score modal `MISSED` (§6) and **lower** criterion 2.
  - **Neither span is an item yet.** Guideline §2.7 gains a sixth disposition value, **`PENDING_ANNOTATION`** — the original five could not express *"eligibility settled, item not yet drafted"* — which fails segment reconciliation exactly as a missing item does and stays **out** of the `not_annotatable` array for the same reason `AMBIGUOUS` does. Each span owes its own §14.4 drafting session. **`C11-094` stays `ESCALATED` because span #2 is untouched by this ruling; `C04-117` moves to `PENDING_ANNOTATION`.**
  - **A DRAFTING HAZARD FLAGGED INTO BOTH SEGMENT FILES SO IT IS NOT SILENTLY RESOLVED: cold's `MAY` drafts are NOT lockable as written.** Their `action_accept_set`s are `PURCHASE`/`BUY`/`ACQUIRE` (**zero** taxonomy verbs) and `DEFER`/`WITHHOLD`/`SUSPEND` (**one**), against §3.4's *"every **taxonomy** verb"*. §8.8's decision procedure must be run explicitly on `purchase` and `defer`, and **on `C11-094` #3 the answer moves a published figure**: cold's `known_gaps: []` would take §9's in-force denominator **9 → 10** and criterion 2 to `3/10 = 30.0%`, while `action_not_in_taxonomy` firing keeps it at `3/9 = 33.3%`. **§5.1 A1's ruling that off-taxonomy accept-set members are INERT governs annotator comparison (`K`) only and does NOT licence one in a gold item** — a distinction §7.5's *"approve/reject cold's item"* framing did not carry, now added there. Measured across all 41 cold items: **36 (87.8%)** carry ≥1 off-taxonomy member over **52** distinct verbs — a count that **reproduces §5.1 A1's own independently-measured figure exactly**, which is what validated the screen.
  - **A record-only correction made in passing and NOT restamped: `C11-01`'s `annotator_notes` contradict its own data**, claiming *"temporal=null with the literal phrase in `vague_temporal_phrase`"* while the field carries `RELATIVE_TO_TRIGGER(after, …)`. **The data is correct** — §15.5's v0.28 bullet names `C11-01` as its own worked case and rules exactly that; the note predates the generalisation. Corrected in the notes with the **stamp deliberately unchanged**, because `C11-01` is one of §9.2's nine in-force denominator items and a gratuitous restamp would stale `C11-094`'s three cassettes for nothing.

**All eighteen closed Phase 4 checkpoints, investigations, and decisions now live in `docs/PROJECT_HISTORY.md`**, under "Phase 4 checkpoints — closed," in completion order: the seven that make up the compiler pipeline (Obligation IR v1 spec → real obligation parser → real obligation typechecker → Extractor + Span Grounder → IR Compiler → parse-error repair loop → pipeline wiring), plus eleven later entries (obligations-persistence investigation → verifier → ir_hash → eval-harness pilot → leading-subordinate-clause grounding-fix → gold-set corpus and batch 1 → gold-set recalibration → scorer-wiring → failure-driver investigation and the v0.33 scoring amendments → compile-stage bottleneck investigation → the 2026-08-26 classifier-hardening deferral decision). They are moved rather than summarized: every finding, correction, audited test count, and deferred-with-reasons note is intact there. References to them elsewhere in this file ("see the verifier checkpoint", "the gold-set corpus and batch 1 checkpoint", and so on) mean that file, not this one. What stays in this section is current status only — the framing paragraphs and acceptance-criteria table above, and the carried-forward debt list below — not the checkpoint narratives themselves; being part of a still-open effort (the gold-set build, in particular) doesn't keep an individual closed checkpoint's body here, the same way it didn't for the seven pipeline checkpoints while the compiler pipeline they built was itself still open.

When this phase is completed and I confirm it, update this section to reflect Phase 5 before continuing work.

---

## Standing principles (do not violate these without asking)

1. **The system must be demoable at every commit.** Don't leave the stack in a broken state overnight.
2. **Deterministic core, probabilistic edge.** Anything a parser, typechecker, or solver can decide must not be decided by an LLM. If you're about to have a model output something that determines correctness (not just a draft/candidate), stop and ask.
3. **One trust boundary, three enforcement layers** for tenant isolation: gateway → application (`TenantConnectionPreparer` AOP setting the RLS GUC per-transaction + `@PreAuthorize`) → database (RLS, `FORCE`d, fail-closed). All three, always. Note: there is no JPA/Hibernate anywhere in `apps/core` — it's `JdbcTemplate` throughout, so "Hibernate `@Filter`" (as blueprint §10.9's diagram depicts for Layer 2) does not apply to this codebase; Layer 2 is the AOP described above, not a Hibernate filter.
4. **Every LLM output is untrusted input** — including, especially, when it originates from a document the user uploaded. Never concatenate document text into a system prompt.
5. **Prefer deleting a technology over adding one.** If a task seems to need a new piece of infrastructure, check the "explicitly deferred" list below first — it's probably already been considered and rejected for now.
6. **Property-based tests over code that touches real extracted or quoted document text must vary semantic/syntactic complexity, not just surface form, before their coverage counts as adequate.** Added after this exact blind spot cost real time twice, independently, in Phase 4: the IR-Compiler checkpoint's trailing-period bug (hypothesis-generated structural variation — units, amounts, arbitrary trigger text — plus hand-picked textbook phrasings, both passed clean while shipping a silent corruption on a realistic trailing sentence-period) and the leading-subordinate-clause grounding-fix checkpoint's bug (`test_ground.py`'s `NESTED_FIELD_NOT_IN_SPAN` cases and its grounding-invariant hypothesis property both vary only surface form — arbitrary alphabet strings, unrelated fabricated phrases — never real clause-structure complexity like a sentence-fronting subordinate clause). Before trusting a property/hypothesis suite's coverage for anything downstream of real extracted or quoted document text, ask explicitly: does this generator vary semantic/syntactic complexity, or only surface form? A generator that only varies the latter can pass exhaustively while missing an entire failure class — and, for anything whose "function under test" is actually an LLM's behavior rather than pure code (the grounding-fix case, not the IR-Compiler case), no property generator can close the gap at all; only sampling real model output (cassette or live) can.

7. **A verification script's output is not evidence until the script has been checked against a known-answer case.** Added after this failure class cost real time **four** times in a single work session, always the same shape: a lookup or regex **silently defaults to a wrong-but-plausible answer for an input it does not handle**, and the output looks reasonable enough to survive casual inspection. The four, all from the gold-set consolidation pass: (1) §19.7's OCR detector written `[a-hj-z]` to exclude the one-letter words *a* and *I* — but that class still contains `a`, so it matched "in a"/"of a" and fired on **11 of 18 items at a 100% false-positive rate with zero true positives**; (2) a coordination detector whose `{0,140}` window between two modals **missed the very sentence it was written to verify** (`C14-076`'s splitting sentence, ~148 chars), so a "verified" claim rested on a detector that never saw the case; (3) a party-subject classifier anchored `^\s*(?:that\s+)?` that missed `", Antares or its Subcontractor shall provide"` (leading comma) and `"that, if AMAG …, AMAG shall"` (subject not first), producing a 25.7%/48.5% split that was pure detector noise; (4) a verb-pair classifier using `MAP.get(v, '?')`, where two *unmapped* verbs both return `'?'` and compare equal — so `construed + enforced`, a governing-law clause that is not an obligation at all, was reported as a same-taxonomy-verb doublet. **A sibling instance in shell rather than in Python, recorded because the mechanism is distinct and worth finding on its own: a pipeline's exit code is the exit code of the LAST command in the pipe, not of the one you care about.** `pytest ... 2>&1 | tail -4` reported `exit=0` for a run that pytest itself had failed — `tail` succeeded, so the pipeline succeeded, and the real signal was discarded before anything could read it. The failure was visible only in pytest's own summary line, which is prose rather than a status code. Same shape as the four detector cases below: a status that looks authoritative while measuring the wrong thing. **A SECOND instance of the identical rule, found 2026-08-24 during the gold-set recording run — recorded because it generalises the guard beyond pipes.** The exit status of a compound command is that of its LAST component whatever the operator joins them — `;` no less than `|`. A backgrounded `python … > run.log 2>&1; echo "EXIT=$?"` was reported by the harness as **exit code 0** for a script that had in fact exited **1**: the trailing `echo` succeeded, so the wrapping shell succeeded. The job-completion notification therefore announced a clean run for a recording that had crashed mid-set on an HTTP 400, and the correct `EXIT=1` the `echo` itself printed was sitting in the log with nothing reading it, because the status line already said 0. It was caught only by reading the log rather than trusting the reported status — the same move that catches every case above. **A THIRD instance, found 2026-08-25 while pushing the v0.33 scoring work — and it is the one that falsifies this principle's own stated remedy, so the remedy below is corrected rather than merely annotated.** `git push ... 2>&1 | tail -5; echo "PUSH_EXIT=${PIPESTATUS[0]}"` printed **`PUSH_EXIT=`** — empty. Not a wrong number: **no number at all**, which reads as benign at a glance and is arguably worse than a wrong one, since there is no value to look twice at. **Cause: `PIPESTATUS` is a bashism.** This machine's shell is **zsh** (see the Environment block), where the array is `$pipestatus`, **lower-case and 1-indexed**; `${PIPESTATUS[0]}` is simply an unset variable and expands to the empty string with no error. Verified by execution rather than from memory, per this principle's own discipline: `false | true; echo "${PIPESTATUS[0]}"` → `''`, while `${pipestatus[1]}` → `1`.

**The mechanism is distinct from the two above and that is why it is its own instance.** Those two were *"the status came from the wrong command."* This is *"the status expression evaluated to nothing,"* a **shell-dialect** mismatch — the same family (a status that looks authoritative while measuring nothing) reached through a third door. It confirms the family recurs across **shells**, not only across tools and operators.

**CORRECTED REMEDY — the previous wording recommended `${PIPESTATUS[0]}` unconditionally, and on this repo's own default shell that recommendation is silently broken.** Any scripted check whose verdict comes from an exit code must not pipe OR postfix the command being judged. In order of preference: **(a) capture to a file and read `$?` directly on the very next line** — portable, works in every shell, and the only option with no dialect hazard; **(b) let the judged program be the last thing the compound command runs**; **(c) a pipe-status array ONLY with the shell confirmed** — `${pipestatus[1]}` in zsh, `${PIPESTATUS[0]}` in bash — and never assume which is in play. **(d) Best of all, where the thing being judged has a durable artifact: verify the outcome directly rather than through any status** — the push above was confirmed instead by diffing local `HEAD` against `git ls-remote`, which cannot be fooled by a dialect at all.

The pipeline instance was found 2026-08-22 while checking the full suite during the gold-set harness build; the compound-command one 2026-08-24 during the stage-4 recording run; this one 2026-08-25 during the v0.33 push. **A dated RECURRENCE of the compound-command sibling, 2026-09-05 during the F13/Decision-3 ruling session — recorded as a recurrence, not a new principle, because the mechanism is exactly the one already described.** A backgrounded `pytest -q > log 2>&1; echo "EXIT=$?"; tail -20 log` was announced by the harness's job-completion notification as **"exit code 0"**, and the log's own trailer read `[exited with code 0]`, for a run that had **1 failed, 493 passed, 53 skipped**. The trailing `tail` succeeded, so the wrapping shell succeeded; the correct `EXIT=1` the `echo` itself printed was sitting on the log's **first line** with nothing reading it. Caught only by reading the log rather than trusting the reported status — and the failure it hid was a real one that mattered (a known-answer reproduction test pinning a published figure the session's own ruling legitimately moved). **The remedy already stated above is the one that would have prevented it: capture to a file and read `$?` on the very next line, and do not postfix the command being judged.** **AN EIGHTH ENTRY, 2026-09-17 during the §8.8.4 amendment session — the compound-command sibling again, and this time it misreported a CI verdict rather than a test run.** A backgrounded `gh run watch <id> --exit-status > log 2>&1; echo "WATCH_EXIT=$?"` was announced by the harness's job-completion notification as **"completed (exit code 0)"**, and the log's own trailer read `[exited with code 0]`, for a CI run that had **FAILED**. `gh run watch --exit-status` did its job and returned 1 — **`WATCH_EXIT=1` is sitting in the log, on its own line, with nothing reading it** — but the trailing `echo` succeeded, so the wrapping shell succeeded. Identical mechanism to the 2026-08-24 and 2026-09-05 instances; the new part is the **domain**, a remote CI conclusion rather than a local test exit, which is the one place a false green is most likely to be taken at face value and shipped. **It was caught only because the run's status was polled separately via `gh run view` rather than read off the notification — sequencing, not discipline.** The remedy already stated above is the one that would have prevented it: capture to a file and read `$?` on the very next line, do not postfix the command being judged — or, best here, verify the outcome directly (`gh run view --json conclusion`), which cannot be fooled by a wrapper at all.

*(The list now holds **eight**: four detector cases plus four exit-code/status siblings — the 2026-08-25 `PIPESTATUS`-in-zsh one being the seventh and the 2026-09-17 `gh run watch` one the eighth. Each count is arrived at by **counting the list**, never by incrementing the previous claim: an earlier session called the 2026-09-05 recurrence the "seventh" when the list in fact held six, which is how a running tally drifts if it is trusted instead of recomputed.)*

**The discipline: before trusting a detector's counts, run it against cases whose answer is already established** — a previously measured ground truth (§2.4's six known OCR segments exposed (1)), or the specific case the detector was written to find (checking flagged context against the known sentence exposed (2)), or simply reading the flagged and unflagged examples rather than only the totals (exposed (3) and (4)). All four were caught by looking at real output against known cases, and none would have been caught by re-reading the pattern. This is the sibling of Standing Principle 6: 6 is about generators that vary only surface form, 7 is about detectors that fail silently on unhandled input — and both say the same thing, that a clean-looking result is not evidence until you have checked what it does on a case whose answer you already know.

---

## Tech choices that are LOCKED IN — do not re-litigate these

| Layer | Choice | Do not suggest instead |
| :--- | :--- | :--- |
| Core service | Java 21 + Spring Boot 3.3 | Quarkus, Micronaut |
| AI/compute service | Python 3.12 + FastAPI | Django, Flask |
| Frontend | Next.js 15 (App Router) + shadcn/ui + Tailwind | Vite SPA, Remix |
| Database | Postgres 16 + pgvector | A separate vector DB |
| Managed DB (demo) | Supabase Free | — |
| Managed DB (CI) | Neon, single persistent branch (see note below) | — |
| Cache / lock / broker | Redis 7, self-hosted | Memcached |
| Task queue | Celery on Redis only | Celery + RabbitMQ (explicitly rejected — see below) |
| Grammar/parser | Lark | ANTLR |
| Solver | Z3 | cvc5 (noted as fallback only) |
| Object storage | Supabase Storage → R2 later | — |
| Orchestration | Docker Compose | Kubernetes/Helm (deferred, see below) |
| MCP server language | Python (FastMCP), separate process | TypeScript (explicitly rejected — see ADR-0011) |
| Identity | Spring owns it entirely | Supabase Auth (explicitly rejected — two identity systems is a security anti-pattern) |
| PDF text/layout extraction | PyMuPDF (`fitz`), Python side | `pdfplumber`/`pypdf` (insufficient layout/block fidelity) — **see the PyMuPDF/AGPL bullet in the Phase 3 record in `docs/PROJECT_HISTORY.md` before assuming this choice is licensing-neutral** |
| OCR engine | **Tesseract 5, local dev only** (Apple Silicon) / **PaddleOCR PP-OCRv4, x86 Hetzner prod target** (blueprint §13.3's own default) | Do not treat this as one locked choice — it's a deliberate, documented dev/prod split, not a preference. **See the OCR bullet in the Phase 3 record in `docs/PROJECT_HISTORY.md` for why**, in short: PaddlePaddle now ships official macOS arm64 wheels, but current (2025-2026) GitHub issues still show it segfaulting/freezing on M1-M3 Macs specifically — a stability class wheel availability doesn't fix. Matches blueprint's own stated fallback ("on non-x86 dev hardware, expect to fall back to Tesseract... on x86 Hetzner, PaddleOCR is fine"). **Revisit at Phase 9 deployment** — same "decided for the current deployment shape only" framing as the PyMuPDF/AGPL note, not a permanent choice for either environment. |

**If you think one of these should change, say so explicitly and ask — don't just start using a different library because it seemed convenient.**

**Correction (found during Phase 3, apps/brain tenancy checkpoint):** this table previously claimed "Neon (branch per PR)" for CI, which was never actually implemented — what exists is a single, persistent CI Neon branch that both `ci-core.yml` and `ci-brain.yml` point at via `secrets.DATABASE_URL` (see each workflow's own comments). The row above now states what's actually true. Genuine branch-per-PR (an ephemeral branch created and migrated per PR, torn down after) is real, not-yet-done future work — see the carried-forward debt list in "Current phase" above — not something to assume already exists when reasoning about CI behavior.

---

## Explicitly deferred — do NOT introduce these yet

These were considered in the blueprint and deliberately cut from MVP. Reintroducing any of them before the stated phase is scope creep, even if it seems like the "more correct" choice in the moment.

| Thing | Deferred to | Why |
| :--- | :--- | :--- |
| Kafka / Redpanda | Phase 8, and only if ≥3 independent event consumers actually exist | Postgres outbox + polling relay is sufficient until then |
| RabbitMQ | Never (cut entirely) | One broker (Redis) for Celery; durability handled via Postgres job-state reconciliation, not a second broker |
| Kubernetes / KEDA / Helm | Phase 9, authored but not operated | Docker Compose on one host is correct at this scale |
| Terraform | Not planned | Negative value for one VPS and one DNS record |
| Stripe / billing | Cut entirely | No product benefit for a portfolio project |
| Sanity CMS | Deferred (YAML playbooks in repo for now) | Standing up a CMS for one author who is also the developer is premature |
| ML risk model (LightGBM) | Phase 9+, only once ≥500 resolved obligations exist | Cold-start: a trained model on no data is worse than a transparent heuristic. Use the deterministic hazard score until then. |
| n8n | Phase 8 | Notifications at MVP = one Spring `NotificationOrchestrator` sending email via SMTP |
| Autonomous evidence agent / MCP writes | Phase 7 | Read-only MCP tools only, until the human-approval flow exists |

---

## Non-negotiable engineering rules

- **Idempotency:** every mutating endpoint accepts `Idempotency-Key`. Every response carries `X-Request-Id`.
- **Tenant isolation:** every tenant-scoped table has `org_id` and an RLS policy from the moment it's created — not retrofitted later. Its repository must implement `TenantScopedRepository` (`apps/core/.../platform/tenancy/`), which `TenantIsolationArchitectureTest` enforces at build time: every public method must be `@Transactional`, and no raw `DataSource`/`Connection` field is allowed. This proves the precondition for isolation, not isolation itself — the actual guarantee is still the RLS policy + `TenantConnectionPreparer`, so write and test each new repository's isolation behavior for real, the ArchUnit rule alone isn't sufficient.
- **No `localStorage`/`sessionStorage` for tokens.** Access tokens live in JS memory only. Refresh tokens are `HttpOnly` cookies.
- **Migrations are immutable once merged.** Corrections are new migrations, never edits to old ones. Use expand/contract for anything destructive.
- **No raw SQL string concatenation, ever.** Parameterized queries only, in both Java and Python.
- **Extraction/compiler output must cite a verbatim source span.** If a candidate obligation's span text doesn't literally appear in the source segment, discard it — don't try to "fix" or reinterpret it.
- **Prompts are versioned files, not inline strings.** Once Phase 4 starts, every prompt lives in `brain/prompts/` with a version and is never edited in place — bump the version.
- **Every LLM call records `model_id`, `prompt_version`, cost, and latency.** Without this, nothing about the AI pipeline is reproducible or debuggable later.

---

## When you're unsure

- If a requested change conflicts with something in this file or the blueprint: **say so and ask**, don't silently comply or silently refuse.
- If you're about to add a new dependency, new service, or new piece of infrastructure not listed in the "locked in" table above: **ask first.**
- If a task seems to require skipping ahead to a later phase to "do it properly": **flag it, propose the minimal Phase-1-appropriate version instead**, and note what's being deferred.
- If tests are failing and the fix isn't obvious: **stop and explain what you found**, rather than working around the test or weakening an assertion to make it pass.

---

## Reference

Full detail for everything above lives in `OBLIGO_ENGINEERING_BLUEPRINT.md`:
- §0 — Critical review and the reasoning behind every deferral above
- §7 — MCP server design (read this before touching `apps/mcp`)
- §10 — Auth (read this before touching `apps/core/identity` or `apps/core/authz`)
- §19.7 — The eval harness and gold-set design (read this before Phase 4)
- §21 — Canonical phase plan, deliverables, and acceptance criteria per phase
- §22 — Repository structure (the exact folder layout to follow)
