package dev.obligo.core.platform.identity;

/** The five roles from blueprint §10.7 — deliberately no more, so permissions stay memorable. */
public enum Role {
    OWNER,
    ADMIN,
    LEGAL_OPS,
    MEMBER,
    AUDITOR
}
