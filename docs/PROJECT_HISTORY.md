# Obligo — Project History

This file holds the closed-phase and closed-checkpoint record that used to live in `CLAUDE.md`. It was moved verbatim, for context-budget reasons only — `CLAUDE.md` crossed 150k characters, and every session pays for that on every turn.

**Nothing here is superseded.** Every decision, finding, correction, and count below is still authoritative for the checkpoint it describes. If a claim here conflicts with something in `CLAUDE.md`, that is a real discrepancy worth flagging, not a case of this file being out of date.

`CLAUDE.md` remains the operating contract; it keeps the standing principles, the locked-in tech choices, the deferred list, the full carried-forward debt list, and the current-phase summary and acceptance-criteria table. Every individual Phase 4 checkpoint, investigation, and decision — including ones still feeding an open effort like the gold-set build — lives here once it is itself closed. Read this file when you need the detail behind a specific past decision.

---

## Phase 3 — Ingestion, Storage & Segmentation (complete)

Phase 3 (Ingestion, Storage & Segmentation) is complete and verified, locally and in CI, against real infrastructure (no mocks) — all four blueprint §21 acceptance criteria Met or explicitly, permanently deferred:
- **Presigned upload flow (§11.2)** — `sources` table with RLS from creation, upload-intent/commit against real Supabase Storage, SHA-256 dedup-on-reupload. Real finding: Supabase's signed-upload token ignores a caller-supplied expiry (always ~2h, not blueprint's 5 min target) — verified live that a leaked/replayed URL still can't do anything once the legitimate upload lands (`upsert:false`, no read scope, path-bound signature), so the real exposure is narrower than it first looked (a pre-upload race on an abandoned intent); bounded server-side to a 30 min staleness cap, not "solved."
- **File-security hardening (§11.6)** — magic-byte sniffing, size cap, PDF active-content rejection (JS/launch actions/embedded files), a three-part decompression-bomb guard (page count ≤500, 200:1 expansion ratio, 10s timeout), and server-side SHA-256 recomputation (a deliberate deviation from blueprint's sync/async two-tier split — done synchronously up to the full 50MB cap instead, since no async-worker infra exists yet). All proven with real PDFBox-generated and downloaded malicious/benign fixtures — including a real page-count-cap gap (blueprint's own literal "600-page file" case) found completely untested and closed during the Phase 3 sign-off pass, not assumed covered by the byte-size cap it was previously conflated with.
- **Tenant isolation ported to apps/brain (§10.9)** — the first Python-side Postgres access, proven with the same rigor as the Java-side suite (identical `pg_backend_pid` across tenant contexts, zero leakage) plus one write-path case Java's own test doesn't cover. `contextvars.ContextVar`, not `threading.local` — FastAPI's asyncio concurrency model means a naive port would leak one request's `org_id` into another's.
- **PyMuPDF text/layout extraction, offsets derived by construction, not post-hoc matching** — `page_text[char_start:char_end] == text` holds by construction for every segment, because the persisted text is literally what produced the offsets. Real, documented, not-fully-solved limitation: round-trip correctness is guaranteed; reading-order correctness on genuinely multi-column layouts is not (PyMuPDF's block order is a position heuristic, demonstrated on a real two-column fixture and asserted so a future fix changes the test loudly).
- **PyMuPDF is AGPL-3.0-or-commercial, not permissive** — confirmed against PyPI/GitHub, no permissive alternative exists with comparable layout fidelity. Accepted for the *current* deployment shape only (local/personal use — AGPL's network-copyleft clause is deployment-triggered, not distribution-triggered): **revisit at Phase 9 deployment**, not before, either confirming the AGPL obligation is acceptable or acquiring a commercial license.
- **OCR via Tesseract 5 (local dev, Apple Silicon) / PaddleOCR PP-OCRv4 (x86 Hetzner prod target)** — a deliberate dev/prod split, not a preference: PaddlePaddle ships arm64 wheels now, but current PaddleOCR still segfaults on M1-M3 Macs (a stability class wheel availability doesn't fix). Per-page (not per-document) scanned-page detection, MIN-aggregated block confidence (a deliberate choice over averaging — one badly-misread date/party-name/dollar-figure is a real correctness failure, not noise to smooth over), `LOW_OCR_CONFIDENCE_THRESHOLD` flagging proven against real degraded scans. **Revisit at Phase 9 deployment**, same footing as the PyMuPDF/AGPL decision.
- **Async segmentation via Celery on Redis** — job state lives in Postgres (`segmentation_jobs`), not a second broker, per blueprint's own stated design intent. `acks_late=True` plus a real macOS fork-safety finding (opening a network connection inside a forked child segfaults — CPython's documented limitation, not a bug here — fixed via `--pool=solo` on this machine, `--pool=prefork` on Linux CI/prod where the hazard doesn't exist) plus a Postgres-based staleness-reconciliation net for the real, verified gaps in how promptly `acks_late` redelivery actually happens under concurrent access.
- **SSE ingest-progress streaming** — apps/core polls apps/brain's existing job-status endpoint on an interval (not Postgres LISTEN/NOTIFY, which Neon's pooled connections don't support, and not a new Redis pub/sub channel), reusing the existing shared-secret-gated core→brain boundary rather than reopening it. Two real async-dispatch bugs found and fixed: this was the first async (`SseEmitter`-returning) endpoint the codebase has ever had, and `TenantJwtAuthenticationFilter` needed `shouldNotFilterAsyncDispatch()`/`shouldNotFilterErrorDispatch()` explicitly overridden to `false`, or Spring Security's own `AuthorizationFilter` failed closed on the container's internal completion/error dispatch.
- **10 varied, genuinely distinct PDF fixtures** (not padding — two new born-digital layouts and one mechanically-distinct OCR degradation, each checked for real structural variety before being added) prove offset-exactness end to end. The one permanent, explicit scope exclusion: **macro DOCX** — DOCX is rejected at upload-intent by the MIME allow-list (`{"application/pdf"}` only) before any macro-specific detection could ever run against it; revisit only if DOCX ingestion is ever added to the allow-list.

---

## Phase 4 checkpoints — closed

These are the closed Phase 4 checkpoints, in the order they were completed. Together they are the compiler pipeline `CLAUDE.md`'s own "Current phase" section describes as "complete and CI-verified end to end": candidate extraction → span grounding → compilation → repair → typecheck.

A later pass moved eleven more entries here (2026-08-27): the obligations-persistence investigation, the verifier, `ir_hash`, the eval-harness pilot, the leading-subordinate-clause grounding fix, and the gold-set effort's own checkpoints — corpus and batch 1, recalibration, scorer-wiring, the failure-driver investigation and v0.33 amendments, the compile-stage bottleneck investigation, and the 2026-08-26 classifier-hardening deferral decision. Each is individually closed even though the gold-set effort they belong to is not — the same relationship the seven compiler-pipeline checkpoints above had to the still-open compiler pipeline when they moved. `CLAUDE.md`'s acceptance-criteria table remains the compressed, current-status view; these entries are the detail behind it.

### Obligation IR v1 spec

**Obligation IR v1 spec checkpoint (§21 Phase 4) — `packages/ir-spec/` written and frozen. No parser, no typechecker, no extraction pipeline code exists yet — this checkpoint is the IR's shape only.**
- **Real discrepancy found and resolved, not guessed past:** blueprint §6.2's grammar sketch lists six temporal alternatives (`BY`, `WITHIN...OF`, `EVERY`, `DURING`, `AFTER`, `BEFORE`) against §21's stated freeze of five. Resolved by merging `AFTER`/`BEFORE` into one directional `RELATIVE_TO_TRIGGER(direction, trigger)` form — keeps all six alternatives' expressive power, lands on exactly five named forms as §21 requires.
- **"Conditions only, no nested exceptions" read deliberately as "no `UNLESS`/exception construct exists in v1's grammar at all,"** not "flat exceptions are allowed, just not nested ones." The safer reading for a tool whose whole purpose is catching what a human reader would otherwise miss — a silent drop of a legal carve-out is worse than a loud refusal to compile it. **Verified mechanically, not just documented:** `grammar.lark`'s own `IF` production parses correctly while a string containing `UNLESS` genuinely fails to parse (`UnexpectedCharacters`) — there is no accidental path by which an exception clause sneaks through this grammar's own rules.
- **A real compositional gap named and given its own worked "not representable" fixture:** an obligation's `temporal?` slot is singular, so `EVERY` (recurrence) and `DURING` (its outer bound) can't compose on one obligation even though each exists individually. v1's answer: `EVERY` alone, with the outer bound left implicit to the source document's own `effective_date`/expiration — real compositional temporal expressions are v2 scope, the same footing as nested exceptions.
- **Obligor/obligee reconciled from the grammar sketch's single abstract `party` slot against the ER schema's two distinct FK columns** (`obligor_party_id`, `obligee_party_id`, a CHECK that they differ) — `PartyRef` is a two-state (`UNRESOLVED` alias / `RESOLVED` party_id) tagged union, applied as a consistent convention everywhere the typechecker, not the parser, is responsible for resolving a reference (`DateRef`, `TriggerRef` use the identical shape).
- **`packages/ir-spec/` built per §22.4's own description** ("documentation-as-artifact... the grammar, the type rules, worked examples, and the shared test fixtures both runtimes validate against") — mirrors `packages/contracts/`'s existing JSON-Schema-as-source-of-truth precedent, not a Python-only Pydantic model set Java would have no way to check itself against: `SPEC.md` (12-section narrative spec) + `grammar.lark` (frozen Lark-syntax reference, not wired to any parser) + `schema/obligation-ir.schema.json` (the canonical both-runtimes artifact) + `examples/*.json` (9 worked examples covering all 4 modalities, all 5 temporal forms, both `PartyRef` states, both underspecification mechanisms) + `examples/not-supported/*.json` (4 boundary fixtures: bare `UNLESS`, `IF`+`UNLESS` mixed, multi-level nesting, the `EVERY`+`DURING` gap).
- **Verified for real, twice** — once while drafting, once fresh from a clean directory state per this file's own standing discipline: the schema checks as well-formed JSON Schema 2020-12; `grammar.lark` loads and parses a real sample obligation with no grammar/lexer conflicts; every example's `ir` key validates against the schema with every `alias`/`raw`/`raw_text` string confirmed a literal substring of its own `source_text` (the same span-grounding discipline the real pipeline will enforce, applied to the fixtures too, not assumed). **Two real classes of self-authored bugs caught by this discipline, not by eyeballing:** four `char_end` arithmetic mistakes (hand-computed offsets that went stale after editing the sentence they were computed against), and four internal section cross-references in `SPEC.md`/the schema that pointed at the wrong section number after the document was drafted out of the order it now reads in — caught by grepping every `section N` reference and mapping each one against the real header list, not by rereading and trusting.
- **Open item, explicitly carried, not silently invented:** the closed action taxonomy (~40 verbs; blueprint names 10 examples and no more) is validated in the schema only for lexical shape, not a closed enum. Finalizing the full list is deferred to writing `apps/brain/compiler/grammar/obligation.lark` (the next checkpoint) — decide it there, against real extraction candidates, don't invent it now. The object-class taxonomy carries the identical open-item status.

### Real obligation parser

**Real obligation parser checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/compiler/` (`grammar/obligation.lark`, `ast.py`, `parser.py`, `unparse.py`, `version.py`) written and wired to a real Lark parser, proven against `packages/ir-spec/`'s own frozen examples. Closes the action-taxonomy open item the checkpoint above explicitly deferred to this one. No typechecker, no extraction pipeline, no LangGraph code exists yet — this checkpoint is the grammar, AST, and parser only.**
- **Closed action taxonomy resolved from real language, not invented in the abstract** — 34 verbs. The PDF fixtures already in the repo were checked first and ruled out (they're Phase 3 layout-testing fixtures — an ML paper, a tax form, a definitions section — with no obligation-bearing sentences at all, a real negative finding, not an assumption); pulled real obligation sentences instead from three actual SEC EDGAR contract exhibits (a clinical-services MSA, a cross-border business-separation agreement with a DPA schedule, a transition-services agreement). All 10 of blueprint's named verbs confirmed with real quotes; 24 more derived the same way, capped comfortably under the ~40 estimate rather than padded to hit it. Verbs present in the corpus only as nouns (`REGISTER`, `AUDIT`, `CERTIFY`, `SUBMIT`, `FILE`, `DEFEND`, `INVESTIGATE`, `LICENSE`) were deliberately left out rather than forced in on vibes.
- **Four real, mechanically-verified bugs found in the reference `packages/ir-spec/grammar.lark`** while building the real one (that file was documentation-only — "not wired to any parser," per its own header — and had never actually been run through Lark this way before):
  1. `obligation` only referenced `party_ref` once despite the schema requiring both `obligor` and `obligee` — fixed with two distinct grammar rules at different positions.
  2. The bare `CNAME (" " CNAME)*` alias alternative, confirmed empirically via a minimal repro grammar (not just reasoned about), silently swallows the following `ACTION` keyword — `"Vendor DELIVER"` lexes as one alias, not two tokens — a silent misparse, not a clean failure. Fixed by requiring a quoted `ESCAPED_STRING` only.
  3. `predicate: predicate "AND" predicate | predicate "OR" predicate`, confirmed empirically, produces structurally identical parse trees for both operators with no surviving token to tell them apart — an AST built from that tree cannot distinguish `"a" AND "b"` from `"a" OR "b"` at all. Fixed with Lark `->` rule aliases.
  4. Caught by the round-trip property test itself, not by inspection: 3+-term `AND`/`OR` chains are ambiguous without grouping — a hypothesis-generated left-nested `AndPredicate` printed and reparsed came back right-nested, same flat string, different tree. Fixed by adding parenthesized-predicate grammar support and always parenthesizing compound operands in the printer.
- **`EVERY`+`DURING` composition resolved exactly as `examples/not-supported/every-during-composition.json` asked "the next checkpoint's grammar" to decide**: parses (not a hard failure like `UNLESS`, which still has no production at all — reverified mechanically against the real grammar), compiles as `EVERY` alone per `SPEC.md` section 7's documented v1 workaround, and the parser emits a real `ObligationCompositionWarning` rather than silently dropping the bound.
- **Round-trip proof against all 9 `packages/ir-spec/examples/*.json` and all 4 `not-supported/*.json` fixtures** — 6 of 9 examples match the frozen fixture bit-for-bit; the other 3 have documented, deliberate divergences (`RESOLVED` parties, `underspecified`/`missing_fields`) rather than being silently faked, since both are typechecker output and the typechecker isn't built yet.
- **Property-based tests: 2 of blueprint §19.3's 8 named properties are buildable at this checkpoint, and both are built** (round-trip, termination). The other 6 (idempotence, hash stability, typecheck soundness, temporal totality, grounding invariant, conflict symmetry) each genuinely require a component this checkpoint was told not to build (the normalizer, the typechecker, the Z3 verifier) — documented as deferred with reasons in `test_properties.py`'s own module docstring, not silently skipped.
- **New dependencies asked about before adding, not assumed:** `hypothesis` and `jsonschema` added to `apps/brain`'s dev dependency group (alongside `lark`, already locked in) — approved explicitly before any code was written, per this file's own "ask first" rule.
- Also surfaced during this checkpoint's own audit of what actually gates `apps/brain`, not one of its deliverables: the lint/type-check CI gap — see the carried-forward debt list in `CLAUDE.md`.

### Real obligation typechecker

**Real obligation typechecker checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/compiler/{typecheck.py,symbols.py}` written and proven against real Neon Postgres. No extraction pipeline, no LangGraph, no Z3 code exists yet — this checkpoint is symbol resolution and underspecification detection over an already-parsed AST only.**
- **The `parties` table (blueprint §8.2) didn't exist and was a hard blocker, not a design choice** — party-alias resolution (type rule 3) is this checkpoint's own named Phase 4 deliverable, and it had nothing to query against: checked every migration V1–V15 directly, confirmed absent. Not a Phase 5 pull-forward (Phase 5's own exclusion, per `packages/ir-spec/SPEC.md` section 11, is specifically the `obligations` table's lifecycle columns, not `parties`). Built as `apps/core/.../V16__create_parties.sql`, matching §8.2's inventory entry exactly (`id`; `org_id`; `canonical_name`; `aliases text[]`; `kind`; `external_ids jsonb`; `merged_into` self-FK; unique `(org_id, lower(canonical_name))`; GIN on `aliases`), RLS enabled and `FORCE`d identically to `organizations`/`sources`/`segments`, `obligo_brain` granted `SELECT` only (party creation/merge is a human-facing Phase 5 domain-layer concern, not this typechecker's). Applied to the real Neon branch via the Flyway CLI (the same recipe `ci-brain.yml` already uses) and verified directly against `information_schema`/`pg_class`, not assumed from the migration file: RLS on and forced, `obligo_brain`'s grant is `SELECT`-only, no grant added for `obligo_app` (no party-management endpoint exists yet to use it).
- **Tenant isolation is reused, not rebuilt.** `typecheck()` takes no `org_id` parameter; it relies on the caller having already called `TenantContext.set(org_id)` and opens its own `tenant_scope()` internally for the party-resolution query, the identical ambient-context convention every other apps/brain DB access already uses (`platform/tenancy/db.py`, `api/v1/sources.py`, `tasks/segmentation.py`). Deliberately not given an explicit `org_id` parameter instead, even though that would read as more obviously "pure" — introducing a second, different tenant-passing convention alongside the ambient one used everywhere else in this codebase was judged the worse inconsistency. Proven with the same real-Neon, skip-if-unset, owner-seeds/`obligo_brain`-reads discipline as `test_tenant_isolation.py`: a party registered in one org is invisible when typechecking under a different org's `TenantContext`, over RLS, not app-level filtering.
- **Of the six type rules, three fire for real, two never reach this function, and one is a permanent unconditional flag in v1:**
  - Rule 3 (party-alias resolution) is the one rule with real, data-backed resolution in this checkpoint — `symbols.resolve_party()` matches an alias against `parties.canonical_name`/`aliases` and returns a `ResolvedParty`, or leaves the alias `UnresolvedParty` (the AST's existing two-state `PartyRef` shape *is* the `UNRESOLVED_PARTY` signal; no separate sentinel was invented for it, per `packages/ir-spec/SPEC.md` section 4's own convention). An ambiguous multi-row alias match is treated as no match, not a guess.
  - Rules 2 (`BY`/`DURING` date resolution) and 4 (trigger resolution) fire consistently — an unresolved `DateRef`/`TriggerRef` always marks `underspecified` and records its dotted path in `missing_fields` — but always resolve to "stays unresolved" in this checkpoint, since neither has a real data source to resolve against yet. See the two carried-forward debt entries in `CLAUDE.md` for exactly what's missing for each and which future checkpoint owns building it — not restated here.
  - Rule 6 (business-day jurisdiction) isn't a resolution attempt at all, just an unconditional presence check: any `Duration` with `unit == "bd"` is flagged `underspecified` outright in v1, for the same missing-data reason covered by the debt entry in `CLAUDE.md`.
  - Rules 1 (`WITHIN` requires an explicit unit) and 5 (`ACTION` must be in the closed taxonomy) are **already fully enforced by the grammar** (`duration: NUMBER UNIT` makes the unit mandatory; `ACTION`'s own terminal alternation is the closed 34-verb taxonomy) — a `WITHIN` with no unit, or an action outside the taxonomy, fails to parse and never reaches `typecheck.py` at all. Confirmed by re-reading `grammar/obligation.lark`, not assumed from the type-rule list — these two rules have no corresponding logic in `typecheck.py` because there is nothing left for them to check by the time an AST exists.
- **One deliberate non-decision: `temporal is None` is never flagged**, even though `packages/ir-spec/examples/underspecified-missing-unit.json`'s own note assigned "how to recognize a vague temporal phrase as underspecified" to "the next checkpoint" — this one. Once "notify Customer promptly" has been through candidate extraction and DSL generation, the word "promptly" itself is gone; `temporal: None` in the parsed AST is genuinely indistinguishable from an obligation that correctly has no temporal element at all, using only the AST this function receives. Recovering that distinction needs the source text or the extraction candidate, neither of which `typecheck.py` has access to by design (it takes an AST, not a document) — deferred to the extraction/critic stage (blueprint §6.3 stage 6), documented as a reasoned deferral in `typecheck.py`'s own module docstring, not a silent gap.
- **`typecheck(obligation: ast.Obligation) -> ast.Obligation`** — no separate result/diagnostics wrapper. `packages/ir-spec/SPEC.md` section 4's own convention (any typechecker-resolved field already carries a two-state UNRESOLVED/RESOLVED shape) plus the AST's existing `underspecified`/`missing_fields` fields are sufficient to represent every outcome this checkpoint produces; a caller checks `isinstance(result.obligor, ast.UnresolvedParty)` for the `UNRESOLVED_PARTY` signal rather than reading a bolted-on sentinel. The review-queue *routing* this implies (blueprint §6.3 stage 4: "unresolved party → review queue") is left to whatever owns the `obligations` table and its UI (Phase 5's domain model), per `SPEC.md` section 11's own scope boundary — this function's job stops at producing a correctly-annotated IR.
- **Proven against real Neon Postgres throughout, no mocking** — `tests/compiler/test_symbols.py` (5 cases: canonical-name match, alias match, no-match, ambiguous-match, cross-org RLS isolation) and `tests/compiler/test_typecheck.py` (12 cases, one per rule-and-disposition above, obligations built via `parser.parse()` on real DSL strings rather than constructed AST literals). Full `apps/brain` suite re-run clean afterward: 96 passed, 5 skipped (pre-existing, unrelated — fork-safety and OCR platform skips), 0 failed.

### Extractor + Span Grounder

**Extractor + Span Grounder checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/graphs/{extraction.py,state.py}`, `apps/brain/src/obligo_brain/models/providers/{base.py,groq.py}`, `apps/brain/src/obligo_brain/prompts/{registry.py,registry.yaml,extraction/v1.yaml}` written and proven against a real Groq model, real cassettes, and real Neon Postgres. No IR Compiler, no LangGraph, no Z3 code exists yet — this is the first LLM call this codebase ever makes, plus the deterministic gate that makes its output trustworthy.**
- **Groq adapter is module-level `httpx` functions, deliberately not the `groq` PyPI package** — Groq's REST API is OpenAI-compatible (`POST /openai/v1/chat/completions`), `httpx` is already a dependency, and CLAUDE.md's "ask first before adding a dependency" rule plus blueprint §13.1 rule 2 ("every model sits behind an adapter interface") are both better served by one thin adapter than a second, differently-shaped SDK Gemini's eventual adapter would need its own version of anyway. `ChatModel` is a `Protocol`, not an ABC — `groq.complete()` (a plain function, same style as `platform/storage/supabase.py`) satisfies it structurally, and any test double (lambda, cassette-backed stub) *is* a `ChatModel` with no fake class hierarchy required. `model_id` is passed in by the caller from the versioned prompt's `model_constraints`, never hardcoded in the adapter — the pin belongs in the prompt artifact (§13.8), and `list_models()` exists as a manual, by-hand verification step (confirmed live against Groq's model list before `llama-3.3-70b-versatile` was written into `extraction/v1.yaml`), not an automatic boot check that doesn't exist yet.
- **The prompt registry structurally prevents document text from reaching the system prompt — not by convention, by construction.** `registry.render()`'s system value is `prompt.system`, returned completely unmodified — never passed through `.format()` or any other substitution — so `segment_text` cannot reach it even by a future coding mistake in that function; only the user message is templated (`{nonce}`, `{segment_text}` via plain `str.format`, no jinja2 — CLAUDE.md's "ask first"/Standing Principle 5 both argue against a templating dependency for two placeholders). The user message wraps `segment_text` in a nonce-delimited fence (`<<<SEGMENT_{nonce}>>>` / `<<<END_SEGMENT_{nonce}>>>`) so a document can't forge its own closing delimiter and make its content look like it ends before it does; the system prompt declares fenced content data, never instruction (§17.5 defense-in-depth). The fence is defense-in-depth, not the actual control — the real control is that nothing downstream (grammar, typechecker, grounder) can mutate state without all three agreeing first. Prompts are versioned files (`prompts/extraction/v1.yaml` + `registry.yaml` mapping task→environment→version), never edited in place; `Prompt.prompt_hash` is a SHA-256 of the raw unrendered template (system + NUL + user_template), independent of `input_hash` (which is per-call, segment-specific) — the pair `(prompt_hash, model_id)` alone answers "did this call use the prompt version we think it did" (NFR-10, §13.5).
- **Span grounding (`ground_candidates()`) is the actual correctness gate — pure, no I/O — and gets the heaviest test suite of anything built in this checkpoint**, per blueprint §21's acceptance criterion (span-grounding rate exactly 100%, zero ungrounded obligations reach the database, by construction). Two tiers, deliberately not three: Tier A (EXACT, `segment.text.find`) then Tier B (NORMALIZED — NBSP/whitespace-run collapse, zero-width-character removal, smart-quote/dash folding, with an index map back to original offsets so the stored span is always `segment.text[char_start:char_end]`, never the model's string). **Tier C (Levenshtein-tolerant fuzzy matching, blueprint §6.3's "≤5% tolerance for OCR noise") was deliberately rejected, with a concrete adversarial case proving why**, not just argued abstractly: in a 200-character clause, "shall" → "shall not" is a ~2%-edit-distance mutation that inverts the obligation's meaning — comfortably under a 5% threshold, which disqualifies fuzzy tolerance on the single most important correctness control in this project by that example alone (`test_ground.py::test_modality_inverting_mutation_is_rejected_the_tier_c_argument_made_executable` makes this concrete, not hypothetical). The OCR-noise case Tier C exists for is already covered by `segments.ocr_confidence`/`LOW_OCR_CONFIDENCE` (Phase 3), which doesn't carry this hazard. An ambiguous multi-occurrence match (Tier A or B) is rejected outright (`AMBIGUOUS_SPAN`), never guessed between candidates — the same precedent `symbols.resolve_party()`'s ambiguous-match handling set.
- **`agent_runs` (blueprint §8.2's own inventory, never migrated — checked V1–V16 directly) was built now, at this checkpoint, not deferred again.** CLAUDE.md's debt list was already three entries deep in "called non-negotiable, never built" (Idempotency-Key/X-Request-Id, the cross-tenant leakage suite, `apps/brain`'s lint/type-check CI gate) before this checkpoint started — the first real LLM call this codebase makes was judged exactly the wrong moment to add a fourth. `input_hash` (`sha256(segment_id, prompt_hash, model_id)`) is recorded now even though the response cache that would key off it (§6.10) isn't built yet, specifically because backfilling it after these calls have already happened would be impossible. `cost_usd` is left nullable — computing it needs a real per-model pricing table, model-router scope, not this checkpoint's. RLS enabled and `FORCE`d identically to every other tenant-scoped table; `obligo_brain` granted `INSERT` only (nothing in this checkpoint reads `agent_runs` back — the eval harness and any future agent-run UI are separate, not-yet-built consumers), no grant to `obligo_app` (no endpoint reads/writes it yet).
- **Three-layer test strategy (blueprint §19.2), most of it never touching a network:** Layer 1 — pure unit tests with no I/O at all (`test_ground.py`, 20 cases including 2 hypothesis properties closing out `test_properties.py`'s deferred "grounding invariant"; `test_extract_candidates.py`, 5 cases, a fake `chat_model`, no live call). Layer 2 — cassette-replay (`test_extraction_cassette.py`, 5 cases, an `httpx.MockTransport` replaying a real previously-recorded Groq response — proves this code's handling of what the model actually said, not a guess at what it might say; each test skips individually if its cassette isn't recorded yet, rather than the whole file skipping as a block). Layer 3 — one deliberate, opt-in exception to "unit tests never hit a network": `test_extraction_live.py`, gated on `OBLIGO_LIVE_LLM=1` + `GROQ_API_KEY` (same skip-if-unset discipline as `DATABASE_URL`/`SUPABASE_*` elsewhere in this repo), not wired into CI — a rate-limited external dependency doesn't belong on the merge path, and catching model drift over time is the eval harness's job (§19.7), not built yet.
- **The cassette-mismatch finding: the real model was materially more robust than the 4 adversarial segments (each crafted to elicit one specific grounding-gate failure) assumed it would be — a real, live finding, not a shortfall in what got tested.** Recording session 2026-08-10, one live call per case against the real `llama-3.3-70b-versatile` extraction prompt, no retries: only `injection_canary` reproduced its intended category on the first try; `span_not_found`, `paraphrase`, and `malformed_json` did not, each producing a real-but-different outcome instead. **All three were kept and repurposed to prove something else real rather than discarded or silently re-recorded until they "worked"** — matching this file's own "a recorded finding is a false-positive signal with a prompt_version attached; throwing it away would make the extractor un-evaluable" reasoning (`graphs/state.py`'s `RejectionReason` docstring). Full per-cassette account (what each was recorded to elicit vs. what it actually proved, and exactly what's still genuinely unproven against real model output) lives in the carried-forward debt list in `CLAUDE.md` (the "No cassette demonstrates..." bullet) and `apps/brain/tests/cassettes/extraction/README.md` — not restated here.

### IR Compiler

**IR Compiler checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/compiler/ir_compile.py` written and proven, no DB, no LLM, no network. This is the deterministic bridge between the Extractor/Span Grounder checkpoint's `GroundedCandidate` and the parser/typechecker checkpoint's `parser.parse()` — this checkpoint is DSL construction and classification only.**
- **`compile_candidate(candidate: GroundedCandidate) -> ast.Obligation | CompileFailure`**, plus a batch form `compile_candidates()` mirroring `ground_candidates()`'s partition shape (every input ends up in exactly one of the two output lists — nothing silently dropped). Pure function, same "do NOT use LangGraph when the flow is linear" reasoning already applied to the Extractor/Grounder — and deliberately NOT the parse-error repair loop (§6.3 stage 3's own, separate, not-yet-built failure path): no retry, no model call, classify-or-fail once.
- **Two real classification problems, both resolved by deterministic pattern matching, not a second LLM call** — Standing Principle 2 applies directly, since which of the five frozen `Temporal` forms an obligation gets, and what its extracted duration/date/trigger actually is, determines the compiled IR's meaning, not a draft:
  - `temporal_raw` → one of the five frozen `Temporal` forms, via five regexes each anchored to its own distinct leading keyword (`within`/`by`/`every`/`during`/`before`|`after`) — a closed-vocabulary problem structurally identical to what the grammar already does for `ACTION`/modality, not open-ended NLU. Because the five keywords are mutually exclusive leading tokens, no two forms can ever match the same phrase (`AMBIGUOUS_TEMPORAL` is structurally unreachable under this design and wasn't built as a separate reason).
  - `condition_raws` → each entry becomes exactly one `AtomPredicate` wrapping its raw text verbatim; multiple entries become multiple separate `IF "..."` clauses (implicit conjunction), never structured by detecting `AND`/`OR`/`NOT` inside a single string — unlike the temporal keyword vocabulary, boolean connectives in free legal text are genuinely ambiguous (`"not later than"` contains "not" but isn't a boolean negation), a real misclassification hazard a closed-vocabulary regex can't safely resolve. Because every string is trivially a valid atom, there's no failure mode here at all.
- **Unmappable `temporal_raw` rejects the whole candidate (`CompileFailureReason.UNMAPPABLE_TEMPORAL`), never silently compiles with `temporal=None`** — same "never silently drop" precedent as `NESTED_FIELD_NOT_IN_SPAN` discarding a whole candidate rather than patching one field. Dropping just the temporal field would silently understate the obligation (a timed "notify within 5 days" quietly becoming an untimed "notify" is a different, weaker obligation), and there's no AST slot to hold "has a temporal phrase, couldn't classify its shape" — `Temporal` is frozen at exactly five variants. A `GroundedCandidate` whose `temporal_raw` is genuinely `None` (no temporal phrase extracted at all) compiles `temporal=None` correctly — that's not a drop.
- **`modality`/`action`/`object_class` are only lightly, deterministically canonicalized** (case/whitespace normalization) and then left for the real grammar to validate as the closed-taxonomy authority — re-checking `ast.MODALITIES`/`ast.ACTIONS` here would duplicate what `grammar/obligation.lark`'s own terminals already enforce; any mismatch surfaces as `CompileFailureReason.PARSE_ERROR` with the parser's own precise diagnostic.
- **Ordering relative to `typecheck.py`: strictly before, never interleaved**, and this checkpoint changes nothing about tenant-scoped party-resolution timing. `compile_candidate()` opens no DB connection and calls `tenant_scope()` nowhere — pure, like `ground_candidates()`. Party aliases pass through unchanged as quoted strings; `parser.py` still turns them into `UnresolvedParty`; `typecheck.py`'s existing `tenant_scope()`-scoped `symbols.resolve_party()` call runs unchanged, strictly afterward.
- **A real, named scope limit: `DURING` only accepts two ISO-8601 date literals, not a defined-term alias on either side** (`"during the Term"` rejects, `UNMAPPABLE_TEMPORAL`) — splitting one free-text interval phrase into two separate alias termini deterministically is genuinely ambiguous, unlike `WITHIN`/`BY`'s single trailing phrase. Falls through loudly rather than guessing a split.
- **Real bug found and fixed by adversarial testing against realistic (non-textbook) phrasing, not caught by the original hypothesis/fixture suite:** `"by 2026-01-01."` — a trailing sentence-terminating period, exactly what a verbatim quote of the end of a real sentence looks like — silently downgraded a genuinely resolvable date into an unresolved alias (`UnresolvedDate(raw="2026-01-01.")` instead of `ResolvedDate("2026-01-01")`), a **silent corruption**, not a loud failure — the same category of bug this project's span-grounding discipline exists to catch, just one layer downstream of it. `EVERY`/`DURING` had the identical anchor problem, manifesting as safer-but-still-wrong under-rejection instead of corruption. **Fix scoped narrowly, not as a blanket period-strip:** trailing-period tolerance was added only at the three fixed-format token boundaries (`BY`'s date check, `EVERY`'s unit anchor, `DURING`'s second date anchor) — every one a closed, unambiguous format that can never legitimately contain a trailing period itself. Free-text trigger/alias capture (`WITHIN`'s/`RELATIVE`'s trigger, `BY`'s alias fallback) was deliberately left untouched and verified untouched (`"within 5d of Corp."` still preserves `"Corp."` verbatim in trigger text, not stripped) — an abbreviation's period is meaningful and must stay byte-verbatim, per this codebase's span-grounding discipline; only a token with zero legitimate use for a trailing period gets the tolerance.
- **The explicit lesson, not just the bug:** this checkpoint was first built and tested at `/effort medium`, using hypothesis-generated structural variation (units, amounts, arbitrary trigger text) plus hand-picked textbook phrasings ("within 5 business days of X") — and that suite passed clean while still shipping the silent-corruption bug above. A second adversarial pass, specifically testing realistic verbatim-quoted phrasing (trailing punctuation, non-canonical synonyms like "no later than"/"on or before"/"prior to", spelled-out numbers, "calendar days") found it immediately. **Regex/classification code whose input is real extracted document text — not just structurally-varied synthetic input — needs adversarial-phrasing testing as part of its first pass, not as a follow-up prompted by a direct question.** Same standing as the Extractor/Span Grounder checkpoint's real-cassette-over-synthetic-only discipline; apply it by default to any future deterministic classifier over LLM-extracted text (e.g. a future date/currency/jurisdiction normalizer), not only when asked.
- **Realistic phrasings confirmed correctly out of scope, not bugs** — `"no later than"`, `"not later than"`, `"on or before"`, `"prior to"`, spelled-out numbers (`"thirty (30) days"`), `"calendar days"`, and bare recurrence words (`"annually"`) all correctly reject as `UNMAPPABLE_TEMPORAL` rather than being silently misclassified, consistent with this checkpoint's own "strict over loose" decision — pinned as regression tests (`test_realistic_out_of_scope_phrasings_reject_rather_than_misclassify`) so the boundary stays a tested, intentional decision rather than an unexamined gap. Widening coverage to include these is real future scope for whoever next touches this classifier, not something to add quietly.
- **Proven with unit tests per temporal form/failure path plus two hypothesis round-trip properties** over the `WITHIN`/`EVERY` duration classifier across all 6 units (`tests/compiler/test_ir_compile.py`, 34 cases) — the classifier's own analog to `test_properties.py`'s round-trip property for the grammar itself. Full `apps/brain` suite re-run clean afterward: 140 passed, 39 skipped (pre-existing, unrelated), 0 failed.
- **Not built at this checkpoint, deferred, not silently skipped:** the parse-error repair loop (§6.3 stage 3's failure path — feeding the parser's message back to the LLM, ≤2 retries) and LangGraph itself (this function and Extractor/Grounder remain plain functions, not graph nodes, until the first real conditional edge exists — most likely the repair loop). Also still open from this checkpoint's original scope: a round-trip proof of `compile_candidate()` against `packages/ir-spec/examples/*.json`'s frozen fixtures, the way `parser.py`'s own checkpoint proved itself against those same fixtures — not built here, since those fixtures are already-typed IR (post-typecheck), not raw `temporal_raw`/`condition_raws` phrases, and reverse-engineering a plausible `GroundedCandidate` from each fixture's `ir` field to round-trip through was judged out of this checkpoint's scope, not attempted and quietly dropped. A real future task, not assumed done.

### Parse-error repair loop

**Parse-error repair loop checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/graphs/repair.py`, `prompts/repair/v1.yaml`, `apps/core/.../V18__create_compile_quarantine.sql`. The first genuinely branching logic in the extraction pipeline. Still no LangGraph, deliberately — see below. No Z3, no Critic, no Linker.**
- **Budget resolved against the blueprint, not picked: `MAX_REPAIRS = 2`.** Full reasoning in the discrepancy note at the top of this section and in `repair.py`'s module docstring — five blueprint locations say 2, §21 alone says 3, and the blueprint's genuine "≤3" belongs to §13.8's separate, unbuilt schema-failure loop.
- **LangGraph deliberately NOT introduced, for the third time — with a falsifiable trigger written down so the next checkpoint doesn't re-argue it from scratch.** `state.py`/`extraction.py` had both pre-committed to "LangGraph gets introduced at whichever future checkpoint adds the first real conditional edge (most likely the parse-error repair loop)"; that prediction came due here and was declined on the merits. The cycle is one node with one back-edge and a bounded counter — a `while` loop expresses it completely. §6.8's own three justifications are either already satisfied (`agent_runs.node_trace` is the trace §6.8 says the graph maps 1:1 *to*; per-node retry is implemented here with domain-specific hint derivation a generic retry can't supply) or not yet needed (crash-resume: the whole loop is one synchronous call in one process). And Postgres checkpointing means a **second** dependency (`langgraph-checkpoint-postgres`) whose library-managed tables would hold org-scoped intermediate state — segment text, candidate JSON — outside this repo's RLS-from-creation discipline, which is a real security decision, not a packaging detail. **Trigger, in the same shape as the Kafka rule ("only if ≥3 independent event consumers actually exist"): adopt LangGraph when BOTH (a) ≥2 independent conditional edges exist — the Critic or Router node, not just this back-edge — AND (b) the pipeline spans more than one Celery task boundary.** "It's about time" is not a reason. Lives in `graphs/` rather than §6.1's suggested `compiler/repair.py` because it makes LLM calls and DB writes, and `compiler/` is deliberately I/O-free — a documented deviation.
- **Blueprint §6.3 stage 1 assumes "LLM → DSL"; this codebase does "LLM → JSON candidate" + deterministic DSL construction, so "feed the parser error back" needed translating rather than implementing literally.** Handing the model a Lark diagnostic about a string it never wrote would teach it to author DSL, routing around both the deterministic bridge and the grounding gate. Instead the parser's verdict is translated into field-level `RepairHint`s about the JSON the model did write. **That translation is exhaustive, not best-effort:** tracing every path from `_build_dsl` to the grammar (`_q()` is `json.dumps` so every `ESCAPED_STRING` slot is well-formed by construction; `segment_id` comes from the DB; offsets come from the grounder; `format_confidence`/`_format_amount`/`_unit_token`/`_DATE_RE` guard the rest), **exactly three fields can ever cause a `PARSE_ERROR`: `modality`, `action`, `object_class`.** All three are *classification* fields, none is span-grounded — which is precisely what makes repairing them safe. The raw Lark message is recorded in `agent_runs.node_trace` and never sent to the model.
- **No actionable hint ⇒ no model call.** An unexplainable `PARSE_ERROR` quarantines immediately with zero LLM calls; a repair prompt with no actionable hint is a coin flip that costs money and a rate-limit slot. Same "strict over loose" posture `ir_compile.py` takes on `DURING`.
- **A repair is structurally incapable of fabricating a quote.** The repaired candidate is built by copying the original `LLMCandidate` and overriding **only fields that carried a hint**; `span_text`, the aliases, `object_raw_text`, `condition_raws` and `confidence` are never read from the model's response at all. Standing Principle 2 applied to a retry: don't trust the model to obey an instruction that determines correctness — make disobeying it inert. The one repairable field that *is* grounded, `temporal_raw`, gets no bespoke check; the repaired candidate goes back through `ground_candidates()` in full, which stays the sole authority (a rewrite is caught as `NESTED_FIELD_NOT_IN_SPAN` and quarantined as `REPAIR_BROKE_GROUNDING`). A `null` `temporal_raw` is never accepted — that would "fix" the compile by silently turning a timed obligation into a weaker untimed one.
- **`UNMAPPABLE_TEMPORAL` is included but is honestly expected to be mostly unrepairable, and that is written into the code rather than discovered later.** `temporal_raw` must stay verbatim, so if the source says "no later than thirty (30) days after the Effective Date" *no substring will ever match* — the real fix for that class is widening `ir_compile._classify_temporal`'s vocabulary, already named as future scope there. The one genuinely repairable shape is **over-capture** (narrowing to a shorter verbatim substring that is a clean timing phrase). **Measure the real success rate when the eval harness (§19.7) exists; if it lands near zero, demote `UNMAPPABLE_TEMPORAL` to quarantine-immediately.** Worth noting the test fixture for this had to be chosen against the real classifier rather than assumed: the obvious-looking `"within 30 days of X, as extended"` actually compiles fine (WITHIN's trigger capture is `.+`) — the real over-capture case is `"every 30 days during the Term"`, since `_EVERY_RE` is end-anchored.
- **Batched by segment, not by candidate** — one call carries every currently-failing candidate with its own index and hints, the next carries only those still failing. `max_repairs=2` per *candidate* would cost up to 20 extra calls for a 10-candidate segment against a free tier that is rate-limited per minute and per day (§13.7/§13.8), and §6.10's budget-enforcing router isn't built. This bounds repair calls at 2 per segment regardless of candidate count and makes §3.9's "fail 3×" arithmetic exact. An attempt that changes nothing at all short-circuits the loop (`REPAIR_MADE_NO_PROGRESS`) rather than paying for a temperature-0 repeat of an identical payload.
- **`compile_quarantine` (V18) built now rather than deferred to Phase 5's review queue, because `agent_runs` genuinely cannot absorb this** — two independent hard reasons, both found by reading V17 directly: `obligo_brain` holds `INSERT` only (no `UPDATE`), so the repair stage physically cannot amend the extractor's row; and a `NO_ACTIONABLE_HINT` quarantine makes **zero** LLM calls while `agent_runs.model_id`/`prompt_id`/`prompt_version`/`prompt_hash`/`input_hash` are all `NOT NULL` — recording it there means fabricating values into the exact columns whose only purpose is reproducibility. Deliberately **not** the `quarantine` table §8.2 names, which §11.6/§12 define as poison-message/failed-upload storage with a 30-day purge — different lifecycle, different reader, different retention. RLS enabled and `FORCE`d from creation, `obligo_brain` `INSERT`-only, no `obligo_app` grant, no `UPDATE`/`DELETE` (a quarantine row is an immutable record of a decision; "this was later fixed" belongs to whatever supersedes it under a bumped `grammar_version`, per §6.2's recompile job).
- **`agent_runs` accounting: one row per LLM call, never aggregated.** Collapsing a multi-call sequence into one row loses per-call `model_id`/`prompt_version`, which breaks the moment §6.10's router does provider failover mid-sequence. `node='repair'` distinguishes them; cost/latency per segment is a `SUM` over the already-indexed `segment_id`, so no `parent_run_id` column was added — lineage travels via `node_trace` and via `compile_quarantine.agent_run_ids`. `cost_usd` still NULL (needs a pricing table; model-router scope).
- **`input_hash` had to diverge for repair, and the divergence is a real hazard avoided, not a style choice.** `extraction.py`'s `sha256(segment_id : prompt_hash : model_id)` would make two different failing candidates *from the same segment* collide on one `input_hash` — and V17 records that column as the key for §6.10's future content-hash response cache, so a collision would serve one candidate's repair for another's. `repair_input_hash()` hashes the rendered `system`/`user`/`model_id` instead. The general rule is "hash the actual rendered call inputs"; extraction's formula is a valid *special case* (segment_id + prompt_hash fully determine its rendered message). Documented in both places rather than left as two unexplained formulas.
- **The 34-verb taxonomy is hardcoded into `prompts/repair/v1.yaml`, not injected at render time** — an injected taxonomy would let the same `prompt_hash` produce different model behaviour whenever `ast.ACTIONS` changes, breaking exactly what `prompt_hash` exists to guarantee (NFR-10, §13.5). The cost is drift, bought back by `tests/prompts/test_repair_prompt.py` asserting the prompt's verb list equals `ast.ACTIONS` exactly, so a taxonomy change fails CI loudly and forces a version bump. Sidenote, deliberately not acted on: `extraction/v1.yaml` never lists the closed action set (only five examples), which is plausibly the dominant cause of repairable parse errors — whether listing all 34 helps or just biases the model toward a listed verb is an **eval-harness question**, not something to guess at here.
- **`registry.render_repair()` is a separate function rather than a generalized `render(prompt, **vars)`**: `render()`'s Standing-Principle-4 guarantee is a property of its *signature* as much as its body — a caller physically cannot pass anything but `segment_text`, so the set of things that could ever reach a system prompt is enumerable by reading one line. Widening to `**kwargs` would trade that for a convention. Both untrusted inputs (document text, and the failures payload which quotes document text) are nonce-fenced; asserted, not assumed, that a brace in the document cannot create a new substitution point (`str.format` only scans the template).
- **REAL BUG FOUND AND FIXED, in the Extractor checkpoint's code, not this one's: `INSERT ... RETURNING id` against a table granted `INSERT` only.** Postgres requires `SELECT` privilege on any column a `RETURNING` clause names, so `extraction.py`'s `_record_agent_run()` would have failed with `permission denied for table agent_runs` on **every** call against the real database. It survived because V17 had never been applied to the Neon branch *and* because no real-database test exercised `run_extraction` at all (both now separate debt entries in `CLAUDE.md`). Isolated live against the real DB before fixing — the identical statement without `RETURNING` gets past the privilege check and reaches the RLS policy — rather than inferred from the error message. Fixed by generating the surrogate key client-side in both callers, which keeps V17's minimal grant intact; widening it to `SELECT` to satisfy a convenience clause would trade a real privilege for nothing. **The generalizable lesson: a table whose grant is INSERT-only cannot use `RETURNING`, and only a real-database test can catch that** — the same standing as the IR-Compiler checkpoint's "adversarial-phrasing testing belongs in the first pass" lesson. Apply it to any future write path against a minimally-granted table.
- **Proven with 32 no-I/O tests** (`tests/graphs/test_repair.py` + `tests/prompts/test_repair_prompt.py` — 30 plain unit tests, 2 of them hypothesis properties: termination under any model behaviour, and totality — every input lands in exactly one output bucket) **and 6 real-Neon integration tests** (`tests/graphs/test_repair_db.py`) covering both tables' write paths, the `uuid[]` lineage round-trip, the zero-call quarantine path, RLS refusing a cross-org write on the write path, and the `run_extraction` regression above. 38 new tests total — and the arithmetic checks against the audit entry in `CLAUDE.md`: the IR-Compiler checkpoint's own real with-DB-env count was 173 passed, and 173 + 38 = **211**, the exact number the full re-run below reports. Full `apps/brain` suite re-run clean afterward **with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set, so the real-database tiers actually ran rather than skipping**: 211 passed, 6 skipped, 0 failed — versus the 140 passed / 39 skipped the IR-Compiler checkpoint recorded without DB env, which is itself worth noting: **the previous checkpoint's "clean suite" was silently skipping 39 tests.** The 6 remaining skips were checked by name with `-rs`, not assumed from the typechecker checkpoint's older "fork-safety and OCR platform skips" description (which no longer matches — there is no OCR-specific skip anywhere in the current suite, and it undercounts what's actually skipping now): one `test_segment_source.py` case that specifically covers the *no-broker-configured* path and skips because this environment's `CELERY_BROKER_URL` **is** set; `test_extraction_live.py`'s live-LLM opt-in flag; one macOS fork-safety platform skip (`test_fork_safety.py`); and three `test_segmentation_task.py` cases that self-skip at runtime because they need a live Celery worker actually consuming tasks, not merely a configured broker URL. **This 211/6 count is this local machine's (macOS) number specifically, not a platform-independent one, and CI is expected to — and does — report a different total: checked against `ci-brain.yml`'s real run for this commit (`gh run view` on the workflow run this push triggered) rather than assumed to match, `ci-brain` reports 212 passed, 5 skipped, 0 failed.** The one-test delta is exactly the fork-safety skip: `test_fork_safety.py::test_without_the_mitigation_a_forked_child_inherits_the_parents_connection_fd` is itself platform-gated (its own skip reason says "Runs for real on Linux... where this class of fork hazard doesn't exist"), skips here on Apple Silicon, and ran for real and passed on CI's Linux runner — confirmed directly in the CI log, not inferred. The other 5 CI skips are the identical 5 non-platform skips listed above. Both numbers are correct for their own environment; neither is "the" count.

### Pipeline wiring

**Pipeline-wiring checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/graphs/pipeline.py` (`run_pipeline()`, `PipelineResult`), plus a `segment: SegmentRecord` field added to `extraction.py`'s `ExtractionResult`, plus the migration-drift guard (`apps/brain/src/obligo_brain/scripts/check_migrations.py`, `make check-migrations`, a new `ci-brain.yml` step). The first checkpoint that sequences run_extraction() → compile_candidates() → run_repair() → typecheck() into one call for a real segment — every prior checkpoint proved one stage in isolation. No Z3, no Critic, no Linker, no LangGraph, no `obligations` table.**
- **Still a plain function, checked against the falsifiable trigger `repair.py`'s own checkpoint committed to:** adopt LangGraph only when BOTH ≥2 independent conditional edges exist (the Critic or Router node, not just the repair back-edge) AND the pipeline spans more than one Celery task boundary. This checkpoint adds zero new conditional edges — it only sequences already-built stages — and runs synchronously in one process, so neither condition moved. `run_pipeline()` is `extract → compile → repair(if any failures) → typecheck`, a straight-line composition.
- **One small, necessary change to existing code, not new scope:** `run_repair()` needs a `SegmentRecord`, and `run_extraction()` fetched one internally but never exposed it — its own docstring already argues against re-fetching ("a redundant query whose only effect would be a chance for the two to disagree"). Added `segment: SegmentRecord` to `ExtractionResult` (additive; the dataclass had exactly one construction site, `run_extraction()`'s own `return`, confirmed by grep before editing) rather than have `run_pipeline()` query `segments` a second time.
- **Persists nothing beyond what `run_extraction`/`run_repair` already persist — confirmed, not merely still the plan.** This was committed to in the debt list in `CLAUDE.md` *before* `pipeline.py` existed, specifically so the decision wouldn't get quietly revisited once code was in front of it; it's confirmed now that the code is real. `PipelineResult.typechecked` — including a correctly-underspecified obligation with an unresolved party — is returned to the caller in memory and nowhere else, because there is no `obligations` table (Phase 5's deliverable, an event-sourced aggregate owned by `apps/core`, per blueprint §21 Phase 5 and `packages/ir-spec/SPEC.md` §11's identical scope line).
- **Proven against real Neon Postgres and real party-resolution data, no live LLM calls** — `tests/graphs/test_pipeline.py`, 5 tests, one per terminal outcome for a single segment: (1) grounds, compiles, typechecks with both parties resolved (deliberately no temporal clause — `WITHIN`/`RELATIVE`'s trigger is a `TriggerRef`, and `symbols.resolve_trigger()` unconditionally returns `None` in this checkpoint, so *any* trigger-bearing temporal is always underspecified by design; a first draft of this test used `WITHIN` and failed exactly this way before being corrected — a real bug in the test, not in `run_pipeline()`, caught by actually running it rather than trusting the design); (2) a hallucinated span fails grounding and never reaches compile; (3) an out-of-taxonomy action is repaired successfully via one repair call and ends up typechecked; (4) an out-of-taxonomy action that the repair prompt "corrects" to another invalid verb twice exhausts the budget (`REPAIR_BUDGET_EXHAUSTED`, `attempts == 3`, §3.9's "fail 3×") and lands in `compile_quarantine`; (5) an unresolvable obligor alias compiles cleanly and typechecks `underspecified=True` with `"obligor"` in `missing_fields` — a correct, surfaced outcome, not a failure bucket. A fake `ChatModel` drives every test (no live Groq calls); every stage `run_pipeline()` calls opens its own `tenant_scope()` internally, so there is no DB-free "orchestration only" layer to test the way `repair_candidates()` lets `repair.py` test its loop without a database — the branching under test here *is* which DB-touching stage a candidate reaches next.
- **The migration-drift guard is the debt list's own "a startup or `make` step that fails loudly on pending migrations" fix, built now rather than deferred again** — scoped separately from the pipeline itself (a pure documentation/tooling gap, not a pipeline dependency: this checkpoint's own `make check-migrations` run found zero drift, V16–V18 were already applied), but bundled into the same checkpoint because leaving it for "later" a second time was judged worse than the scope creep of building a small, independent, already-designed fix alongside real pipeline work that depends on those same migrations being correct. `check_migrations.py` (`obligo_brain.scripts`) reuses `ci-brain.yml`'s exact Flyway CLI version pin (10.10.0) and download path (`/tmp/flyway-cli/flyway-<version>`, so a CI invocation placed after that workflow's existing "Apply apps/core Flyway migrations" step reuses the binary rather than re-fetching it) and calls `flyway info -outputType=json` — read-only, never `migrate` — verified against a real run against the dev branch before the JSON parser was written, not guessed at (`{"migrations": [{"version", "description", "state", ...}]}`; a state outside `{Success, Baseline}` — `Pending`, `Future`, `Ignored`, `Outdated`, `Failed`, `Missing` — is drift). `make dev` now depends on `check-migrations` (fails loudly before starting services, closing the gap `V17` exposed: `make dev` never migrated and there was no local check at all); `make test` runs it right after `./gradlew test`, on the theory that Spring Boot's own Flyway bean will have auto-migrated by then, mirroring the new `ci-brain.yml` step placed right after that workflow's own explicit `migrate` step. 6 tests (`tests/scripts/test_check_migrations.py`): 5 pure (parsing/decision logic against a recorded fixture shape, including one modeling `V17`'s exact real failure — `Pending` — and one proving every non-`Success` state is caught, not only `Pending`) plus 1 real-Neon test that actually runs `main()` end to end and asserts it returns `0` against whatever `DATABASE_URL` points at right now — the one test in this checkpoint that has to pass for a real reason, not a mock standing in for the claim.
- **Locally verified clean, with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set so the real-database tiers ran rather than skipped: 222 passed, 6 skipped, 0 failed** — 211 (the repair-loop checkpoint's own audited with-DB-env count) + 11 new (5 pipeline + 6 migration-check) = 222, checked arithmetically rather than assumed. The 6 skips checked by name with `-rs` and confirmed identical to the repair-loop checkpoint's own list (no new or missing skip): `test_segment_source.py`'s no-broker-path case (skips because `CELERY_BROKER_URL` **is** set here), `test_extraction_live.py`'s live-LLM opt-in, `test_fork_safety.py`'s macOS platform gate, and the three `test_segmentation_task.py` cases needing a live Celery worker. **CI checked for real, not assumed to match local** — `gh run watch` on the actual run this push triggered (`ci-brain` run `31730813367`, success, 14m23s), then the real log pulled with `gh run view --log` rather than trusting the green checkmark alone: the new "Check for migration drift" step passed as its own named step immediately after "Apply apps/core Flyway migrations," with real output `check-migrations: 18 migrations, all applied cleanly. No drift.` — proving the checker behaves correctly in CI's environment, where Flyway is a separate prior workflow step, not merely locally. **CI reports 223 passed, 5 skipped, 0 failed** — local's 222 plus exactly one, and the delta is the same one the repair-loop checkpoint already documented: `test_fork_safety.py::test_without_the_mitigation_a_forked_child_inherits_the_parents_connection_fd` skips on this macOS machine but ran for real and **passed** on CI's Linux runner, confirmed directly in the log rather than inferred. The other 5 CI skips are the identical 5 non-platform skips listed above.

### Obligations-persistence investigation

**Obligations-persistence investigation (post pipeline-wiring checkpoint) — no code written, nothing built, recorded because the design conclusion is real and durable even though nothing was implemented from it.** A session started by asking `apps/brain` to persist `run_pipeline()`'s output — an `obligations` table `apps/brain` would write directly, choosing between `obligo_app`/`obligo_brain` as the writer. That framing was caught and rejected before any migration or code was written, on three independently sufficient grounds already on record: (1) the pipeline-wiring checkpoint entry now in `docs/PROJECT_HISTORY.md`, written before this investigation started, already says the table is "an event-sourced aggregate owned by `apps/core`, not a table `apps/brain` should insert into directly"; (2) `packages/ir-spec/SPEC.md` §11 draws the identical scope line — `status`/`risk_score`/`due_at`/`owner_user_id`/`version`/`deleted_at`/`binding_strength` belong to "Phase 5's domain model," layered on top of a compiled IR, not part of it; (3) blueprint §5.3/§5.7/§5.8 specify `Obligation` as the system's one event-sourced aggregate — its real persistence is `obligation_events` + `obligation_snapshots` replayed through a hand-rolled Java state machine, with the `obligations` row itself written only as part of one transaction `{state write; event append; outbox insert}` (§5.8). A raw `INSERT` from Python wouldn't be a smaller version of that design — it would bypass the state machine, the event log, and the outbox entirely, and would mean a Java-owned aggregate's write path has a second, inconsistent entry point from day one. **Confirmed with the user and explicitly deferred, not built:** the correct shape is a real Phase 5 slice — `apps/core` owns `obligations`/`obligation_events`/`obligation_snapshots` plus a minimal state machine, and exposes an endpoint `apps/brain` calls (over the existing interim shared-secret gate, or its eventual real replacement — see that debt entry in `CLAUDE.md`) to submit a compiled `Obligation`, rather than `apps/brain` writing the table itself. This is genuinely out of Phase 4's scope, not merely deferred for being hard — it belongs to its own planning session once Phase 5 formally opens. `run_pipeline()` is unchanged: `PipelineResult` is still returned to the caller in memory only.

### Verifier

**Verifier checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/verifier/{intervals,actions,candidates,z3_lowering,unsat_explain,verify}.py`, `docs/VERIFIER.md`. First code in this repo to touch Z3. Closes §21 acceptance criterion 3 and unblocks two of `test_properties.py`'s six deferred properties. No Critic, no Linker, no LangGraph, no `findings`/`verification_runs` tables, not wired into `run_pipeline()`.**
- **Scoped before any code was written, in a dedicated conversation, per this phase's own standing discipline** — the conflict-candidate set, the conflict condition itself, the candidate/solver boundary, and the deterministic-vs-LLM question for explanation were all proposed, reviewed, and corrected across several rounds before implementation started. Two real corrections landed during scoping, both worth keeping on record since they shaped the final lowering: (1) an early worked example used an interval-*intersection* framing for what makes two obligations conflict — caught as wrong before code existed, because a `MUST DELIVER` in January and a `MUST_NOT DELIVER` in June never intersect and are trivially satisfiable, while two obligations whose windows *do* intersect can still be either SAT (partial overlap) or UNSAT (full containment) depending on direction. (2) A confidentiality-vs-payment worked pair was walked through concretely to confirm the candidate-set filter's object-class clause actually excludes unrelated subject matter between the same two parties, not merely asserted to.
- **The conflict condition actually implemented is residual-emptiness, not intersection and not subset-in-either-direction as a standalone rule** — `docs/VERIFIER.md`'s own "The conflict condition" section has the full derivation. For the two-obligation case this reduces to a clean, testable statement: `MUST(W)`/`MUST_NOT(P)` is unsat iff `W ⊆ P`. Proven two ways: worked fixtures (`tests/verifier/test_verify.py`'s Example A — containment, UNSAT; Example A′ — partial overlap, SAT; Example S/T — prohibition nested inside a wider duty window, SAT because January and March survive outside the carved-out freeze; Example B — a genuine 3-way conflict where every pair is individually SAT and only the group together is UNSAT, the concrete justification for solving candidate sets rather than pairs) and a hypothesis property checking the real Z3 verdict against an independent, pure-Python set-containment oracle across hundreds of generated interval pairs (`test_must_must_not_unsat_iff_duty_window_is_subset_of_prohibition`).
- **Act keys are why two `MUST`s never conflict with each other regardless of their windows, by construction, not by a runtime check** — every MUST/SHOULD gets its own fresh Z3 `Int`; two duties to perform the same kind of act are two acts, not a shared performance. This is what keeps ordinary supersession (a later amendment's new deadline) from being read as a fabricated contradiction, and it's pinned as its own hypothesis property (`test_must_must_never_conflicts_regardless_of_windows`, and symmetrically for `MUST_NOT`/`MUST_NOT`) rather than left implicit in the worked examples alone.
- **The action-antagonism table (`RETAIN`→`DELETE`, `WITHHOLD`→`DISCLOSE`) closes the product's own headline example** ("a retention policy contradicted a deletion promise," this file's §1.3 vision) — `DELETE` and `RETAIN` are different actions in the closed 34-verb taxonomy, so a literal `MUST RETAIN`/`MUST DELETE` pair shares no act key and a plain modal check never fires on it without this table. Deliberately one-directional, not a symmetric pair, and that's a real semantic distinction, not an oversight: `RETAIN`/`WITHHOLD` describe a continuous state held throughout a window (the same shape as a `MUST_NOT`'s own forbidding window), `DELETE`/`DISCLOSE` describe a point-in-time act — "must delete by March 1" does not forbid retaining right up until the deletion, so the reverse direction is deliberately absent and pinned as its own regression test (`test_antagonism_is_not_symmetric_must_not_retain_does_not_imply_prohibition_on_delete`, `test_delete_must_never_implies_a_prohibition_on_retain`). Seeded strictly with exactly these two pairs, matching `ir_compile.py`'s own "strict over loose" posture — plausible-but-arguable pairs (`TERMINATE`/`MAINTAIN`) are deliberately left out.
- **Explanation is deterministic, template-based per finding kind, with zero LLM calls anywhere in the module — approved as proposed, without qualification, before implementation started.** Every fact in a rendered sentence is read directly from the minimal core's own labels (obligor name, action, object phrase, real dates); Standing Principle 2 and blueprint §6.7's own "the claim comes from the core... never let the model invent the reason" apply directly, and the span-grounding Tier-C precedent (`"shall"` → `"shall not"` inverting a clause's meaning at ~2% edit distance) is the concrete reason a polish step isn't added even as an option in this checkpoint.
- **Conditions lower to plain Z3 `Bool`s asserted as bare literals, not used to guard the modal/window facts** — a simplification found during implementation, not part of the original scoping proposal (which described `Implies`-guarded assertions): since condition literals are already assumed true per the approved design, guarding the window facts by them changes no verdict, only adds unused machinery. Same atom text across two different obligations' conditions is the same Z3 variable, which is what lets `IF "X"` in one obligation and `IF NOT "X"` in another genuinely contradict each other (`CONDITION_CONTRADICTION`, `tests/verifier/test_verify.py::test_condition_contradiction_without_any_modal_conflict`) — and, separately, what lets a `.cond` fact and a `.window`/`.prohibition` fact be found as two *independent* minimal cores in one solve when both are genuinely present (`test_condition_contradiction_isolated_no_temporal_overlap_at_all`, which real-world both contradicts on conditions and conflicts modally, and the multi-core enumeration correctly reports both).
- **Multiple independent conflicts in one candidate group are enumerated via Z3 assumptions, not `push`/`pop`** — another implementation-level refinement of the scoping proposal's conversational description. Every fact is asserted as `Implies(selector, constraint)`, with the selector passed as an *assumption* to `solver.check()` per call rather than permanently asserted true; this is what makes deletion-based minimization and multi-core search both possible by varying which selectors are active per call, reusing one `Solver` object throughout a group's search, bounded at 5 cores total and a 120s timeout (`Z3_TIMEOUT_MS`, matching blueprint §4.2's own constant) that surfaces as `VerificationResult.timed_out_groups`, never a silently-claimed SAT.
- **`TEMPORAL_IMPOSSIBILITY` (an obligation's own reversed `DURING` bounds) is detected in pure Python, not through Z3** — `intervals.is_empty()` on the obligation's own window, checked before candidate grouping even runs. Routing a self-contained defect through the solver would risk it being misattributed to whichever obligation it happened to be grouped with; detecting it separately also means a `TEMPORAL_IMPOSSIBILITY`-flagged obligation needs no candidate partner at all to be caught, unlike every other finding kind.
- **A real, adjacent bug found and fixed, unrelated to the verifier's own logic:** the local `/tmp/flyway-cli` download `check_migrations.py` depends on (pipeline-wiring checkpoint, `docs/PROJECT_HISTORY.md`) had gone stale between sessions — the directory existed but its `lib/flyway/` jars were empty, so `flyway info` failed with `ClassNotFoundException`, and `check_migrations.py`'s own download logic only re-fetches when the directory is entirely absent, so it didn't self-heal. Confirmed as pre-existing and unrelated to this checkpoint's diff before touching it (git status showed only `pyproject.toml`/`uv.lock`/the new `verifier/` and `docs/VERIFIER.md` files), then fixed by clearing the stale cache directory and letting the existing download logic repopulate it — a scratch-cache repair, not a code change.
- **Proven with 66 new tests, real Z3 calls throughout, no mocking of the solver anywhere** (`tests/verifier/`: `test_intervals.py`, `test_actions.py`, `test_candidates.py`, `test_verify.py`, `test_properties.py`) — worked-example regressions for every conflict kind (A, A′, B, S/T, the antagonism pair, both condition-contradiction shapes), the candidate-set exclusion/admission cases from the scoping conversation (confidentiality-vs-payment excluded, same-string-different-subject-matter admitted and confirmed as a named, accepted risk rather than a bug), `MAX_CANDIDATE_SET` scope-exceeded, order-independence, and six hypothesis properties (`MUST`/`MUST` never conflicts, `MUST_NOT`/`MUST_NOT` never conflicts, the subset-iff-unsat property, the empty-intersection-implies-SAT corollary, conflict symmetry, typecheck soundness — the last reusing the compiler's own `tests/compiler/strategies.py` generator rather than a bespoke one). Full `apps/brain` suite re-run clean afterward, with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set so the real-database tiers ran rather than skipped: **288 passed, 6 skipped, 0 failed** — 222 (the pipeline-wiring checkpoint's own audited with-DB-env count) + 66 new = 288, checked arithmetically rather than assumed. The 6 skips checked by name with `-rs` and confirmed identical to the pipeline-wiring checkpoint's own list, no new or missing skip. **CI not yet checked for this checkpoint** — unlike every prior checkpoint's own entry in this file, no commit has been made and none is authorized without being asked first (this file's own git-workflow discipline); the 288/6/0 count above is a local-only number until a commit exists for `ci-brain.yml` to run against, and should not be read as a CI-confirmed number the way the pipeline-wiring checkpoint's 223/5/0 was.

### ir_hash

**ir_hash checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/compiler/ir_hash.py`, `tests/compiler/{test_ir_hash,test_ir_hash_db}.py`. Scoped in a dedicated conversation before any code was written, the same discipline the verifier checkpoint used. Closes blueprint §19.3's "Hash stability" property and the new acceptance criterion 5a (the hash mechanism itself); does NOT close criterion 5 or the broader "`ir_hash` reconciliation for idempotent re-extraction" deliverable — both need the `obligations` table (Phase 5). No normalizer, no dedup-against-storage logic, no wiring into `run_pipeline()`.**

- **The scoping conversation's central question, answered before any code: is "build and prove the pure canonicalization+hash function, defer the dedup-against-storage half to Phase 5" a legitimate split, or does it duck the real problem?** Resolved as legitimate, on the same grounds the obligations-persistence investigation already established for the table itself: `packages/ir-spec/SPEC.md` section 10 already frames `ir_hash` as "a derived value... not a field this schema defines directly," and blueprint §19.3 names "Hash stability" as its own standalone, persistence-free property. The dedup-*enforcement* half (the unique index, the exclusion constraint, the actual insert-vs-reject decision) has no meaningful version provable without a table to not-duplicate into — there is nothing to reject against.
- **Field-by-field canonicalization, decided before implementation, not invented while coding:** `confidence`, `object.raw_text`, and `source.char_start`/`char_end` are excluded entirely — provenance/evidence about *how* an obligation was extracted, not part of *what* it is; including any of them would make two genuinely identical re-extractions hash differently on confidence drift or minor span-boundary drift, the exact false-negative dedup failure this function exists to prevent. `grammar_version` was never actually a field to exclude — it's injected into `to_dict()` from a module constant, never stored on `ast.Obligation` itself. `PartyRef`/`DateRef`/`TriggerRef` hash their RESOLVED value (`party_id`; the ISO date; `ref_type`+`ref_id`) when resolved, normalized (trim + collapse whitespace, case preserved) raw text when not — `packages/ir-spec/SPEC.md` section 10's own stated rule for `PartyRef`, applied uniformly to all three two-state refs rather than inventing a second rule.
- **Condition ordering, resolved as two genuinely distinct cases, not one:** multiple top-level `Condition` entries on one obligation are an implicit conjunction (`ir_compile.py`'s own docstring) and therefore commutative — the tuple is sorted by canonical form before hashing, closing a real false-negative dedup hazard (`test_condition_order_does_not_affect_hash`). AND/OR structure *within* one condition's `Predicate` tree is deliberately left unreordered — real boolean-algebra commutativity/associativity canonicalization needs `normalize.py`, not built — and pinned as a named, accepted gap (`test_and_or_internal_operand_order_is_not_canonicalized_known_gap`) rather than silently left untested. That gap is real but currently dormant on the actual extraction path: `ir_compile.py` never constructs an `AndPredicate`/`OrPredicate` from LLM output today; only `parser.py` parsing literal DSL `AND`/`OR` keywords can produce one.
- **Collision safety named concretely, not left as an unexamined adjective:** SHA-256, 256-bit hex digest — the same primitive already used for `agent_runs.input_hash`/`prompt_hash`/`repair_input_hash` (`graphs/extraction.py`, `graphs/repair.py`), so no new dependency. Birthday-bound accidental collision risk is negligible at any scale this project will reach (non-negligible only near 2^128 hashed items). The real risk named and guarded against is semantic aliasing from the canonicalization choices above being too aggressive, not hash-primitive collision — guarded by the distinctness tests (field-flip regressions for modality/action/`object.class_`/`segment_id`/alias) rather than a runtime tie-breaker, since a tie-breaker can't fix a canonicalization bug.
- **A real, deliberate divergence found and documented, not fixed — a follow-up audit checked whether this checkpoint's PartyRef-tagging discipline was novel or already established, and found it was already established, one module over, with a different normalization rule.** `verifier/candidates.py`'s `party_key()` (predates this checkpoint, reused by `z3_lowering.py`'s `_act_key()` for its candidate-set prefilter and its Z3 act-identity notion) already tags RESOLVED/UNRESOLVED `PartyRef` the identical way `ir_hash._canon_party()` does here — but `party_key()` additionally casefolds unresolved alias text (`party.alias.casefold()`), while `ir_hash._canon_party()` deliberately does not (only trims/collapses whitespace, per this checkpoint's own field table above). Both choices are correct for their own module, not an inconsistency to reconcile: `party_key()` is a conflict-detection *recall* mechanism, where treating "Vendor" and "vendor" as the same candidate party is the safer failure mode (a missed conflict candidate never reaches Z3 at all); `ir_hash` is a dedup *identity* mechanism, where the span-grounding discipline means two extractions of the same real clause should already quote the same case verbatim, so folding case buys less and risks silently merging two genuinely-different unresolved aliases. Recorded here so a future reader of `z3_lowering.py`'s own docstring reference to "the solver's own identity notion" doesn't assume it means the same canonical form as `ir_hash`'s — the two modules canonicalize the same `PartyRef` type on purpose, differently, for different reasons.
- **Acceptance criterion 5 reclassified, not silently left implying this checkpoint closes it:** split into new row 5a (**Met** — the hash mechanism itself) and row 5 (**Blocked — `obligations` table + reconciliation logic, Phase 5**), using **Blocked** rather than **Unmeasured** for row 5 specifically because CLAUDE.md's own state definitions distinguish "a named missing structural component" (Blocked) from "a sound mechanism nothing has run against real numbers yet" (Unmeasured) — row 5's gap is the former, not the latter. See `CLAUDE.md`'s acceptance-criteria table for the exact wording.
- **Proven with 21 new tests, real Neon Postgres for the one case that needs it, no mocking anywhere.** `tests/compiler/test_ir_hash.py` (19 tests, pure, no I/O — every base obligation built via the real `parser.parse()`, either directly from a hand-written DSL string or via `parse(unparse(strategies.obligation()-drawn))`, the same reparse-through-the-real-parser discipline `test_properties.py`'s round-trip test already established): exclusion tests for confidence/`object.raw_text`/char-offsets, inclusion of `segment_id`, alias/condition-atom whitespace insensitivity, the condition-order-swap regression, the AND/OR-internal-order known-gap regression, field-flip distinctness tests, `ResolvedDate` proven via a real parser-produced literal DATE token (no DB needed — the grammar compiles a literal date straight to `ResolvedDate` at parse time), `ResolvedTrigger` proven by documented direct construction (it has no real producer anywhere in this codebase yet — `symbols.resolve_trigger()` always returns `None` in v1, the identical "real, honestly unreachable" status that module's own docstring already gives it, so there is no real infrastructure to invoke here at all), and 3 hypothesis properties (determinism, stability under confidence+condition-order perturbation of a real parser-built instance, and modality-flip distinctness). `tests/compiler/test_ir_hash_db.py` (2 tests, real Neon Postgres, mirrors `test_symbols.py`'s owner-seeds/`obligo_brain`-reads/skip-if-unset pattern exactly): two different real aliases ("Acme Corp", the canonical name, and "Vendor", an `aliases`-array entry) of the same registered party resolve via a real `typecheck()` call to the same `party_id` and hash identically — the one part of this checkpoint's field table with a real producer today, so it's proven against real infrastructure rather than direct construction.
- **Full `apps/brain` suite re-run clean afterward, with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set so the real-database tiers ran rather than skipped, run from the repo root exactly as `ci-brain.yml` does (`uv run --project apps/brain pytest`).** Running it from inside `apps/brain` first produced a spurious migration-drift failure in `check_migrations.py`'s own live test; traced to that script's locations path being CWD-relative (matching `ci-brain.yml`'s own repo-root invocation), confirmed unrelated to this checkpoint's diff (`git status` showed only the three new files) before rerunning correctly — a local-invocation artifact, not a regression. **309 passed, 6 skipped, 0 failed** — 288 (the verifier checkpoint's own audited with-DB-env count) + 21 new = 309, checked arithmetically rather than assumed. The 6 skips checked by name with `-rs` and confirmed identical to the verifier checkpoint's own list, no new or missing skip. **CI checked for real** — `gh run watch` on the actual run this push triggered (`ci-brain` run `31871497136`, success, 14m38s), then the log pulled with `gh run view --log` rather than trusting the green checkmark alone: "Check for migration drift" reported `check-migrations: 18 migrations, all applied cleanly. No drift.` immediately after the real Flyway-apply step. **CI reports 310 passed, 5 skipped, 0 failed** — local's 309 plus exactly one, and the delta is the identical one every prior checkpoint has documented: `test_fork_safety.py::test_without_the_mitigation_a_forked_child_inherits_the_parents_connection_fd` is itself platform-gated, skips on this macOS machine, and ran for real and **passed** on CI's Linux runner, confirmed directly in the log (that file's other two tests aren't platform-gated and already ran and passed locally too, so they're not part of the delta). The other 5 CI skips (`test_segment_source.py`'s no-broker-path case, `test_extraction_live.py`'s live-LLM opt-in, and 3 `test_segmentation_task.py` cases needing a live Celery worker) are the identical 5 non-platform skips every prior checkpoint has listed.

### Eval-harness pilot

**Eval-harness pilot checkpoint (§21 Phase 4, scoping-only for the harness/gold-set design, one throwaway script executed) — explicitly NOT the 100-item CUAD/EDGAR gold set §19.7 and this file's own acceptance-criteria table call for. That corpus has not been started: no excerpts selected, no annotation guideline written, nothing annotated at that scale. **[Superseded 2026-08-20 — all three clauses are now false; see the gold-set corpus and batch 1 checkpoint below. Left as written, per this file's own corrections-are-new-text discipline: it was true when written.]** This checkpoint is a 10-item synthetic scoring-harness sanity check only, run twice against real infrastructure, and its findings are scoped accordingly — no percentage from it should be read as progress toward criterion 1b (compile success ≥90%) or criterion 2 (Tier-2 fully-correct-IR ≥80%). Both remain exactly as the ir_hash checkpoint's table above left them: Unmeasured. The pilot script itself was never added to this repository — it lives in the session's own scratchpad, was never committed, and is not part of `apps/brain/`, any test suite, or any CI gate. Unlike every other checkpoint in this section, there is no diff, no commit, and no CI run to point to for this one; the evidence for everything below is the two live runs' actual captured output, read directly, not a codebase artifact.**
- **Design resolved five real blueprint discrepancies before any code was written** (§19.7 says "two-tier," §19/§6.11 both actually describe three; two different gold-set specifications between §19.7 and §6.11; three different compile-success targets across §21/§1.14/§18; §19.7's own stated Tier-1 rationale — CUAD F1 as a baseline-comparable number — found unsound on inspection, since CUAD's 41 categories aren't exhaustive for obligation-bearing spans outside its own taxonomy, making precision structurally unmeasurable against it; and a live-call merge-gate requirement that conflicts with this repo's own established "a rate-limited external dependency doesn't belong on the merge path" rule from the Extractor checkpoint). Resolved to: build the real §19.7-shaped 100-item set, not §6.11's 60-document one; defer Tier 1/CUAD-as-baseline entirely, unscoped by either of the two real acceptance criteria; gate on §21's ≥90%/≥80% figures; two-mode (live/replay) cassette design for future CI use, not wired in yet.
- **A precise, mechanical Tier-2 scoring rule was designed before any annotation started:** alignment by span-offset IoU (≥0.5) within one segment, then a conjunctive per-field predicate (modality, action, obligor, obligee, object_class, temporal incl. duration/unit exactness, conditions as an order-insensitive-but-count-sensitive set, underspecified, missing_fields excluded from the predicate but reported). Free-vocabulary fields (object_class, and — a real finding below — action) are scored against an annotator-authored accept-set fixed at gold-authoring time, deliberately replacing §6.11's judge-model proposal (no Gemini adapter exists in this codebase; Standing Principle 2 argues against a second model grading a field's correctness rather than a human-fixed accept-set doing it once).
- **10 synthetic segments were drafted and gold-annotated item by item, under explicit human sign-off per item** — not batch-approved. Real corrections made during that review, not silently accepted as first-drafted: an obligee direction rewritten from inferred to textually explicit ("for the benefit of Customer" added, so the party owed the duty is a real substring rather than a guess); a `DURING` item's accepted-outcome set made to explicitly include "fails as a known, named gap" rather than silently assuming resolution; a multi-condition item's scoring rule confirmed strict (exactly two separate `Condition` entries, not a count-agnostic accept-either) and confirmed as testing the extractor's own list-splitting behavior, not asking `ir_compile.py` to do the AND/OR-within-one-string detection it explicitly doesn't do.
- **Two real, unrelated pacing bugs were found and fixed in the pilot script (a throwaway harness, not production code) before any full run completed cleanly.** First: an unpaced sequence of live calls hit a real `429 Too Many Requests` from Groq at call 12 of an approved 30-call budget — the pilot script had no backoff at all, a real gap between what the design proposal specified (adaptive re-pacing against real rate-limit headers) and what the throwaway script actually implemented. Fixed with a flat inter-call interval, initially. Second, caught before being trusted rather than after failing again: that flat interval was itself shown unsafe by real token math — 13 calls in 37.9s of wall clock is a sustained-equivalent rate of ~25,700 tokens/min, 2.14× the account's real measured 12,000 TPM ceiling (read via one live header-only call: `x-ratelimit-limit-tokens: 12000`, `x-ratelimit-limit-requests: 1000` — both genuinely measured and both wrong relative to blueprint §13.3's stated ~6,000 TPM/~14,400 RPD figures, in opposite directions). The pilot's own clean run at that pace was evidence of unused burst/bucket capacity absorbing a short burst, not evidence the rate was sustainable. Replaced with a sliding-window token-rate limiter keyed on each call's own real returned token count (no header-parsing dependency, self-correcting against actual observed call sizes) before any further live call was made.
- **Also lost and recovered, same session:** the pilot script's first version scored and wrote results only after its entire 10-segment loop finished; the mid-run 429 above meant the whole run's results — including 8 already-successful segments — were discarded when the process crashed, because nothing had been persisted yet. Fixed by moving the score-and-write to run immediately after each segment's own `run_pipeline()` call, inside the loop, so a crash on segment N only costs segment N; segments 1..N-1's JSON files survive on disk regardless, untouched by the (unconditional, and still exception-safe) Neon cleanup in the script's `finally` block, which was verified — by rereading the actual code, not asserted from memory — to still fire correctly on any exception and never touch the result files at all.
- **Run twice, live, real Groq (`llama-3.3-70b-versatile`, temperature=0.0), real Neon (a fresh eval `org_id` + 2 registered parties, ingested and cleaned up per run), real `run_pipeline()` — no mocking anywhere in either run.** Both runs: 13 of 30/380 approved calls used, zero `429`s once the token-aware pacing was in place. **Run 1: 6/10 items scored `FULLY_CORRECT`-or-expected.** Failures: one `MISSED` (temporal trigger clause truncated by the model to `"within 5 days"`, dropping the mandatory `of <trigger>` tail `_WITHIN_RE` requires — `UNMAPPABLE_TEMPORAL`, one repair call made no progress, quarantined); one `UNEXPECTED` (see the clause-exclusion finding below); one `PARTIAL` (an `object_class` accept-set gap, model said `customer_personal_data`, gold's accept-set didn't include it); one more `MISSED` (same clause-exclusion pattern as the `UNEXPECTED` case). **Run 2 (fresh org, identical script, identical 10 sentences): 6/10 again, but a different six** — the first run's temporal-truncation `MISSED` item resolved cleanly this time; a different item that had compiled cleanly in run 1 came back `PARTIAL` this time on an `action` mismatch (model said `PROVIDE`, gold required `REPORT` — both are valid entries in the 34-verb taxonomy for the same real sentence); the other three non-clean items reproduced byte-for-byte identically, including the exact rejection-detail strings.
- **Real, load-bearing finding, confirmed by direct empirical reproduction, not a single run's noise: temperature=0.0 is not a determinism guarantee in practice.** Two live calls against the identical prompt, identical segment text, identical model, identical temperature produced different extractions for 2 of the 10 items. This has a direct design consequence for the real 100-item harness, not yet acted on: a single live run's fully-correct rate is a point estimate with real sampling noise even holding everything else fixed, so either gold items need to be run ≥2× in any regression-gating use of this harness, or every published number needs an explicit single-run caveat rather than being read as a fixed ground truth.
- **A second real finding, reproduced identically across both runs (not one-off): the extractor sometimes excludes a leading subordinate clause from `span_text` while still populating `temporal_raw`/`condition_raws` with content quoted from that excluded clause** — for sentences of the shape "During the period from X to Y, [main clause]." and "If A and if B, [main clause]." — caught correctly both times by `ground_candidates()`'s nested-field check as `NESTED_FIELD_NOT_IN_SPAN`, discarding the whole candidate before it ever reaches `ir_compile.py`. This is the grounding gate doing exactly its documented job (Standing Principle 4, CLAUDE.md's own "if a candidate's span text doesn't literally appear in the source segment, discard it" rule extended to nested fields) holding under live, unscripted model output for the first time — a positive result, not just two failures. But it is real and reproducible, not a fluke (identical `span_text`, identical rejection-detail string, both runs), and it is not universal — a third item with the identical leading-`If`-clause shape succeeded both times, so this is an intermittent, not deterministic, extraction-prompt behavior. **Worth its own investigation before real corpus annotation starts**, since real CUAD/EDGAR obligation sentences plausibly have this exact shape (a conditional or temporal clause fronting the main clause) more often than the synthetic pilot's small sample can characterize, and a systematic recall loss here would be invisible without a dedicated corpus slice or prompt-iteration checkpoint built to isolate it. One direct, secondary consequence: the specific pilot item built to probe whether `_DURING_RE`'s literal `".."`-joined date syntax is ever reachable from real model output never actually reached that check in either run, for this exact reason — that question is still genuinely untested, not answered by either run's `UNEXPECTED`/`MISSED` verdict.
- **A third real finding: the original scoring design's assumption that closed-taxonomy fields (`modality`, `action`) are safe for hard exact-match, reserving accept-sets for open-vocabulary fields (`object_class`) alone, is wrong.** Run 2's `action` mismatch (`PROVIDE` vs. gold's `REPORT`, both valid 34-verb-taxonomy entries for "submit a compliance report") shows a closed taxonomy doesn't guarantee a *unique* correct label for a given sentence — multiple taxonomy verbs can be equally defensible. The real 100-item gold set's scoring rule needs `action` accept-sets too, not just `object_class`, wherever a sentence's verb is genuinely polysemous under the 34-verb list.
- **No annotation-rigor claim is made beyond what was actually done.** All 10 items were reviewed and corrected individually, one at a time, with explicit sign-off per item (see the corrections bullet above) — this is not padded into a claim about the real 100-item set's eventual rigor, which hasn't started and needs its own decision, separately made, about process (repeated solo review vs. an independent second reviewer or held-out double-annotated sample) before it does.
- **Nothing here closes, or partially closes, either open Phase 4 acceptance criterion.** Criterion 1b (compile success ≥90% on the dev corpus) and criterion 2 (Tier-2 fully-correct-IR rate ≥80% on the gold set) remain **Unmeasured**, identically to how the ir_hash checkpoint's table above already listed them — this checkpoint sanity-checked the measuring instrument, not the thing it will eventually measure.

### Leading-subordinate-clause grounding-fix

**Leading-subordinate-clause grounding-fix checkpoint (§21 Phase 4) — `apps/brain/src/obligo_brain/prompts/extraction/v2.yaml`, `registry.yaml` flipped `extraction.environments.default` from `v1` to `v2`, two new cassette pairs (`tests/cassettes/extraction/leading_temporal_clause_{v1,v2}.json`, `leading_condition_clauses_{v1,v2}.json`), 4 new tests in `tests/graphs/test_extraction_cassette.py`. Direct follow-up to the eval-harness pilot checkpoint's own recorded finding above (a leading subordinate clause excluded from `span_text` while its content still appeared in `temporal_raw`/`condition_raws`, reproduced identically across that checkpoint's two live pilot runs) — investigated, root-caused, and fixed narrowly, not the broader clause-boundary question the investigation itself raised.**
- **Root-caused before any fix was written, per this phase's own standing discipline, not guessed past.** Reproduced offline (no live call) by reconstructing the pilot's implied `LLMCandidate`s from its own recorded rejection details and running the real `ground_candidates()` against them — the rejection strings came back byte-for-byte identical to the original pilot output. That confirmed `_locate_span()`/`_is_grounded_substring()` do no trimming of any kind; the narrowed `span_text` was already exactly what the model itself had emitted. Root cause is entirely LLM-extraction-side: the model was drawing `span_text`'s boundary at the main clause while independently quoting a fronting clause's content into `temporal_raw`/`condition_raws` — directly violating the v1 prompt's own already-stated "every nested field MUST be a literal substring of span_text" rule, not a rule the prompt was missing.
- **Confirmed by reading `test_ground.py` directly that this was a testing-blind-spot, not a code bug that slipped through.** Its three existing `NESTED_FIELD_NOT_IN_SPAN` cases (`test_temporal_raw_not_in_span_is_rejected` etc.) each construct a nested-field value that doesn't appear *anywhere* in the segment at all — proving the gate rejects hallucination, a different scenario from this bug's actual shape: a nested field that is real, verbatim, and present in the full segment, just outside the narrower span the model chose. The grounding-invariant hypothesis property never touches nested fields at all (`temporal_raw`/`condition_raws` stay at their `None`/`[]` defaults throughout its strategy, which only varies `prefix`/`span`/`suffix` over a flat alphabet). No layer of the existing suite could have caught this by design, not by gap: Layer 1 hand-constructs self-consistent-except-for-the-field-under-test candidates, which proves the gate's logic, never model behavior; Layer 2's 5 cassettes (recorded 2026-08-10) deliberately targeted three *different* named failure categories (hallucinated span, paraphrase, malformed JSON) and never included a fronted-clause segment; Layer 3 is opt-in and had never been run against this shape either. See Standing Principle 6 in `CLAUDE.md`, added at this checkpoint specifically because this is the second live instance of the identical testing-blind-spot pattern the IR-Compiler checkpoint's trailing-period bug already exposed once.
- **Deliberately narrow, on explicit instruction — not the broader clause-boundary fix the investigation itself flagged as plausible.** `prompts/extraction/v2.yaml` only strengthens the rule for the confirmed failure shape: a subordinate clause fronting the main clause. Trailing comma-set-off qualifiers, nested "provided that" clauses, and multi-obligation/multi-sentence spans were deliberately NOT investigated or touched at this checkpoint — carried forward in `CLAUDE.md`'s debt list, not silently assumed covered by this wording.
- **Fix is prompt-only** (Standing Principle 2/4) — no change to `ground_candidates()`/`_locate_span()`/`_is_grounded_substring()`, which this checkpoint's own offline repro re-confirmed were already correct. `v2.yaml` adds one clause to the `span_text` bullet naming the leading-clause case explicitly, an explicit self-check step before each candidate is emitted ("is temporal_raw / every condition_raws entry a literal substring of THIS candidate's own span_text?"), and a second worked few-shot example showing a fronted clause folded correctly into `span_text` (the existing example only showed an already-fused trailing phrase, so the model had no positive pattern for the shape it was getting wrong). `registry.yaml`'s flip is the only "code" change to production behavior — no redeploy, matching the registry's own designed rollback mechanism (`prompts/registry.py`'s docstring).
- **A grounding-stage repair loop, symmetric to the existing post-compile `PARSE_ERROR` loop, was considered and explicitly rejected for this checkpoint.** `span_text` is the grounded field itself, not a pure classification field like the three fields (`modality`/`action`/`object_class`) the existing repair loop touches — repairing it means asking the model to re-propose the single most safety-critical value in the pipeline, the same risk category Tier C fuzzy matching was already rejected for. Try the minimal prompt-only fix first, measure with the real eval harness once it exists, and only build a second repair mechanism if the rate doesn't fall enough.
- **Verified against real, live Groq output, twice each, not assumed from the prompt wording.** Both real pilot segments (s5: "During the period from 2027-01-01 to 2027-12-31, Vendor may access..."; s10: "If the Agreement is renewed and if Customer requests it in writing, Vendor should provide...") were replayed live against the unmodified v1 prompt first — both reproduced the exact bug fresh (`leading_temporal_clause_v1.json`, `leading_condition_clauses_v1.json`), confirming the original pilot's finding wasn't a one-off — then against v2 after the registry flip — both now ground cleanly with `span_text` correctly widened to include the leading clause in both real responses (`leading_temporal_clause_v2.json`, `leading_condition_clauses_v2.json`). All four cassettes are real, unedited model output (same discipline as every other cassette in this repo, recorded via a new one-off script, `tests/cassettes/record_clause_boundary_cassettes.py`, mirroring `record_cassettes.py`'s existing shape) and are kept permanently: the v1 pair as a live regression proof the bug was real, the v2 pair as the fix's real-model proof and a drift tripwire for any future prompt version.
- **4 new tests** in `tests/graphs/test_extraction_cassette.py` (a v1-reproduces/v2-is-fixed pair per segment), plus one existing test in `tests/prompts/test_registry.py` (`test_load_extraction_v1_round_trips_expected_fields`, renamed `test_load_extraction_round_trips_expected_fields`) updated from a hardcoded `prompt.version == "v1"` assertion to check against whatever version is currently active — the registry-flip mechanism's whole point is that a version bump needs no code change, so the test asserting on it shouldn't hardcode a version either. Full `apps/brain` suite re-run clean afterward, with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set so the real-database tiers ran rather than skipped, from the repo root exactly as `ci-brain.yml` does: **313 passed, 6 skipped, 0 failed** — 309 (the ir_hash checkpoint's own audited with-DB-env count) + 4 new = 313, checked arithmetically. The 6 skips checked by name with `-rs` and confirmed identical to the ir_hash checkpoint's own list, no new or missing skip. **CI checked for real** — `gh run watch` on the actual run this push triggered (`ci-brain` run `31967436769`, success, 12m22s), then the real log pulled with `gh run view --log` rather than trusting the green checkmark alone. **CI reports 314 passed, 5 skipped, 0 failed** — local's 313 plus exactly one, and the delta is the identical one every prior checkpoint has documented: `test_fork_safety.py::test_without_the_mitigation_a_forked_child_inherits_the_parents_connection_fd` is platform-gated, skips on this macOS machine, and ran for real and **passed** on CI's Linux runner, confirmed directly in the log. The other 5 CI skips are the identical 5 non-platform skips every prior checkpoint has listed.

### Gold-set corpus and batch 1

**Gold-set corpus and batch 1 checkpoint (§21 Phase 4) — `docs/eval/GOLD_SET_GUIDELINE.md` (v0.17, DRAFT, not frozen), `docs/eval/corpus_manifest.json` (v0.5), `apps/brain/evals/corpus.py`, `apps/brain/evals/goldens/batch01/` (10 items, 17 exclusions, `draw.json`, `SUMMARY.md`). This is the real 100-item Tier-2 gold set the eval-harness pilot checkpoint above said had not been started — it has been started, and this entry is the correction. 10 of 100 items are locked. Nothing has been scored against them: no harness run, no rate computed, both open acceptance criteria still Unmeasured.**
- **Direct correction to a stale claim this file was still making.** The eval-harness pilot checkpoint above says "That corpus has not been started: no excerpts selected, no annotation guideline written, nothing annotated at that scale." All three clauses are now false. That sentence is left as written — it was true on 2026-08-14, and this file's own corrections-are-new-text discipline says a superseded claim stays visible with the correction attached, not silently edited — but a superseded marker was added inline next to it pointing here. The open-items paragraph and acceptance-criteria rows 1b/2 near the top of this section were *edited* rather than marked, because those are live status statements, not a dated record: a status table that knowingly states the wrong status is a defect, not history.
- **Corpus: 28 documents, verified byte-for-byte, not merely downloaded.** 22 CUAD + 6 EDGAR (`corpus_manifest.json` v0.5). Re-acquired from archival sources on 2026-08-18 and checked against recorded SHA-256s — 22/22 CUAD, 6/6 EDGAR, zero mismatches — via `apps/brain/evals/corpus.py` (`fetch | verify | profile --compare | pool | draw | source`), so the corpus is reproducible from the manifest alone rather than being a folder of files someone once collected. CUAD provenance is pinned to Zenodo DOI `10.5281/zenodo.4595826` (`CUAD_v1.zip`, 105,883,672 bytes, sha256 `88b694d9…`), and its CC BY 4.0 license was read from the Zenodo record's own metadata on 2026-08-18 rather than assumed — the same "check the license, don't assume permissive" discipline the PyMuPDF/AGPL finding established in Phase 3.
- **Two of the six originally-sourced EDGAR documents were not contracts at all, and both were caught and retired rather than annotated.** E06 was a complete Form 10-K annual report labelled `contract_type: maintenance` in which the string "MAINTENANCE AGREEMENT" appears zero times; E04 was a complete Form 8-K (0 "by and between", 0 WHEREAS, 9 "shall" in 20,631 chars). **A 33% defect rate for the pre-audit sourcing method** — which is why guideline §12.1's four-step EDGAR sourcing rule was made *binding* for all future draws, and why the retired documents stay in the manifest's `retired_documents` array with their reason and sha256 rather than being deleted. Both replacements (E07, E08) were sourced under the tightened rule.
- **Batch 1: 10 items locked, 17 exclusions logged, 27 candidates walked — a 37% acceptance rate**, 3 hard / 7 standard (on target), drawn with a reviewer-held seed (`20260817`) from a pool of 1,547 segments (408 hard, 1,139 standard). **The 17 exclusions are logged with a written reason each, not discarded** — cross-reference dependence and 4+-chained-obligation sentences are the two recurring ones, alongside non-party subjects, definitions clauses, absence-of-obligation statements, bare list items under a parent stem, rights with no correlative duty, liability exclusions, and status statements. That log is what makes the 37% acceptance rate a real, auditable number rather than a ratio someone remembers.
- **Six structural gap categories were discovered, every one from a real drawn segment with its motivating sentence recorded in the rule that came out of it** — not invented in the abstract ahead of the data, the same discipline the 34-verb action taxonomy was built under: `unless_unsupported` (§8.2, from `E01-047` — meaning carried by an `UNLESS` that IR v1 deliberately cannot parse at all), `redacted_value` (§8.1, `E03-005` — clause survives, value withheld), `redacted_clause` (§3.7.1 — whole clause withheld, ground truth genuinely **unknowable**, so these spans leave *both* criteria's denominators rather than counting as failures), `compound_action` (§8.3, `C03-192`), `mutual_obligation` (§8.4, `C17-021` — reciprocal duty whose second direction is unrepresentable), `within_preposition` (§8.6, `C17-066`). Plus §3.5.1 (joint obligors → `obligor_accept_set`, deliberately **not** a gap, distinguished from §8.4 mutual obligations inside the same segment) and §2.3 (PII redaction convention).
- **`within_preposition` is a real production-code finding, not only an annotation rule.** `ir_compile.py`'s `_WITHIN_RE` requires the preposition `of`; `"within 24 months following the Commencement Date"` has a bare numeral and still rejects. Verified against the production regex directly, then measured pool-wide rather than extrapolated from the one item: the word following `within N <unit>` is `of` 40× against `after` 34× / `from` 17× / `following` 8× — **rejected prepositions outnumber the accepted one 59:40.** This is the same class of finding as the IR-Compiler checkpoint's trailing-period bug (a closed-format regex too strict for real quoted document text), and it is exactly the widening of `_classify_temporal`'s vocabulary the repair-loop checkpoint predicted `UNMAPPABLE_TEMPORAL` would mostly need. Not fixed here — this checkpoint annotates honestly and reports; the fix is its own scoped change.
- **THE FINDING THAT MATTERS MOST, and it is a scope finding about Phase 4's own acceptance bar, not an extraction-quality one: 5 of 10 batch-1 items carry a gap tag, and 4 of those cannot compile to a fully-correct IR by construction** — regardless of how good extraction gets — because IR v1 has nowhere to put a carve-out, a withheld value, a reciprocal duty, or a rejected preposition. **If that ~40% structurally-uncompilable rate holds across the corpus, §21's criterion 2 (Tier-2 fully-correct-IR ≥80%) is arithmetically unreachable.** Recorded as a rate from 10 items, which is not enough to act on: batch 2 either confirms or refutes it, and only then does this get its own conversation. Do not quietly redefine criterion 2's denominator to make this go away — the honest options are widening IR v1 (which the blueprint's own §21 risk note freezes against), or restating the criterion against a stated exclusion set, and either is a decision to make explicitly.
- **[SUPERSEDED 2026-08-21 — the 814/≈90 figures are raw wall-clock including 693 min of overnight idle; the real measured cost is 143.0 min active, 14.3 min/item. The "37% acceptance rate is the forecasting number" claim is also wrong — acceptance is not the cost driver. See the recalibration checkpoint below. Left as written, per this file's corrections-are-new-text discipline.]** **Pace: 814 minutes wall-clock, ≈90 min/item — and the headline is deliberately called misleading in batch 1's own close-out rather than quoted as a planning number.** It is dominated by reviewer turnaround across 20 round trips; it measures this conversation, not the process. **The forecasting number is the 37% acceptance rate** (~2.7 candidates assessed per locked item → ~243 candidates for the remaining 90). **No revised total schedule estimate is recorded, deliberately**: batch 1 was discovery-heavy — most of its 11 reviewer rulings *created* rules (§8.1–§8.6, §3.5.1, §3.7.1, §2.3) that later batches only apply — so extrapolating from it would forecast the wrong activity. **Batch 2 is the second calibration point; the schedule gets revised with two real numbers, not one.**
- **The eval-harness pilot's own third finding is confirmed in practice, not just adopted on paper:** `action` needs accept-sets, not exact match on the closed 34-verb taxonomy. `C17-02` ships `action_accept_set: [PROCURE, ENSURE, NEGOTIATE]` for one real sentence. The guideline generalizes it (§3.4 `action` accept-set required, §3.6 `object_class` accept-set required, §3.3 modality exact-match and explicitly *not* an accept-set).
- **Real discrepancy found while auditing this work, not fixed here, and it needs a decision before the §10 freeze: batch 1's items stamp six different guideline versions (v0.12 ×1, v0.13 ×3, v0.14 ×2, v0.15 ×1, v0.16 ×1, v0.17 ×2), monotonically increasing with annotation timestamp — but three places describe batch 1 as annotated under one version.** `SUMMARY.md`'s header says "Guideline version: v0.17"; the guideline's changelog says "v0.17 is the version batch 1 is annotated under" **and** still says "v0.15 is the version batch 1 is annotated under" two entries earlier. The per-item stamps are the honest artifact and §10's own rule ("every item stamps its `guideline_version`") is being followed correctly; the summary claims are what overstate. §10 already specifies the mechanism that would make a single-version claim true — "Batches 1–3 are conformed to the frozen version" — and **that conforming pass has not run.** Not corrected in this checkpoint because amending the guideline means a v0.18 with its own logged re-check, which is a decision, not a typo fix.
- **Second, smaller discrepancy in the same file, same reason for leaving it: `GOLD_SET_GUIDELINE.md`'s own header still reads "Status: No items have been annotated against this document yet."** Ten items have been. Fold both corrections into whatever version bump next touches that file.
- **Nothing here closes, or partially closes, either open acceptance criterion, and the criteria rows were updated to say so precisely rather than left claiming the artifacts don't exist.** Criterion 1b (compile success ≥90% on the dev corpus) and criterion 2 (Tier-2 fully-correct-IR ≥80% on the gold set) both remain **Unmeasured** — the state `CLAUDE.md` reserves for "the mechanism may be sound but nothing has run it against the real numbers." What changed is the blocker: it is no longer "no corpus and no gold set exist," it is "18 of 100 items are annotated and no scoring run has happened." The scoring harness itself — the mechanical IoU-alignment + conjunctive-field-predicate scorer the pilot designed — is still not built as a committed artifact; the pilot's version was a scratchpad script that was never added to this repository.
- **Still open before the §10 freeze (after batch 3), carried from batch 1's own close-out rather than restated as resolved:** `C03-02`'s compound-action clause count (`DRAFTER_JUDGMENT_PENDING_REVIEW`); `C11-01`'s non-party human obligor ("the executor, administrator, or personal representative"), which no rule covers; §8.3's **split branch is untested** — its only candidate failed on the obligor first; **document concentration** — `C03` supplied 3 of 10 items, all from one segment, a fair draw over a still-narrow sample; and a measured corpus-artifact rate of 7.0% of pool segments carrying page headers, address blocks or divider runs, checked for difficulty correlation and found not to be (hard 1.2%, standard 9.1%), with 73 of 101 being CUAD's own `Source:` footers present in the raw text.

### Gold-set recalibration

**Gold-set recalibration checkpoint (§21 Phase 4) — `docs/eval/GOLD_SET_GUIDELINE.md` v0.26 (§19 added; §9 and §10 amended; header and two changelog entries corrected). Analysis and documentation only: no annotation rule changed, no item annotated, no code written. Batch 2 is paused at 8 of 10 items pending a consolidation pass. Neither open acceptance criterion moves; criterion 2's row is rewritten because its *reachability*, not its status, changed.**

- **Timing was measured from session transcripts, not estimated — and that overturned the figures this file previously carried.** Batch 2's cost was reconstructed from 300 message-level UTC timestamps in the session transcript, batch 1's from the corresponding window of its own. **Batch 1: 143.0 min active, 14.3 min/item. Batch 2: 221.0 min active over 8 items, 27.6 min/item.** The previously recorded "814 minutes wall-clock, ≈90 min/item" (batch 1) and the in-session "1,028 min · 17.1 h → 128 min/item" (batch 2) are both raw wall-clock **including overnight idle** — 693 and 807 minutes of it respectively — and overstate real cost by 4–6×. Both are marked superseded above rather than edited away.
- **The reviewer's own hypothesis was tested and disproved: reviewer-error correction cycles cost 14.6 min of batch 2's 221.0 — 6.6%.** The two cycles are identified by exact transcript boundaries (the `18%` figure exchange, 18:17:25–18:22:55; the phantom-item-7 exchange, 09:33:04–09:42:09). Error-adjusted rate 25.8 min/item versus 27.6 raw. **Both cycles produced a real corpus measurement as a byproduct** — §2.4's OCR-damage class was discovered *inside* the first one. Correcting for reviewer error changes no planning number.
- **The real driver, measured: reviewer adjudication latency, not annotation.** Drafter working time *fell* between batches (74.1 → 57.7 min) while producing the same number of rules over fewer items; the entire increase is reviewer turnaround (68.8 → 163.4 min). Time per adjudication is essentially constant (17.2 → 18.2 min); what changed is the count of blocking decisions (4 → 9, i.e. 0.40 → 1.13 per item).
- **THE FINDING THAT MATTERS MOST: there is no evidence the rule-discovery rate is tapering, and the honest answer at n=2 is "we cannot tell."** Bumps per item went **up**, 0.50 → 1.00 (ratio 2.00, exact p = 0.168, 95% CI on the ratio **[0.65, 6.11]**); a Poisson regression over the 17-item sequence gives slope **+0.0322/item**, 95% CI on the multiplier **[0.924, 1.159]**, LR χ² = 0.321, **p > 0.05**. Compatible with anything from a 7.6% decline to a 15.9% rise per item. **Every planning figure below therefore assumes the non-tapered batch-2 rate.** A taper would be a welcome surprise, never a premise. A document-saturation model was proposed and **falsified on this data** (batch 1: 7 documents → 5 bumps; batch 2: **3** documents → **8**) and is recorded in guideline §19.2 so it is not re-proposed.
- **Batch 1's ~40% structurally-uncompilable finding is confirmed by batch 2, not refuted: 7 of 18 locked items (38.9%, 95% CI [20.3%, 61.4%]), or 9 of 18 counting `corpus_artifact_in_span` spans.** Consequence, and it is a scope finding about Phase 4's own acceptance bar rather than an extraction-quality one: **criterion 2's ceiling over its stated all-items denominator is ~39–61% at every point in the measured interval, so ≥80% is arithmetically unreachable there.** The recommended fix is §11's existing treatment for criterion 1b — dual-report over all items and over `len(known_gaps) == 0` items, with the ceiling stated in advance. **Recorded in guideline §9 as RECOMMENDED and explicitly NOT in force**: it is a blueprint §21 amendment and needs its own decision.
- **The target size was audited on request and downgraded from a recommendation to a working figure.** An earlier draft of this recalibration argued "100 is approximately the minimum defensible size" with the same confidence as the measured numbers. It does not deserve that: the argument depends on (1) an **unmeasured** true fully-correct rate — required scoreable N is 25 at a true 95%, 54 at 90%, 87 at 88%, **230 at 85%**, and no rate has ever been computed; (2) a gap-rate interval too wide to discriminate — 100 annotated items yields anywhere from **39 to 80** scoreable, straddling the threshold; and (3) a Wilson-interval independence assumption **the data violates**, since items cluster by segment (batch 2's 8 items came from 4 segments, 3 from `C02-021` alone), which makes every required-N figure an underestimate. **100 stands because it is the existing spec and nothing yet justifies changing it, not because it was derived.** See guideline §19.5.
- **Planning figures, with their status stated rather than implied.** The cost model `min/item = (adjudications/item × 18.2) + 7` is an **assumption**: a two-parameter model fitted to two data points, zero residual degrees of freedom, so it **cannot fail to fit** — a structural story about where time goes, not a validated law. On it: **remaining 82 items ≈ 38 h at the non-tapered rate, contingency 55 h; total to a scored criterion-2 number ≈ 62–73 h** (consolidation 6 · harness build and run 4–8 · 82 items 38 · freeze-time conforming 6 · §7 held-out 8–15). **The harness, held-out, and per-re-check components are guesses with no measured basis** and are labelled as such in guideline §19.6. An unquantified `K ≥ 4 → REDESIGN` branch in §7 would reset most of it.
- **Batch 2 paused at 8 of 10, and the pause is the decision this checkpoint actually makes.** Two items remain undrawn. They are cheap (~55 min) but cannot answer anything — adding 2 items to a 17-item sequence narrows the trend interval by ~5%. The consolidation pass instead closes, in one sitting: the **seven open decisions** (five rules at `DEFAULT, PENDING CONFIRMATION` — §15 v0.4, §16 v0.5, §17 v0.6, §8.3.1 v0.23, §8.8 v0.24 — plus `C03-02` and `C11-01` at `DRAFTER_JUDGMENT_PENDING_REVIEW`), **three of which have been open since before batch 1 existed**; **154 outstanding item × rule re-checks** (fifteen of the 18 locked items were annotated against a since-changed ruleset; `E01-01` has never been checked against 13 later rule versions). *Corrected from the 127 first recorded in this entry and in guideline v0.26 — both counted version bumps rather than rules, and v0.16, v0.17 and v0.21 each introduced two; corrected at guideline v0.27.* Also: `SUMMARY.md`'s single-version claim, and a §7 amendment for a held-out draw against an 8-item batch. §10's conforming step is mandatory before the freeze regardless — deferring it to the freeze would face an estimated ~390 re-checks in one tangle instead of 154 now. **The freeze stays after batch 3**, unchanged.
- **Three false claims in the guideline were corrected at v0.26**, all pre-existing: its header read *"No items have been annotated against this document yet"* from v0.1 through v0.25 (18 have, since 2026-08-19); and the **v0.15** and **v0.17** changelog entries each claimed to be "the version batch 1 is annotated under," contradicting each other — batch 1's items in fact stamp **six** versions. Both changelog claims are struck in place with a superseded marker rather than deleted. `apps/brain/evals/goldens/batch01/SUMMARY.md` carries the same defect and is deliberately **not** fixed here: it is consolidation-pass work.
- **Consolidation pass begun: the mechanical sweep clears 139 of the 154 re-checks, leaving 15 for the reviewer** (guideline §19.7). Detectors are deliberately high-recall/low-precision — a false positive costs minutes, a false negative leaves a silent conformance gap. This cuts the pass's re-check component from ~3.2 h to well under an hour, so the ~6 h estimate becomes ~2.5–3 h. **A detector bug was found and fixed mid-sweep and is recorded rather than quietly corrected:** §2.4's OCR detector was written `[a-hj-z]` intending to exclude the one-letter words *a* and *I*, but that class still contains `a`, so it matched "in a"/"of a" and fired on 11 of 18 items across 8 documents at a **100% false-positive rate with zero true positives**. §2.4's own measured ground truth — 6 segments, all in `C11` — is what exposed it. Same shape as the trailing-period and leading-clause bugs already on record: a check that passes casual inspection while being wrong on every real case.
- **The highest-value next action is not annotation.** Building the scoring harness and running it against the **18 already-locked items** requires no further annotation and is the cheapest way to learn which column of the required-N table this project is in. At a true ~95% the set could stop near 50 items; at ~85% no achievable N certifies criterion 2 and it must be restated instead. Target size and criterion-2 framing are both deferred to that number.
- **Tripwire, recorded because the variable that drives everything is currently unrecorded.** Adjudications per item per batch must be logged going forward — today it is recoverable only by mining transcripts, which is how this entire checkpoint had to be produced. **If batch 3 produces ≥15 bumps over 10 items (adj/item ≥ 1.50), the 38 h plan is broken and target size re-opens.** Because discovery is draw-dependent and high-variance, evaluate against the cumulative rate across batches 2–3, never batch 3 alone.
### Scorer-wiring

**Scorer-wiring checkpoint (§21 Phase 4) — `apps/brain/evals/harness/{run_scoring,conditions}.py`, `report.py` extended (G5, miss diagnosis), `tests/evals/test_harness_{run_scoring,conditions}.py` (36 new tests). Scoped in a dedicated conversation before any code was written, the same discipline the verifier and `ir_hash` checkpoints used. Produces the FIRST real criterion-2 number this multi-week effort has been building toward. Spends nothing: every model response is replayed from the 35 committed cassettes.**

- **THE NUMBER: `5/18 = 27.8%` fully-correct over all items — the in-force criterion 2 denominator — and `3/8 = 37.5%` over `len(known_gaps) == 0`, labelled RECOMMENDED / NOT IN FORCE exactly as §9 requires.** Both are emitted by `report.py`'s G3, which this checkpoint deliberately did not touch: a real number existing is not a reason to quietly promote the recommended denominator, and that decision still needs its own approval. **This is 18 of the intended 100 items, so it is a measurement over 18% of the specified gold set, not a criterion-2 verdict.** 6 of 18 items are unstable across runs.
- **`C17-01` is scored over 2 runs, not 3, and says so on its own line in the rendered report** — §6.1, wired through `SHORT_RUN_REASONS` → `report.build(short_run_reasons=...)` → `render()`. `build()` **raises** on a short run with no stated reason rather than rendering it (an unexplained short run means a run was silently dropped, which is a bug, not a caveat). Guideline **v0.30** reconciled §6.1's n=2 tie clause with `report.py`'s pre-existing G2 worst-outcome rule — §6.1 as written at v0.29 contradicted it, caught while scoping this session before any code depended on either reading.
- **A real finding the first run surfaced, and the instrument was changed to express it: the model extracts at a COARSER GRANULARITY than the gold set annotates.** `C22-01`'s gold span is `[22:128]` while the model predicted `[22:529]` — **identical start offset**, 4× the length, IoU 0.21; `C14-076` produced one span `[12:253]` covering two separate gold items. `align()` is faithfully applying §4.1's IoU ≥ 0.5, but a plain `MISSED` cannot distinguish *no prediction at all* from *a prediction whose boundary fell short* — different failures with different fixes. Every `MISSED` now reports `NO_PREDICTION_OVERLAPS` or `BELOW_IOU_THRESHOLD` with the best IoU achieved. Measured across the set: `C04-03` and `C17-02` are `NO_PREDICTION_OVERLAPS` on all three runs (genuine compile failures — and `C17-02` is the known `within_preposition` gap manifesting exactly as predicted), while `C22-01`/`C14-02` are boundary mismatches, not extraction failures. **Do not read the 27.8% as a pure extraction-quality number until this granularity question is separated out.**
- **The `condition_raws` prompt-alignment question is ANSWERED, and the answer is MIXED — neither reading of the debt entry's two hypotheses is clean.** Of 8 gold condition entries, 2 are `if`-headed and 6 are not. **3 of the 6 non-`if` entries were NEVER emitted** in any run (`'giving any notice required or permitted under this IP Agreement'`, `'As requested by Client'`, `'upon reasonable notice and approval by TIBCO'`); 1 was emitted exactly (`'upon the death or mental incapacity of a Principal'`); 2 were emitted with a different boundary. So the prompt's *"'if'-type"* wording is **not** simply loose and **not** simply narrow. The fix is therefore NOT the automatic `v4` prompt bump the debt entry anticipated — that entry assumed a clean narrow reading — and this checkpoint deliberately does not act on it. **Measured, not fixed.** `repairs_touching_conditions: 0` — a measured fact, not an assumption: the check applies repair patches by index and would have counted any that touched `condition_raws`.
- **Compile success over the gold segments is `48/69 = 69.6%` and is NOT criterion 1b.** The function computing it is literally named `compile_success_not_criterion_1b()` and its own returned `label` field says so, because 1b is defined over the 28-document dev corpus and this is 12 gold segments. **Criterion 1b remains Unmeasured.** The denominator deliberately excludes candidates rejected at grounding (14), which never reached the compiler at all.
- **Committed as harness code, not a run script** — §21 R3's "a published number without its registry is not reproducible" applies to the code computing the number too. The guideline version is read from the items' own stamps rather than the document header, which is what keeps the cassettes honestly non-stale: v0.29 and v0.30 changed no annotation rule, and the day an amendment does, the conforming pass restamps the items and every cassette correctly goes stale at once.
- **36 new tests, each planting one defect** (`tests/evals/test_harness_{conditions,run_scoring}.py`): the `"If,"` punctuation trap that this session's own throwaway script actually got wrong; repair-patch-by-index and a repair touching `condition_raws`; mixed guideline stamps refused; disagreeing `segment_text` refused; both miss kinds; a quarantined candidate never becoming an UNEXPECTED (W1); an unexplained short run refused; a short-run item still counted in criterion 2's denominator; the n=2 worst-outcome reconciliation pinned so the v0.29 contradiction cannot silently return; and **a stale cassette propagating rather than being silently skipped** — a stale cassette treated as a missing run would shrink the run count and arrive at §6.1's short-run state by accident. **Full `apps/brain` suite: 449 passed, 6 skipped, 0 failed** with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set, run from the repo root as `ci-brain.yml` does — 413 prior + 36 new, checked arithmetically. The 6 skips checked by name with `-rs` and confirmed identical to the prior checkpoint's list. **CI CHECKED FOR REAL, 2026-08-25** — and the branch push alone was NOT enough to get it: `ci-brain.yml` triggers only on `push` to `main` or on `pull_request`, so a feature-branch push runs nothing and `gh run list` came back empty. Run `32849472981` was raised via `workflow_dispatch` on `evals/batch02-gold-set`, watched to completion (`conclusion=success`, `headSha=f441c604`), then the log pulled with `gh run view --log` rather than trusting the green check. **CI reports 454 passed, 5 skipped, 0 failed**, with `check-migrations: 18 migrations, all applied cleanly. No drift.` The arithmetic reconciles local to CI exactly and **confirms the two local Celery failures were environment flakes, not defects**: local 451 passed + 2 failed + 6 skipped = 459 collected; CI 454 + 0 + 5 = 459. Both `test_celery_broker_integration.py` tests are logged `PASSED` on CI (+2), and the platform-gated `test_fork_safety.py::test_without_the_mitigation_...` that skips on this macOS machine ran and passed on Linux (+1 passed, −1 skip) — the identical delta every prior checkpoint has documented. Zero `FAILED`/`ERROR` lines in the whole log.
- **What this does NOT move.** Criterion 1b: untouched, still Unmeasured. Criterion 2: measured for the first time, over 18 of 100 items, so still not decided. The dual-denominator amendment: still recommended and not in force. **Next: the granularity question above is the highest-value thing to resolve before annotating more items** — if a systematic span-boundary convention mismatch is depressing the rate, that is a scoring/annotation alignment issue rather than a model-quality one, and it would be expensive to discover after another 82 items are annotated against the current convention.

### Failure-driver investigation and the v0.33 scoring amendments

**Failure-driver investigation and the v0.33 scoring amendments (§21 Phase 4) — `docs/eval/GOLD_SET_GUIDELINE.md` v0.33, `evals/harness/{score,report}.py`, `tests/evals/test_harness_score_v033.py` (31 new tests), two superseded tests amended in place. Investigation first, proposals second, implementation only after per-fix approval — the same discipline the verifier, `ir_hash` and scorer-wiring checkpoints used. Spends nothing: every model response replayed from the 35 committed cassettes.**

- **THE HEADLINE IS THAT THE HEADLINE NUMBER CHANGED MEANING, NOT THAT IT WENT UP. Criterion 2 is now `3/8 = 37.5%` over `len(known_gaps) == 0` — §9's dual denominator is IN FORCE as of v0.33** — with `6/18 = 33.3%` over all items reported alongside and explicitly labelled NOT the criterion. The in-force figure is **unchanged** by everything below; what moved is the all-items figure, 27.8% → 33.3%.
- **The investigation's finding, which is the reason the denominator decision was taken: of eleven `object_class`/`conditions` failure instances, exactly ONE is a genuine model error** (`C17-01` run 1 labelling the efforts qualifier as the object — the §16 hazard — and it gates nothing, since that item fails obligor/obligee/underspecified on both runs regardless). **Three are pure inflectional number**, **two are narrow accept-sets**, **one is structural**, and **four are quote-extent or entry-count conventions neither side was ever told to share.** Measured, not estimated: across the 37 aligned `object_class` comparisons, **25 exact, 7 number-only, 5 genuinely different**, and **zero of the 18 accept-sets hedge on number** — so the passing items pass because gold happened to author the model's number (`required_quantities`, `third_party_royalties` both pass as plurals). Counterfactual: ignoring `object_class` and `conditions` together takes the all-items rate 27.8% → 55.6%, so the two clauses gate 5 of the 13 non-passing items. Same shape as the v0.31 granularity finding, one level down — gold and the pipeline compared on a convention neither was given.
- **THE E01-01/C14-01 PAIRING, AND WHY IT FORCED A DENOMINATOR DECISION RATHER THAN A SCORING TWEAK.** Both carry `exception_unsupported`, both have gold `conditions: []` (§3.8.1 branch 3), both have an `unless` carve-out inside the span. The model quoted it in `E01-01` (clause 7 fails) and did not in `C14-01` (clause 7 passes). **Same gap, opposite verdicts, decided by which of two spec-forbidden behaviours the model happened to exhibit.** Confirmed against `ir_compile.py:251-252` — `parts.append(f"IF {_q(condition_raw)}")`, no `UNLESS` branch — the pipeline has exactly three possible behaviours and **clause 7 ranks them inverted**: emitting the carve-out as a condition (SPEC §6: *"silently absorbed into `condition`"*, forbidden) **fails**; omitting it entirely (SPEC §6: *"silently dropped"*, also forbidden) **passes**; the required loud routed failure has **no code path at all** and would score `MISSED`. The single scoring-optimal outcome available is one SPEC §6 names as forbidden.
- **Is gold's own `conditions: []` convention therefore in tension with SPEC §6? NO — and the question was worth resolving properly rather than assuming.** SPEC §6's prohibition is scoped to the **pipeline** (L1→L2, extraction output → IR); §3.8.1 branch 3 governs the **annotation record** (L3), which SPEC §6 does not address. And the operative word is *silently*: gold sets `conditions: []` **and** tags `known_gaps`, **and** records the carve-out verbatim in `annotator_notes` (mandatory), **and** keeps it inside the span — three independent recovery paths. That is "loud and routed" in the annotation medium. **The real tension was elsewhere**: §8.2 defends the convention on the promise that *"the overstatement is always recoverable from the data rather than baked silently into a score"* — and the in-force all-items denominator **never read the tag**. The convention was sound; the reporting layer was not keeping its half of the bargain.
- **§9's dual denominator adopted on TWO independent grounds, recorded separately because they fail differently.** Ground 1, reachability (v0.26, pre-existing): the all-items ceiling is ~39–61% at the measured gap rate, so ≥80% is unreachable there — **contingent on that rate holding, and its interval is wide**. Ground 2, validity (v0.33, new, and it does **not** depend on the rate): the all-items numerator can count an IR that is knowingly not a faithful representation as `FULLY_CORRECT`. Not hypothetical — **it was already happening before this session**: `C03-02` (`compound_action`) and `C04-02` (`mutual_obligation`) were already in the 5/18 numerator. Both are **incomplete** IRs (a monitor misses a real breach). `C14-01` is the first **overstated** one (a monitor flags a breach the contract exempts) — §8.2's own *"stronger than the one the contract imposes"* — and it is the worse direction for a compliance tool.
- **The coupling that dictated the landing order, and it was flagged before either change was written: fixing the number rule MANUFACTURES the false positive.** `C14-01` failed on `object_class` alone (`taxes` against an accept-set holding `tax`). Under clause 5's number rule it flips to `FULLY_CORRECT` and enters the all-items numerator — the entire `+1` from 27.8% to 33.3% **is** that item. Landing the number rule before the denominator decision would have published a number one item of which certifies behaviour SPEC §6 forbids. Approved and landed in the required order: denominator first, number rule second.
- **What each amendment does and does not touch — the cost axis that governed every choice.** `run_scoring.guideline_version_from_items()` reads the stamp from the **items**, and `Cassette.verify()` treats `guideline_version`/`prompt_version` as staleness dimensions. So a change that restamps an item invalidates **all 35 cassettes at once**, and `C17-021` run 3 is unobtainable (§6.1), making a full re-record impossible — that is §22's blocker. **Every v0.33 change is therefore a reporting rule, a comparison rule, or a *forward* authoring rule. No annotation rule changed, no item was restamped, no cassette went stale**, and the items still stamp `v0.28`. Same footing as v0.29/v0.30.
- **§5 clause 5 normalizes grammatical number on BOTH sides — a comparison rule, not an accept-set widening.** It widens no set and is fitted to no prediction; it deletes a distinction neither side was asked to make (`prompts/extraction/v3.yaml` asks only for *"a short lowercase snake_case label"* and its own worked example `deliverables` is plural). **Deliberately a narrow normaliser, never a stemmer** — Porter would conflate `retention`/`retain` and `provisions`/`provide`, making clause 5 nearly vacuous, which is the worse failure since clause 5 is the only check on an open vocabulary. **Scope is clause 5 only**; clause 7 is NOT number-normalized, because §3.8 requires verbatim quotes and normalizing a quotation is a different act from normalizing a label — pinned as its own test.
- **§3.4's freeze rule gains ONE bounded, named exception, recorded as an amendment and explicitly NOT as a reading of the existing sentence.** The proposal that §3.4 only ever barred fitting to *this* prediction is defensible, but it is a **softening** of a categorical rule, and adopting it silently would mean the rule had been relaxed by the first case that pressed on it. Instead: widening is permitted **only during §10's pre-scheduled freeze conforming pass**, justified by three properties none of which an ad-hoc widening has (pre-scheduled so it cannot be reached for after a disappointing number; restamps and re-records anyway so it is free there; adjudicated as one batch where a pattern of self-serving widenings is visible), and gated by a test that the omitted label be defensible **from the sentence alone, stated without reference to any prediction**. Three locked items are queued under it — `C02-03` (`invoice_costs`), `C11-01` (`principal_interest`), `C02-01` (the whole coordinated party phrase) — and **all three keep scoring as failures until then**, a stated understatement §9's inline disclosure reports.
- **Forward authoring rules added, batch 3 onward, restamping nothing:** §3.6 requires enumerating **both nominal anchors** where the object phrase has two (`principal_interest` *and* `franchise_interest` for *"the Principal's interest in Franchisee"*) and **compounds of members already in the set** (`invoice_costs` where `invoice` and `costs` are both present) — and explicitly does **not** ask for plurals, which clause 5 now handles. §3.5.1 gains a rider requiring the **whole coordinated phrase** in `obligor_accept_set` (`"Antares or its Subcontractor"`), the form the model stably emitted and the rule never enumerated. **§3.8.2 fixes the two conditions conventions §3.8 never had**: a qualifier quote begins at its own introducing marker exclusive of coordinating adverbs (`provided that`, not `further provided that` — `C04-01`'s failure was that single word, **97.8% string similarity**), and adjacent conditional phrases are separate entries even when juxtaposed with only a comma (`C11-094`, which the model split correctly in run 2 and merged in runs 1 and 3).
- **`C04-01` is recorded as a SYMMETRIC ambiguity in which gold's choice was arbitrary rather than correct, and §3.8.2 says so in terms.** Both strings are verbatim substrings and §3.8 gave no rule for where a qualifier quote begins, so gold's inclusion of `further` was an accident. Rule A picks a convention for future items; it must **not** be cited as evidence of an extraction defect, and the alternative — changing the prompt to match gold's arbitrary choice — was considered and rejected as fitting the model to an accident.
- **The narrow-`if` hypothesis in the debt list is DEAD, and the harness's own check killed it.** `conditions.py` reports the model emitted **22 condition strings, 9 of them non-`if`-headed**, including an `upon`-headed one. It reads *"'if'-type"* as a category. So the 3 never-emitted entries (`C22-01`'s participial, `E07-01`'s two fronting adverbials) need a different explanation, and **the automatic `v4` prompt bump that debt entry anticipated is not the indicated fix.** `E07-01` is the sharpest case: the model's span is **byte-identical to gold (IoU 1.000)** with `temporal_raw: null` — it saw both phrases and filed them in no field at all.
- **`C22-01` is genuine content loss on a clause gold itself called ambiguous.** Its own `annotator_notes` record two defensible readings; the model produced **neither**, emitting gold's `obligor` (`"Each party"`) together with the *other* reading's empty `conditions`, so the participial is assigned nowhere. There is no annotation of this item its output would satisfy.
- **`obligor_accept_set` was authored by §3.5.1 since v0.28 and read by NOTHING** — `_party_matches()` took `gold["obligor"]` alone; a grep found the field only in golden and probe JSON, never in harness or test code. Now read, **in the `UnresolvedParty` branch only**: on the resolved path the registry's own `canonical_name`/`aliases` matching already *is* the accept-set, so a second one there could only loosen an identity check. **No obligee accept-set was added** — §3.5.1 measured that field as unnecessary (joint 0 / disjunctive 3 of 1,547) and routes obligee alternatives to `annotator_notes`; inventing one would implement a rule the guideline declined to make. **Effect on the score: zero**, stated plainly rather than claimed as a win — `C17-02` is `MISSED` on `within_preposition`, and `C02-01`'s prediction is the whole disjunction, which matches neither entry. A latent-correctness fix, not a score fix.
- **§22.1's forcing function is deliberately NOT declared met (new §22.2).** v0.33 queues three items for freeze-pass widening, which *would* require conforming, so the question arose immediately. Ruling now would repeat §22.1's own stated error from the other side — settling a general question against a scope estimated **in advance** of the pass that determines it. The freeze pass conforms `C14-02`'s §8.3.1 amendment, these three widenings, and whatever batch 3 adds, all at once; its real combined scope is knowable then and only guessable now. **Trigger re-armed, not fired**, and nothing in v0.33 is blocked by leaving it open.
- **Proven with 31 new tests, each planting one defect, and the suite was checked against the pre-fix code rather than trusted green (Standing Principle 7): 25 of 31 fail without the change; the 6 that pass are the negative guards, which should pass both ways.** The number tests **lead with a known-answer table** — including `taxes`→`tax`, which this session's own first-draft normaliser got **wrong** by stripping only a trailing `s`, caught exactly by running it against cases whose answers were already established. The table also pins the over-match guard (`retention`≠`retain`, `provisions`≠`provide`) so a future "improvement" to a stemmer fails loudly. Other planted defects: an exact hit must not be mislabelled as a number match (or the measurement is lost); clause 7 must NOT be number-normalized; the accept-set must not leak into the obligee slot; a resolved party must ignore it; an unmapped `known_gaps` tag must surface as **UNCLASSIFIED** rather than defaulting into a bucket; and a gapped item **not** in the numerator must not be disclosed, since G6 discloses what the numerator certifies.
- **Two pre-existing tests failed on the denominator flip and were AMENDED IN PLACE, not deleted** — `test_harness_report.py::test_the_not_in_force_figure_is_labelled_as_such_in_the_render` and `test_harness_run_scoring.py::test_both_denominators_are_rendered_and_the_second_is_labelled_not_in_force`. They pinned the superseded labelling and caught the change, which is the suite doing its job. Both now pin the **new** rule and additionally assert the old string is **gone**, so a silent flip back would still be caught.
- **Full `apps/brain` suite: 484 passed, 6 skipped, 0 failed**, with `DATABASE_URL`/`BRAIN_DB_PASSWORD` set, run from the repo root exactly as `ci-brain.yml` does. **Reconciled against collection counts in an isolated `git worktree` at HEAD rather than against a remembered total: HEAD collects 459, this tree collects 490, delta exactly +31 = the new tests.** 484 passed + 6 skipped = 490. The 6 skips were checked by name with `-rs` and are the identical list every prior checkpoint records. **The pytest exit code was captured directly rather than through a pipe or a trailing `echo`** — Standing Principle 7's exit-code sibling, which this project has been bitten by twice. *Noted in passing, not corrected: the scorer-wiring checkpoint's own local figure of "449 passed, 6 skipped" implies 455 collected, 4 short of the 459 that commit actually collects — and 459 is corroborated by that checkpoint's own CI line (454 passed + 5 skipped). Its local number appears to have been taken before the last few tests of that session were written. Pre-existing, immaterial to any claim here, and recorded rather than silently reconciled.*
- **CI CHECKED FOR REAL, 2026-08-25 — run `32857549002`, `workflow_dispatch` on `evals/batch02-gold-set`, `conclusion=success`, `headSha=5728eba0`, 13m37s.** Raised deliberately via `gh workflow run ci-brain.yml --ref evals/batch02-gold-set`, since per the tracked trigger-policy decision above a feature-branch push alone raises nothing; `headSha` was checked against the local commit **before** waiting on the result, so the run is known to be against this work rather than an earlier tip. **CI reports 485 passed, 5 skipped, 0 failed**, with `check-migrations: 18 migrations, all applied cleanly. No drift.` **The log was pulled with `gh run view --log` rather than trusting the green check**, and the **full** summary line was read (`============ 485 passed, 5 skipped, 1 warning ============`) rather than grepped for `[0-9]+ passed` — that grep is the exact detector this file's debt list records as having nearly hidden a `2 failed` immediately preceding a match. Zero `FAILED`/`ERROR` lines in all 1,017 log lines. **Arithmetic reconciles local to CI exactly: local 484 + 6 skipped = 490 collected; CI 485 + 5 = 490.** The +1 passed / −1 skipped is the identical delta every prior checkpoint documents, and it was verified **by name** rather than assumed: `test_fork_safety.py::test_without_the_mitigation_a_forked_child_inherits_the_parents_connection_fd` is platform-gated, skips on this macOS machine, and is logged `PASSED` on CI's Linux runner.
- **What this does NOT move.** Criterion 1b: untouched, still **Unmeasured**. The compile-stage bottleneck: untouched — `C04-03` (IoU 0.988), `C17-02` (0.738), `E01-01` run 2 (**1.000**) and `E03-01` run 1 (**1.000**) are still `EXTRACTED_THEN_QUARANTINED`, clauses found and grounded byte-perfectly with `run_repair` exhausting its budget. Every v0.33 change is scoring-layer work and leaves that exactly where it was. **Not built, tracked against the Normalizer checkpoint: the compile-stage loud path** — `ir_compile` routing a carve-out to `compile_quarantine` instead of `IF`, which is what would make the spec-compliant behaviour reachable at all. **Not run: the Tier-B prompt probe** (below).

### Compile-stage bottleneck investigation

**Compile-stage bottleneck investigation (§21 Phase 4) — analysis only. No code written, no rule changed, no item restamped, no cassette stale, no fix proposed or implemented. Spends nothing: every model response replayed from the 35 committed cassettes. Scoped as its own pass with fixes explicitly out of scope, the same investigate-first discipline the verifier, `ir_hash`, scorer-wiring and v0.33 sessions used.**

- **THE HEADLINE, AND IT SUBSUMES THE THREE TEMPORAL ITEMS THAT MOTIVATED THE INVESTIGATION: the temporal classifier accepted NOTHING the extractor produced. `0 of 25`.** Running first-pass `compile_candidates()` over every grounded candidate with repair disabled: **`classified OK: 0, UNMAPPABLE_TEMPORAL: 25`**. Not a sample — that is every timing phrase that reached `ir_compile._classify_temporal` across all 35 cassettes. **Corollary, and the sharper number: of 48 typechecked obligations, 43 carry `temporal = None`** — they compiled because the model declined to extract a timing phrase at all — **and all 5 that carry a temporal came from the repair loop. Zero survived first-pass extraction.**
- **THE DENOMINATORS ARE DIFFERENT AND THE DISTINCTION IS LOAD-BEARING, so it is written down rather than left to be re-derived.** An earlier statement of this finding in-session said "0 of 28", which is true of the *extractor's* output but **overstates the classifier's denominator by 3**. Instrumented, not inferred by subtraction:
  - **83** obligation objects emitted across all 35 extraction responses. **14** never reached the compiler (**4** `SCHEMA_INVALID`, which carry no `llm_candidate` at all; **10** `SPAN_NOT_FOUND`); **69** grounded = 48 typechecked + 21 quarantined.
  - **28** of the 83 carried a non-null `temporal_raw`. Of those 28: **25 reached `_classify_temporal`**, **1** was lost at grounding (`E07-010` run 2, `'during normal business hours'`), **2** were lost at schema validation (`C02-021` run 1, `'until [***] or such longer period…'` and `'at reasonable intervals'`). 25 + 1 + 2 = 28, checked.
  - **`43 of 48` is a THIRD population, not a restatement of either.** 48 is every typechecked obligation regardless of whether a temporal was attempted; the 43 are drawn from the **55** candidates that emitted `temporal_raw: null`. The three figures relate only by partitioning the same 83 candidates. `48 + 21 + 14 = 83` and `28 + 55 = 83`, both checked rather than assumed to match.
- **20 of the 21 quarantines are `UNMAPPABLE_TEMPORAL`; exactly one is a `PARSE_ERROR`.** The compile-stage bottleneck is, essentially entirely, temporal classification. This **refines rather than contradicts** the v0.33 checkpoint's "THE BOTTLENECK IS THE COMPILER, NOT EXTRACTION" bullet above: that entry established *which stage*, and named `REPAIR_MADE_NO_PROGRESS` as the cause; this one establishes *which mechanism inside that stage*, and finds that cause largely misleading (see the repair-loop bullet below).
- **THIS DOES NOT CHANGE HOW CRITERION 2's `3/8 = 37.5%` SHOULD BE READ, AND THE FACT THAT IT DOESN'T IS ITSELF THE FINDING — it was checked rather than assumed either way.** §9.1's in-force denominator is 8 items: `C02-01, C02-02, C02-03, C03-01, C03-03, C11-01, C22-01, E07-01`. Their five failures are `obligor` · `object_class` · `object_class`/`conditions`/`obligee` · `conditions`+boundary · `obligee`/`conditions`/`underspecified`. **Zero of the five are temporal-caused.** The bottleneck's actual victims — `C04-03`, `C17-02`, `E03-01` — **all three carry `known_gaps` and are therefore already outside the in-force denominator by §9.1**, which is the dual denominator doing precisely what it was adopted at v0.33 to do. On the all-items figure the bottleneck costs **3 of 18**. **So the two investigations are about disjoint item sets**: v0.33's accept-set/conditions work governs the in-force criterion, and this finding governs the gapped remainder and criterion 1b.
- **The one genuine connection between them, recorded because it is not obvious: `C11-01` is in the in-force denominator, its temporal WOULD have failed, and the repair loop is the only reason it did not.** The model quoted `"within a reasonable time after the Principal's death or mental incapacity"` (unmappable) and repair narrowed it to `"after the Principal's death or mental incapacity"` → `AFTER "the Principal's death or mental incapacity"`, on all three runs. **Stated precisely rather than overclaimed:** `C11-01` fails `object_class`/`conditions`/`obligee` regardless, so removing the repair loop would move its outcome label `PARTIAL` → `MISSED` and **leave the numerator at 3/8 unchanged**. The repair loop currently moves **no criterion-2 numerator, in either denominator** — it moves exactly one outcome label.
- **`REPAIR_MADE_NO_PROGRESS` IS MOSTLY CORRECT BEHAVIOUR, NOT A BROKEN LOOP — the opposite of how the v0.33 entry's wording reads.** 18 of 21 quarantines carry that cause. But `prompts/repair/v2.yaml` **explicitly instructs the model to omit candidates it cannot fix** (*"If no shorter verbatim substring of span_text is a clean timing phrase on its own, omit that candidate from your response entirely"*), so `{"repairs": []}` is compliance. Checked against reachability rather than assumed: every quarantined span's substrings were enumerated from each keyword-anchored word boundary and run through the production classifier. **Of the 20 temporal quarantines, 19 had no correct repair available at all.** Verdict over 20 opportunities: **18 correct declines · 1 genuine miss** (`E03-005` run 1 — model non-determinism; the same model got it right in runs 2 and 3) **· 1 design defect** (below).
- **The repair loop is doing real, measurable work, and it partly mitigates §15.5's F7 — a mitigation F7 did not consider.** 11 temporal repair proposals: **5 compile, 6 do not**. Six of the failures were over-captures with a genuinely reachable narrowing and the loop rescued **5 of those 6**. One rescue is *literally* F7's worked case: F7 records the risk as *"the gold answer is reachable **only** if the extractor quotes from the preposition."* Measured, **the repair loop is a second path to that same answer**. This is the first real measurement of the rate `graphs/repair.py`'s own docstring asked for (*"Expect a low success rate here and MEASURE it… if it lands near zero on real cassettes, demoting `UNMAPPABLE_TEMPORAL` to quarantine-immediately is the right follow-up"*) — **it did not land near zero, so that follow-up is NOT indicated**, which is worth recording as a live prediction that came out the other way.
- **Root cause of the four items that motivated this, each verified by execution against the production classifier/parser rather than by reading the regex.** *(A correction to the framing the investigation was handed: `C14-02` is NOT one of the quarantines. The real `EXTRACTED_THEN_QUARANTINED` set is `C04-03` 0.988, `C17-02` 0.738, `E01-01` run 2 1.000, `E03-01` run 1 1.000.)*
  - **`C04-03` — tracked gap §8.9, reaching a locked item in real measurement for the first time.** `temporal_raw = "on the Delivery Date"`; `_RELATIVE_RE` is `^(before|after)\s+(.+)$`. **New, and it is the part worth keeping: gold's answer is UNREACHABLE, and so is §8.9's.** Gold annotates `temporal: {form: BY, alias: "the Delivery Date"}`; under §8.9's own rule (`on` → `after`) the form is arguably `RELATIVE_TO_TRIGGER` instead. **It does not matter, because neither is producible**: `BY` needs a literal `by` and `RELATIVE_TO_TRIGGER` needs a literal `before`/`after`, and the span says `on`. Same structural class as §8.3.1's v0.31 finding and §15.5's F7. All three runs returned `{"repairs": []}`, correctly — the only classifying substrings of that span are `by Bellicum` / `by delivery …`, neither a deadline.
  - **`C17-02` — tracked gap §8.6, already tagged, manifesting exactly as this file predicted.** `"within 24 months following the Commencement Date"`; `_WITHIN_RE` demands `of`. **Zero substrings of the span classify**, confirmed by exhaustive enumeration — so gold's `temporal: null` is equally unreachable while the model emits any timing phrase at all, which is why the item scores `MISSED` rather than `PARTIAL`. Carries the F5 defect below.
  - **`E01-01` run 2 — NOT PREVIOUSLY CATEGORIZED. A genuine failure whose failing component is the MODEL, not the loop.** The only `PARSE_ERROR` quarantine in the set: the model emitted `action: "DEEM"`, reading the surface verb in *"shall be **deemed** waived"*. The loop derived the correct hint — action not in the closed taxonomy, with the full 34-verb list attached, **a list that contains `WAIVE`** — made one call, and the model returned `{"repairs": []}`, read straight from the cassette. This is precisely the class `repair.py`'s docstring calls safe (re-selecting from a closed set, no re-quoting), and **the same model emitted the correct `WAIVE` in runs 1 and 3**; it then invoked the prompt's own "omit if genuinely none fits" escape hatch when one plainly fitted. **Score impact: none** — `E01-01`'s modal outcome is already `PARTIAL` (runs 1 and 3 fail `obligor`/`conditions`), so repairing run 2 moves it `MISSED` → `PARTIAL` and changes no numerator.
  - **`E03-01` run 1 — NOT PREVIOUSLY CATEGORIZED. The repair loop's designed over-capture case, working 2 times out of 3.** `"At least ** before the ** of each Calendar Quarter during the Term of this Supply Agreement"` — an over-capture around a redacted value. Runs 2 and 3 both narrowed to `"before the ** of each Calendar Quarter"`, which compiles; run 1 returned `{"repairs": []}`. Run-to-run instability at temperature 0, not a design defect. **Caveat that matters: a *successful* repair still cannot make this item `FULLY_CORRECT`**, because gold says `temporal: null` per §8.1 — runs 2/3 score `PARTIAL` failing `temporal` and `underspecified`. A second reachability problem, this one on the **annotation** side.
  - **`C14-02` — known, decided, deliberately deferred; nothing here reopens it.** Not a quarantine: run 1 is `NOT_EXTRACTED` (the model emitted *"Each party shall promptly furnish such other party with appropriate tax receipts."*, which fuses the sentence-head subject onto the second verb phrase and is not a verbatim substring → `SPAN_NOT_FOUND`), runs 2–3 are `BELOW_IOU_THRESHOLD` at 0.285/0.286. Gold still stamps §8.3.1's **superseded v0.23** span `[184:253]`; §22.1 (v0.32) ruled that deliberate. **§8.3.1 v0.31's prediction is confirmed byte-for-byte: run 3 emitted exactly `[12:253]`**, the span v0.31 adopted, so under the conformed span it would align at **IoU 1.000**. Run 2 additionally *quarantined* this duty at `[12:254]` with `temporal_raw = "promptly"` — see the §15.3 entry in `CLAUDE.md`'s debt list.
- **What this does NOT move.** Criterion 1b: untouched, still **Unmeasured** — this measured 12 gold segments, not the 28-document dev corpus. Criterion 2: both figures untouched, `3/8 = 37.5%` in force and `6/18 = 33.3%` alongside; nothing was rescored and no item's outcome changed. §22.1's conforming blocker: untouched, trigger still re-armed per §22.2. The Tier-B prompt probe: still not run. **No fix was proposed or implemented for anything below** — widening a regex, rewording a versioned prompt, or amending an annotation rule are each their own decision, and §11's measure-before-changing principle has already been applied three times to exactly this class of change (§11's `_WITHIN_RE`, decision 6's §15.5 restriction, §8.9's `_RELATIVE_RE`).

### Classifier-hardening deferral decision (2026-08-26)

**DECISION (2026-08-26) — no classifier-hardening checkpoint is opened now; the compile-stage bottleneck is DEFERRED with a scheduled revisit, not left as an open to-do. Approved after the scoping pass directly above. This entry is the decision, not a task list.**

- **The decisive evidence, and it is a negative result: a classifier-hardening pass would move criterion 2 by EXACTLY ZERO.** Checked item by item over the 20 temporal quarantines rather than assumed: **8 are cleanly recoverable** by two one-line regex widenings (`_WITHIN_RE` gaining `after|from|following`, 3; `_RELATIVE_RE` gaining `on`/`until`, 5) — and **every one of the 8 compiles without matching gold.** `C04-03`'s gold is `BY` and the widening yields `AFTER`; `C17-02`'s gold is `temporal: null` and the widening yields `WITHIN`; `C11-01` already passes its temporal via the repair loop. They move `MISSED` → `PARTIAL` and **no numerator, in either denominator**. Gold-segment compile success would go `48/69 = 69.6%` → `56/69 = 81.2%` — real movement, still short of §21's 90%, and **not criterion 1b**, which is defined over the 28-document corpus.
- **The full disposition of the 20, hand-classified at n=20 rather than trusted from a detector:** 3 `within N u {following,from}` (**W1, sound**) · 5 `on`/`until <trigger>` (**W2, sound**) · 3 `at each regularly scheduled meeting…` (compiles as a one-shot `AFTER` where the sense is *recurring* — a fidelity loss, not a win) · **4 trigger-less `WITHIN`** (`within 24 months`, `within twelve (12) months` ×2, `within the twelve (12) month period` — **a grammar change, not a regex**, since `within_form` makes the trigger mandatory) · 2 bare vague qualifiers (a design decision, not a widening) · 2 `during <alias>` (an IR semantics change) · 1 `At least ** before…` (the repair loop's job, already working 2 of 3).
- **§8.9's queued one-line `_RELATIVE_RE` widening is NOT SAFE AS WRITTEN, and this is the most actionable finding of the scoping pass.** Verified by execution: `^(before|after|upon|following|prior to|on|at|as of|until)\s+(.+)$` matches *"**At** least ** before the ** of each Calendar Quarter"* → `AFTER "least ** before…"`, and equally *"**on** this matter"* and *"**at** all times"*. Shipping it naively would **manufacture the `_BY_RE` agentive-preposition defect class recorded in `CLAUDE.md`'s debt list, multiplied across seven new prepositions** — a precision failure in exchange for a recall gain, which is the wrong trade for a compliance tool. **A trigger-shaped tail test is therefore a FIRST-CLASS REQUIREMENT of any future widening, specified before implementation, not bolted on after a naive version ships a bug.** That requirement is the substance of this decision; the deferral is the easy half.
- **The approved sequence.** (1) **Batches 3–10 continue unblocked** — nothing in them depends on this. (2) **Run a stratified 60-segment × 1-run criterion-1b probe when convenient**: sized from the stage-4 record (73 calls / 223,723 tokens over 35 invocations ≈ 6,400 tokens per segment) against the **measured 8,000 TPM** ceiling, that is **~125 calls, ~384K tokens, ~50 min wall clock**. This is the missing baseline everything else depends on. (3) **Then** scope the classifier pass against that real baseline, sized by what it would actually recover, with the tail test above as a stated requirement. (4) **Fold the `C04-03`/`C17-02` reachability question into the §10 freeze pass**, where the conforming cost is already being paid.
- **Why deferring is right rather than merely convenient, stated so it is not re-argued from scratch.** The accept-set analogy does **not** hold: accept-set conventions accumulated because *annotation* choices kept needing new rules, whereas the classifier gaps are already fully enumerated as tagged classes (§8, §8.6, §8.9) — batches 3–10 will add **instances, not classes**, and §8's standing posture already handles instances correctly (annotate honestly, tag, measure). And §11's measure-before-changing principle applies with unusual force: it has now been invoked **four** times against this exact class of change (§11's `_WITHIN_RE`, decision 6's §15.5 restriction, §8.9's `_RELATIVE_RE`, and here), and **criterion 1b — the only number a widening would actually move — has never been measured**, so widening first would destroy the baseline that makes its value measurable.
- **A coupling that must not be lost, and it is why step 4 exists: gold's own annotations for `C04-03` and `C17-02` are unreachable regardless of what the classifier does.** `BY` needs a literal `by` and `RELATIVE_TO_TRIGGER` a literal `before`/`after`; `C04-03`'s span says `on`, and `C17-02`'s gold `null` is unreachable while the model emits any timing phrase at all. **Fixing the regex without revisiting those two annotations produces candidates that compile and still score wrong** — the classifier fix and the annotation rules are coupled, and the freeze pass is where that coupling gets paid off.
- **THE INVERSION CONDITION, recorded explicitly because it is the one thing that would make this decision wrong.** This sequencing assumes criterion 2 is the metric being optimised. **If criterion 1b becomes the gate that actually matters next, steps 2 and 3 SWAP priority and the classifier pass becomes urgent rather than deferred** — 1b counts compile success, which is precisely and only what a widening moves (`69.6% → 81.2%` on the gold segments). Whoever revisits this should check which metric is in force before re-reading the recommendation, rather than inheriting the conclusion.
- **What this decision does NOT do.** It does not fix, widen, or reword anything: `ir_compile.py`, `obligation.lark` and `prompts/repair/v2.yaml` are untouched, and the three debt entries in `CLAUDE.md` (`_BY_RE`, the F5 repair-prompt unsatisfiability, the §15.3 falsification) all stand open and unmitigated. It does not schedule the probe in step 2 — "when convenient" is deliberate, since nothing is blocked on it. It changes no annotation rule, restamps no item, and stales no cassette.
- *Method note, and a live instance of Standing Principle 7 rather than a retelling of it: the counterfactual behind the `8 of 20` figure was got **wrong twice** before it was right. The first pass counted `11 of 25` for the `_RELATIVE_RE` widening because `at` matched the "At" of "At least"; the second over-corrected and rejected the genuine `on the Delivery Date`. Both were caught by running the detector against cases whose answer was already known — the same discipline, reached for a third time in this session's own work, and the reason the published figure is a hand-classification at n=20 rather than a regex count.*

- **NEW, CONFIRMED 2026-08-26: the gold-set draw pool has no cross-batch dedup, and it has already produced a real near-miss, not merely a theoretical risk.** `apps/brain/evals/corpus.py`'s `build_pool()` (called from `cmd_draw()`) reconstructs the sampling pool **purely from the raw corpus manifest**, with zero awareness of any prior batch's `draw.json` or `exclusions.json`. `cmd_draw()` then samples independently per batch via `rng.sample()` off that batch's own seed over that same full pool. Nothing anywhere prevents the identical `segment_id` from being drawn into more than one batch's queue, and nothing prevents it being *consumed* — locked or excluded — more than once.
  - **Caught before it could do real damage: `C04-087`, next in batch 3's hard queue, is byte-identical to `C04-03`, already locked in batch 2.** Had it been annotated a second time under a new item id, one real clause would have silently counted as two gold items, corrupting criterion 2's denominator and numerator both. Logged instead with a distinct disposition — `CROSS_BATCH_DUPLICATE` — in `apps/brain/evals/goldens/batch03/exclusions.json`, deliberately **not** folded into any §2 exclusion rule, since skipping it is not a content judgment call, it's a tooling defect being worked around.
  - **`C04-024` is the confirmed sibling instance, and it already happened three times over before this was noticed**: independently drawn and excluded — same segment, same reason — in batch 1, batch 2, **and** batch 3. Wasted, duplicated reviewer effort each time, not scoring corruption (excluded everywhere), but it is the identical bug firing silently across all three batches to date.
  - **`E03-051`** was excluded in batch 1 and sat two slots past `C04-087` in batch 3's hard queue; a full pre-emptive sweep of batch 3's remaining hard-queue candidates (and, for completeness, the entire remaining standard queue) against all three batches' consumed `segment_id`s caught it before the queue reached it naturally, verified byte-identical to batch 1's own logged text rather than assumed from a matching id alone, and logged with the same `CROSS_BATCH_DUPLICATE` disposition. `C22-025`, `C10-029`, `C10-016`, and the rest of the remaining standard queue are clean.
  - **Two additional overlaps checked and found benign, for completeness rather than left as an unexamined risk**: `C06-016` (batch 1 × batch 3) and `C11-005` (batch 2 × batch 3) both appear in two batches' *overdraw* queues but were never actually consumed — locked or excluded — in either, so no double-counting occurred.
  - **Not fixed inline, deliberately.** `cmd_draw()`/`build_pool()` are shared harness code that every future batch's draw depends on; patching it mid-batch risks the exact kind of ad-hoc, unscoped harness change this project's own discipline (the verifier, `ir_hash`, scorer-wiring, and v0.33 checkpoints, all scoped in dedicated conversations before code was written) argues against. **Tied explicitly to the §10 freeze pass, or its own dedicated harness-code session before then if a fourth instance surfaces** — the fix shape is straightforward (filter `build_pool()`'s candidates against every prior batch's locked-item spans and logged `exclusions.json` entries before sampling), but deciding exactly what "consumed" means for a mid-batch collision (this session's own `CROSS_BATCH_DUPLICATE` disposition vs. retrofitting the pool itself) is a real design choice, not a one-line patch, and should not be made under batch-3 time pressure.

**Phase 4 scope, per blueprint §21.** Objective: build the differentiator — LLM candidate extraction → Lark grammar parse → semantic typecheck (the compiler), then Z3 lowering + unsat-core explanation (the verifier). Deliverables: Obligation IR v1 spec (`packages/ir-spec/`) · Lark grammar · AST · typechecker (party-alias symbol resolution, temporal unit validation, underspecification detection, amendment scope/precedence) · LangGraph extraction graph (Router → Loader → Segmenter → Extractor → **Span Grounder** → IR Compiler → Normalizer → Critic → Linker) · parse-error repair loop (≤3 retries, parser message fed back) · model router with budget enforcement, provider failover, content-hash caching · versioned prompt registry · Z3 lowering (interval algebra + modal constraints) · conflict-candidate set definition · unsat-core → plain-English explanation · `ir_hash` reconciliation for idempotent re-extraction · embeddings + hybrid search · the two-tier eval harness and the 100-obligation gold set · property-based compiler test suite. Acceptance criteria: compile success ≥90% on the dev corpus with span-grounding rate exactly 100% (zero ungrounded obligations reach the database, by construction); Tier-2 fully-correct-IR rate ≥80% on the gold set; a planted cross-document conflict detected with a minimal unsat core rendered as one readable sentence; all property tests pass including round-trip and hash stability; re-extracting the same document twice produces zero duplicate obligations.

Named risk worth flagging before writing any Phase 4 code, per blueprint's own risk list: **the IR is the single largest schedule risk if it's allowed to stay open-ended.** Blueprint's own mitigation is to freeze IR v1 at four modalities, five temporal forms, and conditions only (no nested exceptions in v1) — everything else is v2. This is a blueprint-level constraint, not a suggestion to relitigate once Phase 4 work actually starts.

---

### Gold-set freeze pass, batch 1 — the §10.1 queue's first execution (2026-09-02, guideline v0.48)

**Four queue entries ruled, five locked items restamped, one queue entry newly filed. The first version since the batch-1 conforming pass to change locked items.** CI green on the real commit: run `33624977535`, `workflow_dispatch` on `evals/batch02-gold-set`, sha `7740146`, **542 passed / 5 skipped / 0 failed**.

- **F3 — §3.6.1 vs. §4.3.2 resolved: §3.6.1 does NOT yield; §4.3.2's ROOT CHOICE does.** §4.3.2 gains a root constraint (a shared root may not restate either half's own `action`); the `self_`/`third_party_` prefixes are untouched and still recommended. **A premise of the conflict was false**: §4.3.2 *recommends* its convention — §10.1.1 and `OBJECT_CLASS_INVESTIGATION.md` §11 both said *"requires"*, and a recommendation cannot contradict a categorical rule. `C04-04`/`C04-05` → `self_`/`third_party_regulatory_compliance`, on a root **already an accept-set member of both items**, so §3.4's justification test is satisfied by construction. `C04-05`'s move is driven by §4.3.2 alone — that half was already §3.6.1-clean — and is recorded that way so it is never re-read as a second violation. **One live instance, filed as a rule anyway**: a validated screen over all 32 items finds `C04-04` the only §3.6.1 slot violation, but `C22-02` (§4.3.2's other live application) nearly reproduced it — `GRANT` is a taxonomy verb, and only §3.2.1's exclusion of its self-performance half prevented `self_…_grant`/`third_party_…_grant`. **And the finding that made the ruling cheap**: `C22-02` was annotated at `v0.38`, *after* the v0.37 convention, with no prefix and no shared root at all, and is `APPROVED` — the convention was already optional in practice.
- **F11 — `defend` is a GENUINE GAP, not defensibly `INDEMNIFY` (new §8.8.2).** Settled from the corpus rather than asserted: across all 28 documents, 187 sentences use an indemnity-family term and **33 carry `defend`/`defence` with no `indemnify` at all**. Three passages decide it, all the corpus's own words — `C03` disjoins *"failed to defend or indemnify"* (two independently breachable duties); `C03` §(c) owes the defence *"whether or not litigation is actually commenced or the allegations are meritorious"* (a duty owed where there is by definition no loss to indemnify — the black-letter breadth distinction, appearing **in** this corpus rather than imported); `C11` converts a failed defence duty into a payment duty. §8.3 branch 3 fires; `C10-01` → 3 tags. **`hold harmless`→`INDEMNIFY` is untouched, so the two non-member verbs of one triplet land on OPPOSITE sides of §8.3's v0.47 non-mappability limb** — its first exercise against a verb §8.8 had not ruled on, and it discriminates rather than rubber-stamping. Added to §8.8's genuine-gap class as the first entry that is not merely *absent* from `ACTIONS` but affirmatively **distinct from its nearest member**.
- **F8/F7 — v0.47's rulings applied.** `E01-01` **gains TWO tags, not the one its queue row named** — `compound_action` had never been applied to it either. `C04-02` loses `mutual_obligation`.
- **THE ONE PUBLISHED NUMBER THAT MOVED (guideline §9.2): the in-force criterion-2 denominator, 8 → 9.** `C04-02`'s `known_gaps` is now empty, so it enters §9.1's denominator. **Criterion 2 is `? / 9` — denominator asserted, numerator NOT.** `3/8 = 37.5%` is superseded and must not be quoted. **The structural sting: the item that moved the denominator is exactly the item whose evidence the same batch invalidated** — `C04-02` lives on `C04-117`, whose three cassettes went stale, so `3/9` vs `4/9` cannot be resolved from anything on disk.
- **F10 folded in, scoped.** `action_not_in_taxonomy` → `INCOMPLETENESS`, because F8 and F11 apply that tag and shipping them alone would have widened a reporting gap while claiming to close a taxonomy one. Five tags stay `UNCLASSIFIED`; `report.py` now names each with its reason, so **F9's `kind`-axis question is not pre-empted by a `report.py` edit** — the exact masquerade F10 was filed separately to prevent.
- **F12 filed, not decided: §3.6.1 tests the `action` SLOT VALUE only, not `action_accept_set`.** Its v0.44 screen swept `obligor_accept_set` but never the action set, while §5 clause 2 tests *membership* — so the co-variance defect §3.6.1 exists to fix can run through a channel §3.6.1 does not read.
- **A DETECTOR SELF-CORRECTION, and it is the session's most transferable finding.** F12's exposure was first reported as **zero before / one after**, making F3's new root look worse than the old one. It is in fact **1 before and 1 after — the same item both times**: `C04-04`'s old `self_compliant_use` restated `COMPLY` via `compliant`, exactly as the new label does via `compliance`. F3 **inherited** the hazard rather than creating it. The zero came from a stemmer generating `complyance` instead of `compliance`. **This is the THIRD nominalisation miss by that one screen in a single session** — `deliver`→`delivery` and `indemnify`→`indemnification` were the other two, both caught by a known-answer gate before any count was trusted; this one was caught only because the automated count **contradicted a claim reached by reading the label**. All three are the same `y`/`e`-boundary family. **The general lesson, recorded in guideline §3.6.1: a zero is exactly as much a detector output as a positive count, and it reads more reassuringly — so it invites less scrutiny, which makes a wrong zero the more dangerous of the two.** Standing Principle 7's eighth instance.
- **§10.1's cassette-backed column CORRECTED against the directory.** `C04-139` and `C10-016` were **never recorded** — the table had said both were cassette-backed, contradicting §3.6.1's own live text (*"there is no prediction in existence — `C10-016` was never recorded"*). **§22's blocker attached to F7 and F8 only**; F3 and F11 were free and were executed first for that reason. **6 of 36 cassettes now stale, in 2 of 12 segments.** The general rule added: *whenever a queue row asserts a property of an artifact, re-derive it from the artifact at execution time, never trust the row* — both corrections in this batch were found that way and by no other means.
- **`G`-family figures moved and are disclosed rather than frozen**, per §3.6.1 v0.45's precedent for a retroactive edit that moves an `A`-side measurement: `G_swing` **2 → 1**, `D_gold` **15 → 16**, band **(15,17) → (16,17)**, and the **conforming-only band collapses to a POINT (15,15) with `G_swing = 0`** — among conforming pairs the two annotators no longer disagree at all about which items are scoreable. **`K` is UNCHANGED at 14/32**, because it does not read `known_gaps`, which is Finding 1's whole complaint. `G` stays 6/31; both verdicts hold. **Finding 1 is narrowed, not closed** — the all-pairs band still swings, the five non-conforming pairs are excluded rather than resolved, and F9 is untouched. Three tests asserted these figures and were updated with the per-ruling delta; `test_reproduces_the_2026_08_29_holdout_run_exactly` was **renamed**, because it computes from live gold items and could no longer reproduce a dated run.
- **Verification.** All 32 items re-validated — span-offset identity, taxonomy membership, slot ∈ accept-set (32/32), no duplicate members or tags, monotone widening proven against `git HEAD` on all five restamps. The §3.6.1 screen passed a **14-case known-answer gate before any count was read**; after the restamps only `C02-01` and `C14-01` remain, both v0.44 carve-out-1 clears. Full suite **540 passed / 6 skipped with real Neon DB env** (the without-DB number was 494/53 and is not the one reported, per this file's own IR-Compiler audit lesson). One pre-existing failure, `test_check_migrations` against an unmigrated local branch, **reproduced identically at `HEAD` in a clean worktree** and filed as its own debt item rather than fixed here.

**NOT DONE, and scoped as its own dedicated live-spend checkpoint — do not fold this into another session.** Producing criterion 2's missing numerator needs, in order: **(1)** a single guideline stamp across the scored population (`guideline_version_from_items()` raises on more than one; the 18-item cassette-covered population now carries `v0.28`×16 and `v0.48`×2); **(2)** re-record `C04-117` ×3 — mandatory, it carries `C04-02`; **(3)** if step 1 conforms everything, re-record all 12 segments (~36 cassettes), with `C17-021` run 3 still **unobtainable** (§6.1's `json_validate_failed` 400) so that segment stays at 2 runs with the reason stated inline; **(4)** **confirm `openai/gpt-oss-120b` is still in the account's catalogue BEFORE budgeting anything** — a zero-token `GET /v1/models`, and not paranoia: this project has already lost a pinned model mid-effort with no warning. Same treatment as the stage-4 recording run and the model bake-off: its own session, its own budget proposal, catalogue check first.
